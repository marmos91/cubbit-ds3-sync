---
phase: 13.1
plan: 06
type: execute
wave: 3
depends_on: [01, 05]
files_modified:
  - "DS3DriveProvider/S3Lib+Transfers.swift"
  - "DS3DriveProvider/FileProviderExtension+Thumbnails.swift"
  - "DS3DriveProvider/FileProviderExtension+Create.swift"
  - "DS3DriveProvider/FileProviderExtension+Modify.swift"
  - "DS3DriveProvider/FileProviderExtension+Delete.swift"
  - "DS3DriveProviderTests/ProgressFinalizationOnErrorTests.swift"
  - "DS3Drive.xcodeproj/project.pbxproj"
autonomous: false
requirements: [THUMB-13]
must_haves:
  truths:
    - "When a child fetchContents fails (or any per-item operation that the spike identifies), the containing folder's progress state clears within one sync cycle"
    - "The fix targets the exact code path identified by Plan 13.1-01's spike (root cause document)"
    - "Regression test asserts a parent folder transitions out of in-progress state when any child fetch fails"
    - "FileProvider error returns remain in NSFileProviderErrorDomain / NSCocoaErrorDomain (CLAUDE.md rule)"
  artifacts:
    - path: "file_identified_by_spike"
      provides: "Patch implementing the recommended fix from .planning/debug/phase13-parent-folder-stuck-on-child-failure.md `### Recommended fixes`"
    - path: "appropriate_test_file"
      provides: "Regression test pinning the parent-folder progress contract"
  key_links:
    - from: "child_failure_path"
      to: "parent_folder_progress_state"
      via: "mechanism_identified_by_spike"
threat_model_summary: "T-13.1-PARENT-PROGRESS: any change that propagates child failure to parent must NOT leak custom error domains to FileProvider; logging-only changes accepted. Detailed threat model finalized in Task 1 below once the spike's recommended fix shape is read."
---

<objective>
Implement the parent-folder-stuck-on-child-failure fix per CONTEXT.md D-13, D-14:
the spike (Plan 13.1-01) produces `.planning/debug/phase13-parent-folder-stuck-on-child-failure.md`
with a documented root cause and recommended fix at code-line precision. This plan
applies that recommendation, adds a regression test, and ships in a single atomic
commit per D-17.

This plan's `files_modified` and Task 2 `action` are deliberately late-bound to
the spike output. Task 1 reads the spike doc, populates the frontmatter
`files_modified`, and writes the concrete fix shape into a 13.1-06-PREP.md note.

Skip condition: if the spike's `## Scope Assessment` recommends a 13.2 split
(per D-15), Task 1 reports the scope decision and the orchestrator removes this
plan from the wave. The executor of this plan MUST verify scope assessment
before proceeding to Task 2.

Purpose: Restore THUMB-13 conformance — child-fetch failures must propagate to
parent progress so users see correct folder icon state, not a stuck spinner.

Output: Code patch + regression test in a single atomic commit. Per D-19 manual
repro check ships as Task 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-CONTEXT.md
@.planning/debug/phase13-parent-folder-stuck-on-child-failure.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the spike output and verify scope; produce 13.1-06-PREP.md</name>
  <files>
    .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md
    .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PLAN.md
  </files>
  <read_first>
    - .planning/debug/phase13-parent-folder-stuck-on-child-failure.md (front-matter, ## Root Cause, ### Code-line citations, ### Recommended fixes, ## Scope Assessment)
    - CLAUDE.md "File Provider Error Handling" rule
  </read_first>
  <action>
    Read the spike doc end-to-end. Extract:

    1. Scope check (D-15): if `## Scope Assessment` says "split to 13.2",
       STOP. Do not proceed with Tasks 2-3. Report the scope decision in
       13.1-06-PREP.md and exit. The orchestrator will then add a Phase 13.2.

    2. Files to modify: from `### Code-line citations` and
       `### Recommended fixes`, list the exact files and line numbers where
       changes will land. UPDATE this plan's frontmatter `files_modified` array
       to reflect the actual files (replace `"TBD-from-spike-output"`).

    3. Recommended fix shape: copy the spike's `### Recommended fixes`
       text verbatim. This is the input to Task 2.

    4. Regression test target: identify the appropriate test file based on
       the fix location. Likely candidates:
       - Fix in DS3DriveProvider/FileProviderExtension+Thumbnails.swift fetchContents
         path → `DS3DriveProviderTests/FetchThumbnailsTests.swift` (existing) or
         new `DS3DriveProviderTests/FetchContentsParentProgressTests.swift`.
       - Fix in S3Enumerator.swift → existing `DS3DriveProviderTests/S3EnumeratorTests.swift`.
       - Fix in NotificationManager → search for existing tests; if absent,
         add the test alongside the fix file's existing tests.

    5. Threat model finalization: read the spike's recommended mitigations.
       List concrete threat IDs (T-13.1-PARENT-PROGRESS-NN format) covering:
       error-domain leakage, progress-counter race, and any concurrency
       seam touched.

    Write `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md`
    with sections:
    - `## Files to modify` (final list — also patched into 13.1-06-PLAN.md frontmatter)
    - `## Fix shape` (verbatim from spike doc, with line numbers + before/after snippets)
    - `## Test strategy` (file + test function name + assertion shape)
    - `## Threat IDs` (final IDs for Task 2's threat_model block)

    Then update 13.1-06-PLAN.md's frontmatter `files_modified` array using Edit
    tool: replace the `TBD-from-spike-output` line with the actual file paths.
  </action>
  <verify>
    <automated>test -f .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md &amp;&amp; grep -q "## Files to modify" .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md &amp;&amp; grep -q "## Fix shape" .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md &amp;&amp; ! grep -q "TBD-from-spike-output" .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md` exists
    - PREP file contains `## Files to modify` with at least one concrete file path (no `{TBD}` placeholders)
    - PREP file contains `## Fix shape` with verbatim quote from spike's `### Recommended fixes`
    - PREP file contains `## Test strategy` naming a specific test file and test function
    - PREP file contains `## Threat IDs` with at least one concrete T-13.1-PARENT-PROGRESS-NN identifier
    - 13.1-06-PLAN.md frontmatter `files_modified` no longer contains the placeholder `TBD-from-spike-output`
    - If scope assessment is "split to 13.2", PREP.md states this prominently and Task 2/3 are NOT executed
  </acceptance_criteria>
  <done>
    PREP doc complete; plan frontmatter updated with concrete file list; ready
    for Task 2 implementation. OR scope decision recorded for orchestrator
    13.2 split.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the spike's recommended fix and add regression test</name>
  <files>
    Files listed in 13.1-06-PREP.md `## Files to modify` (executor reads PREP first and writes to those files)
  </files>
  <read_first>
    - .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-PREP.md (Task 1's output — single source of truth for fix shape)
    - .planning/debug/phase13-parent-folder-stuck-on-child-failure.md (root-cause doc — for context, not implementation)
    - The exact code files identified in PREP.md `## Files to modify` (read each end-to-end before editing)
    - The test file identified in PREP.md `## Test strategy` (so test follows existing patterns in that file)
    - CLAUDE.md "File Provider Error Handling" — error-domain rules
  </read_first>
  <behavior>
    - Regression test (per PREP.md Test strategy): assert that when a child
      fetchContents fails (or per-item operation as identified by spike), the
      parent folder's progress state transitions to a non-in-progress state
      within one sync cycle. Use mock S3 client to inject the failure; assert
      on the observable state (NotificationManager counter, parent Progress
      completedUnitCount, or signalEnumerator parent-container call — whichever
      the spike identifies as the propagation channel).
    - Existing tests in the same file MUST still pass.
  </behavior>
  <action>
    Read 13.1-06-PREP.md `## Fix shape` and apply the recommended fix VERBATIM
    to the files listed in `## Files to modify`. Do not improvise — the spike
    is the source of truth for what to change. If the recommended fix is
    ambiguous, halt and request clarification rather than guess.

    Add the regression test per `## Test strategy` in PREP.md. The test must:
    1. Reproduce the audit symptom (child fetch fails) via mock injection.
    2. Assert the parent folder's progress observable state has cleared
       (NOT just that the child fetch failed — the test must observe the
       PROPAGATION).
    3. Use the canonical test patterns from the existing test file (read
       FetchThumbnailsTests.swift / S3EnumeratorTests.swift / similar for the
       mock + setup style).

    Verify FileProvider error-domain rules:
    - `grep "throw NSError" {modified_files}` should show only
      `NSFileProviderErrorDomain` or `NSCocoaErrorDomain` — NEVER a custom
      domain (CLAUDE.md hard rule).
    - If the fix involves wrapping/unwrapping errors, ensure the wrapped
      error stays in the supported domain set.

    Verify Swift 6 strict concurrency: `xcodebuild build -scheme DS3Drive`
    must succeed. Local Xcode is looser than CI Xcode 16.2 — if the fix
    introduces new actor hops or @Sendable closures, double-check the
    Sendable conformances per CLAUDE.md "Swift 6 Concurrency" notes.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive &amp;&amp; xcodebuild test -scheme DS3Drive -destination 'platform=macOS' 2>&amp;1 | tail -30 &amp;&amp; xcodebuild build -scheme DS3Drive -destination 'platform=macOS' 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Files in PREP.md `## Files to modify` actually modified (verified via `git diff --stat`)
    - The regression test named in PREP.md `## Test strategy` is added to the named test file (grep succeeds for the test function name)
    - The regression test passes under `xcodebuild test`
    - All existing tests in the modified test file still pass
    - No new error-domain leak: `grep "domain: \"" {modified_files}` returns no matches OR matches only `NSFileProviderErrorDomain` / `NSCocoaErrorDomain`
    - `xcodebuild build` succeeds (Swift 6 strict-concurrency clean)
    - Threat IDs from PREP.md `## Threat IDs` appear in the file's threat_model section (if a new file, or existing threat model is preserved if a code-only edit)
  </acceptance_criteria>
  <done>
    Spike's recommended fix applied; regression test passes; FileProvider
    error contracts preserved; CI strict-concurrency clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual repro check (D-19 parent-folder finding)</name>
  <what-built>
    Parent-folder progress fix landed; regression test pins propagation
    contract; CI strict-concurrency clean.
  </what-built>
  <how-to-verify>
    Per CONTEXT.md D-18 / D-19 parent-folder reproduction:
    1. Build &amp; run macOS app.
    2. Open a drive with at least one cloud-only file in a folder.
    3. Force a child fetchContents failure: simplest reliable reproduction
       (use the same one captured by the spike's `## Symptoms.reproduction`
       — the spike doc is the source of truth for HOW to repro).
    4. Watch the Finder folder icon containing the failed child. Within one
       sync cycle (~5-10s after the failure), the folder icon MUST transition
       OUT of the in-progress state. Take a screen recording or screenshot.
    5. Capture logs:
       `/usr/bin/log show --last 5m --info --debug --predicate "subsystem BEGINSWITH 'io.cubbit.DS3Drive'" --style compact`
       Confirm the failure is logged (per Plan 13.1-05's describeSotoError
       formatter if Soto-throws) AND the parent-progress propagation log
       line (whatever the spike identified as the channel) fires within
       the sync cycle.
  </how-to-verify>
  <resume-signal>
    Type "approved — parent-folder finding fixed" if the parent folder clears
    its in-progress state within one sync cycle. Otherwise paste the
    NotificationManager counter / Progress / signalEnumerator log evidence
    showing it did not.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child fetch failure → parent progress observable | Bug surface; fix must propagate without leaking custom error domains |
| FileProvider system → user-visible folder icon | UNCHANGED — only internal propagation modified |

## STRIDE Threat Register

NOTE: Final threat IDs are populated by Task 1 PREP.md `## Threat IDs`. The
following are placeholders pending spike output:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-PARENT-PROGRESS-DOMAIN | T (Tampering — error domain leak) | error returns from fetchContents/per-item ops | mitigate | All NSError returns stay in NSFileProviderErrorDomain / NSCocoaErrorDomain per CLAUDE.md rule |
| T-13.1-PARENT-PROGRESS-RACE | S (Spoofing — race condition) | counter or progress-state update from concurrent Task | mitigate | Task 1 PREP.md identifies the specific concurrency seam and mitigation; Swift 6 strict-concurrency CI gate enforces |
</threat_model>

<verification>
- 13.1-06-PREP.md exists with concrete files + fix shape + test strategy
- Regression test passes
- All existing tests in modified test file still pass
- xcodebuild build green (Swift 6 strict-concurrency clean)
- No custom error domains introduced
- Manual repro per D-19 confirms parent folder clears in-progress state
</verification>

<success_criteria>
The parent-folder-stuck-on-child-failure reproduction (induce a child fetch
failure) shows the parent folder icon clearing its in-progress state within
one sync cycle. Regression test pins this behavior so the bug cannot recur
silently.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-SUMMARY.md`
</output>
