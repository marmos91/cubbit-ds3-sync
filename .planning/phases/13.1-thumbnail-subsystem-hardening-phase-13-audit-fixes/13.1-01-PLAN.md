---
phase: 13.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/debug/phase13-parent-folder-stuck-on-child-failure.md
autonomous: false
requirements: [THUMB-13]
must_haves:
  truths:
    - "Root cause of the parent-folder-stuck-on-child-failure symptom is documented at code-line precision in .planning/debug/phase13-parent-folder-stuck-on-child-failure.md"
    - "Spike either (a) identifies the buggy code path with a recommended fix shape OR (b) flags scope as too large for 13.1 and recommends 13.2 split"
    - "Spike produces a fresh log capture that reproduces the symptom on a real drive"
  artifacts:
    - path: ".planning/debug/phase13-parent-folder-stuck-on-child-failure.md"
      provides: "Root cause document with status: root_caused, code citations, hypothesis ranking, recommended fix"
      contains: "status: root_caused"
  key_links:
    - from: "Spike findings"
      to: "Plan 13.1-06 fix plan"
      via: "Documented root cause + recommended fix shape feeds the fix plan's <action> blocks"
threat_model_summary: "Spike is read-only investigation; no production code changes. T-13.1-SPIKE-LOG-PII: spike log capture must be redacted of credentials/tokens before committing."
---

<objective>
Spawn the **first plan** of Phase 13.1 per CONTEXT.md D-13: a root-cause spike for the
parent-folder-stuck-on-child-failure symptom. STATE.md mentions this symptom only as a
one-line note ("propagate error to parent folder icon"); we have NO line-level
diagnosis. The spike produces `.planning/debug/phase13-parent-folder-stuck-on-child-failure.md`
to root-cause precision BEFORE any fix is planned (D-14). If the spike reveals
non-trivial scope, the planner has authority (D-15) to flag the fix for 13.2 split.

Purpose: Without a documented root cause, a fix plan would be guesswork. The other
three findings already have debug docs; this one does not. The spike closes that gap.

Output: A debug document mirroring the structure of the three existing Phase 13
debug docs (status front-matter, Symptoms / Evidence / Eliminated / Root Cause /
Resolution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-CONTEXT.md
@.planning/debug/phase13-orphan-sweep-deletes-valid.md
@.planning/debug/phase13-rename-cascade-copy-fails.md
@.planning/debug/phase13-renderer-returns-nil.md
@CLAUDE.md

<spike_targets>
Likely candidates per CONTEXT.md `<code_context>` Integration Points:
- DS3DriveProvider/S3Enumerator.swift (folder enumeration progress)
- DS3DriveProvider/FileProviderExtension+Thumbnails.swift (fetchContents path; the
  "Parent folder stuck in progress when child file fails" note in MEMORY.md
  associates this symptom with fetchContents on children)
- NotificationManager._activeOperations counter (per auto-memory)
- NSFileProviderItem.itemVersion / Progress propagation for parent containers

Read these files end-to-end during the spike. Cross-correlate with NSFileProvider's
documented Progress contract for parent folders and child fetches.
</spike_targets>

<debug_doc_template>
The three existing Phase 13 debug docs share a structure that the spike doc MUST follow:

```markdown
---
status: root_caused | investigating
trigger: "<one-line user-facing symptom>"
created: <ISO-8601>
updated: <ISO-8601>
---

## Current Focus
hypothesis: ...
test: ...
expecting: ...
next_action: report root cause; offer fix options (do not apply per `goal: find_root_cause_only`)

## Symptoms
expected: ...
actual: ...
errors: <log excerpts>
reproduction: <step-by-step on a real drive>

## Evidence
- timestamp: <ISO-8601>
  observation: ...
- code: <file:line>
  observation: ...

## Eliminated
- Hypothesis: ... — Eliminated by ...

## Root Cause
<paragraph or two with code-line citations>

### Code-line citations
- Bug epicenter: <file:line> ...

### Recommended fixes (for plan-fix)
1. ...
2. ...

### Specialist hint for downstream review
specialist_hint: <swift_concurrency | fileprovider | etc.>
```
</debug_doc_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce parent-folder-stuck symptom on a real drive and capture logs</name>
  <files>
    .planning/debug/phase13-parent-folder-stuck-on-child-failure.md
  </files>
  <read_first>
    - .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-CONTEXT.md (D-13, D-14, D-15)
    - .planning/debug/phase13-orphan-sweep-deletes-valid.md (template structure)
    - .planning/debug/phase13-rename-cascade-copy-fails.md (template structure)
    - CLAUDE.md (Debugging section — log commands)
  </read_first>
  <action>
    Reproduce the symptom on a real drive following the v3.1 thumbnails build.
    Reproduction strategy (test multiple, document which works):
    1. Force a child fetchContents failure: open a cloud-only file, simulate
       network drop (turn off Wi-Fi mid-fetch), or rename the file in S3 from
       another client between enumeration and click so the GET returns 404.
    2. Observe Finder folder icon containing the failed child. Expected behavior:
       parent folder icon clears progress within one sync cycle. Actual symptom
       (per STATE.md): parent stays in in-progress state.
    3. Capture logs with the canonical commands from CLAUDE.md:
       `/usr/bin/log show --last 5m --info --debug --predicate "subsystem BEGINSWITH 'io.cubbit.DS3Drive'" --style compact`
    4. Save the relevant log slice (timestamps + lines) into the new debug doc's
       `## Evidence` section. **Redact** any credentials, tokens, or presigned
       URLs in the log capture per T-13.1-SPIKE-LOG-PII.

    Create `.planning/debug/phase13-parent-folder-stuck-on-child-failure.md` with
    front-matter `status: investigating`, `trigger:` describing the symptom in one
    line, `created:` set to the current ISO-8601 timestamp.
  </action>
  <verify>
    <automated>test -f .planning/debug/phase13-parent-folder-stuck-on-child-failure.md &amp;&amp; grep -q "status: investigating" .planning/debug/phase13-parent-folder-stuck-on-child-failure.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/debug/phase13-parent-folder-stuck-on-child-failure.md` exists
    - File front-matter contains `status: investigating` (will be flipped to `root_caused` in Task 2)
    - File contains `## Symptoms` section with `expected:`, `actual:`, `reproduction:` fields
    - File contains `## Evidence` section with at least one timestamped log observation
    - No credentials, tokens, presigned URLs, or session cookies appear in evidence (grep for `token`, `Authorization`, `X-Amz-Signature`, `key=` should return only false positives or be absent)
  </acceptance_criteria>
  <done>
    Symptom reproduced on a real drive with timestamped log evidence captured into the debug doc.
  </done>
</task>

<task type="auto">
  <name>Task 2: Read candidate code paths and identify root cause at code-line precision</name>
  <files>
    .planning/debug/phase13-parent-folder-stuck-on-child-failure.md
  </files>
  <read_first>
    - DS3DriveProvider/FileProviderExtension+Thumbnails.swift (fetchContents path; lines 1-200 minimum)
    - DS3DriveProvider/S3Enumerator.swift (folder enumeration + Progress)
    - DS3DriveProvider/FileProviderExtension+Errors.swift (NSError mapping — the parent-progress contract may require specific NSFileProviderErrorDomain codes)
    - DS3Lib/Sources/DS3Lib/NotificationManager.swift if present (operation counter per MEMORY.md auto-memory)
    - .planning/STATE.md (the "Remaining Work (after f8917ee)" note about parent-folder propagation)
  </read_first>
  <action>
    Read each candidate code path end-to-end. For each path that touches the failed
    child's progress reporting or parent-folder state machine, classify whether it
    could produce the symptom. Hypotheses to test (enumerate explicitly, eliminate
    each in `## Eliminated` if disproved):

    H1: `fetchContents` returns a non-NSFileProviderErrorDomain error that
        FileProvider does not propagate to parent progress (per CLAUDE.md
        "Provider returned error 0 from domain ... which is unsupported").
    H2: Progress object passed back to FileProvider on the failure path is left
        with `completedUnitCount &lt; totalUnitCount`, so the system reads it as
        still-in-progress.
    H3: NotificationManager's `_activeOperations` counter (per MEMORY.md) is
        incremented for the child fetch but the failure path skips the
        decrement, leaving the counter stuck above 0 and suppressing idle.
    H4: The completion handler is invoked with a non-nil URL OR a nil error on
        a failed fetch, breaking FileProvider's parent-progress contract.

    For the surviving hypothesis, write `## Root Cause` with:
    - One-paragraph explanation of the failure mode
    - `### Code-line citations` listing exact `<file:line>` epicenters
    - `### Recommended fixes` (numbered) with code-line precision (e.g., "at
      FileProviderExtension+Thumbnails.swift:NNN, replace `complete(nil, nil, error)`
      with `complete(nil, nil, NSError(domain: NSFileProviderErrorDomain, code: ...))`")
    - `### Specialist hint for downstream review` set to `fileprovider` (or
      `swift_concurrency` if the bug is in counter logic)

    Update front-matter `status:` from `investigating` to `root_caused` and
    `updated:` to current ISO-8601 timestamp.

    **Scope-flag authority (D-15):** if the spike reveals the fix requires
    rearchitecting the fetchContents → parent progress flow OR touches
    NSFileProviderReplicatedExtension protocol surfaces beyond a single error-mapping
    line, write a `## Scope Assessment` section recommending split into Phase 13.2.
    Otherwise write `## Scope Assessment: fits in 13.1` with rationale.
  </action>
  <verify>
    <automated>grep -q "status: root_caused" .planning/debug/phase13-parent-folder-stuck-on-child-failure.md &amp;&amp; grep -q "## Root Cause" .planning/debug/phase13-parent-folder-stuck-on-child-failure.md &amp;&amp; grep -q "### Code-line citations" .planning/debug/phase13-parent-folder-stuck-on-child-failure.md &amp;&amp; grep -q "## Scope Assessment" .planning/debug/phase13-parent-folder-stuck-on-child-failure.md</automated>
  </verify>
  <acceptance_criteria>
    - Front-matter contains `status: root_caused`
    - Document contains `## Root Cause` section with at least one `<file>:<line>` citation in `### Code-line citations`
    - Document contains `### Recommended fixes` with at least one numbered fix carrying code-line precision (line number OR exact function name + 5-line context)
    - Document contains `### Specialist hint for downstream review` with `specialist_hint:` value
    - Document contains `## Scope Assessment` section that either fits in 13.1 OR recommends 13.2 split
    - All eliminated hypotheses listed in `## Eliminated` with one-line elimination rationale each
  </acceptance_criteria>
  <done>
    Root cause documented at code-line precision; scope assessment made; downstream fix plan (13.1-06) has enough information to write a concrete `<action>` block.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review — confirm root cause + scope decision</name>
  <what-built>
    Root-cause spike document at .planning/debug/phase13-parent-folder-stuck-on-child-failure.md
    with code-line citations, recommended fixes, specialist hint, and scope assessment.
  </what-built>
  <how-to-verify>
    1. Read the new debug doc end-to-end.
    2. Confirm the root cause matches the symptom you observed (parent folder
       stays in-progress when a child fetch fails).
    3. Confirm the recommended fix is implementable (line numbers + code shape).
    4. Confirm the scope assessment matches your judgment (fits 13.1 vs split to 13.2).
    5. If `Scope Assessment` recommends 13.2 split, the orchestrator will halt
       Phase 13.1 here and add a 13.2 phase. If `fits in 13.1`, plans 13.1-02
       through 13.1-07 proceed in parallel waves.
  </how-to-verify>
  <resume-signal>
    Type "approved — fits 13.1" to continue with the planned waves, OR
    "approved — split to 13.2" to halt and add 13.2 phase, OR describe issues with
    the spike doc.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log capture → debug doc | Production logs may contain credentials/tokens that must not be committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-SPIKE-LOG-PII | I (Information disclosure) | spike doc Evidence section | mitigate | Redact credentials/tokens/presigned URLs in log capture before committing; grep for `Authorization`, `X-Amz-Signature`, `token`, `Bearer` and confirm absent |
| T-13.1-SPIKE-NO-CODE | — | spike is read-only | accept | Spike performs no production code changes; threat surface is the doc itself |
</threat_model>

<verification>
- Debug doc exists with `status: root_caused`
- Recommended fix is concrete enough that 13.1-06 can write code without re-investigating
- Scope assessment is explicit (fits 13.1 / split to 13.2)
- No PII or secrets in committed log evidence
</verification>

<success_criteria>
The downstream fix plan (13.1-06) can be written without further investigation:
file paths, line numbers, function signatures, and recommended code shape are all
present in the debug doc.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-01-SUMMARY.md`
</output>
