---
phase: 13.1
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md
autonomous: false
requirements: [THUMB-13, THUMB-18, THUMB-19]
must_haves:
  truths:
    - "All four 2026-04-27 audit reproductions (Findings 2, 4, 5, parent-folder) re-run on a real drive and DO NOT recur"
    - "Each finding's per-plan manual repro check (already gated in 13.1-02 through 13.1-06) was approved before this plan starts"
    - "The integrated repro confirms no NEW interactions between fixes broke another finding's behavior (e.g., the orphan sweep MetadataStore backstop didn't accidentally suppress a legitimate orphan)"
    - "Phase 13-11 integration smoke tests still pass (no regression to Phase 13's integrated upload→cascade→fetch flow)"
    - "STATE.md is updated to reflect Phase 13.1 ship status"
  artifacts:
    - path: ".planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md"
      provides: "Ship-gate document with timestamp, log capture summary, and pass/fail per finding"
      contains: "## Finding 2"
  key_links:
    - from: "Per-plan repro checks"
      to: "Phase 13.1 ship gate"
      via: "Integrated re-run on a real drive (D-18, D-19)"
threat_model_summary: "T-13.1-SHIP-GATE-LOG-PII: ship-gate log capture must be redacted of credentials/tokens/presigned URLs before committing (same posture as Plan 13.1-01 spike doc)."
---

<objective>
Execute the Phase 13.1 ship gate per CONTEXT.md D-18: re-run ALL FOUR audit
reproductions from 2026-04-27 15:06–15:20Z on a real drive after Plans 13.1-02
through 13.1-06 have all landed and passed their individual manual repro checks.
Confirm no recurrence; confirm no cross-fix regression; record the result in a
ship-gate doc; update STATE.md.

Per D-19 the four reproductions are:

1. **Finding 4 (orphan sweep):** paste a file into a folder while a long BFS pass
   is in flight; assert the new thumbnail is NOT deleted by pass-tail sweep.
2. **Finding 5 (rename cascade):** rename a raster file whose source thumbnail
   was just swept (or just deleted manually if 13.1-02 made the sweep race
   impossible to reproduce naturally); assert log line is human-readable AND at
   .info level for NoSuchKey.
3. **Finding 2 (renderer):** bulk-paste 8 raster files; assert ≥ 7 of 8
   thumbnails render successfully.
4. **Parent-folder:** induce a child fetch failure; assert parent folder icon
   clears its in-progress state within one sync cycle.

Per-plan manual repro checks (in 13.1-02, 13.1-03, 13.1-04, 13.1-06) verify each
finding individually. This plan verifies them INTEGRATED — same drive, same
session, sequential runs — to catch any cross-fix interaction. It also re-runs
the Phase 13-11 integration smoke tests.

Purpose: Final D-18 ship-gate. Phase 13.1 cannot be marked complete until this
plan passes.

Output: 13.1-SHIP-GATE.md with timestamps + log evidence + pass/fail per
finding. STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-CONTEXT.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-02-SUMMARY.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-03-SUMMARY.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-04-SUMMARY.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-05-SUMMARY.md
@.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-06-SUMMARY.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-11-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 13-11 integration smoke tests; verify no regression</name>
  <files>
    .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md
  </files>
  <read_first>
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-11-PLAN.md (the smoke test surface that 13.1 must not regress per CONTEXT.md `<code_context>`)
    - DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift
  </read_first>
  <action>
    Run the Phase 13 integration smoke suite plus the entire DS3DriveProviderTests
    + DS3LibTests suites. The Phase 13.1 fixes must not regress any Phase 13 test.

    Begin 13.1-SHIP-GATE.md with:

    ```markdown
    # Phase 13.1 Ship Gate

    **Date:** <ISO-8601 timestamp of run>
    **Build:** <git commit SHA HEAD>

    ## Test suite (gate 1: automated regression)

    - Phase13IntegrationSmokeTests: <PASS/FAIL>
    - DS3DriveProviderTests (full): <PASS/FAIL with count>
    - DS3LibTests (full): <PASS/FAIL with count>
    - Build status: <xcodebuild build clean/dirty>
    ```

    Run the tests:
    ```
    xcodebuild test -scheme DS3Drive -destination 'platform=macOS' \
      -only-testing:DS3LibTests/Phase13IntegrationSmokeTests
    xcodebuild test -scheme DS3Drive -destination 'platform=macOS'
    ```

    Record results into 13.1-SHIP-GATE.md. If any test fails, STOP and report;
    do not proceed to manual reproductions until automated regression is clean.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive &amp;&amp; xcodebuild test -scheme DS3Drive -destination 'platform=macOS' -only-testing:DS3LibTests/Phase13IntegrationSmokeTests 2>&amp;1 | tail -10 &amp;&amp; test -f .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase13IntegrationSmokeTests passes (xcodebuild exit 0)
    - Full DS3Drive test suite passes
    - 13.1-SHIP-GATE.md exists with `## Test suite (gate 1: automated regression)` section
    - No test marked FAIL in the gate doc
  </acceptance_criteria>
  <done>
    Automated regression gate green; ship-gate doc started.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Integrated manual reproduction of all four audit scenarios on a real drive</name>
  <what-built>
    All five fix plans (13.1-02, 13.1-03, 13.1-04, 13.1-05, 13.1-06) landed and
    individually verified. Phase 13-11 smoke suite passes.
  </what-built>
  <how-to-verify>
    Per CONTEXT.md D-18 / D-19. Build &amp; run the macOS app on a real drive with
    thumbnails enabled. In a SINGLE session, run all four reproductions
    sequentially and capture logs into
    `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md`:

    ### Reproduction 1 — Finding 4 (orphan sweep)
    1. Wait for or trigger a long BFS pass on a moderately-sized drive.
    2. While the pass is in flight (verify in logs: "BFS pass starting"), paste
       a raster image into a folder BFS has already visited.
    3. Wait for "BFS pass complete" log line.
    4. Capture the next 30s of logs.
    5. Verify: NO log line "Orphan sweep deleted N orphan thumbnails" mentions
       the freshly-pasted file's S3 key. The thumbnail still exists in S3
       and Finder shows it.

    ### Reproduction 2 — Finding 5 (rename cascade)
    1. After Reproduction 1, the orphan sweep should not pre-delete thumbnails
       on the steady-state path. To still trigger the rename-NoSuchKey case,
       MANUALLY delete the source thumbnail from S3 (via aws CLI or Cubbit
       console) for one specific raster file.
    2. Rename that raster file in Finder.
    3. Capture logs.
    4. Verify: log line for the rename-cascade copy failure is at .info level
       (not .error), contains a human-readable error code (e.g., "noSuchKey:")
       NOT the opaque "SotoS3.S3ErrorType error 1" form, AND the new key was
       marked .pending for backfill.

    ### Reproduction 3 — Finding 2 (renderer)
    1. Bulk-paste 8 raster files (mix of PNG + JPG + HEIC) into a Drive folder.
    2. Wait ~30s for upload-hooks.
    3. Verify (via S3 console / aws CLI): ≥ 7 of 8 thumbnails exist under
       .thumbnails/. PNG/JPG must always render; HEIC may have orientation-
       quirky failures (separate concern, NOT Finding 2).
    4. Capture logs and grep for "render returned nil" — should NOT match
       any PNG or JPG file in the bulk paste.

    ### Reproduction 4 — Parent-folder
    1. Use the reproduction recipe documented in
       `.planning/debug/phase13-parent-folder-stuck-on-child-failure.md`
       `## Symptoms.reproduction`.
    2. Watch the parent folder's icon. Within one sync cycle (~10s) it MUST
       transition out of the in-progress state.
    3. Capture logs showing the failure propagation through whatever
       channel the spike identified (NotificationManager counter / Progress /
       signalEnumerator).

    ### Cross-fix interaction check
    - After all 4 reproductions, verify the drive still functions normally:
      enumerate the drive, browse folders, all thumbnails visible, no
      stuck progress, no orphan-sweep storms, no log noise.

    ### Recording in 13.1-SHIP-GATE.md
    Add four sections:

    ```markdown
    ## Finding 2 — Renderer eager-snapshot
    **Repro time:** <ISO-8601>
    **Result:** PASS / FAIL
    **Log evidence:** <log slice with PII redacted>

    ## Finding 4 — Orphan sweep MetadataStore backstop
    ...

    ## Finding 5 — Rename cascade NoSuchKey demote
    ...

    ## Parent-folder propagation
    ...

    ## Cross-fix interaction
    Result: PASS / FAIL
    Notes: <any interaction observed>
    ```

    All four findings + cross-fix check MUST be PASS for the ship gate to clear.
    Redact credentials/tokens/presigned URLs from log evidence per
    T-13.1-SHIP-GATE-LOG-PII.
  </how-to-verify>
  <resume-signal>
    Type "approved — Phase 13.1 ship gate clear" if all four reproductions and
    the cross-fix check passed. Otherwise describe which finding recurred and
    paste the recurrence evidence — the orchestrator will re-open the relevant
    fix plan as a gap-closure cycle.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and ROADMAP.md with Phase 13.1 ship status</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/ROADMAP.md Phase 13.1 entry (line ~215-223)
    - .planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-SHIP-GATE.md (Tasks 1+2 output)
  </read_first>
  <action>
    Update STATE.md:
    - `last_updated` → current ISO-8601 timestamp
    - `last_activity` → current date
    - `stopped_at` → "Phase 13.1 shipped"
    - `progress.completed_plans` → +7 (the 7 plans of 13.1)
    - Add `## Decisions` entry: "[Phase 13.1] Thumbnail subsystem hardening
      shipped: OrphanSweeper MetadataStore freshness backstop (Finding 4),
      cascade NoSuchKey demote + describeSotoError sweep (Finding 5),
      ThumbnailRenderer eager Data snapshot (Finding 2), parent-folder progress
      propagation fix (spike + fix). 7 plans total. THUMB-13/THUMB-18/THUMB-19
      conformance restored."

    Update ROADMAP.md Phase 13.1 entry (current entry at ~line 215-223):
    - Change `**Goal:** [Urgent work - to be planned]` to a finalized goal
      reflecting what shipped: `**Goal:** Phase 13 audit fixes — Findings 2,
      4, 5, and parent-folder progress propagation, restoring THUMB-13,
      THUMB-18, THUMB-19 conformance under real-world load`
    - Change `**Requirements:** TBD` to `**Requirements:** THUMB-13, THUMB-18,
      THUMB-19 (no new requirements; bugs violated existing ones)`
    - Change `**Plans:** 0 plans` to `**Plans:** 7 plans (complete)`
    - Replace the placeholder Plan list with the seven finalized plans:
      - [x] 13.1-01-PLAN.md — Parent-folder root-cause spike
      - [x] 13.1-02-PLAN.md — OrphanSweeper MetadataStore freshness backstop (Finding 4)
      - [x] 13.1-03-PLAN.md — Cascade NoSuchKey demote + log opacity fix (Finding 5)
      - [x] 13.1-04-PLAN.md — ThumbnailRenderer eager Data snapshot (Finding 2)
      - [x] 13.1-05-PLAN.md — Codebase-wide describeSotoError sweep (Finding 5b)
      - [x] 13.1-06-PLAN.md — Parent-folder progress propagation fix
      - [x] 13.1-07-PLAN.md — Phase 13.1 ship gate
    - Update the Progress table row: "13.1. Thumbnail subsystem hardening |
      v3.1 | 7/7 | Complete | <date>"
  </action>
  <verify>
    <automated>grep -q "Phase 13.1 shipped" .planning/STATE.md &amp;&amp; grep -q "13.1-07-PLAN.md" .planning/ROADMAP.md &amp;&amp; ! grep -q "Goal: \[Urgent work" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md `stopped_at` updated to "Phase 13.1 shipped"
    - STATE.md `## Decisions` contains the new Phase 13.1 entry
    - ROADMAP.md Phase 13.1 Goal no longer says `[Urgent work - to be planned]`
    - ROADMAP.md Phase 13.1 Requirements is `THUMB-13, THUMB-18, THUMB-19`
    - ROADMAP.md Phase 13.1 Plans count is 7
    - All 7 plan files referenced in the Plans list (grep for `13.1-01-PLAN` through `13.1-07-PLAN`)
    - Progress table row for Phase 13.1 shows 7/7 Complete
  </acceptance_criteria>
  <done>
    STATE.md and ROADMAP.md reflect Phase 13.1 ship status.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log capture → ship-gate doc | Production logs may contain credentials/tokens that must not be committed to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-SHIP-GATE-LOG-PII | I (Information disclosure) | 13.1-SHIP-GATE.md log evidence | mitigate | Redact credentials/tokens/presigned URLs before commit; grep for `Authorization`, `X-Amz-Signature`, `Bearer`, `token=` and confirm absent |
| T-13.1-SHIP-GATE-NO-CODE | — | ship gate is verification-only | accept | No production code changes; threat surface is the doc itself |
</threat_model>

<verification>
- 13.1-SHIP-GATE.md exists with all 4 finding sections + cross-fix check section, all PASS
- Phase 13-11 smoke suite passes
- STATE.md + ROADMAP.md updated
- No PII / secrets in committed log evidence
</verification>

<success_criteria>
Phase 13.1 ships when:
1. Automated regression gate (Task 1) clean
2. All 4 manual reproductions (Task 2) pass on a real drive
3. Cross-fix interaction check (Task 2) passes
4. STATE.md + ROADMAP.md (Task 3) reflect ship status

Per D-18 the manual gate is the binding ship criterion.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-thumbnail-subsystem-hardening-phase-13-audit-fixes/13.1-07-SUMMARY.md`
</output>
