---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
  - DS3Lib/Sources/DS3Lib/DS3Authentication.swift
  - DS3Lib/Sources/DS3Lib/DS3DriveManager.swift
  - DS3Lib/Sources/DS3Lib/DS3SDK.swift
  - DS3Lib/Sources/DS3Lib/AppStatusManager.swift
  - DS3Lib/Sources/DS3Lib/SharedData/SharedData.swift
  - DS3Lib/Sources/DS3Lib/Utils/Notifications+Extensions.swift
  - DS3DriveProvider/FileProviderExtension.swift
  - DS3DriveProvider/S3Enumerator.swift
  - DS3DriveProvider/S3Lib.swift
  - DS3DriveProvider/S3Item.swift
  - DS3DriveProvider/NotificationsManager.swift
  - DS3Drive/DS3DriveApp.swift
autonomous: true
requirements:
  - FOUN-02

must_haves:
  truths:
    - "All three targets (DS3Drive, DS3DriveProvider, DS3Lib) produce structured log entries visible in Console.app"
    - "Log entries can be filtered by subsystem (io.cubbit.DS3Drive or io.cubbit.DS3Drive.provider) and category (sync, auth, transfer, extension, app, metadata)"
    - "No print() calls remain in the codebase"
    - "copyFolder bug is fixed (no early return)"
    - "EnumeratorError.unsupported is spelled correctly"
    - "Empty catch blocks contain at least a logger.error call"
  artifacts:
    - path: "DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift"
      provides: "LogCategory enum with all 6 categories"
      contains: "enum LogCategory"
    - path: "DS3DriveProvider/FileProviderExtension.swift"
      provides: "Logger with correct subsystem io.cubbit.DS3Drive.provider"
      contains: "io.cubbit.DS3Drive.provider"
    - path: "DS3DriveProvider/S3Lib.swift"
      provides: "Logger with transfer category, copyFolder bug fixed"
      contains: "LogCategory.transfer"
  key_links:
    - from: "DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift"
      to: "DS3DriveProvider/FileProviderExtension.swift"
      via: "LogCategory enum used for category strings"
      pattern: "LogCategory\\."
    - from: "DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift"
      to: "DS3Drive/DS3DriveApp.swift"
      via: "LogCategory enum used for category strings"
      pattern: "LogCategory\\."
---

<objective>
Add structured OSLog-based logging with domain categories across all targets, replace all print() calls with proper Logger usage, and fix known code quality bugs.

Purpose: Observability is critical for debugging File Provider extensions (separate process, limited tooling). Structured logging with categories makes Console.app filtering reliable. Bug fixes prevent known issues from confusing future work.
Output: All three targets emit categorized logs filterable in Console.app. Known bugs fixed.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Logger pattern to establish across all files -->
```swift
// Defined in DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
enum LogCategory: String {
    case sync       // File sync operations
    case auth       // Authentication flow
    case transfer   // Upload/download data transfer
    case `extension` // File Provider extension lifecycle
    case app        // Main app lifecycle
    case metadata   // SwiftData/metadata operations
}

// Subsystems (exactly two):
// Main app + DS3Lib: "io.cubbit.DS3Drive"
// Extension:         "io.cubbit.DS3Drive.provider"

// Per-class usage pattern:
private let logger = Logger(
    subsystem: "io.cubbit.DS3Drive.provider",
    category: LogCategory.transfer.rawValue
)
```

<!-- Log level conventions -->
// .debug   — Flow details (method entry/exit, intermediate values)
// .info    — Milestones (operation completed, state loaded)
// .notice  — State changes (drive connected, sync started)
// .warning — Recoverable issues (retry needed, fallback used)
// .error   — Failures (operation failed, data missing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logging infrastructure and update all Logger instances</name>
  <files>
    DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
    DS3Lib/Sources/DS3Lib/DS3Authentication.swift
    DS3Lib/Sources/DS3Lib/DS3DriveManager.swift
    DS3Lib/Sources/DS3Lib/DS3SDK.swift
    DS3Lib/Sources/DS3Lib/AppStatusManager.swift
    DS3Lib/Sources/DS3Lib/SharedData/SharedData.swift
    DS3DriveProvider/FileProviderExtension.swift
    DS3DriveProvider/S3Enumerator.swift
    DS3DriveProvider/S3Lib.swift
    DS3DriveProvider/S3Item.swift
    DS3DriveProvider/S3Item+Metadata.swift
    DS3DriveProvider/NotificationsManager.swift
    DS3Drive/DS3DriveApp.swift
  </files>
  <action>
    **Step 1 -- Add LogCategory enum to DefaultSettings.swift:**
    Add a `LogCategory` enum inside DefaultSettings (or at top level in the same file) with cases: `sync`, `auth`, `transfer`, `extension`, `app`, `metadata`. Each case has a `rawValue: String`.

    **Step 2 -- Add LogSubsystem constants to DefaultSettings.swift:**
    ```swift
    enum LogSubsystem {
        static let app = "io.cubbit.DS3Drive"
        static let provider = "io.cubbit.DS3Drive.provider"
    }
    ```

    **Step 3 -- Update every Logger instance across the codebase:**
    Search for all `Logger(subsystem:` declarations and update:

    DS3Lib files (loaded by both app and extension -- use `.app` subsystem, extension overrides at init):
    - DS3Authentication.swift: subsystem `.app`, category `.auth`
    - DS3DriveManager.swift: subsystem `.app`, category `.sync`
    - DS3SDK.swift: subsystem `.app`, category `.auth`
    - AppStatusManager.swift: subsystem `.app`, category `.app`
    - SharedData.swift: subsystem `.app`, category `.metadata`

    DS3DriveProvider files:
    - FileProviderExtension.swift: subsystem `.provider`, category `.extension`
    - S3Enumerator.swift: subsystem `.provider`, category `.sync`
    - S3Lib.swift: subsystem `.provider`, category `.transfer`
    - S3Item.swift: subsystem `.provider`, category `.sync` (if it has a logger)
    - NotificationsManager.swift: subsystem `.provider`, category `.extension`

    DS3Drive files:
    - DS3DriveApp.swift: subsystem `.app`, category `.app`
    - Any View files that log: subsystem `.app`, category `.app`

    **Step 4 -- Replace all print() calls with appropriate logger calls:**
    Search for `print(` across all Swift files. Replace each with the appropriate logger call at the correct level:
    - Debug/trace prints -> `logger.debug(...)`
    - Error prints -> `logger.error(...)`
    - Status prints -> `logger.info(...)`
    The specific instance mentioned in CONTEXT.md is DS3DriveManager.swift:190.

    **Step 5 -- Apply standardized log levels throughout:**
    Review existing `.log(...)` and `.info(...)` calls, ensure they follow the convention:
    - `.debug` for flow details
    - `.info` for milestones
    - `.notice` for state changes
    - `.warning` for recoverable issues
    - `.error` for failures

    Note: Use `privacy: .public` for non-sensitive data like file names and identifiers that are useful for debugging. Use default (private) for potentially sensitive data like user emails, API keys, endpoints.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-sync && grep -r "print(" DS3Lib/Sources/ DS3DriveProvider/ DS3Drive/ --include="*.swift" | grep -v "// " | grep -v "privacy:" | wc -l | tr -d ' ' | xargs -I{} test {} -eq 0 && echo "No print() calls found" || echo "FAIL: print() calls still exist"</automated>
  </verify>
  <done>LogCategory and LogSubsystem enums exist in DefaultSettings.swift. Every Logger instance uses correct subsystem and category. No print() calls remain. Log levels follow documented convention.</done>
</task>

<task type="auto">
  <name>Task 2: Fix known code quality bugs</name>
  <files>
    DS3DriveProvider/S3Lib.swift
    DS3DriveProvider/S3Enumerator.swift
    DS3DriveProvider/FileProviderExtension.swift
    DS3DriveProvider/FileProviderExtension+Errors.swift
  </files>
  <action>
    **Bug 1 -- copyFolder early return (S3Lib.swift ~line 374):**
    In the `copyFolder` method, find the `while !items.isEmpty` loop. The line:
    ```swift
    return try await self.copyS3Item(item, toKey: newKey, withProgress: progress)
    ```
    Remove the `return` keyword so it becomes:
    ```swift
    try await self.copyS3Item(item, toKey: newKey, withProgress: progress)
    ```
    This ensures all items in the folder are copied, not just the first one.

    **Bug 2 -- EnumeratorError typo (S3Enumerator.swift):**
    Rename `case unsopported` to `case unsupported` in the EnumeratorError enum.
    Update all references to `EnumeratorError.unsopported` to `EnumeratorError.unsupported` (in S3Enumerator.swift and FileProviderExtension.swift).

    **Bug 3 -- Empty catch blocks:**
    Search for `catch {` followed by `}` with no body (or only whitespace) across all files. Add appropriate `logger.error("...")` calls in each empty catch block, including the error description: `logger.error("Operation failed: \(error.localizedDescription)")`.

    **Bug 4 -- Force-unwrapped removingPercentEncoding:**
    In S3Lib.swift, find any `removingPercentEncoding!` usages and replace with safe unwrapping:
    ```swift
    guard let decoded = key.removingPercentEncoding else {
        logger.error("Failed to decode percent encoding for key: \(key, privacy: .public)")
        throw FileProviderExtensionError.parseError
    }
    ```

    **Build verification:**
    Run `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` to confirm all fixes compile.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-sync && grep -n "unsopported" DS3DriveProvider/*.swift | wc -l | tr -d ' ' | xargs -I{} test {} -eq 0 && echo "Typo fixed" && xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO 2>&1 | tail -3</automated>
  </verify>
  <done>copyFolder processes all items (no early return). EnumeratorError.unsupported spelled correctly. No empty catch blocks. Force-unwrapped removingPercentEncoding replaced with safe unwrapping. Project builds cleanly.</done>
</task>

</tasks>

<verification>
- `grep -rn "print(" DS3Lib/Sources/ DS3DriveProvider/ DS3Drive/ --include="*.swift"` returns 0 results (excluding comments)
- `grep -rn "unsopported" DS3DriveProvider/` returns 0 results
- `grep -rn "LogCategory" DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift` shows the enum
- `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` succeeds
</verification>

<success_criteria>
- LogCategory enum defines 6 categories: sync, auth, transfer, extension, app, metadata
- LogSubsystem defines exactly 2 subsystems: io.cubbit.DS3Drive, io.cubbit.DS3Drive.provider
- Every Logger instance in the codebase uses LogSubsystem and LogCategory
- Zero print() calls remain in production code
- All 4 known bugs fixed (copyFolder, typo, empty catches, force-unwraps)
- Project builds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
