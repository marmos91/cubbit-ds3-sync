---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DS3Drive.xcodeproj/project.pbxproj
  - DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3Drive.xcscheme
  - DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3DriveProvider.xcscheme
  - DS3Drive.xcodeproj/project.xcworkspace/contents.xcworkspacedata
  - DS3Drive/DS3DriveApp.swift
  - DS3Drive/Info.plist
  - DS3Drive/DS3Drive.entitlements
  - DS3DriveProvider/Info.plist
  - DS3DriveProvider/DS3DriveProvider.entitlements
  - DS3Lib/Package.swift
  - DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
  - DS3Lib/Sources/DS3Lib/Constants/URLs.swift
  - DS3Lib/Sources/DS3Lib/Utils/Notifications+Extensions.swift
  - .github/workflows/build.yml
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "App launches as 'DS3 Drive' with bundle identifier io.cubbit.DS3Drive"
    - "File Provider extension has bundle identifier io.cubbit.DS3Drive.provider"
    - "App Group identifier is group.io.cubbit.DS3Drive throughout all entitlement files"
    - "DS3Lib is a local Swift Package with Package.swift declaring SotoS3 and swift-atomics dependencies"
    - "Project builds successfully with xcodebuild targeting macOS 15"
    - "CI workflow references new project name and scheme"
  artifacts:
    - path: "DS3Drive.xcodeproj/project.pbxproj"
      provides: "Xcode project with renamed targets and SPM local package reference"
      contains: "io.cubbit.DS3Drive"
    - path: "DS3Lib/Package.swift"
      provides: "Swift Package manifest for DS3Lib"
      contains: "SotoS3"
    - path: "DS3Drive/DS3DriveApp.swift"
      provides: "Main app entry point renamed from CubbitDS3SyncApp"
      contains: "DS3DriveApp"
    - path: "DS3Drive/DS3Drive.entitlements"
      provides: "App entitlements with new App Group"
      contains: "group.io.cubbit.DS3Drive"
    - path: ".github/workflows/build.yml"
      provides: "Updated CI workflow"
      contains: "DS3Drive"
  key_links:
    - from: "DS3Drive.xcodeproj/project.pbxproj"
      to: "DS3Lib/Package.swift"
      via: "local package reference"
      pattern: "DS3Lib"
    - from: "DS3Drive/DS3Drive.entitlements"
      to: "DS3DriveProvider/DS3DriveProvider.entitlements"
      via: "shared App Group identifier"
      pattern: "group.io.cubbit.DS3Drive"
---

<objective>
Rename the app from "CubbitDS3Sync" to "DS3 Drive", convert DS3Lib to a local Swift Package, raise deployment target to macOS 15, and update all identifiers, entitlements, CI, and project structure.

Purpose: This is the foundational structural change that every subsequent plan depends on. File paths, bundle IDs, and project structure must be finalized first.
Output: A buildable Xcode project named DS3Drive.xcodeproj with DS3Lib as a local SPM package, targeting macOS 15.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Current DefaultSettings constants that must be renamed -->
From DS3Lib/Constants/DefaultSettings.swift:
```swift
static let appGroup = "group.io.cubbit.CubbitDS3Sync"
static let apiKeyNamePrefix = "DS3Sync-for-macOS"
enum UserDefaultsKeys {
    static let appUUID = "io.cubbit.CubbitDS3Sync.userDefaults.appUUID"
    static let tutorial = "io.cubbit.CubbitDS3Sync.userDefaults.tutorialShown"
    static let syncAnchor = "io.cubbit.CubbitDS3Sync.userDefaults.syncAnchor"
    static let loginItemSet = "io.cubbit.CubbitDS3Sync.userDefaults.loginItemSet"
}
enum Notifications {
    static let driveStatusChanged = "io.cubbit.CubbitDS3Sync.notifications.driveStatusChanged"
    static let driveTransferStats = "io.cubbit.CubbitDS3Sync.notifications.driveTransferStats"
}
```

<!-- Current project structure to rename -->
CubbitDS3Sync/ -> DS3Drive/
  CubbitDS3SyncApp.swift -> DS3DriveApp.swift
  CubbitDS3Sync.entitlements -> DS3Drive.entitlements
Provider/ -> DS3DriveProvider/
  Provider.entitlements -> DS3DriveProvider.entitlements
CubbitDS3Sync.xcodeproj -> DS3Drive.xcodeproj
  xcschemes/CubbitDS3Sync.xcscheme -> DS3Drive.xcscheme
  xcschemes/Provider.xcscheme -> DS3DriveProvider.xcscheme
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories, files, and convert DS3Lib to SPM</name>
  <files>
    DS3Drive/ (renamed from CubbitDS3Sync/)
    DS3Drive/DS3DriveApp.swift (renamed from CubbitDS3SyncApp.swift)
    DS3Drive/DS3Drive.entitlements (renamed from CubbitDS3Sync.entitlements)
    DS3Drive/Info.plist
    DS3DriveProvider/ (renamed from Provider/)
    DS3DriveProvider/DS3DriveProvider.entitlements (renamed from Provider.entitlements)
    DS3DriveProvider/Info.plist
    DS3Lib/Package.swift (NEW)
    DS3Lib/Sources/DS3Lib/ (restructured from DS3Lib/)
    DS3Lib/Tests/DS3LibTests/ (NEW placeholder)
  </files>
  <action>
    **Step 1 -- Rename directories using git mv:**
    - `git mv CubbitDS3Sync DS3Drive`
    - `git mv Provider DS3DriveProvider`
    - `git mv DS3Drive/CubbitDS3SyncApp.swift DS3Drive/DS3DriveApp.swift`
    - `git mv DS3Drive/CubbitDS3Sync.entitlements DS3Drive/DS3Drive.entitlements`
    - `git mv DS3DriveProvider/Provider.entitlements DS3DriveProvider/DS3DriveProvider.entitlements`
    - `git mv DS3DriveProvider/Provider.xcassets DS3DriveProvider/DS3DriveProvider.xcassets`

    **Step 2 -- Restructure DS3Lib for SPM:**
    - Create `DS3Lib/Sources/DS3Lib/` directory
    - Move all .swift files from `DS3Lib/` into `DS3Lib/Sources/DS3Lib/` preserving subdirectory structure (Constants/, Models/, SharedData/, Utils/)
    - Remove `DS3Lib/DS3Lib.h` (Objective-C bridging header not needed for SPM)
    - Create `DS3Lib/Tests/DS3LibTests/` directory with a placeholder test file
    - Create `DS3Lib/Package.swift`:
      ```swift
      // swift-tools-version: 5.9
      import PackageDescription
      let package = Package(
          name: "DS3Lib",
          platforms: [.macOS(.v15)],
          products: [.library(name: "DS3Lib", targets: ["DS3Lib"])],
          dependencies: [
              .package(url: "https://github.com/soto-project/soto", from: "6.8.0"),
              .package(url: "https://github.com/apple/swift-atomics.git", from: "1.2.0"),
          ],
          targets: [
              .target(name: "DS3Lib", dependencies: [
                  .product(name: "SotoS3", package: "soto"),
                  .product(name: "Atomics", package: "swift-atomics"),
              ]),
              .testTarget(name: "DS3LibTests", dependencies: ["DS3Lib"]),
          ]
      )
      ```

    **Step 3 -- Rename Swift types inside files:**
    - In DS3DriveApp.swift: rename `struct CubbitDS3SyncApp` to `struct DS3DriveApp`
    - In all Swift files under DS3Drive/: find-and-replace type name `CubbitDS3Sync` with `DS3Drive` (only type references, not string literals that will be handled in constants)

    **Step 4 -- Update entitlements:**
    - In DS3Drive/DS3Drive.entitlements: change App Group to `group.io.cubbit.DS3Drive`
    - In DS3DriveProvider/DS3DriveProvider.entitlements: change App Group to `group.io.cubbit.DS3Drive`

    **Step 5 -- Update Info.plist files:**
    - DS3Drive/Info.plist: set CFBundleDisplayName to "DS3 Drive", CFBundleIdentifier to `io.cubbit.DS3Drive`, NSHumanReadableCopyright to "Copyright Cubbit"
    - DS3DriveProvider/Info.plist: set CFBundleIdentifier to `io.cubbit.DS3Drive.provider`, update NSExtension host app identifier

    **Step 6 -- Update constants in DefaultSettings.swift:**
    - `appGroup` -> `"group.io.cubbit.DS3Drive"`
    - `apiKeyNamePrefix` -> `"DS3Drive-for-macOS"`
    - `UserDefaultsKeys.appUUID` -> `"io.cubbit.DS3Drive.userDefaults.appUUID"`
    - `UserDefaultsKeys.tutorial` -> `"io.cubbit.DS3Drive.userDefaults.tutorialShown"`
    - `UserDefaultsKeys.syncAnchor` -> `"io.cubbit.DS3Drive.userDefaults.syncAnchor"`
    - `UserDefaultsKeys.loginItemSet` -> `"io.cubbit.DS3Drive.userDefaults.loginItemSet"`
    - `Notifications.driveStatusChanged` -> `"io.cubbit.DS3Drive.notifications.driveStatusChanged"`
    - `Notifications.driveTransferStats` -> `"io.cubbit.DS3Drive.notifications.driveTransferStats"`

    **Important:** All Logger subsystem strings (e.g., `"io.cubbit.CubbitDS3Sync.provider"`) will be updated in Plan 02. For now, leave them as-is -- they will still work, just with old subsystem names.
  </action>
  <verify>
    <automated>ls DS3Drive/DS3DriveApp.swift && ls DS3DriveProvider/DS3DriveProvider.entitlements && ls DS3Lib/Package.swift && ls DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift && ! ls CubbitDS3Sync 2>/dev/null && ! ls Provider 2>/dev/null && echo "Directory structure verified"</automated>
  </verify>
  <done>All directories renamed, DS3Lib restructured for SPM with Package.swift, all entitlements and Info.plist files updated with new identifiers, DefaultSettings constants renamed</done>
</task>

<task type="auto">
  <name>Task 2: Update Xcode project, schemes, CI, and verify build</name>
  <files>
    DS3Drive.xcodeproj/project.pbxproj (renamed from CubbitDS3Sync.xcodeproj)
    DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3Drive.xcscheme
    DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3DriveProvider.xcscheme
    DS3Drive.xcodeproj/project.xcworkspace/contents.xcworkspacedata
    .github/workflows/build.yml
    README.md
    CLAUDE.md
  </files>
  <action>
    **Step 1 -- Rename xcodeproj:**
    - `git mv CubbitDS3Sync.xcodeproj DS3Drive.xcodeproj`
    - `git mv DS3Drive.xcodeproj/xcshareddata/xcschemes/CubbitDS3Sync.xcscheme DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3Drive.xcscheme`
    - `git mv DS3Drive.xcodeproj/xcshareddata/xcschemes/Provider.xcscheme DS3Drive.xcodeproj/xcshareddata/xcschemes/DS3DriveProvider.xcscheme`

    **Step 2 -- Update project.pbxproj (raw text find-and-replace):**
    This is a critical step. The pbxproj is a plist-like text file. Perform these replacements carefully:
    - Replace all `CubbitDS3Sync/` path references with `DS3Drive/` (source file paths)
    - Replace all `Provider/` path references with `DS3DriveProvider/` (source file paths)
    - Replace `CubbitDS3SyncApp.swift` with `DS3DriveApp.swift`
    - Replace `CubbitDS3Sync.entitlements` with `DS3Drive.entitlements`
    - Replace `Provider.entitlements` with `DS3DriveProvider.entitlements`
    - Replace `Provider.xcassets` with `DS3DriveProvider.xcassets`
    - Replace product name "CubbitDS3Sync" with "DS3Drive"
    - Replace product name "Provider" with "DS3DriveProvider" (target product names only, not all occurrences)
    - Replace bundle identifier `io.cubbit.CubbitDS3Sync` with `io.cubbit.DS3Drive` (in build settings)
    - Replace bundle identifier `io.cubbit.CubbitDS3Sync.provider` with `io.cubbit.DS3Drive.provider`
    - Replace App Group `group.io.cubbit.CubbitDS3Sync` with `group.io.cubbit.DS3Drive`
    - Remove the DS3Lib framework target references and replace with a local package reference to `DS3Lib/`
    - Remove SotoS3 and swift-atomics as project-level SPM dependencies (they are now declared in DS3Lib/Package.swift)
    - Add DS3Lib local package to both DS3Drive and DS3DriveProvider targets as a dependency
    - Set MACOSX_DEPLOYMENT_TARGET to 15.0 for all targets
    - Set SWIFT_VERSION to 5.0 (keep for now, Swift 6 mode enabled in Plan 04)

    **Step 3 -- Update scheme files:**
    - In DS3Drive.xcscheme: update BuildableReference `BlueprintName` and `BuildableName` from "CubbitDS3Sync" to "DS3Drive"
    - In DS3DriveProvider.xcscheme: update references from "Provider" to "DS3DriveProvider"
    - In both: update `ReferencedContainer` from "CubbitDS3Sync.xcodeproj" to "DS3Drive.xcodeproj"

    **Step 4 -- Update workspace data:**
    - In contents.xcworkspacedata: update self-reference from "CubbitDS3Sync.xcodeproj" to "DS3Drive.xcodeproj"

    **Step 5 -- Update CI workflow (.github/workflows/build.yml):**
    - Update scheme references from "CubbitDS3Sync" to "DS3Drive"
    - Update `runs-on` from `macos-13` to `macos-15` (needed for macOS 15 SDK)
    - Update Xcode version from `15.2` to `16.2` (latest stable for macOS 15 SDK)
    - Update secret names if needed (provisioning profile names may reference CubbitDS3)
    - Update any file references from `CubbitDS3Sync` to `DS3Drive`

    **Step 6 -- Update README.md:**
    - Replace all mentions of "CubbitDS3Sync" with "DS3 Drive"
    - Update project file references
    - Update build instructions

    **Step 7 -- Update CLAUDE.md:**
    - Replace all mentions of "CubbitDS3Sync" with "DS3 Drive"
    - Update all file paths (CubbitDS3Sync/ -> DS3Drive/, Provider/ -> DS3DriveProvider/)
    - Update bundle identifiers and App Group references
    - Update build command to reference DS3Drive scheme

    **Step 8 -- Build verification:**
    - Run `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` to verify the rename + SPM conversion compiles
    - Fix any "File not found", "No such module", or reference errors that appear
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-sync && xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>Project builds successfully as DS3Drive.xcodeproj with DS3Lib as local SPM package, macOS 15 deployment target, all references updated in pbxproj/schemes/CI/README/CLAUDE.md, zero build errors</done>
</task>

</tasks>

<verification>
- `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3Drive -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` succeeds
- No file references to `CubbitDS3Sync` in pbxproj (except possibly in comments or git history)
- `grep -r "group.io.cubbit.CubbitDS3Sync" DS3Drive/ DS3DriveProvider/ DS3Drive.xcodeproj/` returns no results
- DS3Lib/Package.swift exists and declares SotoS3 and swift-atomics dependencies
- No old directories exist (CubbitDS3Sync/, Provider/)
</verification>

<success_criteria>
- App target builds as "DS3 Drive" with io.cubbit.DS3Drive bundle ID
- Extension target builds as "DS3DriveProvider" with io.cubbit.DS3Drive.provider bundle ID
- DS3Lib compiles as a local Swift Package
- All entitlements reference group.io.cubbit.DS3Drive
- CI workflow updated for new names
- Zero build errors on macOS 15 target
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
