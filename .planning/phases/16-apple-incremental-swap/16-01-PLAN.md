---
phase: 16-apple-incremental-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apple/DS3Lib/Package.swift
  - apple/DS3Drive.xcodeproj/project.pbxproj
  - .github/workflows/build.yml
  - apple/DS3Lib/Tests/DS3LibTests/DS3CoreFFISmokeTests.swift
autonomous: false
estimated_minutes: 90
requirements: [APPLE-01]
tags: [apple, ffi, uniffi, xcode, ci, xcframework]

must_haves:
  truths:
    - "`import DS3CoreFFI` compiles in DS3Lib without errors"
    - "Xcode Run Script Phase invokes `core/scripts/build-xcframework.sh` before Compile Sources on every build for Debug AND Release configurations"
    - "Cargo profile flag follows $CONFIGURATION (Debug → --debug, Release → --release) per D-10"
    - "CI builds the XCFramework before invoking xcodebuild on both macOS and iOS jobs"
    - "A trivial smoke test in DS3LibTests references `DS3SessionHandle.self` and links successfully"
    - "UniFFI generated `Ds3Error` shape is verified — Assumption A1 (flat_error) and A10 (Sendable) resolved"
  artifacts:
    - path: "apple/DS3Lib/Package.swift"
      provides: "DS3CoreFFI binary target declaration"
      contains: ".binaryTarget(name: \"DS3CoreFFI\""
    - path: "apple/DS3Drive.xcodeproj/project.pbxproj"
      provides: "Run Script Phase invoking build-xcframework.sh"
      contains: "build-xcframework.sh"
    - path: ".github/workflows/build.yml"
      provides: "XCFramework prebuild step on macOS + iOS jobs"
      contains: "build-xcframework.sh"
    - path: "apple/DS3Lib/Tests/DS3LibTests/DS3CoreFFISmokeTests.swift"
      provides: "Compile-link smoke test"
      contains: "import DS3CoreFFI"
  key_links:
    - from: "apple/DS3Lib/Package.swift"
      to: "core/out/DS3CoreFFI.xcframework"
      via: ".binaryTarget(path:)"
      pattern: "binaryTarget.*DS3CoreFFI.*core/out"
    - from: "apple/DS3Drive.xcodeproj/project.pbxproj"
      to: "core/scripts/build-xcframework.sh"
      via: "Xcode Run Script Phase shell invocation"
      pattern: "build-xcframework.sh"

user_setup:
  - service: rustup-targets
    why: "Cross-compile XCFramework for all Apple slices"
    dashboard_config:
      - task: "rustup target add aarch64-apple-darwin aarch64-apple-ios aarch64-apple-ios-sim x86_64-apple-ios"
        location: "Local shell"
---

<objective>
Wire the Phase 15 XCFramework into the Apple build system so that DS3Lib (and any target linking it) can `import DS3CoreFFI`. Establish the Xcode Run Script Phase that invokes `core/scripts/build-xcframework.sh` on every build, add the SPM `.binaryTarget`, and extend CI to build the XCFramework before xcodebuild. Resolve Assumptions A1 (UniFFI flat_error), A2 (aws-sdk-s3 retry), and A10 (Sendable) by reading the generated `core/out/DS3CoreFFI.swift` — these inform downstream plans.

Purpose: Plans 02-07 depend on the build wiring existing. No swap can begin until `import DS3CoreFFI` compiles, the script runs reliably from Xcode and CI, and the FFI surface shape (flat_error vs full enum) is known.

Output: Functional Xcode build with Rust integrated, CI green, smoke test linking the framework, and a documented decision on flat_error/Sendable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-apple-incremental-swap/16-CONTEXT.md
@.planning/phases/16-apple-incremental-swap/16-RESEARCH.md
@.planning/phases/16-apple-incremental-swap/16-PATTERNS.md
@apple/DS3Lib/Package.swift
@core/scripts/build-xcframework.sh
@.github/workflows/build.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify FFI assumptions A1, A2, A10 by inspecting generated Swift bindings</name>
  <files>.planning/phases/16-apple-incremental-swap/16-FFI-AUDIT.md</files>
  <read_first>
    - core/out/DS3CoreFFI.xcframework (run `ls -la` to confirm exists from Phase 15)
    - core/ds3-models/src/error.rs (DS3Error enum + `#[uniffi(flat_error)]` attribute + code() table at lines 97-117)
    - core/ds3-ffi/src/uniffi_exports.rs (DS3SessionHandle struct decl lines 36-40)
    - core/ds3-s3/src/client.rs (aws-sdk-s3 builder; verify retry behavior_version_latest() at line 59)
    - core/ds3-http/src/client.rs (reqwest builder, no retry currently at lines 28-34)
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Assumptions Log" (A1, A2, A3, A10)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"Shared Patterns: Pattern 6"
  </read_first>
  <action>
    Build the XCFramework manually first: `cd core && ./scripts/build-xcframework.sh --debug`. Then extract the generated Swift bindings: locate `core/out/DS3CoreFFI.xcframework/macos-arm64/DS3CoreFFI.framework/Headers/` OR the standalone generated `.swift` file (the script writes it during bindgen).

    For each assumption, grep the generated Swift code and record the answer in a new `.planning/phases/16-apple-incremental-swap/16-FFI-AUDIT.md`:

    **A1 (flat_error):** Search the generated Swift for `Ds3Error` enum definition. Record whether each variant has an associated payload OR whether the enum is collapsed to a single `description` string. Decision: if flat, Plan 02 MUST add `ds3_error_code` free function. If full, switch on case directly.

    **A2 (aws-sdk-s3 retry):** Read `core/ds3-s3/src/client.rs` and verify `behavior_version_latest()` is set on the client builder. If a `MAX_RETRIES` constant exists but is not applied to `RetryConfig`, document this — Plan 02 must wire it.

    **A10 (Sendable on DS3SessionHandle):** Search generated Swift for `extension DS3SessionHandle: Sendable` or `: @unchecked Sendable`. Record presence/absence. If absent, Plan 03 must add `extension DS3SessionHandle: @unchecked Sendable {}` adjacent to its first use.

    Record final findings (one paragraph per assumption) plus any newly observed risks. This document seeds Plan 02 task signatures.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-apple-incremental-swap/16-FFI-AUDIT.md && grep -E "^## A(1|2|10)" .planning/phases/16-apple-incremental-swap/16-FFI-AUDIT.md | wc -l | grep -q "3"</automated>
  </verify>
  <done>
    16-FFI-AUDIT.md exists with sections for A1, A2, A10; each contains a verdict (verified/needs-fix) and a concrete next-step reference for Plan 02 or 03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .binaryTarget to Package.swift and Xcode Run Script Phase</name>
  <files>apple/DS3Lib/Package.swift, apple/DS3Drive.xcodeproj/project.pbxproj</files>
  <read_first>
    - apple/DS3Lib/Package.swift (current 35 lines — full file)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"Package.swift (SPM manifest)" + §"Xcode Run Script Phase (NEW — no in-repo analog)"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Pattern 3: Xcode Run Script Phase" (lines 301-336) + §"Code Examples — Run Script Phase body" (lines 618-640)
    - core/scripts/build-xcframework.sh (confirm `--debug`/`--release` flags accepted)
  </read_first>
  <action>
    **Edit `apple/DS3Lib/Package.swift`** (per D-07 + PATTERNS.md §"Package.swift"):
    - Add `.binaryTarget(name: "DS3CoreFFI", path: "../../core/out/DS3CoreFFI.xcframework")` to `targets` array
    - Add `"DS3CoreFFI"` as a dependency to the `DS3Lib` target's `dependencies` array
    - **Do NOT remove Soto or swift-nio yet** — those are deleted in Plan 05 atomically. Phase 16 must compile through every intermediate state.

    **Edit `apple/DS3Drive.xcodeproj/project.pbxproj`** to add a Run Script Phase to THREE targets: DS3Drive (macOS app), DS3DriveApp (iOS app), and DS3DriveProvider (extension). For each target, the phase must appear **before** "Compile Sources" in `buildPhases`.

    Use the exact script body from PATTERNS.md §"Xcode Run Script Phase" (the 8-line shell script that sets PATH, maps $CONFIGURATION to PROFILE_FLAG, and execs `${SRCROOT}/../core/scripts/build-xcframework.sh $PROFILE_FLAG`).

    Configuration values per PATTERNS.md table:
    - shellPath: `/bin/bash`
    - inputPaths: `["$(SRCROOT)/../core/scripts/build-xcframework.sh"]`
    - outputPaths: `["$(SRCROOT)/../core/out/DS3CoreFFI.xcframework/Info.plist"]`
    - runOnlyForDeploymentPostprocessing: 0 (must run every build per D-09)
    - showEnvVarsInLog: 1
    - name: `"Build DS3CoreFFI XCFramework"`

    pbxproj edits are XML-tree changes. Use the Edit tool with surgical replacements: insert a new PBXShellScriptBuildPhase object, then add its UUID to the target's `buildPhases` array immediately before the Sources phase UUID.

    Add to `.gitignore` (root): `core/out/` (Phase 15 may have done this — verify, skip if present).
  </action>
  <verify>
    <automated>grep -c "binaryTarget(name: \"DS3CoreFFI\"" apple/DS3Lib/Package.swift | grep -q "1" && grep -c "build-xcframework.sh" apple/DS3Drive.xcodeproj/project.pbxproj | awk '$1 >= 3 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Package.swift declares DS3CoreFFI binary target; pbxproj contains 3+ references to build-xcframework.sh (one per target); Xcode can resolve the package without errors (`xcodebuild -resolvePackageDependencies -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive"` exits 0).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DS3CoreFFI smoke test and run xcodebuild verification</name>
  <files>apple/DS3Lib/Tests/DS3LibTests/DS3CoreFFISmokeTests.swift</files>
  <read_first>
    - apple/DS3Lib/Tests/DS3LibTests/ (list existing tests for XCTest pattern)
    - .planning/phases/16-apple-incremental-swap/16-FFI-AUDIT.md (output from Task 1)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"Shared Patterns: Pattern 6 — Sendable + UniFFI Object Storage"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Pitfall 5"
  </read_first>
  <action>
    Create `apple/DS3Lib/Tests/DS3LibTests/DS3CoreFFISmokeTests.swift`:
    - `import XCTest`
    - `import DS3CoreFFI`
    - One test class `DS3CoreFFISmokeTests: XCTestCase`
    - One test `func testCanReferenceSessionHandleType()` — asserts `_ = DS3SessionHandle.self` (just compile-link check, no instantiation)
    - One test `func testConflictKeyFreeFunction()` — calls the existing `conflictKey(originalKey:hostname:nonce:)` free function (UniFFI free fn from Phase 15 per RESEARCH §"Plan Structure Recommendation") with `originalKey: "foo/bar.txt"`, `hostname: "test"`, `nonce: nil` and asserts the result is a non-empty String containing `"Conflict on test"` substring.

    If the FFI-AUDIT (Task 1) recorded `DS3SessionHandle` lacks `Sendable`, add to this test file (above the class):
    ```swift
    extension DS3SessionHandle: @unchecked Sendable {}
    ```

    Run `xcodebuild test -project apple/DS3Drive.xcodeproj -scheme DS3Lib -destination 'platform=macOS' -only-testing DS3LibTests/DS3CoreFFISmokeTests`. The Run Script Phase must fire (verify in the build log that the script ran), and both tests must pass.

    If `xcodebuild` cannot find cargo (Pitfall 4), update the Run Script Phase's exported PATH to also include `/usr/local/bin` and any developer-installed Rust toolchain location. Re-run.
  </action>
  <verify>
    <automated>xcodebuild test -project apple/DS3Drive.xcodeproj -scheme DS3Lib -destination 'platform=macOS' -only-testing:DS3LibTests/DS3CoreFFISmokeTests 2>&1 | grep -E "Test Suite.*DS3CoreFFISmokeTests.*passed" | head -1 | grep -q "passed"</automated>
  </verify>
  <done>
    Smoke test passes via xcodebuild; build log shows "Build DS3CoreFFI XCFramework" Run Script Phase fired; `core/out/DS3CoreFFI.xcframework/Info.plist` mtime updates after build.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add XCFramework prebuild step to CI workflow (macOS + iOS jobs)</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .github/workflows/build.yml (full file — locate macos-build job around line 60+ and ios-build job around line 130+)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §".github/workflows/build.yml CI integration"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §Pitfall 9 + §"Environment Availability"
  </read_first>
  <action>
    Edit `.github/workflows/build.yml`. For BOTH the macOS-build job and the iOS-build job, add steps immediately BEFORE the `xcodebuild -resolvePackageDependencies` step:

    1. `Install Rust toolchain` (use `dtolnay/rust-toolchain@stable` action if not already present from Phase 15)
    2. `Install Rust targets`:
       ```yaml
       - name: Install Rust targets
         run: rustup target add aarch64-apple-darwin aarch64-apple-ios aarch64-apple-ios-sim x86_64-apple-ios
       ```
    3. `Build DS3CoreFFI XCFramework`:
       ```yaml
       - name: Build DS3CoreFFI XCFramework
         working-directory: core
         run: ./scripts/build-xcframework.sh --release
       ```
    4. `Verify XCFramework exists`:
       ```yaml
       - name: Verify XCFramework exists
         run: test -d core/out/DS3CoreFFI.xcframework && ls -la core/out/DS3CoreFFI.xcframework
       ```

    If Phase 15 already added Rust toolchain setup at the workflow level, only steps 2-4 are needed. Verify by reading the file first.

    Do NOT bump `cargo test --workspace --lib` to `--lib --tests` yet — that change belongs to Plan 06 (CI parity gate) where fixtures arrive.
  </action>
  <verify>
    <automated>grep -c "build-xcframework.sh --release" .github/workflows/build.yml | awk '$1 >= 2 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Both macOS and iOS CI jobs invoke `build-xcframework.sh --release` before xcodebuild; rustup targets are installed; verification step lists the XCFramework directory.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification — CI green on a draft PR</name>
  <what-built>
    XCFramework wired into SPM (`Package.swift` binary target), Xcode Run Script Phase added to 3 targets, CI extended with cargo + rustup + XCFramework build steps, smoke test passing locally.
  </what-built>
  <how-to-verify>
    1. Push a draft PR with the changes from Tasks 1-4 to a new branch (e.g. `gsd/phase-16-plan-01-xcframework-wiring`).
    2. Confirm GitHub Actions `macos-build` job runs the new steps in order: rustup → build-xcframework.sh --release → verify → xcodebuild. Check the job log shows the XCFramework was created (size > 0).
    3. Confirm `ios-build` job does the same.
    4. Confirm `rust-check` job remains green (unchanged from Phase 15).
    5. Run a fresh clone smoke test on a colleague's machine (or a clean DerivedData): `git clone`, open `apple/DS3Drive.xcodeproj`, hit Cmd+B for the `DS3 Drive` scheme. Verify the "Build DS3CoreFFI XCFramework" Run Script Phase appears in the build log and completes without error.
    6. Verify `core/out/DS3CoreFFI.xcframework` exists after the build.
    7. Open Xcode's Test Navigator, run only `DS3CoreFFISmokeTests` — both tests pass.
  </how-to-verify>
  <resume-signal>Type "approved" once CI is green on the draft PR and both local smoke tests pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev shell → Xcode build script | Xcode runs script with stripped PATH; cargo path must be explicit |
| GitHub Actions runner → Cubbit credentials | Phase 15 secrets remain; no new secrets added |
| .binaryTarget local path → SPM resolution | Relative path traversal `../../core/out/DS3CoreFFI.xcframework` must resolve from `apple/DS3Lib/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | Xcode Run Script PATH manipulation | mitigate | Script exports a fixed PATH (`$HOME/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:$PATH`) and uses `set -euo pipefail`; no PATH derived from user-supplied env |
| T-16-01-02 | Denial of Service | XCFramework path drift between local + CI | mitigate | "Verify XCFramework exists" step before `xcodebuild`; output is asserted both in CI and in the input/output declarations of the Run Script Phase |
| T-16-01-03 | Information Disclosure | Smoke test logs may print error strings from Rust panic | accept | Tests only invoke pure functions (`conflictKey`); no credentials touched in this plan |
| T-16-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; only existing Phase 15 crates rebuilt |
</threat_model>

<verification>
- `import DS3CoreFFI` compiles in `apple/DS3Lib/Tests/DS3LibTests/DS3CoreFFISmokeTests.swift`
- `xcodebuild test -scheme DS3Lib -only-testing:DS3LibTests/DS3CoreFFISmokeTests` exits 0
- Run Script Phase fires on every build (verified by build log inspection in Task 5)
- CI macOS + iOS jobs run XCFramework prebuild before xcodebuild and both succeed on draft PR
- FFI-AUDIT.md records concrete next-steps for Plans 02 and 03 (Assumption A1, A2, A10 verdicts)
</verification>

<success_criteria>
- Package.swift contains exactly one `.binaryTarget(name: "DS3CoreFFI"` declaration
- pbxproj contains exactly 3 `PBXShellScriptBuildPhase` entries referencing `build-xcframework.sh`, attached to the DS3Drive, DS3DriveApp, and DS3DriveProvider targets
- build.yml `macos-build` AND `ios-build` jobs each invoke `build-xcframework.sh --release` before xcodebuild
- DS3CoreFFISmokeTests passes via `xcodebuild test` on macOS destination
- 16-FFI-AUDIT.md exists with verdicts for Assumptions A1, A2, A10
</success_criteria>

<output>
Create `.planning/phases/16-apple-incremental-swap/16-01-SUMMARY.md` documenting:
- Final `flat_error` decision (Plan 02 must implement `ds3_error_code` or not)
- aws-sdk-s3 retry default verdict (Plan 02 may need to wire MAX_RETRIES)
- `DS3SessionHandle` Sendable status (Plan 03 may need `@unchecked Sendable` extension)
- The exact Run Script Phase shell script body committed (so Plans 02-07 can reference it)
- CI workflow line numbers where prebuild steps were inserted
</output>
