---
phase: 16-apple-incremental-swap
plan: 07
type: execute
wave: 7
depends_on: [16-05, 16-06]
files_modified:
  - .planning/phases/16-apple-incremental-swap/SMOKE-TEST-RESULTS.md
  - apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedS3IntegrationTests.swift
  - apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedAuthIntegrationTests.swift
  - apple/DS3Lib/Package.swift
  - .github/workflows/build.yml
autonomous: false
estimated_minutes: 240
requirements: [APPLE-05]
tags: [apple, testing, integration, smoke-test, manual]

must_haves:
  truths:
    - "Side-by-side smoke test executed on real macOS + real Cubbit account, comparing pre-swap commit vs post-swap HEAD"
    - "All 14 flows from RESEARCH.md §Side-by-Side Smoke Test Plan exercised and recorded as Pass / Fail / Different in SMOKE-TEST-RESULTS.md"
    - "Byte-by-byte diff of `~/Library/Group Containers/group.X889956QSM.io.cubbit.DS3Drive/*.json` between pre-swap and post-swap runs shows zero schema drift (D-25 verified at runtime)"
    - "Integration test suite (RustBackedS3IntegrationTests + RustBackedAuthIntegrationTests) runs against the dedicated Cubbit test bucket (Phase 15 D-15) and passes"
    - "CI integration-test job runs the new suite on PRs during Phase 16 development (downgrades to nightly after merge)"
    - "2FA login path verified by a tester with a 2FA-enabled Cubbit account on both pre-swap and post-swap builds"
    - "Multipart upload of a 100MB file completes in the post-swap build with the same part count (20 parts at default 5MB), within ±20% timing of pre-swap"
  artifacts:
    - path: ".planning/phases/16-apple-incremental-swap/SMOKE-TEST-RESULTS.md"
      provides: "Detailed Pass/Fail tracker for all 14 smoke flows × 2 builds"
      contains: "Pre-swap"
    - path: "apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedS3IntegrationTests.swift"
      provides: "Integration tests exercising DS3S3Client end-to-end against real Cubbit S3"
      contains: "DS3_TEST_BUCKET"
    - path: "apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedAuthIntegrationTests.swift"
      provides: "Integration tests for login + refresh + forge against real Cubbit IAM"
      contains: "DS3_TEST_EMAIL"
  key_links:
    - from: "apple/DS3Lib/Tests/DS3LibIntegrationTests/"
      to: "Cubbit IAM + S3 (real)"
      via: "DS3_TEST_* env vars (Phase 15 D-15/D-16)"
      pattern: "ProcessInfo.processInfo.environment"
    - from: ".github/workflows/build.yml"
      to: "apple/DS3Lib/Tests/DS3LibIntegrationTests/"
      via: "xcodebuild test integration scheme with secrets passed as env vars"
      pattern: "DS3LibIntegrationTests"

user_setup:
  - service: cubbit-test-account
    why: "Manual side-by-side smoke + integration test execution"
    env_vars:
      - name: DS3_TEST_EMAIL
        source: "Phase 15 D-16 GitHub Secrets (existing)"
      - name: DS3_TEST_PASSWORD
        source: "Phase 15 D-16 GitHub Secrets"
      - name: DS3_TEST_BUCKET
        source: "Phase 15 D-15 dedicated test bucket"
      - name: DS3_TEST_TENANT
        source: "Phase 15 D-16"
      - name: DS3_TEST_2FA_SECRET (optional)
        source: "Tester's 2FA-enabled account TOTP secret — only for the 2FA smoke flow"
    dashboard_config:
      - task: "Verify dedicated test bucket exists with read+write API key"
        location: "Cubbit Console → Test project → Buckets"
---

<objective>
Execute the manual side-by-side smoke test required by APPLE-05 (D-24): build the pre-swap commit + the post-swap HEAD, run both against the same Cubbit account, exercise the 14 flows in RESEARCH §"Side-by-Side Smoke Test Plan", and prove identical user-visible behavior.

Add an integration test suite that exercises the new Rust-backed adapters against real Cubbit S3 (the dedicated test bucket established in Phase 15 D-15/D-16). Wire it into CI per RESEARCH §"Integration Test CI Schedule" recommendation (every PR during Phase 16, downgrade to nightly after merge).

This is the final gate before declaring Phase 16 complete. APPLE-05 cannot close without this plan.

Output: A committed SMOKE-TEST-RESULTS.md tracker with all flows recorded, an integration test suite running in CI, and final confirmation that pre-swap and post-swap builds produce identical Finder/Files.app behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-apple-incremental-swap/16-CONTEXT.md
@.planning/phases/16-apple-incremental-swap/16-RESEARCH.md
@.planning/phases/16-apple-incremental-swap/16-PATTERNS.md
@.planning/phases/16-apple-incremental-swap/16-05-SUMMARY.md
@.planning/phases/16-apple-incremental-swap/16-06-SUMMARY.md
@apple/DS3Lib/Package.swift
@.github/workflows/build.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Rust-backed integration test suite (S3 + Auth) gated on DS3_TEST_* env vars</name>
  <files>apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedS3IntegrationTests.swift, apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedAuthIntegrationTests.swift, apple/DS3Lib/Package.swift</files>
  <read_first>
    - apple/DS3Lib/Package.swift (current testTarget declarations)
    - apple/DS3Lib/Tests/DS3LibTests/ (existing unit-test patterns)
    - .planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md (D-15, D-16 — DS3_TEST_* env var pattern from Phase 15)
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Test Strategy" + §"Integration Test CI Schedule" lines 939-948
    - .planning/phases/16-apple-incremental-swap/16-CONTEXT.md D-23
  </read_first>
  <action>
    **Add a new test target in `apple/DS3Lib/Package.swift`** for integration tests, separated from the existing DS3LibTests so the unit-test fast-path stays cheap:

    ```swift
    .testTarget(
        name: "DS3LibIntegrationTests",
        dependencies: ["DS3Lib"]
    )
    ```

    Create the directory `apple/DS3Lib/Tests/DS3LibIntegrationTests/`.

    **Create `RustBackedS3IntegrationTests.swift`:**
    - XCTest test class gated by `setUp()` checking required env vars (DS3_TEST_EMAIL, DS3_TEST_PASSWORD, DS3_TEST_TENANT, DS3_TEST_BUCKET) — call `XCTSkip("integration tests require DS3_TEST_* env vars")` if missing
    - Helper `private func makeAuthenticatedClient() throws -> DS3S3Client` that performs a real login via DS3Authentication.login and constructs a DS3S3Client with the resulting handle + test credentials
    - Tests, each verifying one piece of the FFI swap end-to-end:
      - `testListObjectsReturnsExpectedKeys` — uploads 3 known objects, lists, asserts presence
      - `testHeadObjectReturnsETag` — head a known object, verify ETag normalized
      - `testUploadDownloadRoundTrip` — upload a 1MB random file via uploadObject, download via downloadObject, byte-compare local vs remote
      - `testMultipartUploadOf100MB` — generate 100MB random data, upload via putObjectMultipart, verify part count + ETag; download + verify bytes
      - `testGetObjectDataInMemory` — uploadObject, then getObjectData, byte-compare
      - `testPutObjectDataInMemory` — putObjectData with metadata, head and verify x-amz-meta-* headers
      - `testCopyObjectWithMetadataReplace` — copy with metadata: ["custom-key": "v1"], verify HEAD shows new metadata
      - `testDeleteObjectAndDeleteObjects` — single delete and batch delete (verify count returned)
      - `testPresignedGetURLDownloadsObject` — call presignedGetURL, then URLSession.shared.data(from:) the resulting URL, byte-compare
      - `testPresignUploadPartUrlIssued` — call presignUploadPart, assert URL contains `partNumber=1` and `X-Amz-Signature=` query param
      - `testMultipartCancellation` — start a multipart upload of a large file with a CancellationHandle, cancel mid-flight, verify the upload aborts with the expected error
    - Each test must clean up after itself: delete any object/multipart-upload it created in `tearDown()` to keep the test bucket clean

    **Create `RustBackedAuthIntegrationTests.swift`:**
    - `testLoginSucceedsWithValidCredentials` — DS3Authentication.login(email:password:tenant:); asserts isLogged + accountSession populated
    - `testLoginFailsWithInvalidCredentials` — wrong password; asserts throws DS3AuthenticationError.serverError (or whichever case the translator produces for invalid auth)
    - `testRefreshIfNeededRotatesToken` — force-refresh; asserts new token differs from old; asserts persist() wrote new value to App Group container (cleanup: restore original file)
    - `testForgeIAMTokenReturnsToken` — login + load API keys + forge token; asserts a non-empty Token with future expDate
    - `testCurrentSessionMatchesAccountSession` — login; asserts accountSession.token.token == handle.currentSession().token.token
    - `test2FAFlowRaisesMissing2FA` — login with a 2FA-enabled test account WITHOUT tfaToken; asserts throws DS3AuthenticationError.missing2FA. **Gated separately** on optional `DS3_TEST_2FA_EMAIL` env var; XCTSkip if missing.

    Each test runs only when env vars are present. CI provides them via GitHub Secrets (Phase 15 D-16 already established this).

    Run locally with env vars exported: `DS3_TEST_EMAIL=... DS3_TEST_PASSWORD=... DS3_TEST_TENANT=... DS3_TEST_BUCKET=... xcodebuild test -scheme DS3Lib -only-testing:DS3LibIntegrationTests`. All non-skipped tests must pass.
  </action>
  <verify>
    <automated>test -d apple/DS3Lib/Tests/DS3LibIntegrationTests && test -f apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedS3IntegrationTests.swift && test -f apple/DS3Lib/Tests/DS3LibIntegrationTests/RustBackedAuthIntegrationTests.swift && grep -c "DS3LibIntegrationTests" apple/DS3Lib/Package.swift | awk '$1 >= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Integration test target declared in Package.swift; both test files exist with comprehensive coverage of FFI-backed S3 + auth surfaces; tests gated by DS3_TEST_* env vars; locally with env vars set, all non-skipped tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire integration tests into CI (every-PR initially)</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
    - .github/workflows/build.yml (full file — locate macOS test job)
    - .planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md (D-16 GitHub Secrets — DS3_TEST_* already configured)
    - .planning/phases/16-apple-incremental-swap/16-CONTEXT.md D-23
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Integration Test CI Schedule"
  </read_first>
  <action>
    Edit `.github/workflows/build.yml`. Add a new job `apple-integration-tests` (or extend the existing macos-build job — prefer separate job for clarity and skippability):

    ```yaml
    apple-integration-tests:
      name: Apple integration tests (real Cubbit S3)
      runs-on: macos-15
      needs: [rust-check]
      env:
        DS3_TEST_EMAIL: ${{ secrets.DS3_TEST_EMAIL }}
        DS3_TEST_PASSWORD: ${{ secrets.DS3_TEST_PASSWORD }}
        DS3_TEST_TENANT: ${{ secrets.DS3_TEST_TENANT }}
        DS3_TEST_BUCKET: ${{ secrets.DS3_TEST_BUCKET }}
        DS3_TEST_2FA_EMAIL: ${{ secrets.DS3_TEST_2FA_EMAIL }}
        # DS3_TEST_2FA_SECRET deliberately NOT exposed in CI — 2FA test runs only locally
      steps:
        - uses: actions/checkout@v4
          with:
            lfs: true
        - uses: dtolnay/rust-toolchain@stable
        - name: Install Rust targets
          run: rustup target add aarch64-apple-darwin
        - name: Build DS3CoreFFI XCFramework
          working-directory: core
          run: ./scripts/build-xcframework.sh --release
        - name: Run integration tests
          run: |
            xcodebuild test \
              -project apple/DS3Drive.xcodeproj \
              -scheme DS3Lib \
              -destination 'platform=macOS' \
              -only-testing:DS3LibIntegrationTests
    ```

    **Schedule:** This job runs on every PR during Phase 16 development per RESEARCH recommendation. After Phase 16 merges to main, the project SHOULD downgrade to nightly via a `workflow_dispatch` + `schedule: cron:` trigger in a future PR. Plan 07 does NOT make that downgrade — leave it for the v2.0.0 milestone retrospective.

    **Document:** add a comment in the workflow file noting the post-merge schedule expectation:
    ```yaml
    # NOTE: This job runs on every PR for Phase 16. After v2.0.0 merge,
    # consider moving to schedule: cron + workflow_dispatch (see RESEARCH §Integration Test CI Schedule).
    ```

    **GitHub Secrets check:** Verify the org/repo has DS3_TEST_EMAIL, DS3_TEST_PASSWORD, DS3_TEST_TENANT, DS3_TEST_BUCKET configured (Phase 15 D-16 established these). If any are missing, document in 16-07-SUMMARY which need to be added by the user (this is a manual setup step).

    Push a draft PR. Confirm the new job appears in GitHub Actions, runs, and passes (or fails with a useful diagnostic if a secret is missing).
  </action>
  <verify>
    <automated>grep -c "DS3LibIntegrationTests" .github/workflows/build.yml | awk '$1 >= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    New CI job runs integration tests with secrets; appears as a separate check on PRs; runs on every PR during Phase 16; post-merge schedule downgrade documented as future work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual side-by-side smoke test execution (APPLE-05 / D-24)</name>
  <what-built>
    A pre-swap commit (last commit on main BEFORE Phase 16 started) and a post-swap HEAD (final commit of Plan 06) both built as `DS3 Drive.app`. The test exercises 14 user-visible flows on each build against the same real Cubbit account and records identical-or-different behavior in SMOKE-TEST-RESULTS.md.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Identify the pre-swap commit SHA. From `gsd/phase-16-apple-incremental-swap` branch, run `git log --oneline main..HEAD` to find when Phase 16 work began. The commit immediately before is the pre-swap baseline.
    2. Identify the post-swap HEAD: latest commit on `gsd/phase-16-apple-incremental-swap` after Plan 06 merges.
    3. Have a real Cubbit account with at least one bucket. Note its credentials.
    4. Quit Xcode. Run `killall fileproviderd` once before each build to clear extension caches (per project memory — NEVER delete DerivedData).

    **Capture-and-test rhythm:**
    For each commit (pre-swap, then post-swap):
    1. `git checkout <sha>`
    2. Open `apple/DS3Drive.xcodeproj` in Xcode.
    3. Clean build (Cmd+Shift+K), then Build & Run (Cmd+R).
    4. From the menu bar, run through the flow checklist below, recording observations.
    5. Snapshot the App Group container JSON files: `cp -r ~/Library/Group\ Containers/group.X889956QSM.io.cubbit.DS3Drive /tmp/ds3drive-snapshot-<sha>/`

    **14-flow checklist** (copy this verbatim into SMOKE-TEST-RESULTS.md and fill in PASS / FAIL / DIFFERENT for each on both builds):
    1. Login (no 2FA) — credentials accepted, drives.json present after login
    2. Login (with 2FA enabled) — 2FA prompt appears, TOTP code accepted
    3. Drive setup wizard — project + bucket selection, drive appears in Finder sidebar
    4. Drag 1MB file into drive folder — upload, badge → synced, visible from Cubbit web console
    5. Drag 100MB file — multipart upload progresses, 20 parts uploaded, valid ETag, badge transitions correctly
    6. Open cloud-only file — on-demand fetchContents triggers, file materializes
    7. Rename file (Finder) — server-side copy + delete, name updates
    8. Move file across folders — same copy+delete pattern
    9. Delete file (Trash) — soft-delete reflected
    10. Empty Trash — hard-delete
    11. Concurrent edit conflict — modify file on web console while local has open changes; save local; verify "(Conflict on …)" copy appears
    12. Token expiry mid-sync — wait or force-expire token; background refresh fires silently; no UI interrupt; log shows "Proactive token refresh successful"
    13. Logout / Re-login — clean teardown of NSFileProviderDomain, fresh login works
    14. Quit + Relaunch — app reloads from persistence, drives appear, sync resumes

    **Byte-diff JSON files** between snapshots:
    ```bash
    diff /tmp/ds3drive-snapshot-<pre-sha>/group.X889956QSM.io.cubbit.DS3Drive/drives.json /tmp/ds3drive-snapshot-<post-sha>/group.X889956QSM.io.cubbit.DS3Drive/drives.json
    diff /tmp/ds3drive-snapshot-<pre-sha>/group.X889956QSM.io.cubbit.DS3Drive/credentials.json /tmp/ds3drive-snapshot-<post-sha>/group.X889956QSM.io.cubbit.DS3Drive/credentials.json
    # Repeat for accountSession.json + account.json
    ```
    Expected output: for the SAME logical state (same drive, same login session), the JSON byte content must be byte-identical OR differ only in whitespace / key ordering. ANY field-name drift FAILS APPLE-06 and rolls back the merge.

    **Record results** in `.planning/phases/16-apple-incremental-swap/SMOKE-TEST-RESULTS.md`. Use this template:
    ```markdown
    # Phase 16 Side-by-Side Smoke Test Results

    **Pre-swap commit:** <sha>
    **Post-swap commit:** <sha>
    **Tester:** Marco Moschettini
    **Date:** <YYYY-MM-DD>
    **Cubbit account:** <redacted>
    **Bucket used:** <bucket name>

    ## Flow Results

    | # | Flow | Pre-swap | Post-swap | Notes |
    |---|------|----------|-----------|-------|
    | 1 | Login (no 2FA) | PASS | PASS | identical |
    | 2 | Login (with 2FA) | PASS | PASS | prompt appears at same step |
    | ... | ... | ... | ... | ... |

    ## App Group JSON Byte Diff

    | File | Diff result | Acceptable? |
    |------|-------------|-------------|
    | drives.json | identical | yes |
    | credentials.json | identical | yes |
    | accountSession.json | differs in key order only | yes |
    | account.json | identical | yes |

    ## Performance Comparison (selective)

    | Flow | Pre-swap timing | Post-swap timing | Within ±20% |
    |------|----------------|-----------------|-------------|
    | 100MB multipart upload | 18s | 19s | yes |
    | On-demand file fetch (10MB) | 1.2s | 1.3s | yes |

    ## Overall Verdict
    [PASS] All 14 flows behave identically; JSON schemas preserved; performance within ±20%.
    OR [FAIL] Flow N differs in <detail> — investigation required.
    ```

    Commit SMOKE-TEST-RESULTS.md.

    Type "approved" when the tracker is committed AND the overall verdict is PASS.
  </how-to-verify>
  <resume-signal>Type "approved" once SMOKE-TEST-RESULTS.md is committed with a PASS verdict on all 14 flows + JSON byte-diff acceptable.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration tests → real Cubbit IAM + S3 | Tests authenticate with real credentials; tests must clean up created objects to prevent bucket pollution |
| GitHub Actions secrets → integration tests | Secrets must NEVER appear in logs (use `mask-secrets` action if any value leaks) |
| Smoke test snapshots in /tmp | Contain real account JSON; not committed to repo; delete after testing |
| 2FA TOTP secret | Tester-specific; never in CI secrets (DS3_TEST_2FA_SECRET intentionally local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07-01 | Information Disclosure | Integration tests log credentials/tokens | mitigate | Tests must use `XCTContext.runActivity` with non-secret labels only; verify no `print(token)` or similar; CI logs masked |
| T-16-07-02 | Tampering | Test bucket polluted with orphan objects | mitigate | Each test uses unique object keys (e.g. UUID prefix) AND cleans up in tearDown; multipart aborts on test failure |
| T-16-07-03 | Denial of Service | Integration tests rate-limited by Cubbit S3 | accept | Test bucket is in a dedicated test project (Phase 15 D-15); failures retried with exponential backoff (aws-sdk-s3 retry from Plan 02) |
| T-16-07-04 | Spoofing | Smoke test runs against wrong commit | mitigate | Task 3 explicitly captures SHAs in SMOKE-TEST-RESULTS.md header; tester verifies with `git rev-parse HEAD` before each run |
| T-16-07-05 | Information Disclosure | App Group JSON snapshots in /tmp may leak | mitigate | Tester deletes `/tmp/ds3drive-snapshot-*` after smoke test completes; Task 3 mentions cleanup |
| T-16-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
- `apple/DS3Lib/Tests/DS3LibIntegrationTests/` directory exists with two test files
- Integration tests pass locally when DS3_TEST_* env vars are set (e.g. `DS3_TEST_EMAIL=... xcodebuild test ...` exits 0)
- CI job `apple-integration-tests` runs and passes on a draft PR
- `.planning/phases/16-apple-incremental-swap/SMOKE-TEST-RESULTS.md` exists with all 14 flows recorded
- Smoke test verdict is PASS for all 14 flows on both pre-swap and post-swap builds
- JSON byte-diff between pre-swap and post-swap App Group snapshots is acceptable (identical OR cosmetic key reordering only)
- 2FA flow verified by tester with a 2FA-enabled account
</verification>

<success_criteria>
- Integration test suite committed and passing in CI
- Side-by-side smoke test executed and documented
- All 14 RESEARCH-defined flows behave identically pre- vs post-swap
- App Group JSON schemas verified preserved at runtime (D-25 backed up by real data, not just CI fixtures)
- Phase 16 ready for merge — APPLE-01 through APPLE-06 closed
</success_criteria>

<output>
Create `.planning/phases/16-apple-incremental-swap/16-07-SUMMARY.md` documenting:
- Final smoke-test verdict + commit SHAs tested
- Any flows that differed (even in acceptable ways — e.g. timing within ±20% counts as "different but acceptable")
- Integration test pass count + any tests skipped due to missing env vars
- CI job duration (informs the post-merge nightly-vs-PR decision)
- Recommendation for post-merge integration-test schedule (nightly with workflow_dispatch trigger for manual on-demand)
- Any orphan multipart uploads in the test bucket that the smoke test failed to clean up (`list_multipart_uploads` snapshot)
</output>
