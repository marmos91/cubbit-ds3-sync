---
phase: 16-apple-incremental-swap
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift
  - apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift
  - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Transfers.swift
  - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Presign.swift
  - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Protocol.swift
  - apple/DS3DriveProvider/FileProviderExtension+Errors.swift
  - apple/DS3DriveProvider/FileProviderExtension.swift
  - apple/DS3DriveProvider/FileProviderExtension+Create.swift
  - apple/DS3DriveProvider/FileProviderExtension+Modify.swift
  - apple/DS3DriveProvider/FileProviderExtension+Delete.swift
  - apple/DS3Lib/Tests/DS3LibTests/DS3S3ErrorTranslationTests.swift
  - apple/DS3Lib/Tests/DS3LibTests/DescribeSotoErrorTests.swift
  - apple/DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift
  - apple/DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift
  - apple/DS3Thumbnails/Tests/ThumbnailRenderingTests/Phase13IntegrationSmokeTests.swift
  - apple/DS3DriveProviderTests/FetchThumbnailsErrorMappingTests.swift
autonomous: true
estimated_minutes: 300
requirements: [APPLE-01, APPLE-05]
tags: [apple, s3, adapter, fileprovider, error-mapping]

must_haves:
  truths:
    - "`DS3S3Client` conforms to `DS3S3ClientProtocol` with identical method signatures; internals call `DS3SessionHandle` instead of Soto"
    - "New `DS3S3Error` enum replaces Soto's `S3ErrorType`/`AWSErrorType` typealiases at every call site"
    - "Each adapter method does/catches `Ds3Error` from Rust and rethrows `DS3S3Error.translate(...)` per D-13"
    - "Each catch block logs `code` + `String(describing:)` at `privacy: .public` BEFORE translation (D-16)"
    - "FileProvider extension's 30+ `catch let s3Error as AWSErrorType` sites all changed to `catch let s3Error as DS3S3Error`; behavior is byte-identical NSError domain+code"
    - "All 156+ existing DS3LibTests pass with `MockDS3S3Client` unchanged (only error throws swap from `S3ErrorType.X` to `DS3S3Error.X`)"
    - "2FA path is NOT affected — that's Plan 04. DS3Authentication still calls URLSession in this plan."
    - "DS3S3ErrorTranslationTests covers every Rust error code → Swift enum case mapping"
  artifacts:
    - path: "apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift"
      provides: "New error enum + Rust→Swift translation + NSFileProviderError mapping"
      contains: "func toFileProviderError"
      min_lines: 80
    - path: "apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift"
      provides: "Rust-backed DS3S3ClientProtocol conformance"
      contains: "import DS3CoreFFI"
    - path: "apple/DS3DriveProvider/FileProviderExtension+Errors.swift"
      provides: "DS3S3Error → NSFileProviderError mapping (replaces AWSErrorType extension)"
      contains: "extension DS3S3Error"
    - path: "apple/DS3Lib/Tests/DS3LibTests/DS3S3ErrorTranslationTests.swift"
      provides: "Tests for every Rust error code → DS3S3Error case mapping"
      contains: "func testTranslate"
  key_links:
    - from: "apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift"
      to: "core/out/DS3CoreFFI.xcframework"
      via: "import DS3CoreFFI + DS3SessionHandle method calls"
      pattern: "handle\\.(headObject|listObjects|downloadObject|uploadObject|copyObject|deleteObject|presignGet|multipart)"
    - from: "apple/DS3DriveProvider/FileProviderExtension+Errors.swift"
      to: "apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift"
      via: "extension DS3S3Error.toFileProviderError()"
      pattern: "DS3S3Error.*toFileProviderError"

user_setup: []
---

<objective>
Replace `DS3S3Client` internals with calls to `DS3SessionHandle` from Plan 02. Create the new `DS3S3Error` enum that supersedes Soto's re-exported typealiases. Migrate every `catch let s3Error as AWSErrorType` site in the FileProvider extension and all dependent test files. After this plan, the entire S3 path runs through Rust — but `DS3Authentication` + `DS3SDK` still use URLSession (those are Plan 04). Soto is still in `Package.swift` (Plan 05).

Purpose: This is the highest-volume swap and the moment FileProvider starts using the Rust core in production-shaped flows. Once green, manual smoke can verify upload/download/rename/move/delete are functionally identical via Rust.

Output: A Rust-backed `DS3S3Client`, a new `DS3S3Error` enum, and a FileProvider extension whose catch blocks reference the new enum. 156+ unit tests still green. Manual smoke not yet executed (Plan 07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-apple-incremental-swap/16-CONTEXT.md
@.planning/phases/16-apple-incremental-swap/16-RESEARCH.md
@.planning/phases/16-apple-incremental-swap/16-PATTERNS.md
@.planning/phases/16-apple-incremental-swap/16-02-SUMMARY.md
@apple/DS3Lib/Sources/DS3Lib/DS3S3ClientProtocol.swift
@apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift
@apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Transfers.swift
@apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Presign.swift
@apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Protocol.swift
@apple/DS3DriveProvider/FileProviderExtension+Errors.swift

<interfaces>
<!-- Permanent public API DS3S3Client must conform to (D-02) -->

From apple/DS3Lib/Sources/DS3Lib/DS3S3ClientProtocol.swift:
- 17 protocol methods spanning list, head, get/put (file + memory), copy, delete, multipart, shutdown
- Extensions add: presignedGetURL, presignUploadPart, listMultipartUploads, thumbnail variants

From core/out/DS3CoreFFI.swift (after Plan 02 rebuild):
- DS3SessionHandle methods (per 16-02-SUMMARY signatures):
  - headObject(bucket: String, key: String) throws -> S3ObjectMetadata
  - listObjects(bucket: String, prefix: String?, delimiter: String?, maxKeys: Int32?, continuationToken: String?) throws -> ListObjectsResult
  - downloadObject(bucket: String, key: String, filePath: String, progress: ProgressCallback?) throws -> DownloadResult
  - uploadObject(bucket: String, key: String, filePath: String, progress: ProgressCallback?) throws -> String? (ETag)
  - downloadToMemory(bucket: String, key: String) throws -> Data (NEW Plan 02)
  - uploadFromMemory(bucket: String, key: String, data: Data, metadata: [String: String]) throws -> String? (NEW Plan 02)
  - deleteObject(bucket: String, key: String) throws
  - deleteObjects(bucket: String, keys: [String]) throws -> Int32
  - copyObject(bucket: String, sourceKey: String, destKey: String, metadata: [String: String]?) throws (EXTENDED Plan 02)
  - multipartCreate(bucket: String, key: String) throws -> String
  - multipartUploadPart(bucket: String, key: String, uploadId: String, partNumber: Int32, data: Data, cancelToken: CancellationHandle?) throws -> String (EXTENDED Plan 02)
  - multipartComplete(bucket: String, key: String, uploadId: String, parts: [CompletedPart]) throws -> String? (ETag)
  - multipartAbort(bucket: String, key: String, uploadId: String) throws
  - listMultipartUploads(bucket: String) throws -> [MultipartUploadInfo]
  - presignGet(bucket: String, key: String, expiresInSeconds: Int64) throws -> String
  - presignUploadPart(bucket: String, key: String, uploadId: String, partNumber: Int32, expiresInSeconds: Int64) throws -> String (NEW Plan 02)
  - connectS3(endpoint: String, accessKey: String, secretKey: String, region: String?) throws

- Free functions: func ds3ErrorCode(_ err: Ds3Error) -> Int32 (per 16-02-SUMMARY)

From apple/DS3DriveProvider/FileProviderExtension+Errors.swift (current):
- extension AWSErrorType { func toFileProviderError() -> NSError } switch on errorCode strings
- Maps: InvalidAccessKeyId/SignatureDoesNotMatch/ExpiredToken → .notAuthenticated; AccessDenied → .cannotSynchronize; NoSuchKey/NoSuchBucket/NotFound → .noSuchItem; EntityTooLarge → .insufficientQuota; SlowDown/ServiceUnavailable/InternalError/RequestTimeout → .serverUnreachable
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DS3S3Error enum + Rust→Swift translator + tests</name>
  <files>apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift, apple/DS3Lib/Tests/DS3LibTests/DS3S3ErrorTranslationTests.swift</files>
  <read_first>
    - apple/DS3Lib/Sources/DS3Lib/DS3Authentication.swift lines 6-54 (DS3AuthenticationError enum shape)
    - apple/DS3Lib/Sources/DS3Lib/DS3SDK.swift lines 5-23 (DS3SDKError shape)
    - apple/DS3DriveProvider/FileProviderExtension+Errors.swift lines 49-93 (current AWSErrorType extension to migrate verbatim)
    - core/ds3-models/src/error.rs (DS3Error variants + code() table, lines 97-117)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"DS3S3Error.swift NEW (error enum)"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"DS3S3Error design" (lines 443-522) + §"Error translation table" (lines 526-562)
  </read_first>
  <behavior>
    - DS3S3Error declares cases: noSuchKey, noSuchBucket, accessDenied, invalidAccessKey, signatureDoesNotMatch, expiredToken, entityTooLarge, slowDown, serviceUnavailable, internalError, requestTimeout, missingUploadId, emptyFileData, missingETag, parseError, unableToOpenFile, thumbnailTooLarge(size:limit:), unknown(code:message:)
    - DS3S3Error.translate(rust: Ds3Error) maps each rust error code per the table in PATTERNS.md
    - DS3S3Error.translate of code 2001 → .missingUploadId; 2002 → .emptyFileData; 2003 → .missingETag; 2004 → .parseError; 2005 → .unableToOpenFile
    - DS3S3Error.translate of code 3003 (S3Error string-wrapped): parses message for AWS error code substrings → matching enum case; unknown text → .unknown(code: nil, message: msg)
    - DS3S3Error.toFileProviderError() returns NSError with NSFileProviderErrorDomain and code per PATTERNS.md mapping table (.notAuthenticated for credential errors, .noSuchItem for missing keys, .insufficientQuota for size, .serverUnreachable for transient, .cannotSynchronize as catch-all)
    - DS3S3Error.isNotFound true for noSuchKey/noSuchBucket
    - DS3S3Error.isThrottling true for slowDown/serviceUnavailable/internalError/requestTimeout
    - DS3S3Error.isRecoverableAuthError true for invalidAccessKey/signatureDoesNotMatch/expiredToken
  </behavior>
  <action>
    Create `apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift` following PATTERNS.md §"DS3S3Error.swift NEW (error enum)":
    - `import FileProvider`, `import Foundation`, `import DS3CoreFFI`
    - `public enum DS3S3Error: Error, LocalizedError, Sendable { ... }` with all cases listed under `<behavior>`
    - `public var errorDescription: String?` returning NSLocalizedString per case (mirror DS3AuthenticationError shape from DS3Authentication.swift:19-53)
    - `public extension DS3S3Error { func toFileProviderError() -> NSError }` containing the switch from PATTERNS.md (or copy verbatim from FileProviderExtension+Errors.swift:74-92 logic, inverted from string switch to enum case switch)
    - `public extension DS3S3Error { var isNotFound: Bool; var isThrottling: Bool; var isRecoverableAuthError: Bool }`
    - `extension DS3S3Error { static func translate(_ rust: Ds3Error) -> DS3S3Error }` with code-based switch using `ds3ErrorCode(rust)` from Plan 02. Include the S3Error-string parsing helper from RESEARCH.md lines 547-561 for code 3003. NOTE: if Plan 02 chose `ds3_error_code(err: String) -> Int32` signature, adjust call to `ds3ErrorCode(String(describing: rust))`.

    Create `apple/DS3Lib/Tests/DS3LibTests/DS3S3ErrorTranslationTests.swift` (XCTest):
    - Each numeric code → expected case (write RED tests first; each test must fail before DS3S3Error.swift exists)
    - S3 string parsing for each of the 11 substring patterns (NoSuchKey, NoSuchBucket, AccessDenied, InvalidAccessKeyId, SignatureDoesNotMatch, ExpiredToken, EntityTooLarge, SlowDown, ServiceUnavailable, RequestTimeout, InternalError)
    - `toFileProviderError()` produces the correct `NSFileProviderError.Code` for each case
    - `isNotFound`/`isThrottling`/`isRecoverableAuthError` flags correct for each case
    - Coverage assertion: every variant in `core/ds3-models/src/error.rs` produces a non-`.unknown` translation (catches future Rust variants added without Swift migration)

    Do NOT delete the Soto typealiases yet (typealias S3ErrorType / AWSErrorType in DS3S3Client.swift:11-12). They stay until Task 2 swaps the catch sites. Compilation must succeed at every commit.
  </action>
  <verify>
    <automated>xcodebuild test -project apple/DS3Drive.xcodeproj -scheme DS3Lib -destination 'platform=macOS' -only-testing:DS3LibTests/DS3S3ErrorTranslationTests 2>&1 | grep -E "Test Suite.*DS3S3ErrorTranslationTests.*passed"</automated>
  </verify>
  <done>
    DS3S3Error.swift compiles; DS3S3ErrorTranslationTests passes; toFileProviderError() produces matching NSError domain+code as the existing AWSErrorType extension for equivalent logical errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite DS3S3Client.swift + +Transfers + +Presign internals against DS3SessionHandle</name>
  <files>apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift, apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Transfers.swift, apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Presign.swift, apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Protocol.swift</files>
  <read_first>
    - apple/DS3Lib/Sources/DS3Lib/DS3S3ClientProtocol.swift (full file — the 17 protocol methods + extensions)
    - apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift (current Soto impl — full file)
    - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Transfers.swift (current streaming + multipart impl)
    - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Presign.swift (current signURL impl)
    - apple/DS3Lib/Sources/DS3Lib/DS3S3Client+Protocol.swift (extension conformance — body stays verbatim)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"DS3S3Client.swift rewrite" + §"+Transfers rewrite" + §"+Presign rewrite"
    - .planning/phases/16-apple-incremental-swap/16-02-SUMMARY.md (final FFI signatures)
  </read_first>
  <behavior>
    - DS3S3Client(handle:endpoint:accessKey:secretKey:) calls handle.connectS3(...) (per RESEARCH §DS3SessionHandle Lifecycle: singleton handle borrowed)
    - All 17 protocol methods delegate to handle.* equivalents; result types translated to existing Swift model types
    - headObject result fields mapped: etag via ETagUtils.normalize, lastModified RFC3339 → Date
    - putObject with fileURL=nil uses handle.uploadFromMemory(data: Data(), metadata: [:]) for empty marker case
    - Multipart orchestration (putObjectMultipart withThrowingTaskGroup) stays in Swift; per-part calls swap to FFI; PendingUploadStore interaction unchanged
    - presignedGetURL preserves existing PresignError.invalidPresignExpiry guard (expiresIn 0..604800)
    - Every catch block: log code + String(describing:) at privacy: .public, then throw DS3S3Error.translate(e) (D-16)
    - shutdown() is a no-op (handle lifecycle owned by DS3Authentication post-Plan 04)
  </behavior>
  <action>
    Rewrite `apple/DS3Lib/Sources/DS3Lib/DS3S3Client.swift`:
    - Add `import DS3CoreFFI`
    - Delete `AWSClient` and `S3` (Soto) stored properties + deinit
    - Replace init with the borrowed-handle pattern per PATTERNS.md §"DS3S3Client.swift rewrite" lines 67-73
    - Add `private let handle: DS3SessionHandle` stored property
    - Re-implement all 17 protocol methods using the adapter pattern from PATTERNS.md lines 77-93: log on entry → do { call handle.X; translate result } catch let e as Ds3Error { logger.error("code=\(ds3ErrorCode(e), privacy: .public) \(String(describing: e), privacy: .public)"); throw DS3S3Error.translate(e) }
    - Keep verbatim: `decodeS3Key(_:)`, `readFilePart(at:offset:length:)`, `customEndpoint`
    - Rename: `describeSotoError(_:)` → `describeS3Error(_:)`, switch impl to read `ds3ErrorCode(e)`
    - Delete: `s3ErrorCode(from:)`, `isNotFoundError(_:)`, `isRecoverableAuthError(_:)` static methods (functionality moves to DS3S3Error case properties)
    - Delete the two `typealias` lines (11-12) — compile errors triggered will drive Task 3 + Task 4 migrations

    Rewrite `+Transfers.swift` per PATTERNS.md §"+Transfers rewrite":
    - Delete `streamToFile(...)` Soto helper
    - getObject(toFile:onProgress:) via handle.downloadObject(filePath:progress:); getObjectData via handle.downloadToMemory
    - putObject(fileURL:onProgress:) via handle.uploadObject; putObjectData via handle.uploadFromMemory; nil fileURL → uploadFromMemory(data: Data(), metadata: [:])
    - Keep `putObjectMultipart` Swift orchestration (`withThrowingTaskGroup`); inner calls swap to handle.multipartCreate / multipartUploadPart (cancelToken: nil — Plan 04 may revisit) / multipartComplete / multipartAbort
    - Create `ProgressCallbackBridge` class conforming to FFI's ProgressCallback protocol (Rust trait at core/ds3-ffi/src/progress.rs:10-17); bridge on_progress(bytesTransferred, totalBytes) → existing TransferProgress + handler

    Rewrite `+Presign.swift` per PATTERNS.md §"+Presign rewrite":
    - presignedGetURL → handle.presignGet (PATTERNS lines 164-174)
    - presignUploadPart → handle.presignUploadPart
    - Keep verbatim: `PresignError` enum, `buildObjectURL(endpoint:bucket:key:)` helper

    `+Protocol.swift` body stays verbatim — Swift extension declaring conformance, no Soto types referenced.

    `+Thumbnails.swift` and `+ThumbnailPrefix.swift` are protocol extensions calling DS3S3ClientProtocol methods — zero changes (internals call protocol methods that now route through FFI).

    Verify the DS3Lib target compiles: `xcodebuild build -scheme DS3Lib`. Tests in the test target may not compile yet — Task 4 fixes those.
  </action>
  <verify>
    <automated>xcodebuild build -project apple/DS3Drive.xcodeproj -scheme DS3Lib -destination 'platform=macOS' 2>&1 | tail -5 | grep -qE "BUILD SUCCEEDED"</automated>
  </verify>
  <done>
    DS3Lib library target builds successfully; DS3S3Client uses no Soto types in its public surface; all 17 protocol methods delegate to handle.* calls; every catch block logs before translating to DS3S3Error.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate FileProvider extension catch blocks from AWSErrorType → DS3S3Error</name>
  <files>apple/DS3DriveProvider/FileProviderExtension+Errors.swift, apple/DS3DriveProvider/FileProviderExtension.swift, apple/DS3DriveProvider/FileProviderExtension+Create.swift, apple/DS3DriveProvider/FileProviderExtension+Modify.swift, apple/DS3DriveProvider/FileProviderExtension+Delete.swift</files>
  <read_first>
    - apple/DS3DriveProvider/FileProviderExtension+Errors.swift (current extension AWSErrorType lines 49-93)
    - apple/DS3DriveProvider/FileProviderExtension+Create.swift (catch sites at lines 130, 175, 196, 258, 402)
    - apple/DS3DriveProvider/FileProviderExtension+Modify.swift (catch sites at lines 127, 134, 207, 268, 345, 416, 535)
    - apple/DS3DriveProvider/FileProviderExtension+Delete.swift (all 9 catch sites — grep first)
    - apple/DS3DriveProvider/FileProviderExtension.swift line 332
    - apple/DS3Lib/Sources/DS3Lib/DS3S3Error.swift (from Task 1)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"FileProvider extension catch-block migration"
    - .planning/phases/16-apple-incremental-swap/16-CONTEXT.md D-03, D-21
  </read_first>
  <action>
    **Critical constraint (D-03):** The FileProvider extension is "never touched" beyond catch-block enum type updates. The ONLY changes in this task are identifier swaps in `catch let s3Error as <X>` / `catch is <X>` clauses, plus deleting the `extension AWSErrorType` block from FileProviderExtension+Errors.swift. Any other change is a violation.

    **FileProviderExtension+Errors.swift**:
    - DELETE the `extension AWSErrorType { ... }` block (lines 49-93). Equivalent logic now lives on DS3S3Error (Task 1).
    - Add `import DS3Lib` at the top if not already present.
    - Verify no other helper in this file is Soto-typed.

    **Catch-site identifier swaps in 4 FileProviderExtension+*.swift files** — apply exactly:
    - `catch let s3Error as AWSErrorType` → `catch let s3Error as DS3S3Error`
    - `catch let s3Error as S3ErrorType` → `catch let s3Error as DS3S3Error`
    - `catch is S3ErrorType` (FileProviderExtension+Create.swift line 258) → `catch is DS3S3Error`
    - `catch is AWSErrorType` → `catch is DS3S3Error`

    Body of each catch block stays IDENTICAL — already calls `s3Error.toFileProviderError()`, which now resolves to DS3S3Error.toFileProviderError() from Task 1.

    Confirmed call sites per PATTERNS.md (run `grep -rn "AWSErrorType\|S3ErrorType" apple/DS3DriveProvider/` to enumerate):
    - FileProviderExtension.swift:332 (1)
    - +Create.swift: 130, 175, 196, 258, 402 (5)
    - +Modify.swift: 127, 134, 207, 268, 345, 416, 535 (7)
    - +Delete.swift: 9 sites

    After every edit, verify zero occurrences remain in source (test files migrated separately in Task 4):
    ```
    grep -rn 'AWSErrorType\|S3ErrorType' apple/DS3DriveProvider/ | grep -v Tests
    # must return 0 lines
    ```

    Do not touch anything else in these files (function bodies, imports beyond what DS3S3Error requires, control flow).
  </action>
  <verify>
    <automated>test "$(grep -rn 'AWSErrorType\|S3ErrorType' apple/DS3DriveProvider/ | grep -v Tests | wc -l)" = "0" && xcodebuild build -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' 2>&1 | tail -5 | grep -qE "BUILD SUCCEEDED"</automated>
  </verify>
  <done>
    Zero AWSErrorType/S3ErrorType identifiers remain in apple/DS3DriveProvider/ source (excluding tests); extension catch blocks reference DS3S3Error; full Xcode build succeeds for DS3 Drive scheme.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate Soto-throwing test files + verify full test suite green</name>
  <files>apple/DS3Lib/Tests/DS3LibTests/DescribeSotoErrorTests.swift, apple/DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift, apple/DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift, apple/DS3Thumbnails/Tests/ThumbnailRenderingTests/Phase13IntegrationSmokeTests.swift, apple/DS3DriveProviderTests/FetchThumbnailsErrorMappingTests.swift</files>
  <read_first>
    - apple/DS3Lib/Tests/DS3LibTests/DescribeSotoErrorTests.swift (current S3ErrorType throws)
    - apple/DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift
    - apple/DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift
    - apple/DS3Thumbnails/Tests/ThumbnailRenderingTests/Phase13IntegrationSmokeTests.swift (4 throw sites)
    - apple/DS3DriveProviderTests/FetchThumbnailsErrorMappingTests.swift (import SotoCore)
    - apple/DS3DriveProviderTests/Folder*Tests.swift (4 files — grep for `import SotoS3`)
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Soto usage sites" lines 776-794
  </read_first>
  <action>
    Locate every test file that imports Soto or throws `S3ErrorType.X` / `AWSErrorType.X`:
    ```bash
    grep -rln "S3ErrorType\|AWSErrorType\|import SotoS3\|import SotoCore" apple/*/Tests apple/DS3DriveProviderTests
    ```

    For each match, apply the migration pattern:
    - `import SotoS3` → DELETE (DS3S3Error lives in DS3Lib, already imported)
    - `import SotoCore` → DELETE
    - `throw S3ErrorType.noSuchKey` → `throw DS3S3Error.noSuchKey`
    - `throw S3ErrorType.accessDenied` → `throw DS3S3Error.accessDenied`
    - `throw S3ErrorType.<other>` → matching DS3S3Error case (refer to Task 1's enum cases)
    - `catch is S3ErrorType` → `catch is DS3S3Error`
    - `catch let x as AWSErrorType` → `catch let x as DS3S3Error`

    Files to migrate (confirmed in PATTERNS / RESEARCH §"Soto usage sites"):
    - apple/DS3Lib/Tests/DS3LibTests/DescribeSotoErrorTests.swift (this test verified the OLD Soto describe helper; rewrite to test `describeS3Error(_:)` from Task 2 — file is purpose-built for the legacy behavior so consider renaming to DescribeS3ErrorTests.swift OR leave the filename and update content)
    - apple/DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift
    - apple/DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift
    - apple/DS3Thumbnails/Tests/ThumbnailRenderingTests/Phase13IntegrationSmokeTests.swift (4 throw sites)
    - apple/DS3DriveProviderTests/FetchThumbnailsErrorMappingTests.swift (imports SotoCore — `extension AWSErrorType.toFileProviderError()` is what this test verified; rewrite assertions against `extension DS3S3Error.toFileProviderError()`)
    - apple/DS3DriveProviderTests/Folder*Tests.swift (any of 4 files with `import SotoS3`)
    - apple/DS3Lib/Tests/DS3LibTests/MockDS3S3Client.swift (if it throws S3ErrorType — check)

    Confirm no production source still references Soto types (tests OK since Soto package still in Package.swift until Plan 05):
    ```
    grep -rn 'AWSErrorType\|S3ErrorType\|import SotoS3\|import SotoCore' apple/DS3Lib/Sources apple/DS3DriveProvider | grep -v "//.*Soto"
    # must return 0 lines (excluding comments)
    ```

    Run the FULL test suite:
    ```
    xcodebuild test -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS'
    ```

    All 156+ tests must pass. If a test fails, the failure must be due to Plan 03 work, not pre-existing flakes (compare against Plan 02 baseline summary). Common failure: test asserts on `localizedDescription` text and DS3S3Error's NSLocalizedString differs from Soto's. Fix by aligning DS3S3Error's errorDescription text or updating the test's expected text — match the spirit of the original assertion (test behavior, not error wording).
  </action>
  <verify>
    <automated>test "$(grep -rn 'AWSErrorType\|S3ErrorType\|import SotoS3\|import SotoCore' apple/DS3Lib/Sources apple/DS3DriveProvider 2>/dev/null | grep -v '^\s*//' | wc -l)" = "0" && xcodebuild test -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' 2>&1 | tail -20 | grep -qE "Test Suite.*All tests.*passed|TEST SUCCEEDED"</automated>
  </verify>
  <done>
    No Soto identifiers remain in non-test source; all migrated test files compile; full DS3 Drive test suite via xcodebuild test passes with 0 failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift adapter → UniFFI Rust call | Inputs cross FFI as UTF-8 String / Data; panics caught by Phase 15 panic_guard → DS3Error::Internal |
| DS3S3Client → FileProvider extension | Swift adapter throws DS3S3Error; extension catches and converts via .toFileProviderError() (NEVER custom error type per project memory) |
| Rust DS3Error description → log output | Error messages may contain HTTP response bodies; logged at FFI boundary (D-16) |
| Mock test isolation | Mock implementations throw DS3S3Error; real path stays untouched (D-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure | DS3Error description in adapter log lines | mitigate | All adapter log lines use `privacy: .public` only on numeric code + String(describing: rust); never log raw credentials. Rust's `DS3Error` Display impl is verified (core/ds3-models/src/error.rs) to not include token/refresh-cookie values |
| T-16-03-02 | Tampering | DS3S3Error custom type returned to FileProvider | mitigate | FileProvider catch blocks explicitly call `.toFileProviderError()` which returns NSError(NSFileProviderErrorDomain). NEVER throw raw DS3S3Error past the extension boundary (verified by D-21 + project memory rule) |
| T-16-03-03 | Spoofing | DS3S3Error.translate string-parsing of S3 error codes | mitigate | parseS3StringError uses substring match against well-known AWS error codes; case-sensitive; falls back to .unknown(code: nil, message:) on no match. Future Rust addition of structured S3ErrorCode enum will tighten this (deferred — researcher recommendation line 565) |
| T-16-03-04 | Denial of Service | DS3S3Error.unknown(message:) leaks Rust internals | accept | Rust Display strings are bounded (no infinite recursion); short message strings only. NSFileProviderError mapping for .unknown defaults to .cannotSynchronize which is a safe retry-or-stop signal |
| T-16-03-05 | Tampering | ProgressCallbackBridge memory safety | mitigate | UniFFI manages the Box<dyn ProgressCallback> lifetime via Arc on the Rust side; Swift bridge class is class-based (reference-counted) and the FFI keeps a strong reference for the duration of the call |
| T-16-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
- `xcodebuild build -scheme DS3Lib -destination 'platform=macOS'` exits 0
- `xcodebuild build -scheme "DS3 Drive" -destination 'platform=macOS'` exits 0 (includes FileProvider extension target)
- `xcodebuild test -scheme "DS3 Drive" -destination 'platform=macOS'` shows 0 test failures; all 156+ tests reported as passed
- `grep -rn "AWSErrorType\|S3ErrorType" apple/DS3Lib/Sources apple/DS3DriveProvider | grep -v Tests | grep -v '^\s*//'` returns 0 lines
- DS3S3Error.toFileProviderError() produces the same NSError domain + code as the deleted AWSErrorType extension produced for matching logical errors (verified by DS3S3ErrorTranslationTests + FetchThumbnailsErrorMappingTests rewrite)
- Logger.error lines in DS3S3Client adapters fire BEFORE throw and include `code=...` substring (verify by running a unit test with a mocked Ds3Error throw and capturing log output via `OSLogStore` — or by visual inspection in PR review)
</verification>

<success_criteria>
- DS3S3Error.swift exists with full enum + extensions
- DS3S3Client + +Transfers + +Presign rewritten to call DS3SessionHandle
- FileProvider extension catch blocks reference DS3S3Error
- All 156+ unit tests pass via xcodebuild test
- Zero Soto identifiers remain in non-test Apple source code
- DS3S3ErrorTranslationTests covers every Rust error code → Swift enum case mapping
</success_criteria>

<output>
Create `.planning/phases/16-apple-incremental-swap/16-03-SUMMARY.md` documenting:
- Final list of files touched + count of catch-block migrations performed (expected ~30)
- Any DS3S3Error case that needed adjustment from the originally drafted enum (e.g. missing case discovered during migration)
- Notes on the singleton DS3SessionHandle wiring (does DS3S3Client currently receive a handle from DS3Authentication, or is a placeholder used until Plan 04 wires it?) — this is critical for Plan 04
- Any test that required content changes beyond identifier swaps (and why)
</output>
