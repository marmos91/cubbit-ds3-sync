---
phase: 16-apple-incremental-swap
plan: 05
type: execute
wave: 5
depends_on: [16-04]
files_modified:
  - apple/DS3Lib/Package.swift
  - apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift
  - apple/DS3Lib/Tests/DS3LibTests/SyncAnchorHashTests.swift
autonomous: false
estimated_minutes: 60
requirements: [APPLE-04]
tags: [apple, cleanup, soto, cryptokit, dependencies]

must_haves:
  truths:
    - "apple/DS3Lib/Package.swift no longer declares `https://github.com/soto-project/soto` as a dependency"
    - "apple/DS3Lib/Package.swift no longer declares `https://github.com/apple/swift-nio.git` as a dependency (verified post-Soto-removal that no source file imports NIOCore)"
    - "Zero occurrences of `import SotoS3` / `import SotoCore` / `import NIOCore` in apple/ source and test trees"
    - "Zero occurrences of `import CryptoKit` / `import Crypto` in apple/DS3Lib/Sources/"
    - "apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift uses `import CommonCrypto` + `CC_SHA256`; produces byte-identical SHA256 output for pre-swap inputs"
    - "Full xcodebuild test suite still green after dependency removal"
    - "Package.resolved no longer contains soto or swift-nio entries"
  artifacts:
    - path: "apple/DS3Lib/Package.swift"
      provides: "Cleaned SPM manifest — only DS3CoreFFI + swift-atomics + (audit) swift-nio"
      contains: "DS3CoreFFI"
    - path: "apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift"
      provides: "CommonCrypto-based SHA256"
      contains: "import CommonCrypto"
    - path: "apple/DS3Lib/Tests/DS3LibTests/SyncAnchorHashTests.swift"
      provides: "Regression test asserting byte-identical SHA256 output vs pre-swap fixtures"
      contains: "v1:"
  key_links:
    - from: "apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift"
      to: "CommonCrypto/CommonCrypto.h"
      via: "CC_SHA256 function call"
      pattern: "CC_SHA256"

user_setup: []
---

<objective>
End-of-phase atomic removal of Soto, CryptoKit, and (audit) swift-nio from DS3Lib. Replace `SyncAnchorHash`'s CryptoKit SHA256 with CommonCrypto's CC_SHA256. After this plan, the DS3Lib package depends only on the `DS3CoreFFI` binary target and `swift-atomics` (kept).

This is the visual PR-review gate per D-27: reviewer eyes verify no Soto/CryptoKit symbols remain. No automated grep gate.

Output: A minimal `Package.swift`, a CommonCrypto-based hash utility, and a regression test that locks the SHA256 output format so future changes can't drift the NSFileProviderSyncAnchor format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-apple-incremental-swap/16-CONTEXT.md
@.planning/phases/16-apple-incremental-swap/16-RESEARCH.md
@.planning/phases/16-apple-incremental-swap/16-PATTERNS.md
@.planning/phases/16-apple-incremental-swap/16-04-SUMMARY.md
@apple/DS3Lib/Package.swift
@apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap SyncAnchorHash from CryptoKit → CommonCrypto + lock format via fixture test</name>
  <files>apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift, apple/DS3Lib/Tests/DS3LibTests/SyncAnchorHashTests.swift</files>
  <read_first>
    - apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift (full file — lines 28-36 and 68-79 are the SHA256 call sites)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"SyncAnchorHash.swift CryptoKit removal"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Soto/CryptoKit Removal Audit" lines 740-773
    - .planning/phases/16-apple-incremental-swap/16-CONTEXT.md (search for sync anchor concerns)
  </read_first>
  <behavior>
    - SyncAnchorHash.compute(over: pairs) returns a hex-encoded SHA256 of the serialized (key, etag) pairs
    - Output starts with the `v1:` prefix (unchanged from pre-swap format)
    - For a known input (e.g. `[("foo.txt", "abc123"), ("bar.txt", "def456")]`), output is byte-identical to the pre-swap CryptoKit-generated SHA256 hex
    - For an empty input, output is `v1:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` (SHA256("") prefixed with `v1:`)
    - When called concurrently from multiple threads, returns the same output for the same input (CC_SHA256 is reentrant)
  </behavior>
  <action>
    **First — write the regression test** in `apple/DS3Lib/Tests/DS3LibTests/SyncAnchorHashTests.swift`:

    ```swift
    import XCTest
    @testable import DS3Lib

    final class SyncAnchorHashTests: XCTestCase {
        func testEmptyInputProducesKnownSHA256() {
            let result = SyncAnchorHash.compute(over: [])
            // SHA256 of empty string prefixed with v1:
            XCTAssertEqual(result, "v1:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855")
        }

        func testKnownPairProducesStableOutput() {
            let pairs: [(String, String)] = [("foo.txt", "abc123"), ("bar.txt", "def456")]
            // Capture pre-swap CryptoKit-generated hash by running this exact test BEFORE the swap.
            // Insert the captured value here so post-swap CommonCrypto must match.
            let expectedHex = "<CAPTURE PRE-SWAP VALUE>"  // step in action below
            XCTAssertEqual(SyncAnchorHash.compute(over: pairs), expectedHex)
        }

        func testOrderingMatters() {
            // (a,b) and (b,a) must produce different hashes — assumes compute does NOT sort internally;
            // verify against current implementation behavior.
            let pairs1 = [("a", "1"), ("b", "2")]
            let pairs2 = [("b", "2"), ("a", "1")]
            XCTAssertNotEqual(SyncAnchorHash.compute(over: pairs1), SyncAnchorHash.compute(over: pairs2))
            // OR if compute sorts internally, replace with XCTAssertEqual — read the current impl to decide
        }
    }
    ```

    Step 1 — CAPTURE PRE-SWAP VALUES: BEFORE modifying SyncAnchorHash.swift, run the test once with the current CryptoKit implementation. Read the actual hash output for `testKnownPairProducesStableOutput`'s fixture input, paste it into `expectedHex`. Also capture any additional fixtures by adding sample pairs that exercise edge cases (Unicode keys, empty etag, very long key, sorted vs unsorted lists).

    Step 2 — MODIFY `SyncAnchorHash.swift` per PATTERNS.md §"SyncAnchorHash.swift CryptoKit removal":
    - Change `import CryptoKit` → `import CommonCrypto`
    - Add helper:
      ```swift
      private func sha256(_ data: Data) -> Data {
          var hash = [UInt8](repeating: 0, count: Int(CC_SHA256_DIGEST_LENGTH))
          data.withUnsafeBytes { _ = CC_SHA256($0.baseAddress, CC_LONG(data.count), &hash) }
          return Data(hash)
      }
      ```
    - Replace call sites at lines 33 and 76:
      - Before: `let digest = SHA256.hash(data: Data(joined.utf8))`
      - After: `let digest = sha256(Data(joined.utf8))`
    - Keep `digest.map { String(format: "%02x", $0) }.joined()` unchanged — both CryptoKit's `SHA256.Digest` and Foundation's `Data` are Sequence-of-UInt8 producing identical hex output
    - Preserve the `v1:` prefix at line 24 (or wherever the format string lives)

    Step 3 — RUN TESTS: `xcodebuild test -only-testing:DS3LibTests/SyncAnchorHashTests`. The fixture tests must pass with the captured pre-swap hex values — proves byte-identical output (Assumption A6).

    Step 4 — Sanity check: run the full DS3Lib suite to confirm no other test regressed.
  </action>
  <verify>
    <automated>grep -c "import CryptoKit" apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift | grep -q "^0$" && grep -c "import CommonCrypto" apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift | grep -q "^1$" && xcodebuild test -project apple/DS3Drive.xcodeproj -scheme DS3Lib -destination 'platform=macOS' -only-testing:DS3LibTests/SyncAnchorHashTests 2>&1 | tail -5 | grep -qE "passed|TEST SUCCEEDED"</automated>
  </verify>
  <done>
    SyncAnchorHash.swift uses CommonCrypto; pre-swap-captured hex fixture matches post-swap output; SyncAnchorHashTests passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove soto + audit swift-nio from Package.swift; verify full suite green</name>
  <files>apple/DS3Lib/Package.swift, apple/DS3Lib/Package.resolved</files>
  <read_first>
    - apple/DS3Lib/Package.swift (current dependencies array)
    - apple/DS3Lib/Tests/DS3LibTests/ (full directory — grep for `import NIOCore`)
    - apple/DS3DriveProviderTests/ (grep for `import NIOCore`)
    - apple/DS3Thumbnails/ (grep for `import NIOCore`)
    - .planning/phases/16-apple-incremental-swap/16-PATTERNS.md §"Package.swift (SPM manifest)"
    - .planning/phases/16-apple-incremental-swap/16-RESEARCH.md §"Package Legitimacy Audit" + §Pitfall 10
  </read_first>
  <action>
    **Audit swift-nio first:**
    ```bash
    grep -rln 'import NIOCore\|import NIO' apple/
    ```
    If 0 hits → swift-nio is safe to remove from Package.swift dependencies.
    If hits exist → keep swift-nio, note in 16-05-SUMMARY which tests retain it and why.

    **Audit Soto fully:**
    ```bash
    grep -rln 'import SotoS3\|import SotoCore\|AWSErrorType\|S3ErrorType\|SotoS3\.' apple/
    ```
    Result MUST be 0 (Plans 03 + 04 already migrated all source + test sites). If any hit remains, fix it before proceeding.

    **Edit `apple/DS3Lib/Package.swift`** per PATTERNS.md §"Post-swap target":
    - Remove the `.package(url: "https://github.com/soto-project/soto", from: "6.8.0")` line from `dependencies`
    - Remove the `.product(name: "SotoS3", package: "soto")` line from the DS3Lib target's `dependencies`
    - If swift-nio audit returned 0 hits, also remove `.package(url: "https://github.com/apple/swift-nio.git", from: "2.62.0")` from `dependencies` AND `.product(name: "NIOCore", package: "swift-nio")` from the testTarget's `dependencies`
    - Confirm `.binaryTarget(name: "DS3CoreFFI", path: "../../core/out/DS3CoreFFI.xcframework")` from Plan 01 is still present
    - Final `dependencies` array should contain: `swift-atomics` (kept) + optionally `swift-nio` (only if audit found NIOCore imports)
    - Final `DS3Lib` target dependencies: `["DS3CoreFFI", .product(name: "Atomics", package: "swift-atomics")]`

    **Update Package.resolved:** Run `xcodebuild -resolvePackageDependencies -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive"`. The resolved file at `apple/DS3Lib/Package.resolved` (or `apple/DS3Drive.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`) must no longer reference `soto-project/soto` or (if removed) `apple/swift-nio`.

    **Run the full test suite:**
    ```
    xcodebuild test -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS'
    ```
    Expected: 0 failures. If any test fails, the missing dep is being implicitly relied on — re-audit, restore the dep, document the dependency reason.

    **Final symbol grep verification (per D-27 + PR review):**
    ```
    grep -rn 'SotoS3\|SotoCore\|AWSErrorType\|S3ErrorType\|CryptoKit\|import Crypto\b' apple/ | grep -v '\.planning' | grep -v Package.resolved
    ```
    Must return 0 lines. The grep `import Crypto\b` catches `import Crypto` (Apple's standalone Crypto package) but not `import CommonCrypto`.

    **iOS build verification:** `xcodebuild build -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive iOS" -destination 'generic/platform=iOS'` (or whatever iOS scheme is named — verify by reading project.pbxproj or running `xcodebuild -list -project apple/DS3Drive.xcodeproj`). Must build clean — confirms iOS slices of the XCFramework link correctly without Soto.
  </action>
  <verify>
    <automated>! grep -rn 'SotoS3\|SotoCore\|AWSErrorType\|S3ErrorType\|import CryptoKit\|import Crypto[^F]' apple/ 2>/dev/null | grep -v '\.planning' | grep -v Package.resolved | grep -v Crypto.framework | head -5 | grep -q . ; STATUS=$? ; test $STATUS -ne 0 && xcodebuild test -project apple/DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' 2>&1 | tail -20 | grep -qE "Test Suite.*All tests.*passed|TEST SUCCEEDED"</automated>
  </verify>
  <done>
    Package.swift no longer lists Soto (and swift-nio if audit allowed); Package.resolved updated; zero Soto/CryptoKit/import-Crypto symbols remain in apple/; full macOS test suite passes; iOS build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — visual PR review of dependency removal (D-27)</name>
  <what-built>
    Soto package, all SotoS3/SotoCore identifiers, CryptoKit import (from auth + SyncAnchorHash), and (if audit allowed) swift-nio have been removed from the DS3Lib package. SyncAnchorHash now uses CommonCrypto with a byte-identical SHA256 implementation verified by fixture tests.
  </what-built>
  <how-to-verify>
    1. Open the draft PR for Plan 05 and review the diff of `apple/DS3Lib/Package.swift`. Confirm exactly these changes:
       - REMOVED: `.package(url: "https://github.com/soto-project/soto", ...)` line
       - REMOVED: `.product(name: "SotoS3", package: "soto")` from the DS3Lib target dependencies
       - REMOVED (if Plan 05 audit found 0 NIOCore imports): `.package(url: "...swift-nio.git", ...)` + `.product(name: "NIOCore", ...)`
       - UNCHANGED: `.binaryTarget(name: "DS3CoreFFI", ...)` from Plan 01
       - UNCHANGED: `swift-atomics` declaration
    2. Open `apple/DS3Lib/Package.resolved` and confirm no `soto-project/soto` entries remain.
    3. Run locally: `grep -rn 'SotoS3\|SotoCore\|AWSErrorType\|S3ErrorType' apple/ | grep -v .planning | grep -v Package.resolved` — must return 0 lines.
    4. Run locally: `grep -rn 'import CryptoKit\|import Crypto\b' apple/DS3Lib/Sources/` — must return 0 lines.
    5. Confirm `grep -rn 'import CommonCrypto' apple/DS3Lib/Sources/Enumeration/SyncAnchorHash.swift` returns exactly 1 line.
    6. Verify the regression test in SyncAnchorHashTests passes — open the test file, confirm the captured pre-swap hex values are committed (not stub `<CAPTURE PRE-SWAP VALUE>` placeholders).
    7. Run `xcodebuild test -scheme "DS3 Drive" -destination 'platform=macOS'` locally — full suite must pass.
    8. Optional: Open Xcode, build & launch the DS3 Drive app, log in to a test Cubbit account, set up a drive. Verify the existing NSFileProviderSyncAnchor in `~/Library/Group Containers/group.X889956QSM.io.cubbit.DS3Drive/drives.json` still parses and the drive's enumerator does not trigger a full re-enumeration — confirms anchor byte-equality post-swap.
  </how-to-verify>
  <resume-signal>Type "approved" once visual PR review confirms the dependency surface is clean and SyncAnchor format is preserved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package.swift removal of Soto | Build-time only; no runtime impact since all Soto symbols already migrated in Plans 03/04 |
| SyncAnchorHash output format | Persisted to App Group container; consumed by NSFileProviderSyncAnchor — byte-format drift forces full re-enumeration on upgrade |
| CommonCrypto vs CryptoKit FIPS-180-4 | Both wrap Apple CoreCrypto's underlying SHA-256 primitive (Assumption A6) — algorithm-identical by spec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | SyncAnchorHash output drift causes silent full re-enumeration | mitigate | Pre-swap-captured fixture in SyncAnchorHashTests locks the format; any future drift breaks the test before merge |
| T-16-05-02 | Denial of Service | Removing swift-nio breaks an undeclared transitive test dependency | mitigate | Step 1 of Task 2 audits `import NIOCore` first; only remove if grep returns 0 hits; full xcodebuild test confirms before merge |
| T-16-05-03 | Information Disclosure | Stale Soto code left in dead-code paths | mitigate | Symbol grep in verify gate must return 0; visual PR review (D-27) catches anything grep misses |
| T-16-05-04 | Spoofing | Mistaking `import Crypto` (Apple Crypto package, future) for `import CryptoKit` | mitigate | Grep pattern `'import Crypto\b'` matches both — Plan 05 verifies both are absent from DS3Lib/Sources |
| T-16-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; this plan only REMOVES packages |
</threat_model>

<verification>
- `apple/DS3Lib/Package.swift` does NOT contain the strings "soto" or "SotoS3"
- `apple/DS3Lib/Package.resolved` does NOT contain "soto-project"
- `grep -rn 'SotoS3\|SotoCore\|AWSErrorType\|S3ErrorType\|import CryptoKit' apple/DS3Lib/Sources apple/DS3DriveProvider apple/DS3Drive apple/DS3DriveApp apple/DS3Thumbnails apple/DS3DriveProviderTests` returns 0 lines
- `grep -c "import CommonCrypto" apple/DS3Lib/Sources/DS3Lib/Enumeration/SyncAnchorHash.swift` returns 1
- SyncAnchorHashTests passes (pre-swap fixture hex matches post-swap output)
- `xcodebuild test -scheme "DS3 Drive" -destination 'platform=macOS'` exits 0 with 0 failures
- iOS build (`xcodebuild build -scheme <iOS scheme> -destination 'generic/platform=iOS'`) exits 0
</verification>

<success_criteria>
- Soto removed from Package.swift + Package.resolved
- swift-nio audited and removed if not used
- CryptoKit removed from DS3Lib/Sources
- SyncAnchorHash uses CommonCrypto and produces byte-identical SHA256 output (locked by fixture test)
- Full test suite passes on macOS + iOS builds
- Visual PR review confirms the clean dependency surface (D-27)
</success_criteria>

<output>
Create `.planning/phases/16-apple-incremental-swap/16-05-SUMMARY.md` documenting:
- Final Package.swift dependency list
- swift-nio audit result (removed or kept + reason)
- The pre-swap SHA256 fixture hex values committed in SyncAnchorHashTests
- Any unexpected import cleanup discovered during the audit
- Confirmation that the NSFileProviderSyncAnchor format is preserved (manual inspection of a drives.json from a developer machine, or test fixture-based proof)
</output>
