---
phase: 05-ux-polish
plan: 20
type: execute
wave: 4
gap_closure: true
depends_on: [05-17, 05-18, 05-19]
files_modified:
  - DS3Drive/Views/Tutorial/Views/TutorialView.swift
  - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
  - DS3Drive/Assets/Assets.xcassets/tutorial
  - DS3Drive/Assets/Localizable.xcstrings
autonomous: false
requirements: [UX-02]

must_haves:
  truths:
    - "Tutorial slides use the corrected Plan 05-17 brand tokens (Figtree, #0E0E15 backdrop, #005CE8 primary)"
    - "All 7 slides share DS3Gradients.brandVerticalBackground — no bookend/flat jump (redundant with Plan 05-18 Task 3 Step 3; this plan re-verifies)"
    - "Screenshot assets in Assets.xcassets/tutorial reflect the FINAL post-05-18/05-19 UI, not the Plan 05-14 placeholders or Plan 05-11 wrong-brand UI"
    - "Human captured fresh screenshots on both macOS and iOS surfaces"
  artifacts:
    - path: "DS3Drive/Assets/Assets.xcassets/tutorial"
      provides: "Fresh, final-brand screenshots for all 7 tutorial slides"
  key_links:
    - from: "DS3Drive/Views/Tutorial/Views/TutorialView.swift"
      to: "DS3Colors.brandBackground + DS3Gradients.brandVerticalBackground (corrected)"
      via: "backdrop ZStack"
      pattern: "brandVerticalBackground|brandHeroSubtle"
---

<objective>
Closes the Gap 3 follow-up (tutorial screenshots) and re-verifies Gap 20 (slide background unification) after Plans 05-17, 05-18, 05-19 land. This is the final polish step — identical in structure to Plan 05-14 but using the corrected brand and fresh screenshots of the actually-shipped UI.

**Critical context:** Plan 05-14 captured placeholder screenshots but the build was blocked on human screenshot capture (per STATE.md `blockers`). Because Plan 05-11's brand was wrong, those placeholders would have been wrong anyway. This plan supersedes Plan 05-14's screenshot checkpoint — the user captures NEW screenshots against the final, correct UI.

Output: tutorial ships with real, on-brand screenshots on both platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-14-SUMMARY.md
@.planning/phases/05-ux-polish/05-17-SUMMARY.md
@.planning/phases/05-ux-polish/05-18-SUMMARY.md
@.planning/phases/05-ux-polish/05-19-SUMMARY.md
@CLAUDE.md
@DS3Drive/Views/Tutorial/Views/TutorialView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify TutorialView uses correct tokens + unified backdrop</name>
  <files>
    DS3Drive/Views/Tutorial/Views/TutorialView.swift,
    DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
  </files>
  <read_first>
    - DS3Drive/Views/Tutorial/Views/TutorialView.swift (full)
    - .planning/phases/05-ux-polish/05-14-SUMMARY.md (original tutorial work)
    - .planning/phases/05-ux-polish/05-18-SUMMARY.md (verify Plan 05-18 Task 3 Step 3 landed the unified backdrop)
  </read_first>
  <action>
    Step 1 — Read TutorialView.swift. Verify:
    - Backdrop uses `DS3Gradients.brandVerticalBackground` (or `brandHeroSubtle`) unconditionally for all slides
    - No slide-index conditional for background (should already be fixed by Plan 05-18 Task 3 Step 3 — if not, fix it here)
    - Typography uses `DS3Typography.h1/h2/body` (Figtree)
    - Slide card (if any) uses `.brandCard()` modifier
    - Navigation buttons use `BrandPrimaryButtonStyle`
    - Text colors use `DS3Colors.textPrimary/Secondary`

    Step 2 — If any token reference is stale (old brandGrey800, brandBlue400, etc.), fix it.

    Step 3 — Localization check: `Localizable.xcstrings` tutorial copy (UX-01..UX-07) already landed in Plan 05-14 — verify still present, no regressions.
  </action>
  <verify>
    <automated>xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" build 2>&1 | grep -E "error:" | head -10</automated>
  </verify>
  <done>
    - Build clean
    - grep shows no stale token names in TutorialView
    - Manual: walk through all 7 slides, backdrop is consistent, text is Figtree, buttons are brand blue
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human captures fresh tutorial screenshots</name>
  <what-built>
    TutorialView rebound to corrected brand tokens. The 7 placeholder screenshots in Assets.xcassets/tutorial need to be replaced with real captures of the shipped UI.
  </what-built>
  <how-to-verify>
    **IMPORTANT:** Plan 05-18 and 05-19 must be fully complete and visually approved before this checkpoint runs.

    For each of the 7 tutorial slides (UX-01 through UX-07), capture a PNG screenshot of the relevant surface at 2x retina resolution and replace the placeholder in `DS3Drive/Assets/Assets.xcassets/tutorial/`:

    | Slide | Surface | What to capture |
    |-------|---------|-----------------|
    | UX-01 | Finder badges | A Finder window showing synced/syncing/error badge states on real files |
    | UX-02 | Tray menu | The full tray popover with at least one drive row + footer |
    | UX-03 | Speed display | Tray with an active transfer showing upload/download speed |
    | UX-04 | Recent Files | Tray side panel with recent files list |
    | UX-05 | Quick actions | Gear menu opened on a drive row |
    | UX-06 | Setup wizard | The confirmation step showing project badge + summary card |
    | UX-07 | Drive limit | Preferences or a modal showing the 3-drive limit |

    Capture instructions:
    1. Run the app in Release mode (`xcodebuild -configuration Release`) for the final brand look
    2. For each slide, navigate to the surface, wait for any animations to settle, take a screenshot (`Cmd+Shift+4`, then `Space`, then click the window for a clean window capture)
    3. Save as `{UX-XX}.png` in `DS3Drive/Assets/Assets.xcassets/tutorial/` replacing placeholders
    4. If there's a 2x / 3x variant convention, match it
    5. Also capture Italian (`it`) variants if the tutorial localizes screenshots (check `Localizable.xcstrings` tutorial section for locale-specific asset keys)
    6. Optional: also capture the iOS tutorial screenshots if the iOS app has a separate tutorial

    After dropping the files:
    - Rebuild the app
    - Walk through the tutorial once to confirm each slide's image loads correctly
    - Confirm no placeholder images remain

    Respond with "approved" or list of issues.
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 screenshots are replaced and verified in-app</resume-signal>
</task>

</tasks>

<verification>
- All 7 tutorial assets updated
- Tutorial walkthrough shows real final UI
- Phase 05 fully closed
</verification>

<success_criteria>
- Gap 3 follow-up and Gap 20 closed
- Phase 05 UX Polish milestone complete
- STATE.md `blockers` (Plan 05-14 placeholder screenshots) cleared
</success_criteria>

<output>
Create `.planning/phases/05-ux-polish/05-20-SUMMARY.md` with:
- Screenshot paths updated
- Human approval
- Final Phase 05 retrospective: what actually landed in Round 2 vs Round 1 claims, and which Round 1 plans should be marked as "incomplete" in the roadmap
</output>
