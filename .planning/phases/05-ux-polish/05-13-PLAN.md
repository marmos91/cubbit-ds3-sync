---
phase: 05-ux-polish
plan: 13
type: execute
wave: 3
gap_closure: true
depends_on: [05-11]
files_modified:
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Spacing.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - DS3DriveApp
  - DS3DriveShareExtension
  - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
  - DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset
autonomous: true
requirements: [UX-02, UX-03]

must_haves:
  truths:
    - "DS3Colors, DS3Typography, DS3Spacing, DS3Gradients live in DS3Lib (or are accessible to both macOS and iOS targets)"
    - "iOS app views read from the same brand tokens as the macOS app — visual parity"
    - "DS3DriveShareExtension upload UI uses the brand tokens"
    - "Wizard empty detail pane shows a brand-aligned illustration / hero, no longer bare"
    - "iOS App Icon present at all required sizes"
  artifacts:
    - path: "DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift"
      provides: "Brand tokens accessible from both macOS and iOS targets"
      contains: "brandPrimary"
  key_links:
    - from: "DS3DriveApp/ iOS views"
      to: "DS3Lib DesignSystem brand tokens"
      via: "import DS3Lib"
      pattern: "DS3Colors.brand"
---

<objective>
Closes Gaps 17 and 5.

Gap 17: iOS app needs the same Cubbit brand identity treatment as macOS — apply brand tokens from Plan 05-11 to the iOS target (DS3DriveApp + DS3DriveShareExtension), achieving visual parity.

Gap 5: Wizard empty detail pane is bare — add a brand-aligned illustration / 3-step "how this works" hint to the empty state.

Both depend on Plan 05-11 brand tokens. Bundled together because Gap 5 is small and naturally lands when the brand work is being applied broadly.

Purpose: brand parity across platforms + the wizard empty state polish.
Output: shared design system in DS3Lib, iOS views swept for brand tokens, wizard empty pane illustration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
@.planning/phases/05-ux-polish/05-11-SUMMARY.md
@.planning/phases/05-ux-polish/05-05-SUMMARY.md
@CLAUDE.md

Per CLAUDE.md, DS3Lib is the shared library between macOS app and File Provider extension. The iOS targets (DS3DriveApp, DS3DriveShareExtension) likely also import DS3Lib. Verify target membership before moving the design system.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move DesignSystem (DS3Colors, DS3Typography, DS3Spacing, DS3Gradients) into DS3Lib</name>
  <files>
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Spacing.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  </files>
  <read_first>
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Typography.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Spacing.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift,
    DS3Lib/Package.swift,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </read_first>
  <action>
    1. Verify DS3Lib's `Package.swift` exposes a SwiftUI-capable target. If `import SwiftUI` is not yet allowed in DS3Lib, check the platforms list — it should include both `.macOS(.v15)` and `.iOS(.v17)` (or current minimum). Add the iOS platform if missing.
    2. Move the four design-system files from `DS3Drive/Views/Common/DesignSystem/` into `DS3Lib/Sources/DS3Lib/DesignSystem/`:
       - `DS3Colors.swift`
       - `DS3Typography.swift`
       - `DS3Spacing.swift`
       - `DS3Gradients.swift`
       Mark every public type and `static let` as `public`.
    3. Wrap any AppKit-only references (e.g., `NSColor`) in `#if os(macOS)` and provide UIKit equivalents under `#if os(iOS)` (`UIColor`). For tokens that use `Color(red:green:blue:)` literals, no platform guard is needed.
    4. In `DS3Drive/Views/Common/DesignSystem/DS3Colors.swift` (and the other three files in the macOS app target), replace the contents with a re-export shim:
       ```swift
       @_exported import DS3Lib
       ```
       Or simply delete the old files if the macOS target can `import DS3Lib` cleanly. Update the Xcode project file membership accordingly via xcodeproj edits or `xcodebuild` confirms target membership.
    5. Verify both schemes still build:
       - `xcodebuild build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS"`
       - `xcodebuild build -project DS3Drive.xcodeproj -scheme "DS3DriveApp" -destination "generic/platform=iOS"` (or whichever the iOS scheme is named)
    6. Verify the Swift 6 concurrency posture: any `static let Color` should be safe on both targets; if a sendability warning surfaces, mark the type with `@unchecked Sendable` only as a last resort, otherwise prefer `@MainActor` or computed properties.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && test -f DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift && test -f DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift && test -f DS3Lib/Sources/DS3Lib/DesignSystem/DS3Spacing.swift && test -f DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift && xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - file DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift exists
    - file DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift exists
    - file DS3Lib/Sources/DS3Lib/DesignSystem/DS3Spacing.swift exists
    - file DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift exists
    - grep `public static let brandPrimary` in DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift returns at least one match
    - grep `\.iOS\|iOS(.v` in DS3Lib/Package.swift returns at least one match
    - macOS scheme builds clean
  </acceptance_criteria>
  <done>DesignSystem files live in DS3Lib with public access; both macOS and iOS platforms supported in Package.swift; macOS build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep iOS views (DS3DriveApp + ShareExtension) for brand tokens + verify iOS App Icon</name>
  <files>
    DS3DriveApp,
    DS3DriveShareExtension,
    DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset
  </files>
  <read_first>
    DS3DriveApp,
    DS3DriveShareExtension,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </read_first>
  <action>
    1. List all .swift files in `DS3DriveApp/` and `DS3DriveShareExtension/`. For each file:
       - Replace inline color literals (`Color(red:green:blue:)`, `Color(.systemBackground)`, `.darkWhite`, `.darkMainStandard`, hex strings) with the appropriate `DS3Colors.brand*` token.
       - Replace inline font specs (`.font(.system(size: ...))`) with `DS3Typography.X` tokens.
       - Replace magic spacing values (`.padding(16)`, `.padding(.horizontal, 24)`) with `DS3Spacing.md`/`.lg` tokens.
       - Add `import DS3Lib` to any file that now references DS3Colors/DS3Typography/DS3Spacing.
    2. Apply brand background to the iOS root container (login, drive list, file viewer, settings, share extension) so the iOS app feels like the same product as macOS.
    3. Verify the iOS App Icon catalog `DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset` (or the iOS-specific icon set) has all required sizes. If sizes are missing, regenerate from the 1024x1024 master using `sips` or note the missing sizes in the SUMMARY for follow-up by the design team.
    4. Build the iOS scheme: `xcodebuild build -project DS3Drive.xcodeproj -scheme "DS3DriveApp" -destination "generic/platform=iOS"`. (If the scheme name differs, use `xcodebuild -list -project DS3Drive.xcodeproj` to discover it.)
    5. After the sweep, run a final grep to confirm zero inline `darkWhite`/`darkMainStandard` references and no hex literals in iOS source files.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -rE "darkWhite|darkMainStandard" DS3DriveApp DS3DriveShareExtension 2>/dev/null; xcodebuild -list -project DS3Drive.xcodeproj 2>&1 | head -40; xcodebuild build -project DS3Drive.xcodeproj -scheme "DS3DriveApp" -destination "generic/platform=iOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep `darkWhite\|darkMainStandard` in DS3DriveApp and DS3DriveShareExtension returns zero matches
    - grep `DS3Colors.brand\|import DS3Lib` across DS3DriveApp/*.swift returns at least one match per major view file
    - grep `DS3Colors.brand\|import DS3Lib` across DS3DriveShareExtension/*.swift returns at least one match
    - iOS scheme build succeeds (or, if scheme not found, SUMMARY documents the discovered scheme name)
  </acceptance_criteria>
  <done>iOS targets sweep complete; brand tokens applied; iOS build succeeds; AppIcon set verified.</done>
</task>

<task type="auto">
  <name>Task 3: Wizard empty detail pane illustration (Gap 5)</name>
  <files>
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    DS3Drive/Assets/Assets.xcassets
  </files>
  <read_first>
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </read_first>
  <action>
    1. In `TreeNavigationView.swift`, locate the `else` branch around line 522-534 where the empty detail pane is rendered (text "Select a project, bucket, or folder from the tree" on a blank background).
    2. Replace the bare layout with a centered `VStack(spacing: DS3Spacing.lg)`:
       - A muted Cubbit logo / brand mark from the asset catalog as `Image(systemName: "cube.transparent")` (or a brand-specific asset if one exists in the asset catalog) sized 64x64 with `DS3Colors.brandPrimary.opacity(0.6)`.
       - Heading text using `DS3Typography.title2.bold()`: "Set up your first drive" (localized)
       - 3-step hint list using `DS3Typography.body.foregroundStyle(DS3Colors.brandTextSecondary)`:
         1. Choose a project
         2. Pick a bucket
         3. Select a folder to sync
       - Each step prefixed with a small numbered circle using `DS3Colors.brandPrimary`.
    3. Wrap the VStack in a `.frame(maxWidth: .infinity, maxHeight: .infinity)` and `.background(DS3Colors.brandBackground)`.
    4. Add localization keys to `Localizable.xcstrings`:
       - `wizard.empty.title` — "Set up your first drive" / "Configura il tuo primo drive"
       - `wizard.empty.step1` — "Choose a project" / "Scegli un progetto"
       - `wizard.empty.step2` — "Pick a bucket" / "Seleziona un bucket"
       - `wizard.empty.step3` — "Select a folder to sync" / "Seleziona una cartella da sincronizzare"
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `wizard.empty\|Set up your first drive\|cube.transparent` in DS3Drive/Views/Sync/Views/TreeNavigationView.swift returns at least one match
    - grep `wizard.empty.title\|wizard.empty.step` in DS3Drive/Assets/Localizable.xcstrings returns at least one match
    - grep `Configura il tuo primo drive` in DS3Drive/Assets/Localizable.xcstrings returns at least one match
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>Wizard empty pane shows centered brand mark + heading + 3-step hint instead of bare text; localized in English and Italian.</done>
</task>

</tasks>

<verification>
- DS3Lib hosts the design system; both targets compile against it
- iOS views reference brand tokens
- Wizard empty pane has brand-aligned hero
- Both macOS and iOS schemes build green
</verification>

<success_criteria>
- All three tasks complete with grep-verifiable acceptance criteria met
- Both macOS and iOS builds green
- Visual parity between macOS and iOS confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-13-SUMMARY.md` documenting Gaps 17 and 5 closure, list of iOS files swept, iOS scheme name confirmed, and any AppIcon sizes still missing.
</output>
