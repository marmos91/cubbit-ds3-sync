---
phase: 05-ux-polish
plan: 11
type: execute
wave: 2
gap_closure: true
depends_on: []
files_modified:
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift
  - DS3Drive/Assets/Assets.xcassets/Colors
  - DS3Drive/Views/Login/Views/LoginView.swift
  - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
  - DS3Drive/Views/Preferences/Views/PreferencesView.swift
  - DS3Drive/Views/Tutorial/Views/TutorialView.swift
  - .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
autonomous: true
requirements: [UX-02, UX-03, UX-06]

must_haves:
  truths:
    - "DS3Colors.swift contains Cubbit brand tokens extracted from the Cubbit Figma design file (not generic dark charcoal)"
    - "Login window applies the Cubbit brand treatment (brand background or gradient + brand accent)"
    - "Wizard, Preferences, Tutorial all read from the same brand tokens"
    - "A brand tokens reference file (05-11-BRAND-TOKENS.md) exists documenting which Figma variables map to which Swift tokens"
  artifacts:
    - path: "DS3Drive/Views/Common/DesignSystem/DS3Colors.swift"
      provides: "Cubbit brand color palette as static let tokens (brandPrimary, brandSecondary, brandAccent, brandSurface, brandBackground, brandGradientStart, brandGradientEnd)"
      contains: "brandPrimary"
    - path: "DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift"
      provides: "Brand gradient definitions for hero areas"
    - path: ".planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md"
      provides: "Mapping table from Figma variable names to Swift token names"
  key_links:
    - from: "DS3Drive/Views/Login/Views/LoginView.swift"
      to: "DS3Colors brand tokens"
      via: "background, accent, gradient"
      pattern: "DS3Colors.brand"
---

<objective>
Closes Gap 2 — visual identity does not match Cubbit brand.

Establishes the Cubbit brand foundation that downstream plans (05-12 tray redesign, 05-13 iOS parity, 05-14 tutorial refresh) all depend on. Extracts brand tokens from the Cubbit Figma design file via the Figma MCP and applies them to the macOS app's design system.

Purpose: replace generic dark charcoal with Cubbit brand identity so all windows feel like the same Cubbit product.
Output: brand tokens in DS3Colors + DS3Gradients, applied to login (hero), wizard, preferences, tutorial; documented in BRAND-TOKENS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-05-SUMMARY.md
@CLAUDE.md

Cubbit brand Figma file: https://www.figma.com/design/esjch8fneVvEwGd4dCLafW/Cubbit-%7C-Design-File?node-id=0-1
File key: `esjch8fneVvEwGd4dCLafW`
Node id: `0:1`

Use the Figma MCP server tools (`mcp__plugin_figma_figma__get_variable_defs`, `mcp__plugin_figma_figma__get_design_context`) to extract the brand variables. Per the MCP server instructions: prioritize this server when the user mentions Figma URLs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Cubbit brand tokens from Figma + write BRAND-TOKENS.md reference</name>
  <files>
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift
  </files>
  <read_first>
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    1. Use the Figma MCP tool `mcp__plugin_figma_figma__get_variable_defs` with `fileKey: "esjch8fneVvEwGd4dCLafW"` and `nodeId: "0:1"` to fetch all brand color and gradient variables. Capture the raw output.
    2. If the variable defs call returns insufficient detail, also call `mcp__plugin_figma_figma__get_design_context` on the same node to pull design tokens enriched with screenshot context.
    3. Write `.planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md` containing:
       - The Figma file URL and node id
       - A mapping table: `Figma Variable Name` -> `Hex` -> `Swift token (DS3Colors.brandX)` -> `Usage notes`
       - At minimum the following Swift token names must be defined in the table: `brandPrimary`, `brandSecondary`, `brandAccent`, `brandBackground`, `brandSurface`, `brandTextPrimary`, `brandTextSecondary`, `brandGradientStart`, `brandGradientEnd`
       - If the Cubbit Figma uses light/dark variants, capture both
    4. Add the corresponding `static let` tokens to `DS3Colors.swift`. Use `Color(red:green:blue:)` literals derived from the Figma hex values. For light/dark adaptive, use `Color(NSColor(name: nil, dynamicProvider: { appearance in ... }))`.
    5. Create `DS3Gradients.swift` with:
       ```swift
       import SwiftUI
       enum DS3Gradients {
           static let brandHero = LinearGradient(
               colors: [DS3Colors.brandGradientStart, DS3Colors.brandGradientEnd],
               startPoint: .topLeading,
               endPoint: .bottomTrailing
           )
       }
       ```
    6. Do NOT delete the existing legacy `DS3Colors.background` token yet — downstream plans 05-12 and 05-13 will sweep call sites; this task only adds the new tokens.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && test -f .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md && grep -c "brandPrimary\|brandAccent\|brandGradientStart" DS3Drive/Views/Common/DesignSystem/DS3Colors.swift && xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - file .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md exists
    - grep `brandPrimary` in DS3Drive/Views/Common/DesignSystem/DS3Colors.swift returns at least one match
    - grep `brandSecondary\|brandAccent` in DS3Drive/Views/Common/DesignSystem/DS3Colors.swift returns at least one match
    - grep `brandGradientStart\|brandGradientEnd` in DS3Drive/Views/Common/DesignSystem/DS3Colors.swift returns at least one match
    - file DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift exists
    - grep `brandHero\|LinearGradient` in DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift returns at least one match
    - grep `Figma Variable\|brandPrimary` in .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md returns at least one match
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>BRAND-TOKENS.md exists with Figma->Swift mapping; DS3Colors has brand tokens; DS3Gradients has brandHero; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Apply brand tokens to Login (hero), Wizard, Preferences, Tutorial</name>
  <files>
    DS3Drive/Views/Login/Views/LoginView.swift,
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Tutorial/Views/TutorialView.swift
  </files>
  <read_first>
    DS3Drive/Views/Login/Views/LoginView.swift,
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Tutorial/Views/TutorialView.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </read_first>
  <action>
    1. In `LoginView.swift`, replace the root window background with `DS3Gradients.brandHero` (or `DS3Colors.brandBackground` if a flat brand background is more appropriate per Figma). Add a subtle brand-colored hero treatment behind the DS3 Drive logo (e.g., a soft radial gradient from `brandGradientStart`).
    2. In `TreeNavigationView.swift`, replace `DS3Colors.background` references in the wizard chrome (sidebar background, detail-pane background) with `DS3Colors.brandSurface` for cards and `DS3Colors.brandBackground` for the window background.
    3. In `PreferencesView.swift`, apply `DS3Colors.brandBackground` to the window background and `DS3Colors.brandSurface` to the tab content cards.
    4. In `TutorialView.swift`, apply the same brand background. (Tutorial screenshots themselves are addressed in Plan 05-14; this task only updates chrome.)
    5. After each file update, verify that no inline hex literals or `.darkWhite` / `.darkMainStandard` references remain in these specific files. If found, replace with the appropriate brand token.
    6. Build and verify the four windows render with the new brand treatment in both light and dark mode.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -E "darkWhite|darkMainStandard|Color\(red: 0\.|#[0-9a-fA-F]{6}" DS3Drive/Views/Login/Views/LoginView.swift DS3Drive/Views/Sync/Views/TreeNavigationView.swift DS3Drive/Views/Preferences/Views/PreferencesView.swift DS3Drive/Views/Tutorial/Views/TutorialView.swift; xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep `DS3Colors.brand\|DS3Gradients.brand` in DS3Drive/Views/Login/Views/LoginView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Sync/Views/TreeNavigationView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Preferences/Views/PreferencesView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Tutorial/Views/TutorialView.swift returns at least one match
    - grep `darkWhite\|darkMainStandard` across the four files above returns zero matches
    - xcodebuild clean build analyze succeeds
  </acceptance_criteria>
  <done>Login, wizard, preferences, tutorial all read from DS3Colors.brand* tokens; no legacy dark color references remain in those files; build succeeds.</done>
</task>

</tasks>

<verification>
- BRAND-TOKENS.md documents the Figma -> Swift mapping
- DS3Colors and DS3Gradients expose brandX tokens
- Four primary windows apply brand treatment
- xcodebuild clean build analyze succeeds
</verification>

<success_criteria>
- All grep-verifiable acceptance criteria met
- Build green
- Plans 05-12, 05-13, 05-14 can read DS3Colors.brand* and DS3Gradients without further extraction work
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-11-SUMMARY.md` documenting Gap 2 closure, the brand token names exposed, and which Figma node ids were sourced. Note any tokens that were inferred rather than directly extracted (so future work can verify against the design team).
</output>
