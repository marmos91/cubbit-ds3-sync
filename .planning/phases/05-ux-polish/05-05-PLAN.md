---
phase: 05-ux-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - DS3Drive/Assets/Localizable.xcstrings
  - DS3Drive/Views/Common/ButtonStyles.swift
  - DS3Drive/Views/Common/BorderedSectionView.swift
  - DS3Drive/Views/Common/CubbitTextField.swift
  - DS3Drive/Views/Common/IconButtonView.swift
  - DS3Drive/Views/Common/IconTextFieldView.swift
  - DS3Drive/Views/Common/LinkView.swift
  - DS3Drive/Views/Common/LoadingView.swift
  - DS3Drive/Views/Common/OutlineLink.swift
  - DS3Drive/Views/Tutorial/TutorialView.swift
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - UX-07

must_haves:
  truths:
    - "All user-facing strings say 'DS3 Drive' (no 'Cubbit DS3 Sync' references remain)"
    - "Italian localization available in .xcstrings for all user-facing strings"
    - "All common view components use SF Pro and semantic colors (no Nunito, no hardcoded dark colors)"
    - "Tutorial view matches new design language"
    - "Full UX flow verified by human: login, wizard, tray, badges, preferences"
  artifacts:
    - path: "DS3Drive/Assets/Localizable.xcstrings"
      provides: "All localized strings with Italian translations"
      contains: "it.*translation"
    - path: "DS3Drive/Views/Common/ButtonStyles.swift"
      provides: "Updated button styles with design system colors"
      contains: "accentColor|system"
  key_links:
    - from: "DS3Drive/Assets/Localizable.xcstrings"
      to: "All views"
      via: "NSLocalizedString keys resolve to Italian when system language is it"
      pattern: "NSLocalizedString"
---

<objective>
Audit and fix all remaining copy/branding references, apply design system to common view components not yet updated, add Italian localization to the String Catalog, and perform a final human verification of the complete UX flow.

Purpose: This final plan ensures visual consistency across ALL views (including shared components and tutorial), correct branding throughout, and Italian language support. The checkpoint ensures all 7 UX requirements work end-to-end.

Output: Updated common components, Italian translations in .xcstrings, all "Cubbit DS3 Sync" references fixed, human-verified UX flow.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md

@DS3Drive/Views/Common/ButtonStyles.swift
@DS3Drive/Views/Common/BorderedSectionView.swift
@DS3Drive/Views/Common/CubbitTextField.swift
@DS3Drive/Views/Common/IconTextFieldView.swift
@DS3Drive/Assets/Localizable.xcstrings

<!-- Load all prior summaries for full context -->
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-02-SUMMARY.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@.planning/phases/05-ux-polish/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update common components, audit copy, add Italian localization</name>
  <files>
    DS3Drive/Views/Common/ButtonStyles.swift,
    DS3Drive/Views/Common/BorderedSectionView.swift,
    DS3Drive/Views/Common/CubbitTextField.swift,
    DS3Drive/Views/Common/IconButtonView.swift,
    DS3Drive/Views/Common/IconTextFieldView.swift,
    DS3Drive/Views/Common/LinkView.swift,
    DS3Drive/Views/Common/LoadingView.swift,
    DS3Drive/Views/Common/OutlineLink.swift,
    DS3Drive/Views/Tutorial/TutorialView.swift,
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <action>
    **Common component design system sweep:**
    Search all files in DS3Drive/Views/Common/ and DS3Drive/Views/Tutorial/ for:
    - `.font(.custom("Nunito", ...))` -> replace with `.font(.system(size: ...))` or DS3Typography constants
    - `Color(.darkWhite)` -> `.foregroundStyle(.secondary)`
    - `Color(.darkMainStandard)` -> remove or use system background
    - `Color(.background)` -> remove ZStack or use system default
    - `Color(.hover)` -> use system hover color or `.background(.quinary)` for hover states
    - `Color(.darkMainBorder)` -> use `.border(Color(nsColor: .separatorColor))`
    - `Color(.darkMainTop)` -> remove or use system background

    **Specific files to update:**
    - ButtonStyles.swift: PrimaryButtonStyle should use Color.accentColor (Cubbit blue). OutlineButtonStyle should use .secondary border. Replace any Nunito fonts.
    - BorderedSectionView.swift: Replace custom border colors with system separator color. Replace background with system control background.
    - CubbitTextField.swift: Replace Nunito font, replace custom colors with system text field styling. Consider using native TextField styling where possible.
    - IconTextFieldView.swift (aka IconTextField): Same -- system fonts and colors.
    - LinkView.swift: Use Color.accentColor for link color.
    - LoadingView.swift: System font, system colors.
    - OutlineLink.swift: System styling.
    - TutorialView.swift: System font, semantic colors, ensure tutorial text says "DS3 Drive" not old branding.

    **Copy audit:**
    Search the entire DS3Drive target for old branding strings:
    - "Cubbit DS3 Sync" -> "DS3 Drive"
    - "Cubbit DS3 sync" -> "DS3 Drive" (lowercase)
    - "Start Cubbit DS3 sync at login" -> "Start DS3 Drive at login"
    - "Start Cubbit at startup" -> "Start DS3 Drive at login"
    - Any other "Cubbit" references in user-facing strings that should be "DS3 Drive" (Cubbit is fine in URLs and API contexts)
    - Verify all NSLocalizedString comment strings are helpful for translators

    **Italian localization:**
    - Edit DS3Drive/Assets/Localizable.xcstrings to add Italian (it) translations
    - The .xcstrings format is JSON -- add an "it" entry in the "localizations" dict for each string
    - Key user-facing strings to translate (translate ALL strings found in the file, these are the critical ones):
      - "Synchronizing" -> "Sincronizzazione in corso"
      - "Idle" -> "Inattivo"
      - "Error" -> "Errore"
      - "Offline" -> "Non in linea"
      - "Paused" -> "In pausa"
      - "Add a new Drive" -> "Aggiungi un nuovo Drive"
      - "You have reached the maximum number of Drives" -> "Hai raggiunto il numero massimo di Drive"
      - "Help" -> "Aiuto"
      - "Preferences" -> "Preferenze"
      - "Open web console" -> "Apri console web"
      - "Sign Out" -> "Esci"
      - "Quit" -> "Esci dall'applicazione"
      - "Log in" -> "Accedi"
      - "Email" -> "Email"
      - "Password" -> "Password"
      - "Loading..." -> "Caricamento..."
      - "Forgot your password?" -> "Password dimenticata?"
      - "Sign up" -> "Registrati"
      - "Advanced" -> "Avanzate"
      - "Tenant name" -> "Nome tenant"
      - "Coordinator URL" -> "URL Coordinator"
      - "General" -> "Generale"
      - "Account" -> "Account"
      - "Sync" -> "Sincronizzazione"
      - "Start DS3 Drive at login" -> "Avvia DS3 Drive all'accesso"
      - "Disconnect account" -> "Disconnetti account"
      - "Edit on web console" -> "Modifica sulla console web"
      - "Synced" -> "Sincronizzato"
      - "Syncing" -> "In sincronizzazione"
      - "Cloud Only" -> "Solo cloud"
      - "Conflict" -> "Conflitto"
      - "Signed in as" -> "Accesso effettuato come"
      - "Connection Info" -> "Info connessione"
      - "Copied" -> "Copiato"
      - "Recent Files" -> "File recenti"
      - "No recent files" -> "Nessun file recente"
      - "Disconnect" -> "Disconnetti"
      - "View in Finder" -> "Mostra nel Finder"
      - "View in web console" -> "Visualizza nella console web"
      - "Manage" -> "Gestisci"
      - "Refresh" -> "Aggiorna"
      - "Pause" -> "Pausa"
      - "Resume" -> "Riprendi"
      - "Copy S3 Path" -> "Copia percorso S3"
      - (and all other strings in the .xcstrings file)
    - Ensure the .xcstrings JSON structure is valid after editing
    - NOTE: The Xcode project must have Italian listed in Project > Info > Localizations. If not possible via code edit, document that the user must add "Italian" in Xcode project settings.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild build -scheme DS3Drive -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>All common components use SF Pro and semantic colors. No "Cubbit DS3 Sync" remains in user-facing strings. Italian translations added to .xcstrings for all strings. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final UX verification of all Phase 5 requirements</name>
  <files>N/A</files>
  <action>
    Human verifies the complete UX flow end-to-end. Build and run the app in Xcode, then test each of the 7 UX requirements plus visual design, copy, and localization.

    What was built: Complete UX polish for DS3 Drive Phase 5 -- Finder sync badges, menu bar tray with colored status indicators, real-time speed display, side panels (recent files + connection info), per-drive pause/resume, expanded gear menu, 2-step drive setup wizard, redesigned login (centered card) and preferences (tabbed), design system (SF Pro, semantic colors, Cubbit blue accent), copy audit, and Italian localization.

    Steps to verify:
    1. Finder Sync Badges (UX-01): Open a synced drive in Finder, check badge overlays on files
    2. Menu Bar Tray Status (UX-02): Click tray icon, verify colored dots per drive
    3. Transfer Speed (UX-03): Trigger a sync, verify speed displays in tray
    4. Recent Files (UX-04): Click drive row, verify side panel with recent files
    5. Quick Actions (UX-05): Test gear menu actions including Pause/Resume and Copy S3 Path
    6. Wizard (UX-06): Add new drive, verify 2-step flow with tree view
    7. Drive Limit (UX-07): With 3 drives, verify add is disabled
    8. Visual Design: Check SF Pro font, light/dark mode, Cubbit blue accent
    9. Copy: Search for "Cubbit DS3 Sync" -- should not appear
    10. Localization: Set macOS to Italian, verify translated strings
  </action>
  <verify>Human approval</verify>
  <done>Human confirms all 7 UX requirements pass. Phase 5 complete.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme DS3Drive -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO` succeeds
- `grep -r "Nunito" DS3Drive/Views/` returns zero results
- `grep -r "Cubbit DS3 Sync" DS3Drive/` returns zero results (case-insensitive)
- `grep -r "darkWhite\|darkMainStandard\|\.background)" DS3Drive/Views/Common/` returns zero results
- Localizable.xcstrings contains "it" locale entries
- All 7 UX success criteria from ROADMAP verified by human
</verification>

<success_criteria>
- Zero Nunito font references in the entire DS3Drive target
- Zero "Cubbit DS3 Sync" branding references in user-facing strings
- Italian localization present for all strings
- Common components (ButtonStyles, CubbitTextField, etc.) use design system
- Human approves all 7 UX requirements after testing
- Full phase complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-05-SUMMARY.md`
</output>
