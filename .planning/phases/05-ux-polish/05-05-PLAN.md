---
phase: 05-ux-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Spacing.swift
  - DS3Drive/Views/Tutorial/Views/TutorialView.swift
  - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
  - DS3Drive/Views/Common/ShimmerModifier.swift
  - DS3Drive/Assets/Localizable.xcstrings
autonomous: true
requirements: [UX-01, UX-02, UX-06]

must_haves:
  truths:
    - "All views use DS3Colors/DS3Typography/DS3Spacing tokens — no inline Color(), .font(.custom()), or magic number padding"
    - "Tutorial view matches the new design language (SF Pro, semantic colors, Cubbit accent)"
    - "Italian localization covers all user-facing strings added in plans 01-04"
    - "No remaining Cubbit DS3 Sync branding references in user-facing strings"
  artifacts:
    - path: "DS3Drive/Views/Tutorial/Views/TutorialView.swift"
      provides: "Redesigned tutorial matching card login style"
      contains: "DS3Colors"
    - path: "DS3Drive/Assets/Localizable.xcstrings"
      provides: "Complete Italian translations for all user-facing strings"
  key_links:
    - from: "DS3Drive/Views/Tutorial/Views/TutorialView.swift"
      to: "DS3Drive/Views/Common/DesignSystem/DS3Colors.swift"
      via: "import and usage of design tokens"
      pattern: "DS3Colors\\."
---

<objective>
Design system sweep, tutorial polish, copy audit, and Italian localization verification

Purpose: Ensure all views consistently use design system tokens, the tutorial matches the new design language, and all user-facing copy is correct and localized.
Output: Consistent design language across all views, polished tutorial, complete Italian translations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@.planning/phases/05-ux-polish/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system sweep and tutorial redesign</name>
  <read_first>
    - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
    - DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
    - DS3Drive/Views/Common/DesignSystem/DS3Spacing.swift
    - DS3Drive/Views/Tutorial/Views/TutorialView.swift
    - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
    - DS3Drive/Views/Login/Views/LoginView.swift (reference: card style to match in tutorial)
    - DS3Drive/Views/Tray/Views/TrayMenuView.swift
    - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
    - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
    - DS3Drive/Views/Tray/Views/TrayMenuItem.swift
    - DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
    - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
    - DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift
    - DS3Drive/Views/Sync/Views/SetupSyncView.swift
    - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
    - DS3Drive/Views/Sync/Views/DriveConfirmView.swift
    - DS3Drive/Views/Preferences/Views/PreferencesView.swift
    - DS3Drive/Views/Preferences/Views/GeneralTab.swift
    - DS3Drive/Views/Preferences/Views/AccountTab.swift
    - DS3Drive/Views/Preferences/Views/SyncTab.swift
    - DS3Drive/DS3DriveApp.swift
  </read_first>
  <files>
    DS3Drive/Views/Tutorial/Views/TutorialView.swift,
    DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Typography.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Spacing.swift,
    (plus any view files still using inline colors/fonts/spacing)
  </files>
  <action>
    **Design System Sweep:**
    Scan ALL Swift view files in DS3Drive/Views/ for these anti-patterns and replace:
    - `Color(.*darkWhite.*)` or `Color(.*darkMainStandard.*)` or `Color(.*background.*)` → replace with appropriate `DS3Colors.` token (e.g., DS3Colors.backgroundPrimary, DS3Colors.backgroundSecondary, DS3Colors.textPrimary)
    - `.font(.custom("Nunito"...))` or any Nunito reference → replace with `DS3Typography.` token (e.g., DS3Typography.body, DS3Typography.heading)
    - Magic number padding/spacing (e.g., `.padding(16)`, `.padding(12)`) → replace with `DS3Spacing.` token (e.g., DS3Spacing.md for 16, DS3Spacing.sm for 8, DS3Spacing.lg for 24)
    - Hardcoded hex colors → replace with DS3Colors tokens

    If DS3Colors/DS3Typography/DS3Spacing are missing tokens needed for a specific use case, ADD new tokens to those files.

    **Tutorial Redesign (per D-UI-03):**
    Redesign TutorialView.swift to match the centered card style used in LoginView.swift:
    - Use DS3Colors.backgroundPrimary for the window background
    - Use a centered card container with rounded corners and shadow (same pattern as LoginView)
    - DS3 Drive logo at the top of the card (Image("DS3DriveLogo") or existing app icon asset)
    - Replace any Nunito fonts with DS3Typography tokens
    - Replace any hardcoded colors with DS3Colors tokens
    - Use DS3Spacing tokens for all padding/margins
    - Buttons should use Color.accentColor (Cubbit blue) for primary actions
    - Ensure the tutorial looks cohesive with login, wizard, and preferences windows
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -rn "Nunito" DS3Drive/Views/ --include="*.swift" | grep -v "^.*//.*Nunito" | wc -l | xargs test 0 -eq && grep -rn "darkWhite\|darkMainStandard" DS3Drive/Views/ --include="*.swift" | grep -v "^.*//.*dark" | wc -l | xargs test 0 -eq && echo "PASS: No inline style anti-patterns"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Nunito" DS3Drive/Views/ --include="*.swift" | grep -v "//"` returns 0 results
    - `grep -rn "darkWhite\|darkMainStandard" DS3Drive/Views/ --include="*.swift" | grep -v "//"` returns 0 results
    - TutorialView.swift contains `DS3Colors.` (at least 2 occurrences)
    - TutorialView.swift contains `DS3Typography.` (at least 1 occurrence)
    - TutorialView.swift contains `DS3Spacing.` (at least 1 occurrence)
    - TutorialView.swift contains `.shadow(` or `RoundedRectangle` (card style)
  </acceptance_criteria>
  <done>All views use design system tokens exclusively. Tutorial matches the card-style design language used in login and other windows. No Nunito or hardcoded dark color references remain in view code.</done>
</task>

<task type="auto">
  <name>Task 2: Copy audit and Italian localization verification</name>
  <read_first>
    - DS3Drive/Assets/Localizable.xcstrings
    - DS3Drive/Views/Tutorial/Views/TutorialView.swift
    - DS3Drive/Views/Login/Views/LoginView.swift
    - DS3Drive/Views/Preferences/Views/PreferencesView.swift
    - DS3Drive/Views/Preferences/Views/GeneralTab.swift
    - DS3Drive/Views/Preferences/Views/AccountTab.swift
    - DS3Drive/Views/Preferences/Views/SyncTab.swift
    - DS3Drive/Views/Tray/Views/TrayMenuView.swift
    - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
    - DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
    - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
    - DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift
    - DS3Drive/Views/Sync/Views/SetupSyncView.swift
    - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
    - DS3Drive/Views/Sync/Views/DriveConfirmView.swift
    - DS3Drive/DS3DriveApp.swift
  </read_first>
  <files>
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <action>
    **Copy Audit (per D-LOC-01 and CONTEXT.md copy decisions):**
    1. Search ALL Swift files in DS3Drive/ for string literals containing "Cubbit DS3 Sync" (case-insensitive) and replace with "DS3 Drive"
    2. Search for any other old branding: "Cubbit Sync", "DS3Sync", "ds3sync" — replace with "DS3 Drive" or "ds3drive" as appropriate
    3. Review all user-facing strings in views added by plans 01-04 (tray views, wizard views, preferences tabs, side panels) and ensure they are wrapped in NSLocalizedString or String(localized:) for localization

    **Italian Localization Verification:**
    4. Open Localizable.xcstrings and verify all existing Italian (it) translations are present and correct
    5. For any NEW strings added by plans 01-04 (e.g., "Pause", "Resume", "Copy S3 Path", "Recent Files", "Connection Info", "General", "Account", "Sync", "Signed in as", speed display strings, etc.), add Italian translations:
       - "Pause" → "Pausa"
       - "Resume" → "Riprendi"
       - "Copy S3 Path" → "Copia percorso S3"
       - "Recent Files" → "File recenti"
       - "Connection Info" → "Info connessione"
       - "General" → "Generali"
       - "Account" → "Account"
       - "Sync" → "Sincronizzazione"
       - "Signed in as %@" → "Accesso come %@"
       - "Add Drive" → "Aggiungi unita"
       - "Disconnect" → "Disconnetti"
       - "View in Finder" → "Mostra nel Finder"
       - "View in Console" → "Visualizza nella Console"
       - "Manage" → "Gestisci"
       - "Refresh" → "Aggiorna"
       - "Preferences" → "Preferenze"
       - "Quit DS3 Drive" → "Esci da DS3 Drive"
       - "Start DS3 Drive at login" → "Avvia DS3 Drive all'accesso"
       - "Show sync badges in Finder" → "Mostra badge di sincronizzazione nel Finder"
       - Any other user-facing strings found in task 1 that lack Italian translations
    6. Ensure the .xcstrings file sourceLanguage is "en" and has an "it" localization section
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -rni "cubbit ds3 sync\|cubbit sync\|ds3sync" DS3Drive/ --include="*.swift" | grep -v "^.*//.*" | grep -v ".xcstrings" | wc -l | xargs test 0 -eq && echo "PASS: No old branding references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "cubbit ds3 sync" DS3Drive/ --include="*.swift" | grep -v "//"` returns 0 results
    - Localizable.xcstrings contains `"it"` localization section
    - Localizable.xcstrings contains Italian translation for "Pause" (string "Pausa")
    - Localizable.xcstrings contains Italian translation for "Recent Files" (string "File recenti")
    - Localizable.xcstrings contains Italian translation for "Connection Info" (string "Info connessione")
  </acceptance_criteria>
  <done>All user-facing strings use "DS3 Drive" branding. Italian localization covers all strings from plans 01-04. No old branding references remain.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Nunito" DS3Drive/Views/ --include="*.swift" | grep -v "//"` returns 0 results
2. `grep -rn "darkWhite\|darkMainStandard" DS3Drive/Views/ --include="*.swift" | grep -v "//"` returns 0 results
3. `grep -rni "cubbit ds3 sync" DS3Drive/ --include="*.swift" | grep -v "//"` returns 0 results
4. TutorialView.swift uses DS3Colors, DS3Typography, DS3Spacing tokens
5. Localizable.xcstrings has Italian translations for all new strings
</verification>

<success_criteria>
- Zero inline style anti-patterns (Nunito, hardcoded dark colors) in view code
- Tutorial matches card-style design language
- All branding says "DS3 Drive" (not "Cubbit DS3 Sync")
- Italian localization complete for all user-facing strings
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-05-SUMMARY.md`
</output>
