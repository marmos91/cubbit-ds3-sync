---
phase: 05-ux-polish
plan: 08
type: execute
wave: 3
depends_on: [05-05, 05-06, 05-07]
files_modified:
  - DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset/Contents.json
  - DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset/*.png
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07]

must_haves:
  truths:
    - "User verifies all 7 macOS UX requirements pass on a real Mac"
    - "User verifies iOS UX works correctly on a real iPhone/iPad"
    - "App icon looks polished and follows macOS icon guidelines"
    - "All views are visually consistent in both light and dark mode"
  artifacts:
    - path: "DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "Polished app icon at all required sizes"
  key_links: []
---

<objective>
UI/brand polish (icon, color/spacing audit) and final human verification on both macOS and iOS

Purpose: Per D-UI-01/02/03/04 and D-VER-01/02/03, final visual polish and human verification ensures the app is ship-ready on both platforms.
Output: Polished app icon, verified visual consistency, human sign-off on both platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-05-SUMMARY.md
@.planning/phases/05-ux-polish/05-06-SUMMARY.md
@.planning/phases/05-ux-polish/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App icon polish and color/spacing visual audit</name>
  <read_first>
    - DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset/Contents.json
    - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
    - DS3Drive/Views/Common/DesignSystem/DS3Spacing.swift
    - DS3Drive/Views/Login/Views/LoginView.swift
    - DS3Drive/Views/Tutorial/Views/TutorialView.swift
    - DS3Drive/Views/Preferences/Views/PreferencesView.swift
    - DS3Drive/Views/Preferences/Views/GeneralTab.swift
    - DS3Drive/Views/Preferences/Views/AccountTab.swift
    - DS3Drive/Views/Preferences/Views/SyncTab.swift
    - DS3Drive/Views/Tray/Views/TrayMenuView.swift
    - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
    - DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
    - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
    - DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift
    - DS3Drive/Views/Sync/Views/SetupSyncView.swift
    - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
    - DS3Drive/Views/Sync/Views/DriveConfirmView.swift
    - DS3Drive/Views/Common/ShimmerModifier.swift
  </read_first>
  <files>
    DS3Drive/Assets/Assets.xcassets/AppIcon.appiconset/Contents.json,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    (plus any view files needing spacing/color fixes)
  </files>
  <action>
    **App Icon Polish (D-UI-01):**
    1. Check current AppIcon.appiconset/Contents.json for required sizes:
       - macOS: 16x16, 32x32, 64x64, 128x128, 256x256, 512x512, 1024x1024 (all @1x and @2x where applicable)
       - iOS: 20x20, 29x29, 40x40, 60x60, 76x76, 83.5x83.5, 1024x1024
    2. Verify all icon files exist and are referenced in Contents.json
    3. If any sizes are missing, generate them from the highest resolution source using `sips` command:
       `sips -z <height> <width> source.png --out icon_<size>.png`
    4. Ensure the icon follows macOS guidelines: rounded rect mask (automatic in macOS), no manual rounding in the asset
    5. Verify the icon looks clean at small sizes (16x16, 32x32) — if the current design has too much detail for small sizes, note this for human verification

    **Color/Spacing Audit (D-UI-02, D-UI-04):**
    6. Audit ALL view files for visual consistency:
       - Verify all primary buttons use `Color.accentColor` (Cubbit blue #009EFF)
       - Verify all destructive buttons use `DS3Colors.statusError` or `.red`
       - Verify all secondary/cancel buttons use `.secondary` or `DS3Colors.textSecondary`
       - Verify window backgrounds use `DS3Colors.backgroundPrimary` or system default
       - Verify card/container backgrounds use `DS3Colors.backgroundSecondary` with rounded corners
       - Verify text uses `DS3Colors.textPrimary` for headings, `DS3Colors.textSecondary` for descriptions
       - Verify consistent padding: outer edges use `DS3Spacing.lg` (24pt), inner elements use `DS3Spacing.md` (16pt), tight groups use `DS3Spacing.sm` (8pt)

    7. Check dark mode compatibility:
       - `grep -rn "Color.white\|Color.black" DS3Drive/Views/ --include="*.swift"` — replace with semantic colors
       - `grep -rn "\.background(Color" DS3Drive/Views/ --include="*.swift"` — verify using DS3Colors, not hardcoded
       - Verify no hardcoded opacity values that break in dark mode (e.g., `.opacity(0.1)` on white backgrounds)

    8. Fix any inconsistencies found — apply DS3Colors/DS3Spacing tokens uniformly.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' 2>&1 | grep -c "error:" | xargs test 0 -eq && echo "PASS: Builds after polish"</automated>
  </verify>
  <acceptance_criteria>
    - AppIcon.appiconset/Contents.json references icon files for all required macOS sizes (at minimum: 128x128, 256x256, 512x512, 1024x1024)
    - `grep -rn "Color.white\b\|Color.black\b" DS3Drive/Views/ --include="*.swift" | grep -v "//" | grep -v "\.colorScheme"` returns 0 results (no hardcoded black/white)
    - All view files use DS3Colors tokens for status colors (not inline Color(.red), Color(.green) for status indicators — DS3Colors.statusError, DS3Colors.statusSynced instead)
    - `xcodebuild clean build -scheme "DS3 Drive"` succeeds
  </acceptance_criteria>
  <done>App icon is polished with all required sizes. Color and spacing are consistent across all views in both light and dark mode. No hardcoded colors remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final human verification on macOS and iOS</name>
  <files>N/A — verification only, no files modified</files>
  <action>
    Present the user with the verification checklist below. Wait for human to test on both macOS and iOS. Record pass/fail for each item.
  </action>
  <what-built>
    Complete UX polish phase across both platforms:
    - Design system sweep (plan 05-05): all views use DS3Colors/DS3Typography/DS3Spacing, tutorial redesigned, Italian localization complete
    - Swift 6 concurrency cleanup (plan 05-06): zero warnings on all targets
    - Bug sweep (plan 05-07): parent folder error propagation, HTTP/2 retry hardening, runtime bug fixes
    - UI polish (this plan): app icon, color/spacing audit, dark mode consistency

    Previously completed (plans 01-04):
    - Finder sync badges with 5 states (UX-01)
    - Menu bar colored status indicators (UX-02)
    - Real-time transfer speed display (UX-03)
    - Recent files side panel (UX-04)
    - Quick actions gear menu with pause/resume (UX-05)
    - 2-step drive setup wizard (UX-06)
    - 3-drive limit enforcement (UX-07)
  </what-built>
  <how-to-verify>
    **macOS Verification (D-VER-01):**

    1. **UX-01 Finder Badges:** Open a synced drive in Finder. Verify files show sync status badges (green checkmark for synced, blue arrows for syncing, gray cloud for cloud-only). Trigger an error condition and verify the red X badge appears. Verify the parent folder shows aggregate badge state.

    2. **UX-02 Tray Status:** Check the menu bar tray. Verify each drive row shows a colored dot (green for synced, blue for syncing). Verify the tray icon animates during sync.

    3. **UX-03 Speed Display:** Start a file transfer. Verify the speed summary at the top of the tray shows upload/download speed. Verify per-drive speed is shown inline.

    4. **UX-04 Recent Files:** Click a drive row in the tray. Verify the recent files side panel opens showing up to 10 recent files sorted by status (syncing first, then errors, then completed).

    5. **UX-05 Quick Actions:** Right-click a drive row or click the gear icon. Verify all actions work: Disconnect, View in Finder, View in Console, Manage, Refresh, Pause/Resume, Copy S3 Path. Verify pause/resume toggles the drive status.

    6. **UX-06 Wizard:** Click "Add Drive". Verify 2-step flow: tree navigation (project > bucket > prefix) then name/confirm. Verify shimmer loading states. Verify auto-suggested drive name.

    7. **UX-07 Drive Limit:** If 3 drives exist, verify "Add Drive" is disabled or shows a limit message.

    8. **Visual Consistency:** Check all windows (login, tutorial, wizard, preferences, tray) in BOTH light and dark mode. Verify consistent fonts, colors, spacing.

    9. **Italian Localization:** Change system language to Italian. Verify all UI strings are translated.

    **iOS Verification (D-VER-02):**

    10. **Build and run on iOS device** (or simulator). Verify:
        - Login flow works
        - Drive list displays correctly
        - File browsing works
        - Share extension can upload files
        - No crashes during normal usage

    **Both Platforms (D-VER-03):**

    11. Check Console.app for any error-level logs from `io.cubbit.DS3Drive` or `io.cubbit.DS3Drive.provider` during the above testing.
  </how-to-verify>
  <verify>Human confirms all checks pass or provides list of issues</verify>
  <done>All 7 macOS UX requirements verified. iOS functionality verified. Both platforms pass human verification.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. App icon has all required sizes in asset catalog
2. No hardcoded Color.white/Color.black in view files
3. All 7 UX requirements verified by human on macOS
4. iOS app verified working by human on real device
5. Both light and dark mode look consistent
</verification>

<success_criteria>
- Human approves all 7 macOS UX requirements
- Human approves iOS functionality
- App icon polished at all sizes
- Visual consistency verified in both light and dark mode
- Phase 05 is complete and v1.0 milestone can be marked done
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-08-SUMMARY.md`
</output>
