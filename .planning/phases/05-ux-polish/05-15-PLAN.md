---
phase: 05-ux-polish
plan: 15
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - DS3Lib/Sources/DS3Lib/Utils/RecentFilesTracker.swift
  - DS3Lib/Sources/DS3Lib/Models/RecentFileEntry.swift
  - DS3Lib/Sources/DS3Lib/DS3DriveManager.swift
  - DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift
  - DS3Lib/Tests/DS3LibTests/AggregateStatusTests.swift
  - DS3Lib/Tests/DS3LibTests/RecentFilesTrackerTests.swift
  - DS3DriveProvider/NotificationsManager.swift
  - DS3DriveProvider/FileProviderExtension.swift
  - DS3DriveProvider/FileProviderExtension+Create.swift
  - DS3DriveProvider/FileProviderExtension+Modify.swift
  - DS3Drive/Views/Tray/Views/TrayMenuView.swift
  - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
  - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
  - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
  - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
  - DS3Drive/DS3DriveApp.swift
autonomous: true
requirements: [UX-02, UX-03, UX-05]

must_haves:
  truths:
    - "A file going through .started -> .completed appears once in Recent Files (no duplicates) at runtime, verified by triggering an upload from Finder"
    - "Tray header, footer, drive row, and menu bar icon all read from a single derived AggregateStatus enum and always agree"
    - "Clicking the gear icon on a drive row opens the SwiftUI Menu with all per-drive actions"
    - "Click-for-Updates posts a result notification (up-to-date / available / failed) via UNUserNotificationCenter"
    - "No SwiftUI runtime error 'No Scene with id ...drive.manage' in the console"
    - "xcodebuild clean build with SWIFT_STRICT_CONCURRENCY=complete produces zero weakSelf data race / never-mutated warnings in DS3DriveViewModel.swift"
  artifacts:
    - path: "DS3Lib/Sources/DS3Lib/DS3DriveManager.swift"
      provides: "computed AggregateStatus property derived from drives.map(\\.status) and pause flags"
      contains: "var aggregateStatus: AggregateStatus"
    - path: "DS3Lib/Tests/DS3LibTests/AggregateStatusTests.swift"
      provides: "Exhaustive unit tests for the aggregate reduction (empty, all-idle, mixed-error, paused, indexing)"
    - path: "DS3Lib/Tests/DS3LibTests/RecentFilesTrackerTests.swift"
      provides: "Tests for upsert-by-key dedup, terminal-state transitions, watchdog, clearAll"
  key_links:
    - from: "DS3Drive/Views/Tray/Views/TrayMenuView.swift"
      to: "DS3DriveManager.aggregateStatus"
      via: "@Observable binding for header row"
      pattern: "aggregateStatus"
    - from: "DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift"
      to: "DS3DriveManager.aggregateStatus"
      via: "@Observable binding for footer state"
      pattern: "aggregateStatus"
---

<objective>
Closes Gaps 14, 15, 16, 25, 26, 27, 32 — re-fix bugs that Round 1 (Plans 05-09 / 05-10) shipped code for but DID NOT actually resolve at runtime, plus the Swift 6 weakSelf data race warnings that surfaced after Round 1 tweaked the weakSelf captures in DS3DriveViewModel.swift.

**Critical context:** Plans 05-09 and 05-10 were verified by grep-matching acceptance text and confirming `xcodebuild clean build` succeeded. They never actually ran the app and reproduced the bugs. This plan re-opens the same gaps and lands runtime-verified fixes plus unit tests so future regressions are caught at test time, not by humans pointing at screenshots.

Gaps closed:
- Gap 14: Recent Files duplicate entries + stuck transferring (Round 1 added a tracker rewrite but identifiers diverge across call sites)
- Gap 15 + 27: Three-way state disagreement (header/footer/drive row read from different sources; Plan 05-09's `aggregateStatus` was added but nothing consumes it)
- Gap 16: "Manage" button references non-existent WindowGroup `io.cubbit.CubbitDS3Sync.drive.manage` — runtime error in console
- Gap 25: Gear icon does nothing on click (regression introduced by Plan 05-09's Gap 10 mutual-exclusion fix — the SwiftUI Menu got conditionally wrapped and no longer presents on tap)
- Gap 26: Check for Updates spinner appears but never shows result (Plan 05-10 did level 1+3 of Gap 9, skipped level 2)

Output: bugs actually fixed AND covered by unit tests so the next agent can't claim closure without proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-09-SUMMARY.md
@.planning/phases/05-ux-polish/05-10-SUMMARY.md
@CLAUDE.md
@DS3Lib/Sources/DS3Lib/Utils/RecentFilesTracker.swift
@DS3Lib/Sources/DS3Lib/DS3DriveManager.swift
@DS3Drive/Views/Tray/Views/TrayMenuView.swift
@DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
@DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single source of truth for tray aggregate state (Gaps 15 + 27 + 16)</name>
  <files>
    DS3Lib/Sources/DS3Lib/DS3DriveManager.swift,
    DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift,
    DS3Lib/Tests/DS3LibTests/AggregateStatusTests.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift,
    DS3Drive/DS3DriveApp.swift
  </files>
  <read_first>
    - DS3Lib/Sources/DS3Lib/DS3DriveManager.swift (read in full to understand current driveStatuses publication)
    - DS3Drive/Views/Tray/Views/TrayMenuView.swift (find where the header "All drives up to date" string is bound)
    - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift (find where footer "Paused" / "Indexing" / "Idle" text is bound)
    - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift (find lines 200-220 — the openWindow call and the gear menu)
    - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift (find Plan 05-09's aggregateStatus property if it exists)
    - .planning/phases/05-ux-polish/05-08-GAPS.md sections "Gap 15", "Gap 27", "Gap 16"
    - .planning/phases/05-ux-polish/05-09-SUMMARY.md (verify what Plan 05-09 actually shipped vs claimed)
  </read_first>
  <behavior>
    - AggregateStatus.from(drives: []) -> .noDrives
    - AggregateStatus.from(drives: [idle, idle]) -> .allIdle
    - AggregateStatus.from(drives: [idle, syncing]) -> .syncing
    - AggregateStatus.from(drives: [idle, error]) -> .error(count: 1)
    - AggregateStatus.from(drives: [paused, paused]) -> .allPaused
    - AggregateStatus.from(drives: [error, paused]) -> .mixed (or .error — pick one and document)
    - AggregateStatus.from(drives: [indexing, idle]) -> .indexing
  </behavior>
  <action>
    Step 1 — Define `AggregateStatus` enum in DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift (or new file `AggregateStatus.swift` in same dir):
    ```swift
    public enum AggregateStatus: Equatable, Sendable {
        case noDrives
        case allIdle
        case syncing
        case indexing
        case error(count: Int)
        case allPaused
        case mixed
    }
    ```
    Add a static reducer:
    ```swift
    public static func from(drives: [DS3Drive]) -> AggregateStatus {
        if drives.isEmpty { return .noDrives }
        let errorCount = drives.filter { $0.status == .error }.count
        let pausedCount = drives.filter { $0.isPaused == true }.count
        if errorCount == drives.count { return .error(count: errorCount) }
        if pausedCount == drives.count { return .allPaused }
        if errorCount > 0 { return .mixed }
        if drives.contains(where: { $0.status == .syncing }) { return .syncing }
        if drives.contains(where: { $0.status == .indexing }) { return .indexing }
        return .allIdle
    }
    ```
    NOTE: read the actual `DS3Drive.status` enum cases first; map them to the reducer correctly (do NOT invent cases that don't exist).

    Step 2 — Add unit tests covering every case in the behavior block above. File: `DS3Lib/Tests/DS3LibTests/AggregateStatusTests.swift`. Run with `swift test --package-path DS3Lib --filter AggregateStatusTests`.

    Step 3 — Expose `aggregateStatus` as an `@Observable` property on DS3DriveManager that recomputes via `AggregateStatus.from(drives: drives)` on every `drives` update. **Delete or deprecate** any duplicate `aggregateStatus` Plan 05-09 added on `DS3DriveViewModel`.

    Step 4 — In `TrayMenuView.swift`, replace the binding for the "All drives up to date" / "Paused" / "Indexing" header row to switch on `manager.aggregateStatus`. Provide a copy + icon + color for every case. Hide the row entirely on `.noDrives`.

    Step 5 — In `TrayMenuFooterView.swift`, replace the footer status binding (which currently reads from `AppStatusManager` or a separate pause flag) with `manager.aggregateStatus`. Same switch.

    Step 6 — In `DS3DriveApp.swift`, the menu bar icon binding must read from `manager.aggregateStatus` — NOT from `AppStatusManager.shared.indexingState` or any independent flag. Confirm the menu bar icon updates when `aggregateStatus` changes.

    Step 7 — Gap 16: in `TrayDriveRowView.swift` line ~207, **delete** the `Manage` Button calling `openWindow(id: "io.cubbit.CubbitDS3Sync.drive.manage", value: ...)`. The other gear menu actions (Disconnect, View in Finder, Refresh, etc.) cover the use case. Also `grep -rn "CubbitDS3Sync"` across the repo and remove any other stale-bundle-prefix references found. Remove the corresponding "Manage" Localizable.xcstrings key.

    Verification (runtime, not grep):
    1. `xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" clean build` succeeds
    2. Run the app from Xcode. Open the tray. Header, footer, drive row icons must all show the same state. Pause a drive — pause appears in all three places.
    3. Click the gear menu — no console error about missing scene `drive.manage`.
  </action>
  <verify>
    <automated>cd DS3Lib && swift test --filter AggregateStatusTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AggregateStatusTests pass with full coverage of the behavior block
    - Header, footer, and menu bar icon all read from `manager.aggregateStatus` (grep confirms only ONE source)
    - `grep -rn "CubbitDS3Sync" DS3Drive/ DS3DriveProvider/` returns zero matches
    - `grep -rn "drive.manage" DS3Drive/` returns zero matches
    - Manual: Tray displayed states agree across header/footer/row in all of: idle, paused, error, syncing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecentFilesTracker upsert-by-key + watchdog + Clear (Gap 14)</name>
  <files>
    DS3Lib/Sources/DS3Lib/Utils/RecentFilesTracker.swift,
    DS3Lib/Sources/DS3Lib/Models/RecentFileEntry.swift,
    DS3Lib/Tests/DS3LibTests/RecentFilesTrackerTests.swift,
    DS3DriveProvider/FileProviderExtension.swift,
    DS3DriveProvider/FileProviderExtension+Create.swift,
    DS3DriveProvider/FileProviderExtension+Modify.swift,
    DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
  </files>
  <read_first>
    - DS3Lib/Sources/DS3Lib/Utils/RecentFilesTracker.swift (full file — see Plan 05-09's implementation)
    - DS3Lib/Sources/DS3Lib/Models/RecentFileEntry.swift
    - DS3DriveProvider/FileProviderExtension.swift — find every call site that creates/updates a recent file entry
    - DS3DriveProvider/FileProviderExtension+Create.swift
    - DS3DriveProvider/FileProviderExtension+Modify.swift
    - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
    - .planning/phases/05-ux-polish/05-08-GAPS.md sections "Gap 14"
    - .planning/phases/05-ux-polish/05-09-SUMMARY.md
  </read_first>
  <behavior>
    - tracker.upsert(entry) twice with same identifier produces ONE entry, status = latest, startedAt preserved from first
    - tracker.upsert(entry) with different identifiers produces two entries
    - Entry stuck in .transferring for > 5 minutes is auto-transitioned to .failed("timeout") by the watchdog
    - On startup, tracker.cleanupStaleEntries() transitions any leftover .transferring entries to .failed("interrupted")
    - tracker.clearAll() empties storage and persists empty state
    - All mutations are serialized (Swift 6 Sendable-clean, no data race)
  </behavior>
  <action>
    Step 1 — In `RecentFilesTracker.swift`, replace the underlying storage with `private var entriesByKey: [String: RecentFileEntry] = [:]` guarded by an `NSLock` (or refactor to actor for Swift 6 compliance). Add:
    - `public func upsert(_ entry: RecentFileEntry)` — merge by `entry.identifier`. Preserve earliest `startedAt`, take latest `status` and `updatedAt`.
    - `public func clearAll()` — wipe and persist
    - `public func cleanupStaleEntries(olderThan: TimeInterval = 300)` — transition any `.transferring` older than threshold to `.failed("timeout")`
    - `public var recentEntries: [RecentFileEntry]` — sorted by `updatedAt` desc

    Step 2 — In `RecentFileEntry.swift`, add `updatedAt: Date` and a `merging(with: RecentFileEntry) -> RecentFileEntry` helper. Make sure `identifier` is the canonical `NSFileProviderItemIdentifier.rawValue` (the full S3 key) — NOT a local file URL or display name. **This is the critical bug** — Plan 05-09's agent likely passed two different identifier types from different call sites, which is why dedup didn't work at runtime.

    Step 3 — Audit every call site in `FileProviderExtension.swift`, `FileProviderExtension+Create.swift`, `FileProviderExtension+Modify.swift`. For each:
    - Pass the SAME identifier format (`item.itemIdentifier.rawValue`) to both the `.started` and `.completed`/`.failed` notifications
    - Ensure `defer { tracker.upsert(.failed(...)) }` covers the error path so no entry can be left stuck on a thrown error or extension restart

    Step 4 — Call `tracker.cleanupStaleEntries()` once on extension startup (in `FileProviderExtension.init` or the equivalent lifecycle hook).

    Step 5 — Add Watchdog: a `Task` started by `RecentFilesTracker.startWatchdog()` that runs every 60 seconds and calls `cleanupStaleEntries()`.

    Step 6 — In `RecentFilesPanel.swift`:
    - Use `entry.identifier` as the `ForEach` id (stable, dedup-safe)
    - Add a "Clear" button at the top right of the panel that calls `tracker.clearAll()`. Disable if `recentEntries.isEmpty`.

    Step 7 — Tests in `DS3LibTests/RecentFilesTrackerTests.swift`:
    - testUpsertDeduplicatesByIdentifier
    - testUpsertPreservesEarliestStartedAt
    - testWatchdogTransitionsStuckEntriesToFailed
    - testClearAllEmpties
    - testStartupCleanupConvertsTransferringToFailed
  </action>
  <verify>
    <automated>cd DS3Lib && swift test --filter RecentFilesTrackerTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All RecentFilesTrackerTests pass
    - grep confirms every Recent Files call site in DS3DriveProvider passes `.itemIdentifier.rawValue` (same identifier format)
    - Manual runtime: drag a file into a synced Finder folder, watch tray Recent Files — single row appears, transitions to checkmark, no duplicate
    - Manual runtime: kill the extension mid-upload, restart app, Recent Files no longer shows stuck transferring entries
    - Clear button in RecentFilesPanel works
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore gear menu (Gap 25) + Check for Updates result notification (Gap 26)</name>
  <files>
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift,
    DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift,
    DS3Drive/DS3DriveApp.swift,
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <read_first>
    - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift (find the gear button — verify if Plan 05-09 wrapped Menu in `if activePopover ==` — that's the bug)
    - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift (find the `activePopover` state and `checkForUpdates` method)
    - DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift (verify the result enum exists)
    - .planning/phases/05-ux-polish/05-08-GAPS.md sections "Gap 25", "Gap 26"
    - .planning/phases/05-ux-polish/05-10-SUMMARY.md
  </read_first>
  <behavior>
    - Tap gear icon -> SwiftUI Menu opens with all per-drive actions
    - Opening gear menu dismisses any open Recent Files side panel (mutual exclusion via dismiss-on-tap, NOT conditional rendering)
    - Click "Check for Updates" -> spinner -> after check completes, UNUserNotification with one of three result variants is delivered
  </behavior>
  <action>
    Step 1 — Gap 25 fix in `TrayDriveRowView.swift`. **Diagnose first**: read the file, find the gear button. Likely Plan 05-09 changed `Menu { ... }` to `if driveViewModel.activePopover == .gearMenu(driveID: ...) { Menu { ... } }`. SwiftUI `Menu` opens itself on tap — you cannot conditionally render it on tap. Replace with:
    ```swift
    Menu {
        Button("Disconnect") { ... }
        Button("View in Finder") { ... }
        // ... all existing per-drive actions
    } label: {
        Image(systemName: "gear")
    }
    .menuStyle(.button)
    .onTapGesture {
        driveViewModel.activePopover = .none  // dismiss side panel before menu opens
    }
    ```
    Make sure the Menu is unconditionally present in the view tree. Mutual exclusion happens by DISMISSING the side panel on gear tap, not by conditionally rendering the menu.

    Step 2 — Gap 26 fix. In `UpdateChecker.swift`, verify (or add) a result enum:
    ```swift
    public enum UpdateCheckResult: Sendable {
        case upToDate(currentVersion: String)
        case updateAvailable(version: String)
        case failed(message: String)
    }
    public func checkForUpdates() async -> UpdateCheckResult { ... }
    ```

    Step 3 — In `DS3DriveViewModel.swift`'s `handleCheckForUpdatesTapped()` (or wherever the tray menu binds), after `checkForUpdates()` returns, post a UNUserNotification:
    ```swift
    let content = UNMutableNotificationContent()
    switch result {
    case .upToDate(let v):
        content.title = String(localized: "DS3 Drive is up to date")
        content.body = String(localized: "You're running version \(v), the latest available.")
    case .updateAvailable(let v):
        content.title = String(localized: "Update available")
        content.body = String(localized: "Version \(v) is available. Open Preferences to install.")
    case .failed(let m):
        content.title = String(localized: "Couldn't check for updates")
        content.body = m
    }
    let request = UNNotificationRequest(identifier: "ds3.update-check.\(UUID().uuidString)", content: content, trigger: nil)
    try? await UNUserNotificationCenter.current().add(request)
    ```

    Step 4 — In `DS3DriveApp.swift`, ensure UNUserNotificationCenter authorization is requested at first launch (`requestAuthorization(options: [.alert, .sound])`) — if not already.

    Step 5 — Add the three localization variants (en + it) to `Localizable.xcstrings`.
  </action>
  <verify>
    <automated>xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Build succeeds clean
    - Runtime manual: gear icon click -> menu appears with Disconnect/Pause/View in Finder/etc.
    - Runtime manual: Check for Updates -> system notification appears with up-to-date message
    - grep `Localizable.xcstrings` shows the three new copy variants in en + it
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix Swift 6 weakSelf data race warnings in DS3DriveViewModel (Gap 32)</name>
  <read_first>
    - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift (both weakSelf captures at lines 133 and 278)
    - .planning/phases/05-ux-polish/05-08-GAPS.md (Gap 32 section — full proposed fix with Option A/B/C)
  </read_first>
  <files>
    DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
  </files>
  <action>
    **Choose Option B from Gap 32: replace `Timer.scheduledTimer` with Swift Concurrency `Task` + `Task.sleep`.**

    Rationale: aligns with Swift 6, eliminates the weakSelf / MainActor.assumeIsolated dance, and provides cleaner cancellation semantics. Option A (restore nonisolated(unsafe)) is a band-aid; Option C (Timer + Task@MainActor) keeps the Timer indirection for no benefit.

    **Concrete changes in DS3DriveViewModel.swift:**

    1. Rename property `transferStatsResetTimer: Timer?` to `transferStatsResetTask: Task<Void, Never>?`
    2. Rename property `idleDebounceTimer: Timer?` to `idleDebounceTask: Task<Void, Never>?`
    3. Replace every `transferStatsResetTimer?.invalidate()` call with `transferStatsResetTask?.cancel()`
    4. Replace every `idleDebounceTimer?.invalidate()` call with `idleDebounceTask?.cancel()`
    5. At line 127-141 (the Timer that resets transfer stats after inactivity):
       - Remove the `nonisolated(unsafe) weak let weakSelf = self` / `weak let weakSelf = self` line
       - Replace the `Timer.scheduledTimer(...) { _ in MainActor.assumeIsolated { guard let self = weakSelf else { return } ... } }` block with:
       ```swift
       self.transferStatsResetTask = Task { [weak self] in
           try? await Task.sleep(for: .seconds(DefaultSettings.Tray.driveStatsReset))
           guard !Task.isCancelled, let self else { return }
           self.lastReportedSize.removeAll()
           self.lastReportedDuration.removeAll()
           self.totalTransferredSize = 0
           self.totalTransferDuration = 0
           // (preserve the exact body of the original Timer closure — do not drop any lines)
       }
       ```
    6. At line 272-290 (the Timer that debounces idle status transitions):
       - Same transformation: remove the weakSelf capture, wrap body in `Task { [weak self] in try? await Task.sleep(for: .seconds(2.0)); guard !Task.isCancelled, let self else { return }; /* body */ }`
       - The enclosing class is `@MainActor @Observable` so the Task inherits MainActor isolation — no `MainActor.assumeIsolated` needed
    7. In `deinit` (if present): replace any Timer invalidation calls with `transferStatsResetTask?.cancel()` and `idleDebounceTask?.cancel()`
    8. Add a brief comment on each Task block: `// Swift Concurrency replacement for Timer — inherits @MainActor from enclosing class, no weakSelf needed`
  </action>
  <verify>
    <automated>xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -iE "sending.*weakSelf|never mutated.*weakSelf" | wc -l | xargs test 0 -eq && echo "PASS: Zero weakSelf warnings under strict concurrency"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "weakSelf" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` returns 0 matches (the variable is gone entirely)
    - `grep -n "nonisolated(unsafe)" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` returns 0 matches
    - `grep -n "Timer.scheduledTimer" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` returns 0 matches
    - `grep -n "MainActor.assumeIsolated" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` returns 0 matches (only used with the old Timer pattern)
    - `grep -n "transferStatsResetTask" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` shows the new Task property + cancel sites
    - `grep -n "idleDebounceTask" DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift` shows the new Task property + cancel sites
    - `xcodebuild clean build ... SWIFT_STRICT_CONCURRENCY=complete` shows 0 `"Sending 'weakSelf'"` warnings
    - `xcodebuild clean build ... SWIFT_STRICT_CONCURRENCY=complete` shows 0 `"never mutated"` warnings for weakSelf
    - Debounce behavior preserved: rapid `.sync` then `.idle` transitions still get coalesced with the 2s debounce (manual runtime verification — trigger a small file upload, watch the tray icon, verify no flashing between sync and idle)
  </acceptance_criteria>
  <done>
    - Zero Swift 6 concurrency warnings related to weakSelf or sending
    - Zero `Timer.scheduledTimer` usage in DS3DriveViewModel.swift
    - Both debounce behaviors (transfer stats reset, idle transition) still fire correctly at runtime
    - Runtime walkthrough: drag a small file into a drive in Finder, watch tray, confirm tray icon does not flash between sync and idle states
  </done>
</task>

</tasks>

<verification>
- All new unit tests pass (`swift test --package-path DS3Lib`)
- App builds clean via xcodebuild, AND under `SWIFT_STRICT_CONCURRENCY=complete` with zero weakSelf warnings
- Runtime walkthrough required (this is the entire point of the plan):
  1. Tray header/footer/drive row state agree under: idle, paused, error, syncing
  2. Drag a file in Finder -> Recent Files shows ONE entry
  3. Gear icon -> menu opens
  4. Check for Updates -> result notification delivered
  5. Console shows zero "No Scene with id" errors
  6. Tray icon does not flash between sync/idle during transfers (debounce still works after Timer -> Task conversion)
- grep for `CubbitDS3Sync` returns zero matches
- grep for `weakSelf` in DS3DriveViewModel.swift returns zero matches
</verification>

<success_criteria>
- Gaps 14, 15, 16, 25, 26, 27, 32 are CLOSED at runtime, not just by grep
- AggregateStatusTests + RecentFilesTrackerTests provide regression coverage
- Zero Swift 6 concurrency warnings in DS3DriveViewModel under strict checking
- No regressions to previously-working features (debounce behavior preserved after Timer -> Task conversion)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-15-SUMMARY.md` documenting:
- Which Plan 05-09 / 05-10 issues were diagnosed and what the actual root cause was vs what those plans claimed
- Test results
- Manual runtime verification steps performed
</output>
