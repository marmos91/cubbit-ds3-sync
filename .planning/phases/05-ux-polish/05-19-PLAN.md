---
phase: 05-ux-polish
plan: 19
type: execute
wave: 3
gap_closure: true
depends_on: [05-17]
files_modified:
  - DS3DriveApp/Views/Common/IOSDesignSystem.swift
  - DS3DriveApp/Views/Common/IOSButtonStyles.swift
  - DS3DriveApp/Views/App/IOSAppRootView.swift
  - DS3DriveApp/Views/App/IOSMainTabView.swift
  - DS3DriveApp/Views/Login/IOSLoginView.swift
  - DS3DriveApp/Views/Login/IOSMFAView.swift
  - DS3DriveApp/Views/Dashboard/DriveListView.swift
  - DS3DriveApp/Views/Dashboard/DriveCardView.swift
  - DS3DriveApp/Views/Dashboard/DriveDetailView.swift
  - DS3DriveApp/Views/Dashboard/EmptyDrivesView.swift
  - DS3DriveApp/Views/Setup/IOSSetupWizardView.swift
  - DS3DriveApp/Views/Setup/ProjectListView.swift
  - DS3DriveApp/Views/Setup/BucketListView.swift
  - DS3DriveApp/Views/Setup/PrefixListView.swift
  - DS3DriveApp/Views/Setup/DriveConfirmView.swift
  - DS3DriveApp/Views/Settings/IOSSettingsView.swift
  - DS3DriveShareExtension/ShareExtensionView.swift
  - DS3DriveShareExtension/ShareDrivePickerView.swift
  - DS3DriveShareExtension/ShareFolderPickerView.swift
  - DS3DriveShareExtension/ShareUploadProgressView.swift
  - DS3DriveShareExtension/ShareUnauthenticatedView.swift
autonomous: false
requirements: [UX-01]

must_haves:
  truths:
    - "iOS app reads DS3Lib.DS3Colors (already re-exported via IOSColors) — new corrected tokens from Plan 05-17 flow through automatically with zero code changes to IOSColors re-exports"
    - "IOSButtonStyles primary CTA uses brandPrimary (#005CE8), not black or old brandBlue400"
    - "Figtree font is registered in DS3DriveApp/Info.plist and DS3DriveShareExtension/Info.plist (UIAppFonts)"
    - "IOS views use Font.custom('Figtree-*', size:) where they previously used .system()"
    - "iOS login hero layout matches macOS Plan 05-18 intent (no 'gradient as outer border' bug)"
    - "iOS drive list uses brandCard-style treatment"
    - "iOS Settings screen uses brandBackground"
    - "CI builds iOS target successfully (agent cannot build iOS locally — CI is the verification)"
  artifacts:
    - path: "DS3DriveApp/Info.plist"
      provides: "UIAppFonts entry registering Figtree weights"
      contains: "Figtree-Regular.ttf"
    - path: "DS3DriveShareExtension/Info.plist"
      provides: "UIAppFonts entry registering Figtree weights"
      contains: "Figtree-Regular.ttf"
  key_links:
    - from: "DS3DriveApp/Views/Common/IOSDesignSystem.swift"
      to: "DS3Lib.DS3Colors (updated in Plan 05-17)"
      via: "re-export — already wired"
      pattern: "DS3Lib.DS3Colors"
    - from: "DS3DriveApp/Views/Login/IOSLoginView.swift"
      to: "IOSColors.brandBackground + DS3Gradients.brandVerticalBackground"
      via: "ZStack backdrop"
      pattern: "brandBackground|brandVertical"
---

<objective>
Closes Gap 17 — iOS brand parity with correct tokens from Plan 05-17.

**Critical context:** Plan 05-13 applied iOS brand parity using Plan 05-11's WRONG tokens. Because `IOSColors` re-exports `DS3Lib.DS3Colors`, most token updates flow through automatically from Plan 05-17. However:
1. `IOSButtonStyles.swift` may have hardcoded black fills or inlined colors — sweep needed
2. Figtree font must be bundled + registered in iOS + Share Extension Info.plist (Plan 05-17 added it to macOS only)
3. iOS views may have their own versions of the Gap 19 "gradient as outer border" bug — audit needed
4. Any iOS view with inline `.system(size:)` or hardcoded hex values needs to be swept

**Also investigate during execution** (these may become mini-gaps 17a/17b/17c in the SUMMARY):
- Does iOS have a broken state machine (like Gap 15/27) via SharedData sync? If yes, Plan 05-15's `AggregateStatus` may need to be consumed by `IOSDriveViewModel` too.
- Does iOS Share Extension have its own login/MFA flow? If yes, sweep it.

Autonomous = false because the agent cannot build iOS locally (per Round 1 notes: "iOS 26.4 is not installed"). CI is the build verification, and a human needs to visually verify on an iOS simulator or device.

Purpose: iOS feels like the same product as macOS after Plan 05-18.

Output: every iOS view + Share Extension view uses the corrected tokens, Figtree is bundled, CI green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-13-SUMMARY.md
@CLAUDE.md
@DS3DriveApp/Views/Common/IOSDesignSystem.swift
@DS3DriveApp/Views/Common/IOSButtonStyles.swift
@DS3DriveApp/DS3DriveApp.swift
@DS3DriveApp/Info.plist
@DS3DriveShareExtension/Info.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigation + Figtree registration on iOS + Share Extension</name>
  <files>
    DS3DriveApp/Info.plist,
    DS3DriveShareExtension/Info.plist,
    DS3DriveApp/Views/Common/IOSDesignSystem.swift
  </files>
  <read_first>
    - DS3DriveApp/Info.plist (full)
    - DS3DriveShareExtension/Info.plist (full)
    - DS3DriveApp/DS3DriveApp.swift (root scene)
    - DS3DriveApp/Views/Common/IOSDesignSystem.swift (full — identify any tokens NOT already re-exported)
    - DS3DriveApp/Views/Common/IOSButtonStyles.swift (full — find any hardcoded colors)
  </read_first>
  <action>
    Step 1 — Investigation (produce an "iOS gap matrix" in the SUMMARY):
    ```bash
    # Find any hardcoded colors / fonts in iOS views
    grep -rn "Color(red:\|Color.black\|Color.white\|\.system(size:" DS3DriveApp/Views/ DS3DriveShareExtension/ | tee /tmp/ios-hardcoded.txt
    # Find any inline hex / NSColor / UIColor that bypasses IOSColors
    grep -rn "UIColor(\|#0\|#1\|#2" DS3DriveApp/Views/ DS3DriveShareExtension/
    # Find any legacy uses of Plan 05-11 brandBlue400/grey900 names
    grep -rn "brandBlue400\|brandGrey900\|brandGrey800" DS3DriveApp/ DS3DriveShareExtension/
    ```
    Document findings in the Plan SUMMARY so a future audit can verify completeness.

    Step 2 — Register Figtree in `DS3DriveApp/Info.plist`:
    ```xml
    <key>UIAppFonts</key>
    <array>
        <string>Figtree-Regular.ttf</string>
        <string>Figtree-Medium.ttf</string>
        <string>Figtree-SemiBold.ttf</string>
        <string>Figtree-Bold.ttf</string>
    </array>
    ```
    Ensure the four .ttf files from Plan 05-17 are also in the iOS target's "Copy Bundle Resources" build phase. If the Figtree files live in `DS3Drive/Assets/Fonts/`, consider moving them to a shared location (e.g., `Shared/Fonts/`) and adding to both target memberships. Document the decision in the SUMMARY.

    Step 3 — Same for `DS3DriveShareExtension/Info.plist` (Share Extension renders independently and needs its own font registration).

    Step 4 — Ensure `IOSColors` in `IOSDesignSystem.swift` re-exports every corrected token from Plan 05-17 — add any missing mappings:
    ```swift
    static let brandPrimaryDark = DS3Lib.DS3Colors.brandPrimaryDark
    static let brandPrimaryLight = DS3Lib.DS3Colors.brandPrimaryLight
    static let statusInfo = DS3Lib.DS3Colors.statusInfo
    static let brandBorderUltraSubtle = DS3Lib.DS3Colors.brandBorderUltraSubtle
    ```
    Add an `IOSTypography` enum mirroring `DS3Typography` with Figtree fonts:
    ```swift
    enum IOSTypography {
        static let h1 = Font.custom("Figtree-SemiBold", size: 34)
        static let body = Font.custom("Figtree-Regular", size: 16)
        // ...
    }
    ```
  </action>
  <verify>
    <automated>grep -q "Figtree-Regular.ttf" DS3DriveApp/Info.plist && grep -q "Figtree-Regular.ttf" DS3DriveShareExtension/Info.plist && echo OK</automated>
  </verify>
  <done>
    - Info.plist files both contain UIAppFonts entry with Figtree weights
    - IOSColors re-exports all corrected tokens
    - IOSTypography uses Figtree
    - Investigation findings documented for downstream tasks
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep iOS companion app views (Login, Wizard, Dashboard, Settings)</name>
  <files>
    DS3DriveApp/Views/Common/IOSButtonStyles.swift,
    DS3DriveApp/Views/App/IOSAppRootView.swift,
    DS3DriveApp/Views/App/IOSMainTabView.swift,
    DS3DriveApp/Views/Login/IOSLoginView.swift,
    DS3DriveApp/Views/Login/IOSMFAView.swift,
    DS3DriveApp/Views/Dashboard/DriveListView.swift,
    DS3DriveApp/Views/Dashboard/DriveCardView.swift,
    DS3DriveApp/Views/Dashboard/DriveDetailView.swift,
    DS3DriveApp/Views/Dashboard/EmptyDrivesView.swift,
    DS3DriveApp/Views/Setup/IOSSetupWizardView.swift,
    DS3DriveApp/Views/Setup/ProjectListView.swift,
    DS3DriveApp/Views/Setup/BucketListView.swift,
    DS3DriveApp/Views/Setup/PrefixListView.swift,
    DS3DriveApp/Views/Setup/DriveConfirmView.swift,
    DS3DriveApp/Views/Settings/IOSSettingsView.swift
  </files>
  <read_first>
    - Every file in the files list (skim for inline colors, system fonts, hardcoded backgrounds)
    - /tmp/ios-hardcoded.txt from Task 1 investigation
    - DS3Drive/Views/Login/Views/LoginView.swift AFTER Plan 05-18 Task 1 completes (reference intent)
  </read_first>
  <action>
    Step 1 — In `IOSButtonStyles.swift`: verify the primary button style uses `IOSColors.brandPrimary` background + white text + Figtree-SemiBold font. If it has a hardcoded black fill or an inline color, replace with `IOSColors.brandPrimary`. Add a `BrandPrimaryButtonStyle` matching the macOS version from Plan 05-17 if not already present.

    Step 2 — `IOSLoginView.swift` + `IOSMFAView.swift`: apply the same fix pattern as macOS Plan 05-18 Task 1. Gradient backdrop via ZStack as the actual window backdrop (`DS3Lib.DS3Gradients.brandVerticalBackground`), transparent card over it, "DS3 Drive" heading (not "Cubbit DS3 Drive"), `BrandPrimaryButtonStyle` on the Log In button. Make sure there's no double-background producing a bright border. This is the iOS equivalent of Gap 19.

    Step 3 — `IOSAppRootView.swift` + `IOSMainTabView.swift`: apply `IOSColors.brandBackground` at the scene root so the window backdrop is correct globally.

    Step 4 — Dashboard views (`DriveListView`, `DriveCardView`, `DriveDetailView`, `EmptyDrivesView`):
    - DriveCardView: apply brand card treatment (`IOSColors.brandSurface` background, `IOSColors.brandBorderSubtle` stroke, 12pt radius)
    - EmptyDrivesView: use `IOSColors.brandBackground` backdrop + Figtree typography
    - DriveListView: list background `IOSColors.brandBackground`, `.scrollContentBackground(.hidden)` on the List

    Step 5 — Setup flow (`IOSSetupWizardView`, `ProjectListView`, `BucketListView`, `PrefixListView`, `DriveConfirmView`):
    - Apply brand backdrop
    - Use `BrandPrimaryButtonStyle` on Continue / Create Drive
    - Project rows use the same project badge styling as macOS (consider creating an iOS `ProjectBadge` mirror — same hash palette)
    - `DriveConfirmView` (iOS): card-wrap the summary form

    Step 6 — `IOSSettingsView.swift`: brand backdrop + card-wrapped sections + Figtree typography.

    Step 7 — Apply `IOSTypography` fonts to replace any `.system(size:)` usage. Use the investigation file from Task 1 to catch all sites.
  </action>
  <verify>
    <automated>grep -rn "\.system(size:\|Color.black" DS3DriveApp/Views/ 2>&1 | head -20</automated>
  </verify>
  <done>
    - Zero `.system(size:)` remaining in iOS views (all replaced with IOSTypography.* Figtree)
    - Zero `Color.black` in iOS views
    - IOSLoginView no longer has the "gradient as outer border" anti-pattern
    - Every iOS view references IOSColors / IOSTypography / BrandPrimaryButtonStyle
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep Share Extension views + CI iOS build check</name>
  <files>
    DS3DriveShareExtension/ShareExtensionView.swift,
    DS3DriveShareExtension/ShareDrivePickerView.swift,
    DS3DriveShareExtension/ShareFolderPickerView.swift,
    DS3DriveShareExtension/ShareUploadProgressView.swift,
    DS3DriveShareExtension/ShareUnauthenticatedView.swift
  </files>
  <read_first>
    - All Share Extension view files (they're small — read each in full)
    - .planning/phases/05-ux-polish/05-13-SUMMARY.md "ShareColors" section if relevant
  </read_first>
  <action>
    Step 1 — Share Extension has its own `ShareColors` mirror (per Plan 05-13). Verify it re-exports from `DS3Lib.DS3Colors` the same way `IOSColors` does. If it uses inline / duplicated values, refactor it to re-export.

    Step 2 — Sweep each Share Extension view:
    - Backdrop: `ShareColors.brandBackground`
    - Cards: `ShareColors.brandSurface` + `ShareColors.brandBorderSubtle` stroke
    - Text: `ShareColors.brandTextPrimary/Secondary` + Figtree fonts
    - Primary buttons: brand primary style
    - Progress indicators (`ShareUploadProgressView`): use `ShareColors.brandPrimary` tint, rotating (not pulsing) icon for in-progress

    Step 3 — ShareUnauthenticatedView: apply the same login-ish hero backdrop used by iOS Login.

    Step 4 — Commit the changes and push to a branch. Trigger the CI workflow (`xcodebuild clean build analyze` per CLAUDE.md). Wait for the iOS build job. If it fails, read the CI logs and fix. The agent CANNOT build iOS locally — CI is the only verification path.

    Step 5 — Document any gaps found during investigation (Task 1) that weren't addressed by this plan — these become candidates for a 05-19-followup plan.
  </action>
  <verify>
    <automated>grep -rn "\.system(size:\|Color.black" DS3DriveShareExtension/ 2>&1 | head -10</automated>
  </verify>
  <done>
    - Zero inline system fonts or hardcoded black in Share Extension views
    - ShareColors re-exports from DS3Lib
    - CI iOS build green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: iOS visual verification on simulator or device</name>
  <what-built>
    - iOS DS3DriveApp + Share Extension swept with corrected brand tokens
    - Figtree bundled in both targets
    - Login hero layout fixed (no gradient-border bug)
    - CI iOS build green
  </what-built>
  <how-to-verify>
    1. Open the project in Xcode, select an iOS Simulator (or a connected device), Run
    2. Walk through: Login -> Tutorial (if present) -> Drive List (empty + populated) -> Setup Wizard (project -> bucket -> prefix -> confirm) -> Drive Detail -> Settings
    3. Open Photos, share an image via the Share Extension -> walk through drive picker + folder picker + upload progress
    4. For each surface confirm:
       - Backdrop is brand dark (#0E0E15)
       - Typography is Figtree (not San Francisco)
       - Primary CTAs are brand blue (#005CE8)
       - Cards have subtle border + optional glow
       - No hardcoded black buttons remain
       - Login is hero + form over gradient (no blue border wrapping a dark card)
    5. Compare side-by-side with the macOS app (post-Plan 05-18) — the two platforms should feel like the same product
    6. Respond with "approved" or a list of specific iOS mismatches
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual mismatches</resume-signal>
</task>

</tasks>

<verification>
- CI iOS build green (agent can't build locally — CI is the gate)
- Human visual verification on simulator/device
- Investigation matrix documented in SUMMARY
</verification>

<success_criteria>
- Gap 17 closed with correct tokens
- iOS Share Extension swept
- Figtree bundled in both iOS targets
- Any newly-discovered iOS-specific gaps documented for followup
</success_criteria>

<output>
Create `.planning/phases/05-ux-polish/05-19-SUMMARY.md` with:
- Full "iOS gap matrix" from Task 1 investigation
- List of iOS-specific gaps discovered (new 17a/17b/17c numbers or just bullet list)
- CI build result
- Human checkpoint outcome
</output>
