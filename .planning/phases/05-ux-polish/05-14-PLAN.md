---
phase: 05-ux-polish
plan: 14
type: execute
wave: 4
gap_closure: true
depends_on: [05-11, 05-12, 05-13]
files_modified:
  - DS3Drive/Views/Tutorial/Views/TutorialView.swift
  - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
  - DS3Drive/Assets/Assets.xcassets/tutorial
  - DS3Drive/Assets/Localizable.xcstrings
autonomous: false
requirements: [UX-02]

must_haves:
  truths:
    - "Tutorial screenshots reflect the post-brand-overhaul UI (login, wizard, tray, preferences, Finder integration)"
    - "Tutorial layout showcases the 7 key UX features (badges, tray, speed, recent files, quick actions, wizard, drive limit)"
    - "Tutorial uses the same Cubbit brand tokens as the rest of the app"
  artifacts:
    - path: "DS3Drive/Assets/Assets.xcassets/tutorial"
      provides: "Fresh screenshots of the post-brand-overhaul UI"
  key_links:
    - from: "DS3Drive/Views/Tutorial/Views/TutorialView.swift"
      to: "DS3Colors.brand* tokens"
      via: "background, accent, typography"
      pattern: "DS3Colors.brand"
---

<objective>
Closes Gap 3 — tutorial window lacks meaningful polish and uses stale screenshots.

Final visual polish task. Depends on Plans 05-11 (brand), 05-12 (tray redesign), 05-13 (iOS parity + brand sweep) so the screenshots captured in this plan reflect the **final** UI rather than something that will be re-shot.

Purpose: refresh tutorial screenshots and redesign the tutorial layout to showcase the 7 key UX features delivered in Phase 05.
Output: fresh screenshot assets, redesigned TutorialView, optional animated/interactive walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-11-SUMMARY.md
@.planning/phases/05-ux-polish/05-12-SUMMARY.md
@.planning/phases/05-ux-polish/05-13-SUMMARY.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@CLAUDE.md

The 7 UX requirements (UX-01 through UX-07) are: Finder badges, menu bar tray, transfer speed, recent files, quick actions, drive setup wizard, drive limit. Each tutorial slide should map to one feature.

Screenshot capture requires the app running and a human checkpoint to verify the screenshots look right (since this is a visual deliverable).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign TutorialView with brand tokens and 7-feature slide structure</name>
  <files>
    DS3Drive/Views/Tutorial/Views/TutorialView.swift,
    DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift,
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <read_first>
    DS3Drive/Views/Tutorial/Views/TutorialView.swift,
    DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift,
    DS3Drive/Assets/Localizable.xcstrings,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </read_first>
  <action>
    1. In `TutorialViewModel.swift`, define a `slides: [TutorialSlide]` array with exactly 7 entries, one per UX requirement:
       - `slide-1` — Finder Sync Badges (UX-01)
       - `slide-2` — Menu Bar Status (UX-02)
       - `slide-3` — Transfer Speed (UX-03)
       - `slide-4` — Recent Files (UX-04)
       - `slide-5` — Quick Actions (UX-05)
       - `slide-6` — Drive Setup Wizard (UX-06)
       - `slide-7` — Drive Limit (UX-07)
       Each slide has: `id`, `titleKey`, `descriptionKey`, `imageName` (e.g. `tutorial-slide-1`).
    2. In `TutorialView.swift`, redesign the layout to use a `TabView(selection:)` with `.tabViewStyle(.page)` for swipe-through slides. Each slide:
       - Hero image at top (the screenshot from Task 2), bordered with `RoundedRectangle(cornerRadius: 12).strokeBorder(DS3Colors.brandPrimary.opacity(0.2))`
       - Title using `DS3Typography.title.bold()` in `DS3Colors.brandTextPrimary`
       - Description using `DS3Typography.body.foregroundStyle(DS3Colors.brandTextSecondary)`
       - Page indicator dots using `DS3Colors.brandPrimary`
       - Background `DS3Colors.brandBackground` (or `DS3Gradients.brandHero` for the first/last slide)
       - "Next" / "Get Started" button using brand accent
    3. Add localization keys for each slide title and description (English + Italian) to `Localizable.xcstrings`. Suggested keys: `tutorial.slide1.title`, `tutorial.slide1.description`, etc., through `tutorial.slide7.*`.
    4. Build and verify TutorialView renders without runtime errors (image assets will be empty placeholders until Task 2 lands).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -c "tutorial.slide" DS3Drive/Assets/Localizable.xcstrings; xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `slides:\s*\[TutorialSlide\]\|TutorialSlide` in DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift returns at least one match
    - grep `tutorial-slide-1\|tutorial-slide-7` in DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift returns at least one match
    - grep `DS3Colors.brand\|DS3Gradients.brand` in DS3Drive/Views/Tutorial/Views/TutorialView.swift returns at least one match
    - grep `tutorial.slide1.title\|tutorial.slide7.title` in DS3Drive/Assets/Localizable.xcstrings returns at least one match
    - exactly 7 slides defined
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>TutorialView restructured with 7 slides, brand tokens, localized strings; build succeeds (with placeholder images).</done>
</task>

<task type="auto">
  <name>Task 2: Capture fresh screenshots for the 7 tutorial slides</name>
  <files>
    DS3Drive/Assets/Assets.xcassets/tutorial
  </files>
  <read_first>
    DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    1. Build and run the app from Xcode (`xcodebuild -scheme "DS3 Drive" build` then launch from DerivedData, or via `xcrun xctrace`).
    2. Use `screencapture -i` (interactive) or `screencapture -R<x,y,w,h>` (rectangle) to capture each of the 7 slides. Save each as `tutorial-slide-N.png` in a temp directory:
       - `tutorial-slide-1.png` — Finder window with sync badges visible on files
       - `tutorial-slide-2.png` — Open menu bar tray
       - `tutorial-slide-3.png` — Tray with active transfer + speed summary
       - `tutorial-slide-4.png` — Recent Files side panel
       - `tutorial-slide-5.png` — Drive gear menu (quick actions) open
       - `tutorial-slide-6.png` — Wizard tree view with selection
       - `tutorial-slide-7.png` — Tray with multiple drives near the limit
    3. Optimize each PNG with `pngquant --quality=65-80 --output {file} {file}` if pngquant is installed; otherwise skip.
    4. Add each PNG to `DS3Drive/Assets/Assets.xcassets/tutorial/` as an imageset with @1x/@2x/@3x variants. Create the imageset directory structure manually:
       ```
       DS3Drive/Assets/Assets.xcassets/tutorial/tutorial-slide-1.imageset/
         Contents.json
         tutorial-slide-1.png
         tutorial-slide-1@2x.png
       ```
       The `Contents.json` follows Apple's standard imageset schema.
    5. If a screenshot cannot be captured automatically (e.g., the human checkpoint hasn't approved the visual state), produce a `tutorial-slide-N-PLACEHOLDER.png` that is a solid `DS3Colors.brandPrimary` 1024x768 image with the slide title as text overlay, so the build doesn't break.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && ls DS3Drive/Assets/Assets.xcassets/tutorial/ 2>&1 | head -20; xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - directory DS3Drive/Assets/Assets.xcassets/tutorial/ contains exactly 7 imagesets named tutorial-slide-1 through tutorial-slide-7
    - each imageset has a Contents.json
    - each imageset has at least one PNG (real screenshot or placeholder)
    - xcodebuild clean build succeeds with no missing-asset warnings
  </acceptance_criteria>
  <done>7 tutorial imagesets exist (real or placeholder); build succeeds without asset warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of tutorial visuals</name>
  <what-built>
    Plans 05-11 through 05-13 brand polish + the redesigned tutorial with fresh screenshots from Task 2.
  </what-built>
  <how-to-verify>
    1. Build and launch the app from Xcode.
    2. Sign out and sign back in to trigger the tutorial flow (or invoke it from Help → Show Tutorial if that exists).
    3. Step through all 7 slides. For each slide verify:
       - The screenshot matches the current UI (no stale UI elements visible)
       - The slide title and description are accurate
       - The brand treatment looks cohesive with the rest of the app
       - English and Italian copy both render correctly
    4. If any slide screenshot is wrong or stale, report which slide and what needs to be re-captured.
    5. Verify the tutorial visuals match the rest of the post-overhaul UI (login, wizard, tray, preferences). All windows should feel like the same product.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 slides look correct, or describe which slides need re-capture.</resume-signal>
</task>

</tasks>

<verification>
- TutorialView restructured with 7 slides
- 7 fresh screenshots in asset catalog
- Human checkpoint confirms visual quality
- Build green
</verification>

<success_criteria>
- All three tasks complete
- Human approves the tutorial visuals
- Build green
- Phase 05 fully closed (all 17 gaps addressed)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-14-SUMMARY.md` documenting Gap 3 closure, the 7 slides delivered, and any remaining visual polish items handed off to a future phase. This should be the final SUMMARY for Phase 05.
</output>
