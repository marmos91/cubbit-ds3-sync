---
phase: 05-ux-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - DS3Drive/Views/Tray/Views/TrayMenuView.swift
  - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
  - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
  - DS3Drive/Views/Tray/Views/TrayMenuItem.swift
  - DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
  - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
  - DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift
  - DS3Drive/DS3DriveApp.swift
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "Each drive row shows a colored dot indicator: green=synced, blue=syncing, red=error, orange=paused"
    - "Drive rows display file count, total size, current speed, and sync time"
    - "Global speed summary at top shows aggregate upload/download speed"
    - "Per-drive speed shown inline on each drive row"
    - "Right-click context menu on drive rows has: Disconnect, View in Finder, View in Console, Manage, Refresh, Pause/Resume, Copy S3 Path"
    - "Clicking a drive row opens the Recent Files side panel for that drive"
    - "Recent Files panel shows last 10 files sorted by syncing > error > completed"
    - "Clicking a recent file reveals it in Finder"
    - "Connection Info panel opens from a dedicated row, shows coordinator URL, S3 endpoint, tenant, console URL"
    - "Only one side panel open at a time"
    - "Tray icon animates during syncing state"
    - "Signed in as user@email.com is in the main tray"
    - "Gear menu includes Pause/Resume and Copy S3 Path actions"
  artifacts:
    - path: "DS3Drive/Views/Tray/Views/TrayMenuView.swift"
      provides: "Restructured tray menu with side panels and new layout"
      contains: "HStack.*sidePanel|activeSidePanel"
    - path: "DS3Drive/Views/Tray/Views/TrayDriveRowView.swift"
      provides: "Drive row with colored dot, metrics, context menu"
      contains: "Circle.*foregroundStyle|contextMenu"
    - path: "DS3Drive/Views/Tray/Views/SpeedSummaryView.swift"
      provides: "Global aggregate speed display"
      contains: "SpeedSummaryView"
    - path: "DS3Drive/Views/Tray/Views/RecentFilesPanel.swift"
      provides: "Side panel showing recent files per drive"
      contains: "RecentFilesPanel"
    - path: "DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift"
      provides: "Side panel for connection info with click-to-copy"
      contains: "ConnectionInfoPanel"
    - path: "DS3Drive/DS3DriveApp.swift"
      provides: "Tray icon animation with Timer-based frame swapping"
      contains: "syncAnimationTimer|syncAnimationFrame"
  key_links:
    - from: "DS3Drive/Views/Tray/Views/TrayMenuView.swift"
      to: "DS3Drive/Views/Tray/Views/RecentFilesPanel.swift"
      via: "activeSidePanel state triggers HStack expansion to show panel"
      pattern: "activeSidePanel.*recentFiles"
    - from: "DS3Drive/Views/Tray/Views/TrayDriveRowView.swift"
      to: "DS3Lib/Sources/DS3Lib/SharedData/SharedData+pauseState.swift"
      via: "Pause/Resume action calls SharedData.setDrivePaused()"
      pattern: "setDrivePaused|togglePause"
    - from: "DS3Drive/Views/Tray/Views/TrayDriveRowView.swift"
      to: "DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift"
      via: "recentFiles property provides data for side panel trigger"
      pattern: "driveViewModel.*recentFiles"
    - from: "DS3Drive/DS3DriveApp.swift"
      to: "DS3Lib/Sources/DS3Lib/Models/AppStatus.swift"
      via: "appStatusManager.status drives tray icon selection and animation"
      pattern: "appStatusManager.*status.*syncing"
---

<objective>
Completely restructure the menu bar tray menu with per-drive colored status indicators, real-time speed display, recent files side panel, connection info side panel, expanded gear menu with pause/resume, and animated tray icon. This is the core UX overhaul.

Purpose: The tray menu is the primary user interface for DS3 Drive. Users need at-a-glance sync status per drive, transfer speed visibility, quick access to recent files, and drive management actions including pause/resume.

Output: Restructured TrayMenuView with HStack side panel pattern, redesigned TrayDriveRowView with colored dots and metrics, SpeedSummaryView, RecentFilesPanel, ConnectionInfoPanel, animated tray icon.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-RESEARCH.md

@DS3Drive/Views/Tray/Views/TrayMenuView.swift
@DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
@DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
@DS3Drive/Views/Tray/Views/TrayMenuItem.swift
@DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
@DS3Drive/DS3DriveApp.swift

<!-- Load summaries from dependencies for interface contracts -->
@.planning/phases/05-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-ux-polish/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 05-01 (design system) -->
From DS3Drive/Views/Common/DesignSystem/DS3Colors.swift:
```swift
enum DS3Colors {
    static let accent: Color         // Cubbit blue (#009EFF)
    static let background: Color     // System window background
    static let statusSynced: Color   // green
    static let statusSyncing: Color  // blue
    static let statusError: Color    // red
    static let statusPaused: Color   // orange
}
```

From DS3Drive/Views/Common/DesignSystem/DS3Typography.swift:
```swift
enum DS3Typography {
    static let body: Font            // .system(size: 14)
    static let caption: Font         // .system(size: 12)
    static let headline: Font        // .system(size: 16, weight: .semibold)
}
```

<!-- From Plan 05-02 (data layer) -->
From DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift:
```swift
public enum DS3DriveStatus: String, Codable, Hashable, Sendable {
    case sync, indexing, idle, error, paused
}
```

From DS3Lib/Sources/DS3Lib/Models/RecentFileEntry.swift:
```swift
public struct RecentFileEntry: Identifiable, Sendable {
    public let id: UUID
    public let driveId: UUID
    public let filename: String
    public let size: Int64
    public var status: TransferStatus
    public let timestamp: Date
    public var displaySize: String { ... }
}
public enum TransferStatus: String, Codable, Sendable, Comparable {
    case syncing, error, completed
}
```

From DS3Lib/Sources/DS3Lib/SharedData/SharedData+pauseState.swift:
```swift
extension SharedData {
    public func setDrivePaused(_ driveId: UUID, paused: Bool) throws
    public func isDrivePaused(_ driveId: UUID) throws -> Bool
}
```

From DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift:
```swift
@Observable class DS3DriveViewModel {
    var drive: DS3Drive
    var driveStatus: DS3DriveStatus
    var driveStats: DS3DriveStats
    var recentFiles: [RecentFileEntry] { ... }  // Added in Plan 02
}
```

From DS3Drive/Assets/Assets.xcassets/icons/:
Existing tray icons: TrayIcon, TrayIconSync, TrayIconError, TrayIconPause, TrayIconInfo, TrayIconOffline
Drive icons: DriveIdleIcon, DriveSyncIcon, DriveErrorIcon, DrivePlayIcon (paused icon exists)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure TrayMenuView and TrayDriveRowView with status indicators, metrics, and expanded gear menu</name>
  <files>
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuItem.swift,
    DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
  </files>
  <action>
    **SpeedSummaryView.swift (new file):**
    - Shows aggregate upload/download speed across all drives at the top of tray
    - Takes an array of DS3DriveViewModel and computes total speed: sum of all driveStats.currentSpeedBs
    - Display format: upload arrow + speed + download arrow + speed (or combined if same direction)
    - When no transfers active, show "All drives up to date" or similar idle text
    - Use DS3Typography.caption for font, DS3Colors.secondaryText for color
    - Compact single-row layout with SF Symbols (arrow.up + arrow.down)

    **TrayDriveRowView.swift redesign:**
    - Replace the drive icon images (DriveIdleIcon, DriveSyncIcon, DriveErrorIcon) with a small colored Circle dot indicator:
      - idle: `Circle().fill(DS3Colors.statusSynced).frame(width: 10, height: 10)` (green)
      - sync/indexing: `Circle().fill(DS3Colors.statusSyncing)` (blue)
      - error: `Circle().fill(DS3Colors.statusError)` (red)
      - paused: `Circle().fill(DS3Colors.statusPaused)` (orange)
    - Drive row metrics (4 items from Figma):
      - File count (placeholder -- use "N files" from metadata if available, or "--" initially)
      - Total size (placeholder -- use drive stats or "--")
      - Current speed (from driveStats.currentSpeedBs, formatted)
      - Sync time (from driveStats.lastUpdate, formatted as relative time)
    - Lay out metrics in a compact 2x2 grid or horizontal row below drive name
    - Replace all `.font(.custom("Nunito", ...))` with DS3Typography constants
    - Replace all `Color(.darkWhite)` with `.foregroundStyle(.secondary)`
    - Replace `Color(.darkMainStandard)` / `Color(.hover)` with system hover/selection colors
    - Replace Image(.settingsIcon) gear menu with SF Symbol (gearshape)
    - **Gear menu actions (expanded per CONTEXT.md):**
      - Disconnect (existing)
      - View in Finder (existing)
      - View in web console (existing)
      - Manage (existing)
      - Refresh (existing)
      - Pause/Resume (NEW): reads current pause state, toggles via SharedData.setDrivePaused()
      - Copy S3 Path (NEW): copies `bucket/prefix` to clipboard via NSPasteboard
    - **Right-click context menu:** Add `.contextMenu { }` with the same actions as the gear menu
    - **onTapGesture:** Instead of opening Finder (current), trigger side panel for recent files. Pass a callback to TrayMenuView to set activeSidePanel.
    - Keep the DS3DriveViewModel observation pattern (DistributedNotificationCenter)

    **TrayMenuView.swift restructure:**
    - Define `@State private var activeSidePanel: SidePanel?` where `enum SidePanel: Equatable { case recentFiles(driveId: UUID); case connectionInfo }`
    - Root layout: `HStack(spacing: 0)` with optional side panel on left + main tray on right
    - When activeSidePanel is set, show the appropriate panel in a 310pt-wide column to the left
    - Main tray column (310pt) contains:
      1. "Signed in as user@email.com" row (keep existing, per locked decision)
      2. SpeedSummaryView (aggregate speed)
      3. Divider
      4. Drive rows (ForEach) -- each TrayDriveRowView triggers activeSidePanel on tap
      5. Divider
      6. "Add a new Drive" row (keep existing logic + maxDrives check)
      7. Divider
      8. Quick action rows: Help, Preferences, Open web console (keep existing)
      9. Divider
      10. "Connection Info" row (tap sets activeSidePanel = .connectionInfo)
      11. Sign Out row (keep existing)
      12. Quit row (keep existing)
      13. Footer (status + version)
    - Remove the inline Connection Info VStack (moved to side panel)
    - Apply design system: remove `Color(.background)` ZStack, use system background
    - Use `.animation(.easeInOut(duration: 0.2), value: activeSidePanel)` for smooth transitions
    - Frame: `minWidth: 310, maxWidth: activeSidePanel != nil ? 620 : 310` to expand for side panel
    - `.fixedSize(horizontal: true, vertical: false)` to let content drive sizing

    **TrayMenuFooterView.swift:**
    - Replace `.font(.custom("Nunito", size: 12))` with `.font(DS3Typography.caption)`
    - Replace `Color(.darkMainStandard)` with `.background(.bar)` or system control background
    - Replace `Color(.darkWhite)` with `.foregroundStyle(.secondary)`

    **TrayMenuItem.swift:**
    - Apply same design system replacements (fonts, colors)
    - Replace any Nunito/custom color references
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild build -scheme DS3Drive -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>TrayDriveRowView shows colored dot indicators. Drive rows display metrics. SpeedSummaryView shows aggregate speed. Gear menu has 7 actions including Pause/Resume and Copy S3 Path. Right-click context menu on drive rows. TrayMenuView has HStack layout with activeSidePanel state. Connection info moved out of main body. All custom fonts/colors replaced. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement side panels, tray icon animation, and Finder right-click actions</name>
  <files>
    DS3Drive/Views/Tray/Views/RecentFilesPanel.swift,
    DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift,
    DS3Drive/DS3DriveApp.swift
  </files>
  <action>
    **RecentFilesPanel.swift (new file):**
    - Displayed when activeSidePanel = .recentFiles(driveId)
    - Width: 310pt (matching main tray)
    - Header: Drive name + "Recent Files" title + close button (X)
    - List of up to 10 recent files from DS3DriveViewModel.recentFiles
    - Each row: status dot (green=completed, blue=syncing, red=error) + filename + size + relative time
    - Rows sorted by status priority: syncing first, errors second, completed last
    - Tap a file row: reveal in Finder via `NSWorkspace.shared.activateFileViewerSelecting([url])`
      - To get the file URL: use NSFileProviderManager(for: domain).getUserVisibleURL(for: itemIdentifier)
      - If the full path isn't available from the recent file entry, at minimum open the drive root in Finder
    - Empty state: "No recent files" centered text
    - Apply design system: SF Pro fonts, semantic colors, Cubbit blue accent
    - Divider between this panel and the main tray content

    **ConnectionInfoPanel.swift (new file):**
    - Displayed when activeSidePanel = .connectionInfo
    - Width: 310pt
    - Header: "Connection Info" title + close button (X)
    - Rows (reuse ConnectionInfoRow pattern from current TrayMenuView):
      - Coordinator URL (click-to-copy)
      - S3 Endpoint (click-to-copy)
      - Tenant (click-to-copy)
      - Console URL (click-to-copy)
    - Move the ConnectionInfoRow private struct from TrayMenuView.swift into this file (or make it shared in a Common file)
    - Apply design system: SF Pro fonts, semantic colors

    **DS3DriveApp.swift -- Tray icon animation:**
    - Add `@State private var syncAnimationFrame: Int = 0` and `@State private var syncAnimationTimer: Timer?`
    - Define syncing icon frame names array: create 2-4 rotation frames of the sync icon in the asset catalog, OR use a single TrayIconSync with opacity/rotation state changes
    - Approach: Use Timer.scheduledTimer on `.common` RunLoop mode with 0.3s interval
    - In MenuBarExtra label section, replace the current switch:
      ```swift
      case .syncing:
          // Cycle through sync animation frames
          Image(syncIconFrames[syncAnimationFrame])
      case .paused:
          Image(.trayIconPause)  // Static paused icon (already exists in assets)
      ```
    - Add `.onChange(of: appStatusManager.status)` to start/stop timer:
      - When status becomes .syncing: start timer that increments syncAnimationFrame % frameCount
      - When status leaves .syncing: stop timer, reset frame to 0
    - Schedule timer on `.common` mode so it fires even when menu is open
    - Keep moderate frame rate (3-4 frames at 0.3s per RESEARCH.md guidance)
    - If creating new frame assets is complex, an alternative is to use a single sync icon and just pulse it (swap between TrayIconSync and TrayIcon at 0.5s interval for a simple blink effect)
    - Add paused case to the MenuBarExtra label switch (AppStatus.paused -> Image(.trayIconPause))

    **Finder right-click actions (NSExtensionFileProviderActions):**
    - Per RESEARCH.md, Finder right-click actions require either a FileProviderUI extension target or handling in the File Provider extension itself
    - Since this is complex and the CONTEXT.md marks it as Claude's discretion, take the simpler approach:
      - Add NSExtensionFileProviderActions to DS3DriveProvider/Info.plist with two actions: "Copy S3 Key" and "View in Web Console"
      - Implement `performAction(identifier:onItemsWithIdentifiers:)` in FileProviderExtension.swift
      - For "Copy S3 Key": the extension is sandboxed and may not have NSPasteboard access. If it doesn't work, log a warning and provide these actions ONLY via the tray menu gear menu (which already has "Copy S3 Path")
      - For "View in Web Console": open URL via NSWorkspace (may work from extension)
    - If the Finder actions prove infeasible during implementation (sandbox restrictions), skip them and document in SUMMARY.md. The tray menu gear actions provide equivalent functionality.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild build -scheme DS3Drive -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <done>RecentFilesPanel shows last 10 files per drive with status dots and click-to-reveal. ConnectionInfoPanel shows connection details with click-to-copy. Side panels appear to the left of main tray via HStack. Tray icon animates during syncing. Paused tray icon state works. Build succeeds.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme DS3Drive -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO` succeeds
- TrayDriveRowView has colored Circle dot indicators matching status
- SpeedSummaryView compiles and shows aggregate speed
- Gear menu has all 7 actions
- RecentFilesPanel and ConnectionInfoPanel compile and are wired via activeSidePanel
- TrayMenuView expands width when side panel is active
- DS3DriveApp MenuBarExtra label has syncing animation and paused state
- No `Color(.darkWhite)`, `Color(.darkMainStandard)`, or `.custom("Nunito")` in any tray files
</verification>

<success_criteria>
- Per-drive colored status dots in tray (green/blue/red/orange)
- Drive rows show speed and metrics
- Global speed summary visible at top
- Gear menu + right-click has Disconnect, View in Finder, View in Console, Manage, Refresh, Pause/Resume, Copy S3 Path
- Side panel expands left with recent files or connection info
- Recent files sorted by priority, clickable to reveal in Finder
- Tray icon animates during sync, shows paused icon when paused
- Unified design system applied (SF Pro, semantic colors, Cubbit blue)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-04-SUMMARY.md`
</output>
