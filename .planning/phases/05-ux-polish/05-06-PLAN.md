---
phase: 05-ux-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - DS3Drive/DS3DriveApp.swift
  - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
  - DS3Drive/Views/Tray/Views/TrayMenuView.swift
  - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
  - DS3Drive/Views/Login/ViewModels/LoginViewModel.swift
  - DS3Drive/Views/Sync/ViewModels/SyncViewModel.swift
  - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
  - DS3Drive/Views/Preferences/ViewModels/PreferencesViewModel.swift
  - DS3DriveProvider/FileProviderExtension.swift
  - DS3DriveProvider/FileProviderExtension+Create.swift
  - DS3DriveProvider/FileProviderExtension+Modify.swift
  - DS3DriveProvider/FileProviderExtension+Delete.swift
  - DS3DriveProvider/FileProviderExtension+Lifecycle.swift
  - DS3DriveProvider/FileProviderExtension+Thumbnails.swift
  - DS3DriveProvider/FileProviderExtension+ThumbnailGenerators.swift
  - DS3DriveProvider/FileProviderExtension+CustomActions.swift
  - DS3DriveProvider/FileProviderExtension+Credentials.swift
  - DS3DriveProvider/FileProviderExtension+Errors.swift
  - DS3DriveProvider/S3Enumerator.swift
  - DS3DriveProvider/S3Item.swift
  - DS3DriveProvider/S3Item+Metadata.swift
  - DS3DriveProvider/S3Lib.swift
  - DS3DriveProvider/S3Lib+Transfers.swift
  - DS3DriveProvider/S3Lib+Trash.swift
  - DS3DriveProvider/S3LibListingAdapter.swift
  - DS3DriveProvider/NotificationsManager.swift
  - DS3DriveProvider/BreadthFirstIndexer.swift
  - DS3DriveProvider/TrashS3Enumerator.swift
  - DS3Lib/Sources/DS3Lib/**/*.swift
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "xcodebuild clean build for macOS target produces zero Swift 6 concurrency warnings"
    - "xcodebuild clean build for iOS target produces zero Swift 6 concurrency warnings"
    - "All @Sendable, @MainActor, nonisolated annotations are correct and complete"
  artifacts:
    - path: "DS3DriveProvider/FileProviderExtension.swift"
      provides: "Concurrency-safe extension entry point"
    - path: "DS3Drive/DS3DriveApp.swift"
      provides: "Concurrency-safe app entry point"
  key_links:
    - from: "DS3DriveProvider/FileProviderExtension.swift"
      to: "DS3Lib/Sources/DS3Lib/**/*.swift"
      via: "Sendable conformance on shared types"
      pattern: "Sendable"
---

<objective>
Fix ALL Swift 6 concurrency warnings across the entire codebase (macOS and iOS targets)

Purpose: Per D-CI-01/02/03, the CI runner (Xcode 26.4 RC) enforces stricter sendability. All concurrency warnings must be resolved to prevent CI failures and ensure thread safety.
Output: Zero-warning builds for both macOS and iOS targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Swift 6 concurrency warnings in DS3DriveProvider (macOS extension) and DS3Lib</name>
  <read_first>
    - DS3DriveProvider/FileProviderExtension.swift
    - DS3DriveProvider/FileProviderExtension+Create.swift
    - DS3DriveProvider/FileProviderExtension+Modify.swift
    - DS3DriveProvider/FileProviderExtension+Delete.swift
    - DS3DriveProvider/FileProviderExtension+Lifecycle.swift
    - DS3DriveProvider/FileProviderExtension+Thumbnails.swift
    - DS3DriveProvider/FileProviderExtension+ThumbnailGenerators.swift
    - DS3DriveProvider/FileProviderExtension+CustomActions.swift
    - DS3DriveProvider/FileProviderExtension+Credentials.swift
    - DS3DriveProvider/FileProviderExtension+Errors.swift
    - DS3DriveProvider/S3Enumerator.swift
    - DS3DriveProvider/S3Item.swift
    - DS3DriveProvider/S3Item+Metadata.swift
    - DS3DriveProvider/S3Lib.swift
    - DS3DriveProvider/S3Lib+Transfers.swift
    - DS3DriveProvider/S3Lib+Trash.swift
    - DS3DriveProvider/S3LibListingAdapter.swift
    - DS3DriveProvider/NotificationsManager.swift
    - DS3DriveProvider/BreadthFirstIndexer.swift
    - DS3DriveProvider/TrashS3Enumerator.swift
    - DS3Lib/Sources/DS3Lib/Models/AppStatus.swift
    - DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift
    - DS3Lib/Sources/DS3Lib/Models/RecentFileEntry.swift
    - DS3Lib/Sources/DS3Lib/Utils/RecentFilesTracker.swift
    - DS3Lib/Sources/DS3Lib/SharedData/SharedData+pauseState.swift
  </read_first>
  <files>
    DS3DriveProvider/*.swift,
    DS3Lib/Sources/DS3Lib/**/*.swift
  </files>
  <action>
    1. Run `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3DriveProvider -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -i "warning:"` to identify all concurrency warnings in the extension target.

    2. Run `swift build --package-path DS3Lib 2>&1 | grep -i "warning:"` to identify DS3Lib warnings.

    3. Fix each warning category:

    **@Sendable closures:** If a closure captures a non-Sendable type, either:
    - Make the type conform to `Sendable` (if it's a value type or immutable class)
    - Use `@unchecked Sendable` with documented justification (if thread safety is managed externally via locks/actors)
    - Restructure to avoid capturing non-Sendable state

    **@MainActor isolation:** ViewModels and UI-bound classes that access @Published or @Observable properties must be annotated `@MainActor`. Check:
    - All `@Observable` classes in DS3Drive/Views/ ViewModels/
    - Any class that calls `DispatchQueue.main` should be `@MainActor` instead

    **Global actor isolation:** Static properties on non-Sendable types that are accessed from multiple isolation domains need `nonisolated(unsafe)` (already done for Schema.Version per MEMORY.md — check for others)

    **Sendable conformance on shared models:** Types passed between main app and extension via SharedData (JSON serialization) should conform to Sendable:
    - DS3Drive model, AppStatus, DS3DriveStatus, RecentFileEntry, DriveTransferStats
    - These are Codable structs/enums — add `: Sendable` if missing

    **NSFileProviderReplicatedExtension methods:** These are called on arbitrary threads. Ensure:
    - No `@MainActor` on extension methods
    - Completion handlers are `@Sendable`
    - Captured self in async callbacks is handled correctly

    4. After fixes, verify with: `xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3DriveProvider -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:"` — must be 0.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build -project DS3Drive.xcodeproj -scheme DS3DriveProvider -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:" | xargs test 0 -eq && echo "PASS: Zero warnings in DS3DriveProvider"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build -scheme DS3DriveProvider -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:"` returns 0
    - No `@unchecked Sendable` added without a comment explaining why
    - `swift build --package-path DS3Lib 2>&1 | grep -c "warning:"` returns 0
  </acceptance_criteria>
  <done>DS3DriveProvider and DS3Lib build with zero Swift 6 concurrency warnings under strict concurrency checking.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Swift 6 concurrency warnings in DS3Drive main app and iOS targets</name>
  <read_first>
    - DS3Drive/DS3DriveApp.swift
    - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
    - DS3Drive/Views/Login/ViewModels/LoginViewModel.swift
    - DS3Drive/Views/Sync/ViewModels/SyncViewModel.swift
    - DS3Drive/Views/Tutorial/ViewModels/TutorialViewModel.swift
    - DS3Drive/Views/Preferences/ViewModels/PreferencesViewModel.swift
    - DS3DriveApp/DS3DriveApp.swift (iOS app entry point)
    - DS3DriveApp/ViewModels/ (all files in this directory)
    - DS3DriveApp/Views/ (all files in this directory)
    - DS3DriveShareExtension/ (all Swift files)
  </read_first>
  <files>
    DS3Drive/*.swift,
    DS3Drive/Views/**/*.swift,
    DS3DriveApp/*.swift,
    DS3DriveApp/**/*.swift,
    DS3DriveShareExtension/*.swift
  </files>
  <action>
    1. Run `xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -i "warning:"` to identify macOS app warnings.

    2. Run `xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3DriveApp" -destination 'generic/platform=iOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -i "warning:"` to identify iOS warnings.

    3. Fix each warning — same categories as Task 1, plus:

    **@MainActor on ViewModels:** All ViewModels in DS3Drive/Views/*/ViewModels/ and DS3DriveApp/ViewModels/ must be `@MainActor` since they drive UI state:
    - DS3DriveViewModel (if not already)
    - LoginViewModel (already fixed per MEMORY.md)
    - SyncViewModel
    - TutorialViewModel
    - PreferencesViewModel (if it exists)
    - Any iOS-specific ViewModels

    **@Observable + @MainActor:** `@Observable` classes that are used as `@State` or `@Environment` in SwiftUI views should be `@MainActor`.

    **Task {} closures in views:** SwiftUI `.task {}` modifiers create `@MainActor`-isolated async contexts. If they call non-MainActor functions, ensure those functions are properly annotated or called with `await`.

    **Share Extension (DS3DriveShareExtension):** Check for concurrency warnings in the share extension — it runs in a separate process with limited memory.

    4. After fixes, verify both schemes build with zero warnings.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:" | xargs test 0 -eq && echo "PASS: Zero warnings in DS3 Drive macOS"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild clean build -scheme "DS3 Drive" -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:"` returns 0
    - `xcodebuild clean build -scheme "DS3DriveApp" -destination 'generic/platform=iOS' SWIFT_STRICT_CONCURRENCY=complete 2>&1 | grep -c "warning:"` returns 0
    - All ViewModels in DS3Drive/Views/*/ViewModels/*.swift contain `@MainActor`
    - No `@unchecked Sendable` added without a comment explaining why
  </acceptance_criteria>
  <done>Both macOS and iOS app targets build with zero Swift 6 concurrency warnings. All ViewModels are properly @MainActor annotated.</done>
</task>

</tasks>

<verification>
1. `xcodebuild clean build -scheme DS3DriveProvider -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete` — zero warnings
2. `xcodebuild clean build -scheme "DS3 Drive" -destination 'platform=macOS' SWIFT_STRICT_CONCURRENCY=complete` — zero warnings
3. `xcodebuild clean build -scheme "DS3DriveApp" -destination 'generic/platform=iOS' SWIFT_STRICT_CONCURRENCY=complete` — zero warnings
4. `swift build --package-path DS3Lib` — zero warnings
</verification>

<success_criteria>
- All four build commands produce zero concurrency warnings
- No new `@unchecked Sendable` without justification comments
- All ViewModels annotated @MainActor
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-06-SUMMARY.md`
</output>
