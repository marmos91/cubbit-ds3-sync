---
phase: 05-ux-polish
plan: 12
type: execute
wave: 3
gap_closure: true
depends_on: [05-11]
files_modified:
  - DS3Drive/Views/Tray/Views/TrayMenuView.swift
  - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
  - DS3Drive/Views/Tray/Views/TrayMenuItem.swift
  - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
  - DS3Drive/Views/Tray/Views/SpeedSummaryView.swift
  - DS3Drive/Views/Tray/Views/RecentFilesPanel.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md
autonomous: true
requirements: [UX-02, UX-03]

must_haves:
  truths:
    - "Tray menu uses card-style drive rows with brand-colored treatment matching the Sync Share 2.0 Figma layout"
    - "Drive name, project name, and status have a clear typographic hierarchy"
    - "Visual separators group sections (drive list / actions / footer)"
    - "Brand accent stripe or badge appears on each drive row"
  artifacts:
    - path: ".planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md"
      provides: "Reference doc with Sync Share 2.0 Figma node analysis and the layout decisions taken"
  key_links:
    - from: "DS3Drive/Views/Tray/Views/TrayDriveRowView.swift"
      to: "DS3Colors.brand* tokens from 05-11"
      via: "card background, accent stripe"
      pattern: "DS3Colors.brand"
---

<objective>
Closes Gap 6 — tray menu visually plain, should match Sync Share 2.0 Figma.

Reimplements the tray components with the Sync Share 2.0 layout (card rows, refined typography, visual separators) using the Cubbit brand palette established in Plan 05-11. This is the highest-impact visual change in the gap closure series.

Depends on Plan 05-11 for `DS3Colors.brand*` tokens and `DS3Gradients`. Folds in the final visual treatments for Gaps 10, 11, 12, 13 (which Plan 05-10 implemented structurally).

Purpose: make the tray feel like the brand. The tray is the most-used surface in the app.
Output: redesigned tray components with card-based layout and Cubbit brand styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-04-SUMMARY.md
@.planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
@.planning/phases/05-ux-polish/05-11-SUMMARY.md
@CLAUDE.md

Sync Share 2.0 Figma: https://www.figma.com/design/E0QXd1ecdYVm9mDKjOntIK/Sync-Share-2.0?node-id=106-1013
File key: `E0QXd1ecdYVm9mDKjOntIK`
Node id: `106:1013` (convert dashes to colons in the node id)

Use `mcp__plugin_figma_figma__get_design_context` (primary tool per Figma MCP server instructions) to extract layout, then adapt to SwiftUI using brand tokens from Plan 05-11. The Figma output is REFERENCE — DS3Spacing/DS3Typography conventions take precedence.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Sync Share 2.0 layout from Figma + write FIGMA-LAYOUT.md reference</name>
  <files>
    .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md
  </files>
  <read_first>
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    1. Call `mcp__plugin_figma_figma__get_design_context` with `fileKey: "E0QXd1ecdYVm9mDKjOntIK"` and `nodeId: "106:1013"` to get the Sync Share 2.0 design context.
    2. Also call `mcp__plugin_figma_figma__get_screenshot` on the same node so the doc has a visual reference.
    3. Write `.planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md` containing:
       - Figma file URL + node id
       - Condensed description of the layout: drive card structure, typographic hierarchy (drive name size/weight, project name size/weight, status size/weight), visual separator placement, header/footer treatment, accent stripe/badge usage
       - Mapping from Sync Share 2.0 colors -> Cubbit brand tokens (do NOT use Sync Share 2.0 colors directly; remap to `DS3Colors.brand*` from 05-11)
       - List of decisions where SwiftUI implementation will diverge from Figma (AppKit menu height constraints, NSStatusItem width, etc.)
    4. Doc must have enough detail that Task 2 implements without re-fetching the Figma context.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && test -f .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md && wc -l .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md</automated>
  </verify>
  <acceptance_criteria>
    - file .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md exists
    - grep `Drive card\|drive row\|typography\|hierarchy` in 05-12-FIGMA-LAYOUT.md returns at least one match
    - grep `brandPrimary\|brandAccent\|brandSurface` in 05-12-FIGMA-LAYOUT.md returns at least one match
    - file is at least 50 lines (substantive reference doc)
  </acceptance_criteria>
  <done>FIGMA-LAYOUT.md exists with Sync Share 2.0 layout decisions remapped to Cubbit brand tokens, ready for Task 2 implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign tray components with brand palette and Sync Share 2.0 layout</name>
  <files>
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuItem.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Tray/Views/SpeedSummaryView.swift,
    DS3Drive/Views/Tray/Views/RecentFilesPanel.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  </files>
  <read_first>
    .planning/phases/05-ux-polish/05-12-FIGMA-LAYOUT.md,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuItem.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Tray/Views/SpeedSummaryView.swift,
    DS3Drive/Views/Tray/Views/RecentFilesPanel.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  </read_first>
  <action>
    Implement against `05-12-FIGMA-LAYOUT.md`:

    1. **TrayDriveRowView.swift** — wrap each drive row in `RoundedRectangle(cornerRadius: 10).fill(DS3Colors.brandSurface)`. Apply `DS3Spacing.md` internal padding. Drive name uses `DS3Typography.body.bold()`, project name uses `DS3Typography.caption.foregroundStyle(DS3Colors.brandTextSecondary)`, status row uses `DS3Typography.footnote`. Add a 2pt-wide accent stripe on the leading edge whose color matches the per-drive status (synced/syncing/error/paused) using `DS3Colors.statusX`. Keep the existing gear button.

    2. **TrayMenuView.swift** — apply `DS3Colors.brandBackground` to the root container. Replace `Divider()` with custom `Rectangle().fill(DS3Colors.brandTextSecondary.opacity(0.12)).frame(height: 1)`. Maintain the menu order from Plan 05-10 Task 2 (Add a new Drive, Preferences, divider, Open web console, Check for Updates, divider, Help, Sign Out, Quit). Maintain the conditional `drives.count >= 2` aggregate row.

    3. **TrayMenuItem.swift** — restyle hover state to `RoundedRectangle(cornerRadius: 6).fill(isHovered ? DS3Colors.brandPrimary.opacity(0.15) : Color.clear)`. Icons use `DS3Colors.brandTextPrimary`.

    4. **TrayMenuFooterView.swift** — apply `DS3Colors.brandSurface.opacity(0.6)` background, keep the state icon from Plan 05-10 Task 3 (`statusIcon` from `aggregateStatus`), add a thin top border using the soft divider style. Version label uses `DS3Typography.caption.foregroundStyle(DS3Colors.brandTextSecondary)`.

    5. **SpeedSummaryView.swift** — apply brand typography for upload/download values; use `DS3Colors.brandPrimary` for the numbers and `DS3Colors.brandTextSecondary` for the labels.

    6. **RecentFilesPanel.swift** — apply `DS3Colors.brandSurface` to the panel background, refine row layout to match the Figma card style. Keep the Clear button from Plan 05-09 Task 1.

    7. **DS3Colors.swift** — if `statusSynced`, `statusSyncing`, `statusError`, `statusPaused`, `brandTextPrimary`, `brandTextSecondary` aren't already exposed, add them as static let tokens.

    8. Verify no inline `Color(red:...)` literals or `.darkWhite`/`.darkMainStandard` references remain in any tray file after edits.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && grep -E "darkWhite|darkMainStandard" DS3Drive/Views/Tray/Views/*.swift; xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep `DS3Colors.brand` in DS3Drive/Views/Tray/Views/TrayDriveRowView.swift returns at least one match
    - grep `RoundedRectangle\|brandSurface` in DS3Drive/Views/Tray/Views/TrayDriveRowView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Tray/Views/TrayMenuView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift returns at least one match
    - grep `DS3Colors.brand` in DS3Drive/Views/Tray/Views/RecentFilesPanel.swift returns at least one match
    - grep `darkWhite\|darkMainStandard` in DS3Drive/Views/Tray/Views/*.swift returns zero matches
    - xcodebuild clean build analyze succeeds
  </acceptance_criteria>
  <done>All tray components restyled with brand tokens; no legacy dark colors remain in tray files; build succeeds; tray visually matches the FIGMA-LAYOUT.md decisions.</done>
</task>

</tasks>

<verification>
- FIGMA-LAYOUT.md exists with substantive layout decisions
- All tray files reference DS3Colors.brand* tokens
- xcodebuild clean build analyze succeeds
- Visual confirmation of redesign happens in milestone-level review wave (not in this plan)
</verification>

<success_criteria>
- Both tasks complete with grep-verifiable acceptance criteria met
- Build green
- Tray feels like the Cubbit brand
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-12-SUMMARY.md` documenting Gap 6 closure, screenshots if available, and the final visual decisions.
</output>
