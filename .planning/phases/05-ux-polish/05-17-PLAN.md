---
phase: 05-ux-polish
plan: 17
type: execute
wave: 2
gap_closure: true
depends_on: []
files_modified:
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift
  - DS3Drive/Views/Common/DesignSystem/DS3CardStyle.swift
  - DS3Drive/Views/Common/Buttons/BrandPrimaryButtonStyle.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift
  - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift
  - DS3Drive/Assets/Fonts/Figtree-Regular.ttf
  - DS3Drive/Assets/Fonts/Figtree-Medium.ttf
  - DS3Drive/Assets/Fonts/Figtree-SemiBold.ttf
  - DS3Drive/Assets/Fonts/Figtree-Bold.ttf
  - DS3Drive/Info.plist
  - DS3DriveApp/Info.plist
  - DS3DriveShareExtension/Info.plist
  - .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
autonomous: false
requirements: [UX-01, UX-06]

must_haves:
  truths:
    - "DS3Colors.brandBackground dark variant is #0E0E15 (verified by color inspection), NOT #252B30"
    - "DS3Colors.brandPrimary is #005CE8 (verified by color inspection), NOT #3384FF"
    - "DS3Typography.body uses Font.custom('Figtree-Regular', size: 14), NOT system font"
    - "Figtree-{Regular,Medium,SemiBold,Bold}.ttf exist in DS3Drive/Assets/Fonts/"
    - "Info.plist registers ATSApplicationFontsPath (macOS) and UIAppFonts (iOS) for Figtree"
    - "BrandCardStyle helper applies brandSurface + 1pt brandBorderSubtle stroke + bottom-right blue radial glow"
    - "BrandPrimaryButtonStyle applies brandPrimary background + white text + 8px radius + Figtree-SemiBold"
  artifacts:
    - path: "DS3Drive/Views/Common/DesignSystem/DS3Colors.swift"
      provides: "Corrected brand color palette matching Composer canary"
      contains: "0x0E/255, green: 0x0E/255, blue: 0x15"
    - path: "DS3Drive/Views/Common/DesignSystem/DS3Typography.swift"
      provides: "Figtree-based typography scale"
      contains: "Figtree"
    - path: "DS3Drive/Views/Common/DesignSystem/DS3CardStyle.swift"
      provides: "Shared brand card style (fill + border + radial glow)"
    - path: "DS3Drive/Views/Common/Buttons/BrandPrimaryButtonStyle.swift"
      provides: "Shared primary CTA button style"
    - path: ".planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md"
      provides: "Updated reference doc with corrected tokens + supersession note"
  key_links:
    - from: "DS3Drive/Info.plist"
      to: "DS3Drive/Assets/Fonts/"
      via: "ATSApplicationFontsPath entry"
      pattern: "ATSApplicationFontsPath"
    - from: "DS3Drive/Views/Common/DesignSystem/DS3Typography.swift"
      to: "bundled Figtree font files"
      via: "Font.custom('Figtree-*', size:)"
      pattern: "Font.custom\\(\"Figtree"
---

<objective>
Closes Gap 31 — redo Plan 05-11 brand foundation with CORRECT tokens extracted from `composer-canary.cubbit.eu` (the live product), not the marketing site CSS.

**Critical context:** Plan 05-11 was authorized to fall back to `cubbit.io` marketing CSS when Figma MCP was unavailable. The marketing site palette is substantially different from the actual product. Plan 05-11 shipped:
- `#252B30` background (actual: `#0E0E15` — way darker)
- `#3384FF` primary (actual: `#005CE8` — deeper saturated blue)
- Solid grey borders (actual: white-alpha `#FFFFFF0A` / `#FFFFFF1A`)
- System font (actual: Figtree)

This plan REPLACES those tokens with values verified against the live product. Plans 05-12 through 05-14 all inherited Plan 05-11's wrong tokens, so Plans 05-18 and 05-19 will sweep their outputs after this plan lands.

**Autonomous = false** because font bundling may require human assistance (license confirmation, Google Fonts download) and final visual confirmation requires a human eye on the app side-by-side with the canary reference.

Purpose: ground every downstream visual plan in correct brand values.

Output: DS3Colors / DS3Typography / DS3Gradients rewritten, Figtree bundled, shared BrandCardStyle + BrandPrimaryButtonStyle components, 05-11-BRAND-TOKENS.md updated and supersession noted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
@.planning/phases/05-ux-polish/05-11-SUMMARY.md
@.planning/phases/05-ux-polish/05-13-SUMMARY.md
@CLAUDE.md
@DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
@DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
@DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift
@DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle Figtree font + verify against live Composer canary</name>
  <files>
    DS3Drive/Assets/Fonts/Figtree-Regular.ttf,
    DS3Drive/Assets/Fonts/Figtree-Medium.ttf,
    DS3Drive/Assets/Fonts/Figtree-SemiBold.ttf,
    DS3Drive/Assets/Fonts/Figtree-Bold.ttf,
    DS3Drive/Info.plist,
    DS3DriveApp/Info.plist,
    DS3DriveShareExtension/Info.plist
  </files>
  <read_first>
    - DS3Drive/Info.plist (check if ATSApplicationFontsPath already exists)
    - DS3DriveApp/Info.plist (check UIAppFonts)
    - DS3DriveShareExtension/Info.plist
    - .planning/phases/05-ux-polish/05-08-GAPS.md "Gap 31" section
  </read_first>
  <action>
    Step 1 — Live re-verify brand tokens from Composer canary:
    ```bash
    curl -sL https://composer-canary.cubbit.eu/en/dashboard | grep -oE '"--[a-z-]+":\s*"[^"]+"' | head -80
    ```
    If curl can't reach authenticated page, use WebFetch tool against https://composer-canary.cubbit.eu asking for "extract all CSS custom property declarations (--*:) from the raw HTML and head styles". Cross-check the extracted values against the table in Gap 31 — if any differ, prefer the live values and document the delta in the commit message.

    Step 2 — Download Figtree from Google Fonts:
    ```bash
    mkdir -p DS3Drive/Assets/Fonts
    curl -sL "https://github.com/google/fonts/raw/main/ofl/figtree/Figtree%5Bwght%5D.ttf" -o /tmp/Figtree-var.ttf
    # OR use the static files if the variable font causes issues:
    curl -sL "https://fonts.google.com/download?family=Figtree" -o /tmp/figtree.zip
    ```
    If the download fails or if the project prefers static weight files, note it as a blocker and ask the user to drop the four `.ttf` files at the paths above. Verify files exist and have non-zero size before proceeding.

    Preferred static weights to bundle:
    - Figtree-Regular.ttf (400)
    - Figtree-Medium.ttf (500)
    - Figtree-SemiBold.ttf (600)
    - Figtree-Bold.ttf (700)

    Step 3 — Register fonts in `DS3Drive/Info.plist`:
    ```xml
    <key>ATSApplicationFontsPath</key>
    <string>Fonts/</string>
    ```
    OR (more reliable on modern macOS): declare each font explicitly:
    ```xml
    <key>ATSApplicationFontsPath</key>
    <string>.</string>
    ```
    and ensure the `.ttf` files are added to the DS3Drive target's "Copy Bundle Resources" build phase. Read the CLAUDE.md target membership guidance — Xcode project requires the font files to be target members, not just files on disk.

    Step 4 — For `DS3DriveApp/Info.plist` (iOS) and `DS3DriveShareExtension/Info.plist`:
    ```xml
    <key>UIAppFonts</key>
    <array>
        <string>Figtree-Regular.ttf</string>
        <string>Figtree-Medium.ttf</string>
        <string>Figtree-SemiBold.ttf</string>
        <string>Figtree-Bold.ttf</string>
    </array>
    ```
    Fonts can be added to the iOS target membership so they're bundled.

    Step 5 — Since this involves Xcode project file changes (adding files to target membership), these MUST be done via Xcode or `xcodeproj` CLI — not by hand-editing `project.pbxproj`. If the agent cannot automate this, stop and create a checkpoint asking the human to add font files via Xcode's File Inspector.
  </action>
  <verify>
    <automated>ls -la DS3Drive/Assets/Fonts/Figtree-*.ttf 2>&1 && xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Four Figtree .ttf files exist with non-zero size in DS3Drive/Assets/Fonts/
    - Info.plist entries registered in all three targets
    - Build succeeds
    - Runtime verification: log `NSFontManager.shared.availableFontFamilies.contains("Figtree")` at app launch prints true
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DS3Colors / DS3Typography / DS3Gradients with correct tokens</name>
  <files>
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Typography.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Typography.swift,
    DS3Lib/Sources/DS3Lib/DesignSystem/DS3Gradients.swift,
    .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md
  </files>
  <read_first>
    - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift (full current file)
    - DS3Drive/Views/Common/DesignSystem/DS3Typography.swift
    - DS3Drive/Views/Common/DesignSystem/DS3Gradients.swift
    - DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift (cross-platform variant)
    - .planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md (to update + supersession note)
    - .planning/phases/05-ux-polish/05-08-GAPS.md "Gap 31" section
  </read_first>
  <action>
    **Use EXACT hex values from Gap 31 — do NOT round, interpret, or adjust.**

    Step 1 — Rewrite `DS3Drive/Views/Common/DesignSystem/DS3Colors.swift`:
    ```swift
    import SwiftUI
    import AppKit

    enum DS3Colors {
        // MARK: - Raw brand palette (Composer canary, 2026-04)

        // Backgrounds
        static let brandBg900 = Color(red: 0x0E/255, green: 0x0E/255, blue: 0x15/255) // bg-default
        static let brandBg800 = Color(red: 0x12/255, green: 0x12/255, blue: 0x12/255) // bg-paper (card dark)
        static let brandBg700 = Color(red: 0x1A/255, green: 0x1A/255, blue: 0x22/255) // hover surface (derived)

        // Primary
        static let brandPrimary     = Color(red: 0x00/255, green: 0x5C/255, blue: 0xE8/255) // #005CE8
        static let brandPrimaryDark = Color(red: 0x00/255, green: 0x48/255, blue: 0xB5/255) // #0048B5
        static let brandPrimaryLight = Color(red: 0x33/255, green: 0x7C/255, blue: 0xEC/255) // #337CEC

        // Status (Material-UI palette from canary)
        static let statusSuccess = Color(red: 0x26/255, green: 0xAB/255, blue: 0x75/255) // #26AB75
        static let statusError   = Color(red: 0xE5/255, green: 0x63/255, blue: 0x63/255) // #E56363
        static let statusWarning = Color(red: 0xFF/255, green: 0xB7/255, blue: 0x4D/255) // #FFB74D
        static let statusInfo    = Color(red: 0x54/255, green: 0x98/255, blue: 0xFF/255) // #5498FF

        // Text (white + alpha)
        static let textPrimary    = Color.white
        static let textSecondary  = Color.white.opacity(0.60) // FFFFFF99
        static let textTertiary   = Color.white.opacity(0.45) // FFFFFF73
        static let textDisabled   = Color.white.opacity(0.30) // FFFFFF4D

        // Borders (white + alpha)
        static let brandBorderUltraSubtle = Color.white.opacity(0.04) // FFFFFF0A
        static let brandBorderSubtle      = Color.white.opacity(0.10) // FFFFFF1A
        static let brandBorderStrong      = Color.white.opacity(0.30) // FFFFFF4D
        static let brandDivider           = Color.white.opacity(0.12)

        // MARK: - Adaptive tokens

        static let brandBackground = Color(nsColor: NSColor(name: nil) { appearance in
            appearance.bestMatch(from: [.darkAqua, .vibrantDark]) != nil
                ? NSColor(red: 0x0E/255, green: 0x0E/255, blue: 0x15/255, alpha: 1)
                : NSColor.white
        })

        static let brandSurface = Color(nsColor: NSColor(name: nil) { appearance in
            appearance.bestMatch(from: [.darkAqua, .vibrantDark]) != nil
                ? NSColor(red: 0x12/255, green: 0x12/255, blue: 0x12/255, alpha: 1)
                : NSColor.white
        })

        // MARK: - Project badge palette (derived from brand hues for Gap 21)
        static let projectBadgePalette: [Color] = [
            brandPrimary, brandPrimaryLight, statusSuccess, statusInfo,
            statusWarning, brandPrimaryDark
        ]
    }
    ```

    Step 2 — Rewrite `DS3Typography.swift`:
    ```swift
    import SwiftUI

    enum DS3Typography {
        // Headings (Figtree SemiBold, tight leading)
        static let h1 = Font.custom("Figtree-SemiBold", size: 40)
        static let h2 = Font.custom("Figtree-SemiBold", size: 32)
        static let h3 = Font.custom("Figtree-SemiBold", size: 24)

        // Body
        static let bodyLarge  = Font.custom("Figtree-Regular", size: 16)
        static let body       = Font.custom("Figtree-Regular", size: 14)
        static let bodyMedium = Font.custom("Figtree-Medium", size: 14)

        // Caption / UI
        static let caption        = Font.custom("Figtree-Regular", size: 12)
        static let captionBold    = Font.custom("Figtree-SemiBold", size: 12)
        static let button         = Font.custom("Figtree-SemiBold", size: 14)

        // Title aliases used across the app (keep these)
        static let title   = h3
        static let heading = Font.custom("Figtree-SemiBold", size: 18)
    }
    ```
    IMPORTANT: grep for existing uses of `DS3Typography.*` in the codebase — preserve every existing symbol name so downstream views don't break. Add aliases if needed.

    Step 3 — Rewrite `DS3Gradients.swift` adding:
    ```swift
    enum DS3Gradients {
        // Hero — subtle vertical gradient used as window backdrop
        static let brandVerticalBackground = LinearGradient(
            colors: [
                Color(red: 0x0E/255, green: 0x0E/255, blue: 0x15/255),
                Color(red: 0x08/255, green: 0x08/255, blue: 0x10/255)
            ],
            startPoint: .top,
            endPoint: .bottom
        )

        // Radial glow used on card bottom-right corner (bluish accent)
        static let brandCardGlow = RadialGradient(
            colors: [
                DS3Colors.brandPrimary.opacity(0.10),
                .clear
            ],
            center: .bottomTrailing,
            startRadius: 0,
            endRadius: 220
        )

        // Hero gradient (for login / tutorial backdrop) — keep existing symbol name if it existed
        static let brandHero = brandVerticalBackground
        static let brandHeroSubtle = brandVerticalBackground
    }
    ```
    Keep previous gradient symbol names aliased so Plans 05-12/05-13/05-14 callers still compile.

    Step 4 — Mirror the same changes into `DS3Lib/Sources/DS3Lib/DesignSystem/DS3Colors.swift`, `DS3Typography.swift`, `DS3Gradients.swift` (the cross-platform variants used by iOS per Plan 05-13). Use `Color(red:green:blue:)` which works on both platforms, avoid `NSColor` conditionals — wrap `NSColor(name:)` in `#if os(macOS)` and fall back to plain `Color` on iOS. The adaptive dark/light split is less important on iOS (use the dark value since brand is dark).

    Step 5 — Update `.planning/phases/05-ux-polish/05-11-BRAND-TOKENS.md`:
    - Add at the top: `**STATUS: SUPERSEDED by Plan 05-17.** The tokens below extracted from cubbit.io marketing CSS were later found to differ from the actual product. See the "Corrected tokens" section below and Gap 31 in 05-08-GAPS.md.`
    - Keep the old values with a strikethrough or "~~" markdown and a note: `# Old (marketing CSS) — do NOT use`
    - Add a new section "Corrected tokens (Composer canary, Plan 05-17)" with every value from Step 1 above.
  </action>
  <verify>
    <automated>xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" build 2>&1 | grep -E "error:|warning:" | head -30</automated>
  </verify>
  <done>
    - Build succeeds
    - grep `0x0E/255, green: 0x0E/255, blue: 0x15` in DS3Colors.swift returns at least one match
    - grep `0x00/255, green: 0x5C/255, blue: 0xE8` returns at least one match
    - grep `Figtree-Regular` in DS3Typography.swift returns at least one match
    - 05-11-BRAND-TOKENS.md has SUPERSEDED marker at top
  </done>
</task>

<task type="auto">
  <name>Task 3: Shared BrandCardStyle and BrandPrimaryButtonStyle</name>
  <files>
    DS3Drive/Views/Common/DesignSystem/DS3CardStyle.swift,
    DS3Drive/Views/Common/Buttons/BrandPrimaryButtonStyle.swift
  </files>
  <read_first>
    - DS3Drive/Views/Common/DesignSystem/ (check existing card/button components to avoid duplication)
    - DS3Drive/Views/Common/Buttons/ (check for existing button styles)
  </read_first>
  <action>
    Step 1 — Create `DS3Drive/Views/Common/DesignSystem/DS3CardStyle.swift`:
    ```swift
    import SwiftUI

    struct BrandCardStyle: ViewModifier {
        var cornerRadius: CGFloat = 12

        func body(content: Content) -> some View {
            content
                .padding(16)
                .background(
                    ZStack {
                        RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                            .fill(DS3Colors.brandSurface)
                        RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                            .fill(DS3Gradients.brandCardGlow)
                            .blur(radius: 40)
                            .blendMode(.plusLighter)
                            .opacity(0.6)
                    }
                )
                .overlay(
                    RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                        .strokeBorder(DS3Colors.brandBorderSubtle, lineWidth: 1)
                )
        }
    }

    extension View {
        func brandCard(cornerRadius: CGFloat = 12) -> some View {
            modifier(BrandCardStyle(cornerRadius: cornerRadius))
        }
    }
    ```

    Step 2 — Create `DS3Drive/Views/Common/Buttons/BrandPrimaryButtonStyle.swift`:
    ```swift
    import SwiftUI

    struct BrandPrimaryButtonStyle: ButtonStyle {
        var fillWidth: Bool = false

        func makeBody(configuration: Configuration) -> some View {
            configuration.label
                .font(DS3Typography.button)
                .foregroundStyle(Color.white)
                .padding(.horizontal, 20)
                .frame(minHeight: 44)
                .frame(maxWidth: fillWidth ? .infinity : nil)
                .background(
                    RoundedRectangle(cornerRadius: 8, style: .continuous)
                        .fill(configuration.isPressed
                              ? DS3Colors.brandPrimaryDark
                              : DS3Colors.brandPrimary)
                )
                .scaleEffect(configuration.isPressed ? 0.98 : 1.0)
                .animation(.easeOut(duration: 0.1), value: configuration.isPressed)
        }
    }
    ```

    Step 3 — Do not wire these into callers here — that happens in Plans 05-18 and 05-19. This task just creates the shared components so the sweep plans can import and apply them uniformly.
  </action>
  <verify>
    <automated>xcodebuild -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" build 2>&1 | grep -E "error:" | head -10</automated>
  </verify>
  <done>
    - Both files exist and compile
    - Usable via `.brandCard()` modifier and `.buttonStyle(BrandPrimaryButtonStyle())`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification against Composer canary</name>
  <what-built>
    - DS3Colors / DS3Typography / DS3Gradients with corrected values from Composer canary
    - Figtree font bundled in all three targets
    - BrandCardStyle + BrandPrimaryButtonStyle helpers
    - 05-11-BRAND-TOKENS.md updated with supersession notice
  </what-built>
  <how-to-verify>
    1. Run the app from Xcode (`Cmd+R`)
    2. Open any existing view (login window or tray)
    3. **Expected:** text renders in Figtree (slightly softer + more geometric than system font). Window backdrop is nearly black (`#0E0E15`), not warm grey.
    4. Open `https://composer-canary.cubbit.eu/en/dashboard` in a browser side-by-side with the DS3 Drive login window
    5. Confirm: backgrounds match in darkness, blue accents match the saturated Cubbit blue `#005CE8`
    6. If the font looks like system font (fallback), the Figtree bundle didn't register — stop and fix the Info.plist + target membership before approving
    7. Respond with "approved" or a list of specific mismatches
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual mismatches</resume-signal>
</task>

</tasks>

<verification>
- Build clean
- Figtree loads at runtime (not fallback)
- Color sampling matches canary hex values
- Human side-by-side check approved
</verification>

<success_criteria>
- Gap 31 closed: foundation rewritten with correct values
- 05-11-BRAND-TOKENS.md shows supersession
- Downstream plans (05-18, 05-19) can consume the new tokens and helpers directly
</success_criteria>

<output>
Create `.planning/phases/05-ux-polish/05-17-SUMMARY.md` with:
- Confirmed hex values shipped
- Figtree bundling method used
- Human checkpoint result
- Note on any delta from Gap 31 spec (if live extraction found different values)
</output>
