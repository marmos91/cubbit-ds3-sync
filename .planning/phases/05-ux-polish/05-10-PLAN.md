---
phase: 05-ux-polish
plan: 10
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - DS3Drive/Views/Sync/Views/TreeNavigationView.swift
  - DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  - DS3Drive/Views/Preferences/Views/PreferencesView.swift
  - DS3Drive/Views/Preferences/Views/ConnectionTab.swift
  - DS3Drive/Views/Preferences/Views/GeneralTab.swift
  - DS3Drive/Views/Tray/Views/TrayMenuView.swift
  - DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift
  - DS3Drive/Views/Tray/Views/TrayDriveRowView.swift
  - DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift
  - DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift
  - DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift
  - DS3Drive/Assets/Localizable.xcstrings
  - DS3Drive/DS3DriveApp.swift
autonomous: true
requirements: [UX-02, UX-03, UX-06]

must_haves:
  truths:
    - "Project badges in wizard tree are readable and visually differentiated per project"
    - "Preferences window default frame is large enough to show full General tab without scrolling"
    - "Connection Info no longer appears in tray; same data lives in Preferences -> Connection tab"
    - "Clicking Check for Updates immediately shows a Checking state and surfaces success/failure feedback"
    - "Gear menu and Recent Files side panel are mutually exclusive (only one open at a time)"
    - "Tray header does not show Signed in as row"
    - "When drives.count == 1, the All drives up to date aggregate row is hidden"
    - "Tray menu items are ordered: Add a new Drive, Preferences, dividers, Open web console, Check for Updates, dividers, Help, Sign Out, Quit"
    - "Tray footer shows a colored state icon next to the status text reflecting aggregate state"
  artifacts:
    - path: "DS3Drive/Views/Preferences/Views/ConnectionTab.swift"
      provides: "New Connection tab in Preferences exposing coordinator URL, S3 endpoint, tenant, console URL with copy buttons"
  key_links:
    - from: "DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift"
      to: "DS3DriveViewModel.aggregateStatus"
      via: "footer state icon binding"
      pattern: "aggregateStatus"
---

<objective>
Closes Gaps 4, 7, 8, 9, 10, 11, 12, 13 — eight small standalone UX/IA fixes bundled into one tray + preferences tidy-up plan.

These gaps are independent of the brand overhaul (Plan 05-11) and can ship before the Cubbit brand identity work. They make the tray cleaner, the preferences window roomier, and the menu bar more scannable.

Purpose: ship the entire small-fix backlog in one focused plan so the brand work can land against a tidy structure.
Output: tidy tray menu, roomier preferences with new Connection tab, readable project badges, Check for Updates feedback, mutual exclusion for popovers, footer state icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ux-polish/05-08-GAPS.md
@.planning/phases/05-ux-polish/05-03-SUMMARY.md
@.planning/phases/05-ux-polish/05-04-SUMMARY.md
@CLAUDE.md

Note: Gap 13 (footer state icon) reads `viewModel.aggregateStatus` which is introduced by Plan 05-09 Task 2. If 05-09 hasn't shipped yet, the footer icon task adds the computed property locally — both plans converge on the same name.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project badge readability + wizard preferences sizing + Check for Updates feedback (Gaps 4, 7, 9)</name>
  <files>
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Preferences/Views/GeneralTab.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift,
    DS3Drive/DS3DriveApp.swift,
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <read_first>
    DS3Drive/Views/Sync/Views/TreeNavigationView.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Preferences/Views/GeneralTab.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift,
    DS3Drive/DS3DriveApp.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    Part A — Gap 4 (project badges):
    1. In `TreeNavigationView.swift`, find the project badge view (search for `projectBadge` or the existing 2-letter `CU`/`DS` rendering). Replace with a `Circle().fill(colorForProject(project.id))` 24x24 with a single white initial centered, font `.system(size: 12, weight: .bold)`.
    2. Add to `DS3Colors.swift`:
       ```swift
       static let projectBadgePalette: [Color] = [
           Color(red: 0.95, green: 0.42, blue: 0.30), // coral
           Color(red: 0.30, green: 0.62, blue: 0.95), // azure
           Color(red: 0.50, green: 0.78, blue: 0.42), // green
           Color(red: 0.92, green: 0.66, blue: 0.20), // amber
           Color(red: 0.65, green: 0.42, blue: 0.85), // violet
           Color(red: 0.30, green: 0.78, blue: 0.78), // teal
           Color(red: 0.92, green: 0.42, blue: 0.62), // pink
           Color(red: 0.42, green: 0.50, blue: 0.65), // slate
       ]

       static func colorForProject(_ id: String) -> Color {
           let hash = abs(id.hashValue)
           return projectBadgePalette[hash % projectBadgePalette.count]
       }
       ```

    Part B — Gap 7 (Preferences window sizing):
    1. In `PreferencesView.swift`, update the root `.frame(...)` modifier to `.frame(minWidth: 720, minHeight: 560, idealWidth: 760, idealHeight: 600)`. Also set the same on the `Settings { }` scene in `DS3DriveApp.swift` if the size is set there.
    2. Verify by inspection that all rows of the General tab fit at the new ideal size with no scrolling.

    Part C — Gap 9 (Check for Updates feedback):
    1. In `UpdateChecker.swift`, add a `public enum UpdateCheckResult { case upToDate(version: String), updateAvailable(version: String), failed(Error) }` and an `@Published var lastResult: UpdateCheckResult?` (or `@Observable` equivalent), plus a `var lastCheckDate: Date?`. After every `checkForUpdates()` invocation, set `lastResult` and `lastCheckDate`.
    2. In `TrayMenuView.swift`, the menu item for "Check for Updates" must:
       - Show label `NSLocalizedString("updates.checking", value: "Checking…", comment: "")` while `updateChecker.isChecking == true`, otherwise the normal `Check for Updates` label.
       - On `lastResult` change, present a toast/alert via SwiftUI `.alert(...)` with three branches: `upToDate` -> "You're up to date — version X.Y.Z is the latest", `updateAvailable` -> reuse the existing update sheet, `failed(error)` -> "Couldn't check for updates: {error}" with a Retry button.
    3. In `GeneralTab.swift`, under the updates section, add a subtitle row showing `Last checked: {relative formatter on lastCheckDate}` using `RelativeDateTimeFormatter`. If `lastCheckDate` is nil, show "Never".
    4. Add to `Localizable.xcstrings`: `updates.checking` (Checking… / Verifica in corso…), `updates.upToDate` (You're up to date — version %@ is the latest / Sei aggiornato — la versione %@ è l'ultima), `updates.failed` (Couldn't check for updates: %@ / Impossibile verificare gli aggiornamenti: %@), `updates.lastChecked` (Last checked: %@ / Ultima verifica: %@), `updates.never` (Never / Mai), `updates.retry` (Retry / Riprova).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep `colorForProject\|projectBadgePalette` in DS3Drive/Views/Common/DesignSystem/DS3Colors.swift returns at least one match
    - grep `colorForProject\|Circle()` in DS3Drive/Views/Sync/Views/TreeNavigationView.swift returns at least one match
    - grep `minWidth: 720\|minHeight: 560` in DS3Drive/Views/Preferences/Views/PreferencesView.swift returns at least one match
    - grep `UpdateCheckResult\|lastResult\|lastCheckDate` in DS3Lib/Sources/DS3Lib/Update/UpdateChecker.swift returns at least one match
    - grep `updates.checking\|Checking` in DS3Drive/Views/Tray/Views/TrayMenuView.swift returns at least one match
    - grep `lastCheckDate\|RelativeDateTimeFormatter` in DS3Drive/Views/Preferences/Views/GeneralTab.swift returns at least one match
    - grep `updates.checking` in DS3Drive/Assets/Localizable.xcstrings returns at least one match
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>Project badges use circle+initial+hash color; Preferences window opens at 760x600; Check for Updates shows Checking state and surfaces result; Last checked timestamp visible in General tab.</done>
</task>

<task type="auto">
  <name>Task 2: Move Connection Info to Preferences + tidy tray header + reorder menu (Gaps 8, 11, 12)</name>
  <files>
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Preferences/Views/ConnectionTab.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift,
    DS3Drive/Assets/Localizable.xcstrings
  </files>
  <read_first>
    DS3Drive/Views/Preferences/Views/PreferencesView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/ConnectionInfoPanel.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    Part A — Gap 8 (move Connection Info to Preferences):
    1. Create `DS3Drive/Views/Preferences/Views/ConnectionTab.swift` with a `ConnectionTab: View` struct showing four read-only rows (Coordinator URL, S3 Endpoint, Tenant, Console URL) using the existing `ConnectionInfoRow` click-to-copy pattern reused from `ConnectionInfoPanel.swift`.
    2. In `PreferencesView.swift`, add a new `TabView` tab for `ConnectionTab` with title `NSLocalizedString("preferences.tab.connection", value: "Connection", comment: "")` and SF Symbol `network`.
    3. In `TrayMenuView.swift`, **remove** the Connection Info `TrayMenuItem` (and the side-panel trigger).
    4. Delete `ConnectionInfoPanel.swift` if no other view uses it. If it is still referenced from the side-panel system, keep it but unwire from the tray menu.

    Part B — Gap 11 (tidy tray header):
    1. In `TrayMenuView.swift`, **remove** the entire "Signed in as %@" header row.
    2. Conditionally render the "All drives up to date" aggregate row only when `viewModel.drives.count >= 2`. Use `if viewModel.drives.count >= 2 { aggregateStatusRow }`.

    Part C — Gap 12 (reorder tray menu):
    1. In `TrayMenuView.swift`, reorder the `TrayMenuItem` declarations to match this exact order with dividers as shown:
       ```
       [drive rows]
       Divider()
       Add a new Drive
       Preferences
       Divider()
       Open web console
       Check for Updates
       Divider()
       Help
       Sign Out
       Quit
       ```
    2. Add `Localizable.xcstrings` entry `preferences.tab.connection` (Connection / Connessione) if not already present.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - file DS3Drive/Views/Preferences/Views/ConnectionTab.swift exists
    - grep `ConnectionTab` in DS3Drive/Views/Preferences/Views/PreferencesView.swift returns at least one match
    - grep `Connection Info\|ConnectionInfoPanel` in DS3Drive/Views/Tray/Views/TrayMenuView.swift returns zero matches
    - grep `Signed in as` in DS3Drive/Views/Tray/Views/TrayMenuView.swift returns zero matches
    - grep `drives.count >= 2\|drives.count > 1` in DS3Drive/Views/Tray/Views/TrayMenuView.swift returns at least one match
    - grep `preferences.tab.connection` in DS3Drive/Assets/Localizable.xcstrings returns at least one match
    - In TrayMenuView.swift the Help menu item appears AFTER both Open web console and Check for Updates (verify by reading the file)
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>Connection Info lives in Preferences -> Connection tab; tray no longer shows Signed in as or single-drive aggregate row; menu items reordered by frequency with dividers.</done>
</task>

<task type="auto">
  <name>Task 3: Mutual exclusion for gear menu + side panel + tray footer state icon (Gaps 10, 13)</name>
  <files>
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift
  </files>
  <read_first>
    DS3Drive/Views/Tray/Views/TrayDriveRowView.swift,
    DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift,
    DS3Drive/Views/Tray/Views/TrayMenuView.swift,
    DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift,
    DS3Drive/Views/Common/DesignSystem/DS3Colors.swift,
    .planning/phases/05-ux-polish/05-08-GAPS.md
  </read_first>
  <action>
    Part A — Gap 10 (mutual exclusion):
    1. In `DS3DriveViewModel`, add:
       ```swift
       enum ActivePopover: Equatable {
           case none
           case sidePanel(driveID: UUID)
           case gearMenu(driveID: UUID)
       }
       @MainActor var activePopover: ActivePopover = .none
       ```
    2. In `TrayDriveRowView`, when the gear button is tapped, set `viewModel.activePopover = .gearMenu(driveID: drive.id)`. When the row hover/click opens the side panel, set `viewModel.activePopover = .sidePanel(driveID: drive.id)`. Use `.onChange(of: viewModel.activePopover)` to dismiss the other surface (e.g., setting `isMenuOpen = false` when the activePopover transitions away from `.gearMenu`).
    3. In `TrayMenuView.swift`, render the side panel only when `viewModel.activePopover` matches `.sidePanel(driveID:)` for the corresponding drive.

    Part B — Gap 13 (footer state icon):
    1. In `TrayMenuFooterView.swift`, add a leading `Image(systemName:)` whose name and color are derived from `viewModel.aggregateStatus` (introduced in Plan 05-09 Task 2 — if not yet present, define the same computed property here):
       ```swift
       private var statusIcon: (name: String, color: Color, animated: Bool) {
           switch viewModel.aggregateStatus {
           case .idle:     return ("checkmark.circle.fill",          DS3Colors.statusSynced,    false)
           case .syncing:  return ("arrow.triangle.2.circlepath",    DS3Colors.statusSyncing,   true)
           case .indexing: return ("magnifyingglass.circle",         DS3Colors.statusSyncing,   true)
           case .error:    return ("exclamationmark.triangle.fill",  DS3Colors.statusError,     false)
           case .paused:   return ("pause.circle.fill",              DS3Colors.textSecondary,   false)
           case .offline:  return ("wifi.slash",                      DS3Colors.textSecondary,   false)
           }
       }
       ```
       Render with `.symbolEffect(.pulse, options: .repeating, isActive: statusIcon.animated)` or `.rotationEffect` for syncing.
    2. Place the icon to the left of the existing footer status text in the same `HStack`.
    3. If `DS3Colors.statusSynced` / `statusSyncing` / `statusError` / `textSecondary` aren't already defined, add them as `static let` properties pointing to `DS3Colors` palette in `DS3Colors.swift`.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive && xcodebuild clean build analyze -project DS3Drive.xcodeproj -scheme "DS3 Drive" -destination "platform=macOS" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep `ActivePopover\|activePopover` in DS3Drive/Views/Tray/ViewModels/DS3DriveViewModel.swift returns at least one match
    - grep `activePopover` in DS3Drive/Views/Tray/Views/TrayDriveRowView.swift returns at least one match
    - grep `statusIcon\|aggregateStatus` in DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift returns at least one match
    - grep `checkmark.circle.fill\|magnifyingglass.circle` in DS3Drive/Views/Tray/Views/TrayMenuFooterView.swift returns at least one match
    - grep `statusSynced\|statusSyncing\|statusError` in DS3Drive/Views/Common/DesignSystem/DS3Colors.swift returns at least one match
    - xcodebuild clean build succeeds
  </acceptance_criteria>
  <done>activePopover state coordinates gear menu and side panel; tray footer renders state icon next to status text bound to aggregateStatus.</done>
</task>

</tasks>

<verification>
- All 8 gaps (4, 7, 8, 9, 10, 11, 12, 13) closed via grep-verifiable changes
- xcodebuild clean build analyze succeeds
- No regressions in tray menu rendering (visual check is part of Plan 05-12 review wave)
</verification>

<success_criteria>
- Three tasks complete with all acceptance criteria green
- xcodebuild clean build analyze passes
- New Connection tab visible in Preferences
- Tray menu order matches the spec exactly
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-polish/05-10-SUMMARY.md` documenting which gaps closed (4, 7, 8, 9, 10, 11, 12, 13), files touched, and any visual nuances handed off to Plan 05-12.
</output>
