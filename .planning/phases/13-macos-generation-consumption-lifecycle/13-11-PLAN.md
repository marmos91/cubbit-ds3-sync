---
phase: 13-macos-generation-consumption-lifecycle
plan: 11
type: execute
wave: 7
depends_on:
  - 02
  - 03
  - 04
  - 05
  - 06
  - 07
  - 08
  - 09
  - 10
files_modified:
  - DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift
autonomous: false
requirements:
  - THUMB-06
  - THUMB-11
  - THUMB-12
  - THUMB-13
  - THUMB-14
  - THUMB-15
  - THUMB-17
  - THUMB-18
  - THUMB-19
  - THUMB-20
  - THUMB-21
  - THUMB-23
tags:
  - thumbnails
  - integration
  - smoke
  - human-verify
must_haves:
  truths:
    - "End-to-end smoke test exercises: upload-hook → marked uploaded → fetchThumbnails cache hit → cascade delete"
    - "Backfill smoke test exercises: pending item → coordinator runBatch → marked uploaded → fetchThumbnails cache hit"
    - "Rename cascade smoke test exercises: copyThumbnail → deleteThumbnail → next fetch on new key hits"
    - "All Phase 13 production code paths run together without surprising interaction"
    - "Human verification covers Finder thumbnail visibility for new uploads + cloud-only files"
    - "Old generator-based fallback in S3Lib+Thumbnails.swift is fully removed if dead (audit decision recorded)"
  artifacts:
    - path: "DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift"
      provides: "2-3 round-trip flows wiring together all Phase 13 components"
      contains: "Phase13IntegrationSmoke"
  key_links:
    - from: "Integration smoke tests"
      to: "All Phase 13 component test surfaces"
      via: "Combined upload + fetch + cascade flows"
      pattern: "ThumbnailUploader.*getThumbnailBytes"
---

<objective>
Phase-13 closing plan. Three concerns:

1. **Integration smoke tests (D-34)** — 2-3 end-to-end flows that wire upload-hook → marked uploaded → cache-first fetch → delete cascade together, in one test, against in-memory MetadataStore + mock S3 client. Catches integration bugs that escape the per-component test suite (e.g., key-mismatch between uploader and fetcher).

2. **Old generator path cleanup audit** — Confirm `DS3DriveProvider/S3Lib+Thumbnails.swift` is either fully deleted or trimmed. Plan 13-06 made the audit decision; this plan re-verifies and ensures pbxproj reflects the choice. Confirm no dead generator code remains in `+Thumbnails.swift`.

3. **Human verification checkpoint** — One blocking checkpoint for the user to verify the user-visible behavior: drag an image into Finder → thumbnail appears within 5s; browse a folder of cloud-only images → thumbnails progressively populate; rename / delete an image → cascade tracks; sync badges still render.

Purpose: Final integration smoke + dead-code sweep + the only required user-facing verification step in Phase 13. Prepare Phase 14 (iOS) to pick up cleanly.
Output: One new test file + audit confirmation + one blocking checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-macos-generation-consumption-lifecycle/13-CONTEXT.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md
@CLAUDE.md
@DS3Lib/Sources/DS3Lib/Thumbnails/ThumbnailUploader.swift
@DS3Lib/Sources/DS3Lib/Thumbnails/ThumbnailBackfillCoordinator.swift
@DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift
@DS3Lib/Sources/DS3Lib/Metadata/MetadataStore+Queries.swift
@DS3DriveProvider/FileProviderExtension+Thumbnails.swift
@DS3DriveProvider/FileProviderExtension+ThumbnailCascade.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase13IntegrationSmokeTests — round-trip flows</name>
  <files>DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift</files>
  <read_first>
    - DS3Lib/Tests/DS3LibTests/ThumbnailUploaderTests.swift (Plan 13-02 — fixture loading + mock S3 setup)
    - DS3Lib/Tests/DS3LibTests/ThumbnailBackfillCoordinatorTests.swift (Plan 13-05 — coordinator setup)
    - DS3Lib/Tests/DS3LibTests/MetadataStoreThumbnailQueriesTests.swift (Phase 12 — in-memory MetadataStore)
    - DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift (Plan 13-03 — copy mock)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-VALIDATION.md (Wave 0 + per-task verification map)
  </read_first>
  <behavior>
    All tests run in-memory (no real S3). Mock client + in-memory MetadataStore + test fixtures via Bundle.module.

    - Test 1 — `testUploadFlowEndToEnd` — given a `.heic` fixture: instantiate `ThumbnailUploader`; call `generateAndUpload`; assert SyncedItem.thumbnailStatus == .uploaded; mock S3 has the thumbnail at `S3PathUtils.thumbnailKey(originalKey, drivePrefix)`. Then call `getThumbnailBytes` against the same key; assert non-nil bytes returned matching what was PUT.

    - Test 2 — `testBackfillFlowEndToEnd` — seed MetadataStore with 3 pending raster SyncedItems; mock S3 has the originals at `getObject(toFile:)`; instantiate `ThumbnailBackfillCoordinator`; call `runBatch(maxItems: 5)`; assert: BatchResult.succeeded == 3; all items now `.uploaded`; mock S3 has 3 `.thumbnails/<key>.jpg` entries.

    - Test 3 — `testCascadeRoundTrip` — upload thumbnail (test 1); call `copyThumbnail(fromKey: oldThumb, toKey: newThumb)`; call `deleteThumbnail(oldThumb)`. Assert: `getThumbnailBytes(newThumb)` returns bytes; `getThumbnailBytes(oldThumb)` returns nil. Set MetadataStore originalKey to the new key; verify everything is consistent.

    - Test 4 — `testStrikeRuleEndToEnd` — seed item with thumbnailFailCount=2; coordinator processes it (renderer returns nil for an unsupported fixture); assert: thumbnailFailCount=3, thumbnailStatus=.failed; subsequent runBatch does NOT pick this item up (`fetchPendingThumbnails` excludes .failed); upsert with new ETag resets count + status; subsequent runBatch picks it up again.

    Tests are intentionally redundant with per-component unit tests — that's the point. The bug class these catch is: "uploader writes to key X, fetcher reads from key Y, no per-component test notices because they don't share the contract".
  </behavior>
  <action>
    Create `DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift`. Wrap all macOS-render-dependent tests in `#if os(macOS)`.

    Use a richer mock S3 client that simulates an actual key-value bucket: `putObject` stores key→data; `getObject` retrieves; `copyObject` reads + writes; `deleteObject` removes. This is heavier than per-component mocks but justified for integration smoke.

    Each test should be ~30-50 lines of clear setup + execution + multi-step assertions. Keep them readable — these are the canonical "what does Phase 13 do" demonstrations.

    Commit: `test(13-11): Phase 13 integration smoke tests`.
  </action>
  <verify>
    <automated>swift test --package-path DS3Lib --filter Phase13IntegrationSmokeTests</automated>
  </verify>
  <acceptance_criteria>
    - File exists with >= 4 test methods
    - All tests green
    - `grep -c "ThumbnailUploader\|ThumbnailBackfillCoordinator\|copyThumbnail\|getThumbnailBytes" DS3Lib/Tests/DS3LibTests/Phase13IntegrationSmokeTests.swift` >= 8
    - Tests demonstrate at least 4 cross-component flows: upload→fetch, backfill→fetch, cascade rename, strike-rule terminal+reset
    - `swift test --package-path DS3Lib` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Integration smoke tests pass; cross-component contracts verified.</done>
</task>

<task type="auto">
  <name>Task 2: Old generator path cleanup audit + final dead-code sweep</name>
  <files>(none — read-only audit; no file edits in this plan)</files>
  <read_first>
    - DS3DriveProvider/FileProviderExtension+Thumbnails.swift (post-Plan-13-06 — verify renderJPEG calls are zero)
    - DS3DriveProvider/FileProviderExtension+Create.swift (post-Plan-13-07 — clean structure)
    - DS3DriveProvider/FileProviderExtension+Modify.swift (post-Plan-13-07 + 13-08 — clean structure)
    - DS3DriveProvider/FileProviderExtension+Delete.swift (post-Plan-13-08)
    - DS3DriveProvider/BreadthFirstIndexer.swift (post-Plan-13-09)
  </read_first>
  <action>
    **Step A — Audit `S3Lib+Thumbnails.swift` (READ-ONLY — Plan 13-06 owns the deletion/trim):**

    Plan 13-06 is the sole owner of the `S3Lib+Thumbnails.swift` fate decision. This task
    only verifies that 13-06's outcome is consistent. NO FILE EDITS HAPPEN IN PLAN 13-11.

    1. `grep -rn "S3Lib+Thumbnails\|s3Lib\.[a-zA-Z]*[Tt]humbnail" DS3DriveProvider/ DS3Lib/` to enumerate any remaining callers (audit only).
    2. Verify the outcome matches Plan 13-06's commit message:
       - If 13-06 deleted the file: confirm `ls DS3DriveProvider/S3Lib+Thumbnails.swift` returns "no such file" AND `grep` returns 0 hits.
       - If 13-06 trimmed the file: confirm any remaining content has known callers documented in 13-06's commit message AND those callers compile.
    3. If the audit reveals a mismatch (e.g., file still exists but no callers, or callers reference
       a deleted symbol): do NOT edit the file here — surface the discrepancy as a Phase-13
       blocker and re-route to a fix-up commit owned by Plan 13-06's scope.

    This task is a pure verification step — it produces no file diffs.

    **Step B — grep for dead code in modified Phase 13 files:**

    Confirm:
    - `grep "renderJPEG" DS3DriveProvider/FileProviderExtension+Thumbnails.swift` → 0 hits (consume path doesn't render — Plan 13-06)
    - `grep "downloadThumbnailImage" DS3DriveProvider/FileProviderExtension+Thumbnails.swift` → 0 hits (old fallback deleted)
    - `grep "ThumbnailGenerator" DS3DriveProvider/` → 0 hits (Phase 11 removed; Phase 13 doesn't reintroduce)
    - `grep -rn "TODO.*[Pp]hase 1[123]" DS3DriveProvider/ DS3Lib/Sources/DS3Lib/Thumbnails/` → audit any remaining TODOs; resolve or carry-forward to Phase 14.

    Any unexpected dead code: delete or document.

    **Step C — Build sanity:**
    - `xcodebuild build -scheme DS3Drive -destination 'platform=macOS'` clean
    - `xcodebuild build -scheme DS3DriveApp -destination 'generic/platform=iOS Simulator'` clean
    - `swift build --package-path DS3Lib` clean
    - `swift test --package-path DS3Lib` ALL green (run full suite, not filter)
    - `xcodebuild test -scheme DS3Drive -destination 'platform=macOS'` ALL green (run full suite — DS3DriveProviderTests + DS3LibTests).

    Commit (only if pbxproj or other tracked files needed touching for the audit log): `chore(13-11): final dead-code audit passes (S3Lib+Thumbnails owned by 13-06)`. If the audit produces no file changes (the expected case), no commit is required for Step A — proceed to Steps B/C.
  </action>
  <verify>
    <automated>swift test --package-path DS3Lib && xcodebuild build -scheme DS3Drive -destination 'platform=macOS' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderJPEG" DS3DriveProvider/FileProviderExtension+Thumbnails.swift` == 0
    - `grep -c "ThumbnailGenerator" DS3DriveProvider/` == 0 (no remaining references)
    - S3Lib+Thumbnails.swift state matches Plan 13-06's commit message (audit-only — no edits in this plan): either deleted (file does NOT exist), or trimmed with documented remaining callers from 13-06
    - All schemes build clean
    - Full DS3Lib test suite green
    - Full DS3DriveProvider test suite green
    - No new SwiftLint warnings on Phase 13 files
  </acceptance_criteria>
  <done>Codebase is clean; no dead generator code; all schemes build; full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Finder thumbnails appear correctly + sync badges + cascade behaviour</name>
  <files>(none — human verification only)</files>
  <action>
    Pause execution and present the verification protocol below. Wait for "approved" or detailed issue report from the user before proceeding.
  </action>
  <verify>
    <automated>echo "human-verify gate: see &lt;how-to-verify&gt; section; awaiting user approval"</automated>
  </verify>
  <done>User responded "approved" or all reported issues have been triaged and either resolved in follow-up commits or filed as Phase 14 deferrals.</done>
  <what-built>
    Phase 13 user-visible behavior: real thumbnails for image files in Finder; backfill of cloud-only images during BFS passes; delete/rename cascade; pause/resume halts/resumes backfill; sync badges still render correctly atop thumbnails. macOS-only — fully silent rollout (no UI surface for the feature).
  </what-built>
  <how-to-verify>
    Pre-checks:
    1. Build and install: `xcodebuild build -scheme DS3Drive -destination 'platform=macOS'`. Launch DS3 Drive from Xcode (Cmd+R). Confirm extension loads (no crash, no "The application cannot be used" error). If extension fails to load, follow CLAUDE.md "Extension Won't Load After Build Changes" recovery sequence.
    2. Confirm at least one drive is set up against a bucket where you have permission to write `.thumbnails/`.

    Verification steps:

    **Upload-time (THUMB-06):**
    1. Drag a JPG image (e.g., a phone photo) into a DS3 Drive folder in Finder.
    2. Within ~5 seconds, the file should display its actual thumbnail (not the default JPEG icon) in Finder icon-view.
    3. Drag a corrupt or unsupported image (or any non-image file like a .pdf). Confirm: file uploads, no error popup, default UTType icon shown (not a thumbnail). No spinner stuck.

    **Cloud-only (THUMB-11):**
    1. From another device or via S3 console, upload 5-10 images directly to the bucket.
    2. Wait for BFS pass to discover them (up to one bfs cycle interval).
    3. Browse the folder in Finder. Initially default icons. Within 1-2 BFS passes, thumbnails populate progressively.
    4. **Verify NO full-file download** — `log show --last 5m --predicate "subsystem == 'io.cubbit.DS3Drive.provider'"` should NOT show getObject calls for the FULL original key on consume path.

    **Sync badges (THUMB-12):**
    1. With a folder showing a mix of synced/syncing/error files: confirm the colored sync badges still render on top of the new thumbnails.
    2. Modify a file (rename or content edit) — badge should reflect syncing state, then transition to synced — thumbnail still visible underneath.

    **Cascade — delete (THUMB-17):**
    1. Delete an image from Finder.
    2. Within ~5 seconds, check S3 (via Cubbit console or CLI): the corresponding `.thumbnails/<key>.jpg` should be gone.

    **Cascade — rename / move (THUMB-18):**
    1. Rename an image: the thumbnail should still display correctly under the new name within ~5 seconds.
    2. Check S3: `.thumbnails/oldName.jpg.jpg` should be gone; `.thumbnails/newName.jpg.jpg` should exist with the original's `x-amz-meta-source-etag` preserved.

    **Pause/Resume (THUMB-21):**
    1. Pause the drive via tray menu.
    2. Drop new images. They upload (still — pause halts BFS, not user uploads). Verify thumbnail PUT does NOT happen for these (check log for absence of putObject calls to `.thumbnails/`).
    3. Resume the drive. Within 1-2 BFS passes, the missing thumbnails populate.

    **No SlowDown (THUMB-14):**
    1. Sync a large folder (~100+ images). Open multiple Finder windows simultaneously to trigger fanout.
    2. `log show --last 10m --predicate "subsystem == 'io.cubbit.DS3Drive.provider'"` — should NOT show `SlowDown` errors. Limiter caps consume at 4.

    **Silent rollout (THUMB-23, D-03):**
    1. On first launch with v3.1, confirm NO notifications, toasts, "What's New" cards, or banners about thumbnails. The feature just works invisibly.
    2. Confirm `~/Library/Group Containers/group.X889956QSM.io.cubbit.DS3Drive/thumbnail-settings-<driveId>.json` was written exactly once on first launch (file's mtime = first launch; no further writes on subsequent launches).

    **Error domain compliance (THUMB-13):**
    1. Disconnect network briefly while Finder is browsing a thumbnail-pending folder.
    2. `log show` — verify any errors funnel through `NSFileProviderErrorDomain` / `NSCocoaErrorDomain`. NEVER a custom domain. (Search `log show` output for "domain" entries — should be these two only.)

    Report:
    - "approved" if all verifications pass.
    - Otherwise describe each failing case (which step, observed behavior, expected behavior). Include relevant `log show` excerpts.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration smoke → in-memory mocks | No real S3; tests verify cross-component contracts only |
| Human verification → real Cubbit DS3 bucket | Real-world load + real Finder rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-54 | Repudiation | Per-component tests pass; integration silently broken (key mismatch, lifecycle race) | mitigate | Phase13IntegrationSmokeTests run all components together; catch the contract drift class. |
| T-13-55 | Tampering | Old generator code lingers and silently runs | mitigate | Task 2's grep audit confirms zero `renderJPEG` references in consume path; zero `ThumbnailGenerator` references anywhere. |
| T-13-56 | Information disclosure | Full-file download on consume path (THUMB-23 violation) | mitigate | Human verification step explicitly checks `log show` for absence of full-key getObject on consume. |
| T-13-57 | Denial of service | Real-world S3 SlowDown under load | mitigate | Human verification step exercises 100+ image fanout + multi-window concurrency; logs verify no SlowDown. |
| T-13-58 | Repudiation | Custom error domain leaks through (CLAUDE.md violation) | mitigate | Human verification + Plan 13-06's Test 13 unit test pin domain == NSFileProviderErrorDomain or NSCocoaErrorDomain. |
| T-13-59 | Tampering | First-launch rollout never fires (THUMB-23 silent enable) | mitigate | Human verification confirms thumbnail-settings-<driveId>.json appears in App Group container after first launch. |
</threat_model>

<verification>
- All Phase 13 unit tests across plans 13-01..13-10 green (full suite run).
- Integration smoke tests green.
- macOS + iOS schemes build clean.
- Human verification report = "approved".
- No dead generator code remains.
</verification>

<success_criteria>
1. 4+ integration smoke tests cover all major Phase 13 cross-component flows.
2. S3Lib+Thumbnails.swift fate finalized; no dead generator code anywhere.
3. macOS scheme builds clean with all tests green.
4. Human verification confirms: real thumbnails on upload, progressive backfill, cascades work, pause halts, sync badges intact, no SlowDown, no custom error domains, silent rollout.
5. Phase 13 ready for retrospective + Phase 14 to begin.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-generation-consumption-lifecycle/13-11-SUMMARY.md` and the phase-level VERIFICATION.md per gsd verify-work flow.
</output>
