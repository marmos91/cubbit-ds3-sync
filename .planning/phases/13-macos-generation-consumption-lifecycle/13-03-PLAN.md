---
phase: 13-macos-generation-consumption-lifecycle
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift
  - DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift
autonomous: true
requirements:
  - THUMB-18
tags:
  - thumbnails
  - s3
  - cascades
must_haves:
  truths:
    - "DS3S3ClientProtocol gains copyThumbnail(bucket:fromKey:toKey:) async throws via the existing protocol extension"
    - "copyThumbnail issues a single S3 CopyObject (no client-side GET+PUT)"
    - "Source metadata (x-amz-meta-source-etag, x-amz-meta-ds3drive-thumb-version) is preserved by passing metadata: nil → AWS default COPY directive"
    - "404 / NoSuchKey throws — caller (rename cascade) handles fallback to .pending"
  artifacts:
    - path: "DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift"
      provides: "copyThumbnail extension method on DS3S3ClientProtocol"
      contains: "func copyThumbnail"
    - path: "DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift"
      provides: "TDD coverage for happy path + 404 + 5xx"
      contains: "CopyThumbnailTests"
  key_links:
    - from: "DS3S3ClientProtocol.copyThumbnail"
      to: "DS3S3Client.copyObject (lines 326-342)"
      via: "metadata: nil (omits metadataDirective; AWS default = COPY)"
      pattern: "copyObject.*metadata: nil"
---

<objective>
Add `copyThumbnail(bucket:fromKey:toKey:)` to `DS3S3Client+Thumbnails.swift` as a `public extension DS3S3ClientProtocol` method (so all conforming clients + mocks pick it up automatically). Implementation delegates to the EXISTING `DS3S3Client.copyObject(...)` (verified at `DS3S3Client.swift:326-342`) with `metadata: nil`, which Soto serializes as omitting the `x-amz-metadata-directive` header — and per AWS S3 spec, the default behavior is COPY (preserve all source metadata, including our `x-amz-meta-source-etag` and `x-amz-meta-ds3drive-thumb-version`).

Purpose: Phase 13's rename/move cascade (Plan 13-08) needs server-side thumbnail copy to avoid downloading + re-uploading bytes. Server-side copy is single-API-call, transfers no bytes through the client, and preserves Phase-12-written staleness metadata.
Output: One method addition + a TDD test file covering happy path, 404 source missing, and 5xx error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-macos-generation-consumption-lifecycle/13-CONTEXT.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md
@DS3Lib/Sources/DS3Lib/DS3S3Client.swift
@DS3Lib/Sources/DS3Lib/DS3S3ClientProtocol.swift
@DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift
@DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift

<interfaces>
<!-- Existing copyObject (Phase 11/earlier) — verified at DS3S3Client.swift:326-342 -->
public func copyObject(
    bucket: String,
    sourceKey: String,
    destinationKey: String,
    metadata: [String: String]?  // nil → omit metadataDirective → AWS default COPY (preserves source metadata)
) async throws

<!-- existing put/get/delete (Phase 12) -->
public extension DS3S3ClientProtocol {
    func putThumbnail(bucket: String, key: String, data: Data, sourceETag: String) async throws
    func getThumbnailBytes(bucket: String, key: String) async throws -> Data?
    func deleteThumbnail(bucket: String, key: String) async throws
}

<!-- NEW (this plan) -->
public extension DS3S3ClientProtocol {
    /// Server-side copy of a thumbnail. Preserves source metadata via AWS default COPY directive.
    /// Throws on 404 (source not found) and on 5xx — caller (rename cascade) handles fallback.
    func copyThumbnail(bucket: String, fromKey: String, toKey: String) async throws
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CopyThumbnailTests covering CopyObject delegation, metadata-preservation, 404, 5xx</name>
  <files>DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift</files>
  <read_first>
    - DS3Lib/Tests/DS3LibTests/DS3S3ClientThumbnailsTests.swift (Phase 12 — mock pattern recording put/get/delete; extend the same mock to record copyObject calls)
    - DS3Lib/Sources/DS3Lib/DS3S3Client.swift lines 326-342 (existing copyObject — review percent-encoding; verifies Pitfall 6 already mitigated)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md "Pattern 6: copyThumbnail via existing copyObject"
  </read_first>
  <behavior>
    - Test 1 — `testCopyThumbnailIssuesSingleCopyObject` — call `copyThumbnail(bucket: "b", fromKey: "src/.thumbnails/a.jpg.jpg", toKey: "src/.thumbnails/b.jpg.jpg")`; assert mock recorded EXACTLY 1 `copyObject` call with the four args; no putObject / getObject calls happened.
    - Test 2 — `testCopyThumbnailPassesNilMetadataForDirectiveCopy` — assert the recorded call's `metadata` argument is `nil` (so AWS uses COPY directive — preserves x-amz-meta-source-etag and x-amz-meta-ds3drive-thumb-version).
    - Test 3 — `testCopyThumbnailRethrowsNoSuchKey` — mock copyObject throws a Soto error mappable as `NoSuchKey`; `copyThumbnail` rethrows the same error (caller maps to fallback). Use the existing `MockS3Error.notFound` precedent or simulate via `DS3S3ClientErrorMock` if present.
    - Test 4 — `testCopyThumbnailRethrowsServerError` — mock throws 5xx-equivalent; `copyThumbnail` rethrows.
    - Test 5 — `testCopyThumbnailKeyArgsArePassedThrough` — fromKey + toKey reach copyObject as `sourceKey` and `destinationKey` byte-for-byte (no munging — Pitfall 6's percent-encoding is `copyObject`'s responsibility, not ours).
  </behavior>
  <action>
    Create `DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift`. Use the same mock client pattern as Phase 12's `DS3S3ClientThumbnailsTests`; extend the mock to record `copyObject` invocations if it doesn't already (likely it doesn't — Phase 12 didn't need it).

    Skeleton:

    ```swift
    import XCTest
    @testable import DS3Lib

    final class CopyThumbnailTests: XCTestCase {
        // setUp: instantiate MockDS3S3Client (extend with copyObjectCalls: [(bucket, sourceKey, destKey, metadata)] = [])
        // tests 1..5 from <behavior>
    }
    ```

    Key decisions:
    - The mock should record `metadata` as `[String: String]?` so test 2 can assert `XCTAssertNil(captured.metadata)`.
    - For 404 / 5xx error simulation, use the same error injection approach Phase 12's get/delete tests used (read DS3S3ClientThumbnailsTests.swift first to copy the pattern).
    - Tests are RED initially — `copyThumbnail` doesn't exist yet.

    Commit: `test(13-03): add failing tests for copyThumbnail`.
  </action>
  <verify>
    <automated>swift test --package-path DS3Lib --filter CopyThumbnailTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift` exists
    - `grep -c "func test" DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift` >= 5
    - `grep "copyThumbnail" DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift` matches >= 5 times
    - `grep "XCTAssertNil(.*metadata)" DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift` matches (test 2)
    - Tests are RED (compile fails because `copyThumbnail` doesn't exist) — confirmed by build output
  </acceptance_criteria>
  <done>Failing test file committed, covering CopyObject delegation + metadata-nil + 404/5xx rethrow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement copyThumbnail on DS3S3ClientProtocol extension</name>
  <files>DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift</files>
  <read_first>
    - DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift (whole file — find existing public extension on DS3S3ClientProtocol; new method goes alongside putThumbnail/getThumbnailBytes/deleteThumbnail)
    - DS3Lib/Sources/DS3Lib/DS3S3Client.swift lines 326-342 (copyObject signature)
    - DS3Lib/Tests/DS3LibTests/CopyThumbnailTests.swift (the RED tests from Task 1)
  </read_first>
  <action>
    Append `copyThumbnail` to the existing `public extension DS3S3ClientProtocol` block in `DS3S3Client+Thumbnails.swift` (do NOT add to the protocol's required methods — extension method is sufficient because all conformers gain it for free, and it forwards to `copyObject` which IS a required protocol method).

    Verbatim implementation (from RESEARCH.md "Pattern 6: copyThumbnail via existing copyObject"):

    ```swift
    public extension DS3S3ClientProtocol {
        /// Server-side copy of a thumbnail. Preserves source metadata
        /// (`x-amz-meta-source-etag`, `x-amz-meta-ds3drive-thumb-version`) by
        /// passing `metadata: nil` — Soto omits the `x-amz-metadata-directive`
        /// header and AWS S3's default behavior is COPY.
        ///
        /// Throws on `NoSuchKey` and on transient/server errors. The Phase 13
        /// rename/move cascade (D-22) maps `NoSuchKey` to "mark new key .pending,
        /// let backfill regenerate", and maps 5xx to the same fallback path.
        ///
        /// Per Phase 13 D-22, D-23.
        func copyThumbnail(
            bucket: String,
            fromKey: String,
            toKey: String
        ) async throws {
            try await copyObject(
                bucket: bucket,
                sourceKey: fromKey,
                destinationKey: toKey,
                metadata: nil
            )
        }
    }
    ```

    Place this directly after the existing `deleteThumbnail` extension method in the same file (do NOT create a new file — keep all four thumbnail S3 methods in one place for cohesion).

    Run tests; should go GREEN. Commit: `feat(13-03): add copyThumbnail server-side copy for rename cascade`.
  </action>
  <verify>
    <automated>swift test --package-path DS3Lib --filter CopyThumbnailTests</automated>
  </verify>
  <acceptance_criteria>
    - `swift test --package-path DS3Lib --filter CopyThumbnailTests` exits 0
    - `swift build --package-path DS3Lib` exits 0 (no Swift 6 strict-concurrency warnings)
    - `grep -c "func copyThumbnail" DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift` == 1
    - `grep "metadata: nil" DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift` matches at least once (the copyThumbnail body)
    - File length under SwiftLint limit (< 200 lines).
  </acceptance_criteria>
  <done>
    `copyThumbnail` ships on the protocol extension, delegates to existing `copyObject`, preserves metadata via nil-metadata default, and is fully tested. Plan 13-08 (rename cascade) can now build on this.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → copyThumbnail | bucket / fromKey / toKey trusted; come from drive's syncAnchor + S3PathUtils-derived keys |
| copyThumbnail → S3 (CopyObject) | Authenticated; metadata copy is server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | Wrong fromKey/toKey leak data across drives | mitigate | Caller (Plan 13-08) computes both keys from `S3PathUtils.thumbnailKey(forOriginalKey:drivePrefix:)` with the SAME `drive.syncAnchor.prefix` — cross-drive copy requires two drives in scope, which the cascade callsite never has. |
| T-13-10 | Information disclosure | Soto's request URL leaks fromKey via x-amz-copy-source header | accept | Same exposure as every other CopyObject in the codebase; bucket-level encryption applies; no PII in S3 keys per project. |
| T-13-11 | Denial of service | Bursty rename storms issue 100s of CopyObjects | mitigate | Cascade lives in `Task.detached`; rename rate is bounded by user-actual-renames; no amplification. CopyObject is single-call (no multipart). |
| T-13-12 | Repudiation (silent failure) | Server-side copy succeeds but metadata gets reset | mitigate | `metadata: nil` test (Test 2) pins this contract — any future drift to non-nil metadata flips the test red. |
</threat_model>

<verification>
- `swift test --package-path DS3Lib --filter CopyThumbnailTests` exits 0.
- `swift build --package-path DS3Lib` exits 0.
- `grep -c "copyThumbnail" DS3Lib/Sources/DS3Lib/DS3S3Client+Thumbnails.swift` == 1.
</verification>

<success_criteria>
1. `copyThumbnail(bucket:fromKey:toKey:)` exists on `public extension DS3S3ClientProtocol` in `DS3S3Client+Thumbnails.swift`.
2. Implementation delegates to existing `copyObject` with `metadata: nil`.
3. TDD tests cover delegation, metadata-nil contract, NoSuchKey rethrow, 5xx rethrow, key passthrough — all green.
4. No new file created (added to existing thumbnails extension file).
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-generation-consumption-lifecycle/13-03-SUMMARY.md`.
</output>
