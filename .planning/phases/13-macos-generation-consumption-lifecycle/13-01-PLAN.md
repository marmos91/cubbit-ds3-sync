---
phase: 13-macos-generation-consumption-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
  - DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift
  - DS3Lib/Tests/DS3LibTests/S3PathUtilsRasterTests.swift
autonomous: true
requirements:
  - THUMB-06
  - THUMB-15
  - THUMB-19
  - THUMB-20
tags:
  - thumbnails
  - constants
  - utilities
must_haves:
  truths:
    - "REQUIREMENTS.md no longer lists THUMB-24 in Phase 13's mapped requirements"
    - "ROADMAP.md Phase 13 success criterion #5 drops tray-progress and skipped-counter clauses"
    - "DefaultSettings.Thumbnail.backfillBatchSize == 5"
    - "DefaultSettings.Thumbnail.maxOrphanDeletesPerPass == 50"
    - "DefaultSettings.Thumbnail.maxFailStrikes == 3"
    - "S3PathUtils.isRasterExtension returns true for jpg/jpeg/png/heic/heif/webp/gif/tiff and false for pdf/mp4/zip"
  artifacts:
    - path: "DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift"
      provides: "Three new Phase 13 constants under DefaultSettings.Thumbnail namespace"
      contains: "backfillBatchSize"
    - path: "DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift"
      provides: "isRasterExtension(_:) static helper for upload-hook + consume-path filtering"
      contains: "isRasterExtension"
    - path: "DS3Lib/Tests/DS3LibTests/S3PathUtilsRasterTests.swift"
      provides: "Coverage for raster allow-list semantics"
      contains: "testIsRasterExtension"
  key_links:
    - from: "S3PathUtils.isRasterExtension"
      to: "DefaultSettings.Thumbnail (allow-list)"
      via: "static helper reads canonical raster set"
      pattern: "isRasterExtension"
---

<objective>
Establish Phase 13 foundations: ratify scope changes (THUMB-24 drop), add the three new `DefaultSettings.Thumbnail` constants Phase 13 wires together (`backfillBatchSize=5`, `maxOrphanDeletesPerPass=50`, `maxFailStrikes=3`), and ship `S3PathUtils.isRasterExtension(_:)` — the suffix allow-list helper used by both the upload-hook pre-filter (D-08) and the consume-path pre-filter.

Purpose: Lock the documented scope before any feature lands, so plans 13-02 .. 13-11 can reference these constants and helpers as already present.
Output: Updated REQUIREMENTS.md / ROADMAP.md, three named constants on `DefaultSettings.Thumbnail`, one new static helper on `S3PathUtils`, and a TDD test file that proves the allow-list semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-CONTEXT.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-VALIDATION.md
@.planning/phases/12-renderer-storage-schema/12-VERIFICATION.md
@CLAUDE.md
@DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift
@DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift

<interfaces>
<!-- From DefaultSettings.swift Phase 12 namespace (lines ~220-247) -->
public enum Thumbnail {
    public static let sourceETagMetadataKey: String = "source-etag"
    public static let formatVersionMetadataKey: String = "ds3drive-thumb-version"
    public static let formatVersion: String
    public static let maxSinglePartBytes: Int
    // Phase 13 ADDS:
    public static let backfillBatchSize: Int = 5
    public static let maxOrphanDeletesPerPass: Int = 50
    public static let maxFailStrikes: Int = 3
}

<!-- S3PathUtils existing helpers (Phase 11) -->
public enum S3PathUtils {
    public static func thumbnailKey(forOriginalKey: String, drivePrefix: String) -> String
    public static func originalKey(fromThumbnailKey: String, drivePrefix: String) -> String?
    // Phase 13 ADDS:
    public static func isRasterExtension(_ pathExtension: String) -> Bool
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply scope changes to REQUIREMENTS.md and ROADMAP.md (THUMB-24 drop)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file — see THUMB-24 row in v3.1 list, traceability table, coverage summary)
    - .planning/ROADMAP.md (Phase 13 section — success criterion #5, Requirements line)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-CONTEXT.md `<scope_changes>` section (verbatim wording for criterion #5 rewrite)
  </read_first>
  <action>
    Apply two edits driven by user decision to drop THUMB-24 (silent macOS rollout):

    **REQUIREMENTS.md edits:**
    1. The THUMB-24 entry in the v3.1 Requirements list is already commented out via the `> **THUMB-24 dropped** ...` paragraph after line 50 — leave that explanatory paragraph in place.
    2. Verify the Traceability table no longer maps THUMB-24 to Phase 13 (table currently shows `~~THUMB-24~~ | _Dropped 2026-04-25 (Phase 13 discuss)_` — leave as historical record).
    3. Verify the Coverage summary footer reads `Phase 13: 12 requirements (THUMB-06, 11, 12, 13, 14, 15, 17, 18, 19, 20, 21, 23) — THUMB-24 dropped 2026-04-25` and `Total: 25/25 ✓ (was 26/26 before THUMB-24 was dropped)`.
    4. If any of (1)-(3) is missing, edit to match. The current REQUIREMENTS.md (read above) appears already correct — confirm with grep, no changes needed if so.

    **ROADMAP.md edits (Phase 13 section, lines ~189-200):**
    1. Verify the `**Requirements**:` line reads exactly `THUMB-06, THUMB-11, THUMB-12, THUMB-13, THUMB-14, THUMB-15, THUMB-17, THUMB-18, THUMB-19, THUMB-20, THUMB-21, THUMB-23 (THUMB-24 dropped 2026-04-25 — fully silent macOS rollout)`. The current ROADMAP.md (read above) already shows this — no edit needed if so.
    2. Verify Success Criterion #5 reads exactly: *"Concurrent thumbnail fetches from Finder never trigger S3 `SlowDown` thanks to the bounded `ThumbnailFetchLimiter`, and every failure path funnels through `NSFileProviderErrorDomain` / `NSCocoaErrorDomain` — never a custom domain. Permanently unprocessable items terminate after 3 strikes (silently — no progress UI surfaces them on macOS)."* — current ROADMAP.md already shows this verbatim. No edit needed.
    3. If verification (1) or (2) shows drift from the canonical wording, apply the edit verbatim from `13-CONTEXT.md` `<scope_changes>` section.

    Outcome: this task is a verify-and-confirm pass. Commit only if changes were necessary; otherwise skip the commit and proceed to Task 2.

    Per D-01..D-05 (silent rollout). NEVER add a tray-progress task or `MetadataStore.countPendingThumbnails` anywhere downstream.
  </action>
  <verify>
    <automated>grep -c "THUMB-24" .planning/ROADMAP.md && grep -c "12 requirements (THUMB-06" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "THUMB-24" .planning/ROADMAP.md` returns the single dropped-marker line (`(THUMB-24 dropped 2026-04-25 — fully silent macOS rollout)`)
    - `grep -c "Permanently unprocessable items terminate after 3 strikes" .planning/ROADMAP.md` >= 1
    - `grep "12 requirements" .planning/REQUIREMENTS.md` matches `Phase 13: 12 requirements (THUMB-06, 11, 12, 13, 14, 15, 17, 18, 19, 20, 21, 23)`
    - No reference to "tray progress" or "Thumbnails: N / M" remains in Phase 13 success criteria of ROADMAP.md
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md and ROADMAP.md reflect the THUMB-24 drop verbatim; no stale tray-progress wording remains; downstream plans can rely on the 12-requirement Phase 13 scope.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 13 constants to DefaultSettings.Thumbnail and ship S3PathUtils.isRasterExtension with tests</name>
  <files>DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift, DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift, DS3Lib/Tests/DS3LibTests/S3PathUtilsRasterTests.swift</files>
  <read_first>
    - DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift (whole file — find the existing `public enum Thumbnail` namespace populated by Phase 12 around lines 220-247)
    - DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift (whole file — find existing `thumbnailKey` / `originalKey` helpers from Phase 11)
    - DS3Lib/Sources/DS3Lib/Thumbnails/ThumbnailRenderer.swift (read lines 100-130 for the canonical raster UTI allow-list — implementation must agree with renderer's allow-list)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md `<user_constraints>` D-08, D-18, D-26, D-29
  </read_first>
  <behavior>
    - Test 1: `DefaultSettings.Thumbnail.backfillBatchSize == 5`
    - Test 2: `DefaultSettings.Thumbnail.maxOrphanDeletesPerPass == 50`
    - Test 3: `DefaultSettings.Thumbnail.maxFailStrikes == 3`
    - Test 4: `S3PathUtils.isRasterExtension("jpg") == true`, plus jpeg, png, heic, heif, webp, gif, tiff
    - Test 5: `S3PathUtils.isRasterExtension("pdf") == false`, plus mp4, zip, txt, "" (empty), and uppercase variants ("JPG", "Png") return true (case-insensitive)
    - Test 6: `S3PathUtils.isRasterExtension(".jpg")` (with dot) returns true — helper strips a leading dot if present
  </behavior>
  <action>
    **Step A — Add three constants to `DefaultSettings.Thumbnail`** (extend the existing namespace; do NOT modify Phase 12 constants `sourceETagMetadataKey`, `formatVersionMetadataKey`, `formatVersion`, `maxSinglePartBytes`):

    Inside the `public enum Thumbnail { ... }` block in `DefaultSettings.swift`, append:

    ```swift
    /// Number of pending thumbnails the BFS-tail backfill coordinator processes per pass.
    /// Per Phase 13 D-18; Phase 14+ may tune this adaptively. Sequential, thermal-gated.
    public static let backfillBatchSize: Int = 5

    /// Maximum number of orphan thumbnail keys deleted per BFS pass tail.
    /// Per Phase 13 D-26 — caps cleanup work per pass; natural BFS cadence handles the rest.
    public static let maxOrphanDeletesPerPass: Int = 50

    /// Maximum render+PUT failures before a SyncedItem transitions thumbnailStatus to .failed.
    /// Per Phase 13 D-29 — terminates retry loop on permanently unprocessable items. ETag change resets count (D-31).
    public static let maxFailStrikes: Int = 3
    ```

    **Step B — Ship `S3PathUtils.isRasterExtension(_:)`** (per D-08):

    Add to `DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift` as a `public static` function on the existing enum:

    ```swift
    /// Suffix-based allow-list for raster image formats Phase 13 will generate thumbnails for.
    /// MUST agree with `ThumbnailRenderer`'s UTI allow-list — both gate the same set.
    /// Case-insensitive; tolerates a leading dot. Returns false for empty / unknown extensions.
    /// Per Phase 13 D-08.
    public static func isRasterExtension(_ pathExtension: String) -> Bool {
        let trimmed = pathExtension.hasPrefix(".") ? String(pathExtension.dropFirst()) : pathExtension
        guard !trimmed.isEmpty else { return false }
        let allowList: Set<String> = ["jpg", "jpeg", "png", "heic", "heif", "webp", "gif", "tiff"]
        return allowList.contains(trimmed.lowercased())
    }
    ```

    **Step C — Write `DS3Lib/Tests/DS3LibTests/S3PathUtilsRasterTests.swift`** (TDD; tests already enumerated in `<behavior>` above). Use `XCTest`, single test class `S3PathUtilsRasterTests`, one test method per behavior bullet. The constants tests can live in a new `DefaultSettingsThumbnailConstantsTests` class in the SAME file (atomic — both surfaces tested together) or split — your discretion, just keep them in this file.

    **Constraints:**
    - Do NOT touch Phase 12 constants in DefaultSettings.Thumbnail; this is purely additive.
    - Keep the allow-list literal in `S3PathUtils.isRasterExtension` (do NOT introduce a `DefaultSettings.Thumbnail.rasterExtensions` set in this plan — adding a parallel allow-list creates two sources of truth; renderer's UTI list and `isRasterExtension` are independently authoritative for their respective concerns per RESEARCH.md `<don't_hand_roll>`).
    - Allow-list lowercased exactly as listed; do NOT include `bmp`, `raw`, `dng`, etc. (those are deferred per REQUIREMENTS.md "Future Requirements").
  </action>
  <verify>
    <automated>swift test --package-path DS3Lib --filter S3PathUtilsRasterTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "backfillBatchSize: Int = 5" DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift` == 1
    - `grep -c "maxOrphanDeletesPerPass: Int = 50" DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift` == 1
    - `grep -c "maxFailStrikes: Int = 3" DS3Lib/Sources/DS3Lib/Constants/DefaultSettings.swift` == 1
    - `grep -c "func isRasterExtension" DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift` == 1
    - `swift test --package-path DS3Lib --filter S3PathUtilsRasterTests` exits 0
    - `swift build --package-path DS3Lib` exits 0
    - `grep -c "S3PathUtils.isRasterExtension" DS3Lib/Tests/DS3LibTests/S3PathUtilsRasterTests.swift` >= 6 (one per behavior assertion)
  </acceptance_criteria>
  <done>
    Three Phase 13 constants live on `DefaultSettings.Thumbnail`; `S3PathUtils.isRasterExtension` ships with case-insensitive + leading-dot-tolerant semantics; full TDD coverage; DS3Lib still builds + tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc edits → public spec | REQUIREMENTS.md / ROADMAP.md changes flow into all downstream plans + executor reads |
| Constant introduction → downstream plans | The three new constants are consumed by plans 13-04, 13-05, 13-09 — wrong values cause silent behavior drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | DefaultSettings.Thumbnail constants | mitigate | Tests pin literal values (5, 50, 3); CI catches drift |
| T-13-02 | Information disclosure | None — no PII / network / file IO in this plan | accept | Plan is doc + constants + pure helper |
| T-13-03 | Denial of service | maxFailStrikes off-by-one (Pitfall 10) — too low could drop legit retries; too high keeps retrying broken files | mitigate | Constant fixed at 3 per D-29; downstream plan 13-04 tests boundary (count==3 → failed, count==2 → pending) |
</threat_model>

<verification>
- `swift build --package-path DS3Lib` succeeds.
- `swift test --package-path DS3Lib --filter S3PathUtilsRasterTests` exits 0.
- `grep "12 requirements (THUMB-06" .planning/REQUIREMENTS.md` matches.
- `grep "Permanently unprocessable items terminate" .planning/ROADMAP.md` matches.
- No stale `THUMB-24` reference outside of the explicit `_Dropped_` historical marker in REQUIREMENTS.md and the parenthetical in ROADMAP.md.
</verification>

<success_criteria>
1. THUMB-24 scope drop ratified in REQUIREMENTS.md and ROADMAP.md (or already correct, verified).
2. `DefaultSettings.Thumbnail.backfillBatchSize`, `.maxOrphanDeletesPerPass`, `.maxFailStrikes` exist with values 5, 50, 3.
3. `S3PathUtils.isRasterExtension(_:)` exists with case-insensitive + leading-dot tolerance.
4. New TDD test file `S3PathUtilsRasterTests.swift` covers allow-list, reject-list, case insensitivity, leading-dot, and constant values — all passing.
5. DS3Lib builds clean under Swift 6 strict concurrency on macOS.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-generation-consumption-lifecycle/13-01-SUMMARY.md` per template.
</output>
