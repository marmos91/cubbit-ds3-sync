---
phase: 13-macos-generation-consumption-lifecycle
plan: 07
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 04
files_modified:
  - DS3DriveProvider/FileProviderExtension+Create.swift
  - DS3DriveProvider/FileProviderExtension+Modify.swift
  - DS3DriveProviderTests/UploadHookTests.swift
autonomous: true
requirements:
  - THUMB-06
tags:
  - thumbnails
  - upload
  - file-provider
must_haves:
  truths:
    - "createItem post-PUT spawns Task.detached invoking ThumbnailUploader.generateAndUpload"
    - "modifyItem (content-change branch) post-PUT spawns the same Task.detached"
    - "Task.detached captures sendable values only — NOT self"
    - "Pre-filter via S3PathUtils.isRasterExtension before kicking the Task; non-raster marks .notApplicable"
    - "Upload completionHandler returns success BEFORE the Task starts work (decoupled lifecycle, THUMB-06)"
    - "Errors are logged + swallowed via try? — never propagated to user-visible upload contract"
  artifacts:
    - path: "DS3DriveProvider/FileProviderExtension+Create.swift"
      provides: "Post-PUT thumbnail upload hook in createItem"
      contains: "ThumbnailUploader"
    - path: "DS3DriveProvider/FileProviderExtension+Modify.swift"
      provides: "Post-PUT thumbnail upload hook in modifyItem (content-change branch)"
      contains: "ThumbnailUploader"
    - path: "DS3DriveProviderTests/UploadHookTests.swift"
      provides: "Hook fires on raster create + modify; skips on non-raster; lifecycle decoupled"
      contains: "test"
  key_links:
    - from: "createItem post-PUT"
      to: "Task.detached → ThumbnailUploader.generateAndUpload"
      via: "fire-and-forget, isolated"
      pattern: "Task.detached.*ThumbnailUploader"
    - from: "modifyItem content-change"
      to: "Task.detached → ThumbnailUploader.generateAndUpload"
      via: "same hook (re-render unconditionally per D-10)"
      pattern: "Task.detached.*ThumbnailUploader"
---

<objective>
Wire upload-time thumbnail generation per THUMB-06: post-PUT fire-and-forget Task.detached invokes `ThumbnailUploader.generateAndUpload` from `createItem` and from `modifyItem`'s content-change branch. Per D-06, the upload completion handler returns success BEFORE the thumbnail Task starts, fully decoupling the upload contract from thumbnail lifecycle. Per D-08, raster pre-filter via `S3PathUtils.isRasterExtension` happens before the Task spawn; non-raster files silently mark `.notApplicable` and avoid render attempts. Per Pitfall 1, the detached Task must NEVER capture `self` (FileProviderExtension is non-Sendable under Swift 6 strict concurrency).

Purpose: First user-visible artifact of Phase 13. Plan 13-08 owns the rename/move/delete cascades; this plan owns create + content-modify.
Output: Two file edits + one new test file. Extension target builds clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-macos-generation-consumption-lifecycle/13-CONTEXT.md
@.planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md
@CLAUDE.md
@DS3DriveProvider/FileProviderExtension+Create.swift
@DS3DriveProvider/FileProviderExtension+Modify.swift
@DS3DriveProvider/FileProviderExtension.swift
@DS3DriveProvider/S3Lib.swift
@DS3Lib/Sources/DS3Lib/Thumbnails/ThumbnailUploader.swift
@DS3Lib/Sources/DS3Lib/Utils/S3PathUtils.swift

<interfaces>
<!-- Plan 13-02 + 13-04 output -->
public struct ThumbnailUploader: Sendable {
    public init(s3Client: any DS3S3ClientProtocol, metadataStore: MetadataStore)
    #if os(macOS)
    public func generateAndUpload(localURL: URL, drive: DS3Drive, sourceETag: String, originalKey: String) async throws
    #endif
}

<!-- Plan 13-01 output -->
S3PathUtils.isRasterExtension(_ pathExtension: String) -> Bool

<!-- Existing helpers — verify by grep before assuming -->
ETagUtils.normalize(_ etag: String?) -> String?
DS3S3Client (or DS3S3ClientProtocol) is Sendable

<!-- Existing FileProviderExtension state — must already hold s3Lib, drive, metadataStore, logger -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write UploadHookTests RED — hook fires on raster create + content-modify, skips on non-raster, decoupled from completion handler</name>
  <files>DS3DriveProviderTests/UploadHookTests.swift</files>
  <read_first>
    - DS3DriveProvider/FileProviderExtension+Create.swift (find createItem; note the structure of how completionHandler returns around line ~231-245)
    - DS3DriveProvider/FileProviderExtension+Modify.swift (find modifyItem; identify the BRANCH for content-change vs metadata-only vs rename/move — only the content-change branch gets this plan's hook; rename/move belongs to Plan 13-08)
    - DS3DriveProviderTests/FetchThumbnailsTests.swift (Plan 13-06 test infrastructure — mock S3 client, in-memory MetadataStore — extend / mirror)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md "Pattern 1: Fire-and-forget Task.detached" (verbatim hook code), "Pitfall 1: Task.detached capturing self"
  </read_first>
  <behavior>
    - Test 1 — `testCreateItemRasterFileTriggersThumbnailUploaderTask` — drop a `.jpg` file via createItem; mock S3 client records both putObject (original) and putThumbnail (thumbnail). Assert: putThumbnail called exactly once with key matching `S3PathUtils.thumbnailKey(...)` AND with the same ETag the original PUT returned. Use `XCTNSPredicateExpectation` to wait for the detached Task.
    - Test 2 — `testCreateItemNonRasterFileDoesNotTriggerUploader` — `.pdf` create; mock records only putObject. NO putThumbnail call. SyncedItem ends with `thumbnailStatus == .notApplicable`.
    - Test 3 — `testCreateItemCompletionHandlerCalledBeforeThumbnailWork` — mock putThumbnail sleeps 500ms; assert createItem's completionHandler is invoked WITHIN ~50ms (proves lifecycle decoupling).
    - Test 4 — `testCreateItemCompletionHandlerSucceedsEvenWhenThumbnailFails` — mock putThumbnail throws; createItem result is success — no error propagated.
    - Test 5 — `testModifyItemContentChangeTriggersThumbnailUploaderTask` — modifyItem with content change on existing raster file; mock records putThumbnail call with the NEW ETag (D-10 unconditional re-render).
    - Test 6 — `testModifyItemMetadataOnlyChangeDoesNotTriggerUploader` — modifyItem with metadata-only change (no content delta); assert NO putThumbnail call.
    - Test 7 — `testTaskDetachedDoesNotCaptureSelf` — compile-time guard: documentation test that asserts the build itself succeeds under Swift 6 strict concurrency. Add `XCTSkip("compile-time guard — assertion is the build succeeding under Swift 6")` and document the intent in the test method comment.
  </behavior>
  <action>
    Create `DS3DriveProviderTests/UploadHookTests.swift`. Use the same mock-S3 + in-memory-MetadataStore pattern from Plan 13-06's tests (read first).

    The test setup constructs a FileProviderExtension test harness — use whatever Phase 12 / earlier tests use to drive `createItem` / `modifyItem` directly. If no harness exists, construct a minimal one that injects the mock dependencies into the extension subclass.

    For "wait for detached Task" pattern in tests 1, 2, 5: use `expectation(for: NSPredicate(...))` polling the mock's recorded calls with a 2-second timeout.

    Tests RED initially since `ThumbnailUploader` is not yet wired into +Create / +Modify.

    Commit: `test(13-07): add failing tests for upload-hook integration in createItem and modifyItem`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DS3Drive -destination 'platform=macOS' -only-testing:DS3DriveProviderTests/UploadHookTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `DS3DriveProviderTests/UploadHookTests.swift` exists with >= 6 functional test methods (test 7 may be a skip-with-doc)
    - `grep -c "putThumbnail" DS3DriveProviderTests/UploadHookTests.swift` >= 4 (assertions across tests)
    - `grep -c "createItem\|modifyItem" DS3DriveProviderTests/UploadHookTests.swift` >= 5
    - Tests RED — hook calls don't yet exist; mock records zero putThumbnail calls
  </acceptance_criteria>
  <done>Failing UploadHookTests committed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire upload hook in createItem + modifyItem (content-change branch); GREEN the tests</name>
  <files>DS3DriveProvider/FileProviderExtension+Create.swift, DS3DriveProvider/FileProviderExtension+Modify.swift</files>
  <read_first>
    - DS3DriveProvider/FileProviderExtension+Create.swift (whole file — locate the `s3Lib.putS3Item` callsite returning `createETag` around line ~231)
    - DS3DriveProvider/FileProviderExtension+Modify.swift (whole file — locate `modifyItem`; identify content-change branch vs. metadata-only branch vs. rename/move branch. THIS PLAN ATTACHES THE HOOK ONLY TO THE CONTENT-CHANGE BRANCH.)
    - DS3DriveProvider/FileProviderExtension.swift (state: confirm `metadataStore`, `s3Lib` (or `s3Client`), `drive`, `logger` accessibility; if `metadataStore` is optional, handle nil by skipping the hook)
    - DS3DriveProvider/S3Lib.swift (verify whether `s3Lib` exposes the underlying `DS3S3Client` via a `dsClient` property or directly conforms to `DS3S3ClientProtocol`)
    - DS3Lib/Sources/DS3Lib/Thumbnails/ThumbnailUploader.swift (signature recap: `init(s3Client:metadataStore:)`, `generateAndUpload(localURL:drive:sourceETag:originalKey:)` is `#if os(macOS)`)
    - .planning/phases/13-macos-generation-consumption-lifecycle/13-RESEARCH.md "Pattern 1" (verbatim hook code with Sendable capture pattern), Pitfall 1
  </read_first>
  <action>
    **Step A — `+Create.swift` hook:**

    Locate the line `let createETag = try await self.withAPIKeyRecovery { try await s3Lib.putS3Item(...) }` (approximately line 231). AFTER `metadataStore.upsertItem(...)` and BEFORE `completionHandler(...)`, insert:

    ```swift
    #if os(macOS)
    if S3PathUtils.isRasterExtension((key as NSString).pathExtension) {
        let normalizedETag = ETagUtils.normalize(createETag) ?? createETag
        let driveCopy = drive
        let s3ClientCopy: any DS3S3ClientProtocol = s3Lib.dsClient // VERIFY field name in S3Lib.swift
        let metadataStoreCopy = metadataStore
        let originalKey = key
        let localURL = url
        let logger = self.logger
        Task.detached(priority: .background) {
            do {
                let uploader = ThumbnailUploader(s3Client: s3ClientCopy, metadataStore: metadataStoreCopy)
                try await uploader.generateAndUpload(
                    localURL: localURL,
                    drive: driveCopy,
                    sourceETag: normalizedETag,
                    originalKey: originalKey
                )
            } catch {
                logger.error("Thumbnail upload-hook failed for \(originalKey, privacy: .public): \(error.localizedDescription, privacy: .public)")
            }
        }
    } else {
        // Non-raster: silently mark .notApplicable so enumeration stops re-checking.
        let metadataStoreCopy = metadataStore
        let driveCopy = drive
        let originalKey = key
        Task.detached {
            try? await metadataStoreCopy.setThumbnailStatus(s3Key: originalKey, driveId: driveCopy.id, status: .notApplicable)
        }
    }
    #endif
    ```

    NEVER capture `self` directly (Pitfall 1). All captures are sendable values via locals.

    **`s3Lib.dsClient` verification:** Read `DS3DriveProvider/S3Lib.swift` first. If `s3Lib` is itself `Sendable` and conforms to `DS3S3ClientProtocol`, pass `s3Lib` directly. If it wraps a `DS3S3Client` via a property, use that property. If neither, request guidance via a checkpoint task — but the canonical Phase 12 pattern means one of these works.

    **Step B — `+Modify.swift` hook (content-change branch only):**

    Locate the content-change branch (likely `if changedFields.contains(.contents)` or `if let url = newContents`). After the PUT completes and the new ETag is in hand, apply the SAME hook code as Step A. To avoid duplication, EXTRACT a private helper:

    ```swift
    // In a new file DS3DriveProvider/FileProviderExtension+ThumbnailHook.swift (or in +Thumbnails.swift):
    extension FileProviderExtension {
        #if os(macOS)
        func enqueueThumbnailUpload(originalKey: String, localURL: URL, sourceETag: String) {
            // raster pre-filter + Task.detached body from Step A
        }
        #endif
    }
    ```

    Then `+Create.swift` and `+Modify.swift` each call `enqueueThumbnailUpload(originalKey:, localURL:, sourceETag:)` — one line each.

    Do NOT attach the hook to the metadata-only branch or the rename/move branch (Plan 13-08 owns rename/move).

    **Step C — Verify Sendable cleanliness:**

    Run `xcodebuild build -scheme DS3Drive -destination 'platform=macOS'`. Watch for any "Capture of 'self' with non-sendable type" or "Sendable violation" errors. Fix by capturing into locals before Task.detached.

    **Step D — Run UploadHookTests until GREEN.** Commit: `feat(13-07): wire upload-hook in createItem and modifyItem (content-change branch)`.

    File-length check: if either +Create.swift or +Modify.swift approaches SwiftLint limit, the extracted helper from Step B is mandatory.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DS3Drive -destination 'platform=macOS' -only-testing:DS3DriveProviderTests/UploadHookTests</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme DS3Drive -destination 'platform=macOS'` exits 0 with NO Swift 6 strict-concurrency warnings on the modified files
    - `xcodebuild test -only-testing:DS3DriveProviderTests/UploadHookTests` exits 0 (all 6+ functional tests green)
    - `grep -c "ThumbnailUploader" DS3DriveProvider/FileProviderExtension+Create.swift` >= 1 (or via the extracted helper file)
    - `grep -c "ThumbnailUploader\|enqueueThumbnailUpload" DS3DriveProvider/FileProviderExtension+Modify.swift` >= 1
    - `grep -c "Task.detached" DS3DriveProvider/FileProviderExtension+Create.swift DS3DriveProvider/FileProviderExtension+Modify.swift DS3DriveProvider/FileProviderExtension+ThumbnailHook.swift 2>/dev/null` >= 1
    - `grep -c "S3PathUtils.isRasterExtension" DS3DriveProvider/FileProviderExtension+Create.swift DS3DriveProvider/FileProviderExtension+ThumbnailHook.swift 2>/dev/null` >= 1
    - `grep -E "self\." DS3DriveProvider/FileProviderExtension+Create.swift | grep -E "Task.detached" -A 5` does NOT show self captures inside Task.detached body (manual review on top of grep)
    - macOS extension scheme builds clean.
  </acceptance_criteria>
  <done>Upload hook wired in createItem + modifyItem content-change branch; tests green; no Sendable violations; metadata-only and rename/move branches untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Provider createItem/modifyItem → Task.detached | Detached Task is a fresh isolation domain; captures must be Sendable |
| Task.detached → ThumbnailUploader → S3 + MetadataStore | Same as Plan 13-02 |
| Failure path → user-visible upload contract | MUST be opaque — try? swallow + log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-29 | Repudiation | Thumbnail failure visible to user as upload failure (THUMB-06 violation) | mitigate | Test 4 explicitly proves: completionHandler succeeds even when putThumbnail throws. The `try?` wrap + outer do/catch + log+swallow pattern. |
| T-13-30 | Tampering (Sendable) | self capture in Task.detached crashes build under Swift 6 (Pitfall 1) | mitigate | Capture sendable values into locals; build is the test (Test 7); CI Xcode 16.2 enforces. |
| T-13-31 | Denial of service | 200-photo drop spawns 200 concurrent Tasks (Pitfall 8) | accept | D-12 explicitly accepts unbounded upload lane. Renderer has memory guard. Phase 14 may add ThumbnailUploadLimiter if extreme batching becomes an issue. |
| T-13-32 | Information disclosure | OSLog of original key | accept | privacy: .public for debug visibility; project precedent. |
| T-13-33 | Tampering | sourceETag drift between PUT and ThumbnailUploader (race) | mitigate | ETag is captured into a local IMMEDIATELY after putS3Item returns; no shared mutable state. Even if the bucket changes underneath, the thumbnail is built against the local file URL we just uploaded — content is consistent. |
| T-13-34 | Elevation of privilege | Modify rename/move branch accidentally fires upload hook (re-render on rename) | mitigate | Hook gated behind content-change branch only; Plan 13-08 owns rename/move with its own server-side copy hook. Test 6 validates metadata-only + rename does NOT trigger upload. |
</threat_model>

<verification>
- macOS build green: `xcodebuild build -scheme DS3Drive -destination 'platform=macOS'`.
- iOS build green: `xcodebuild build -scheme DS3DriveApp -destination 'generic/platform=iOS Simulator'`.
- UploadHookTests all green.
- No regressions in existing +Create / +Modify tests.
- Manual smoke (Plan 13-12 / human verification): drop a JPG into a DS3 Drive folder; thumbnail PUT visible in `log show` within 5s.
</verification>

<success_criteria>
1. createItem post-PUT spawns Task.detached → ThumbnailUploader.generateAndUpload for raster files.
2. modifyItem content-change branch does the same; metadata-only and rename/move branches untouched.
3. Pre-filter via `S3PathUtils.isRasterExtension`; non-raster marks `.notApplicable`.
4. Sendable safe under Swift 6 strict concurrency.
5. Lifecycle decoupled — completion handler returns before Task work; failures swallowed + logged.
6. All 6+ UploadHookTests green; macOS build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/13-macos-generation-consumption-lifecycle/13-07-SUMMARY.md`.
</output>
