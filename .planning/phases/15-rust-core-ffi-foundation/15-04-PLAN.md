---
phase: 15-rust-core-ffi-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 15-02
files_modified:
  - core/ds3-s3/src/lib.rs
  - core/ds3-s3/src/client.rs
  - core/ds3-s3/src/list.rs
  - core/ds3-s3/src/transfer.rs
  - core/ds3-s3/src/multipart.rs
  - core/ds3-s3/src/crud.rs
  - core/ds3-s3/src/markers.rs
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "DS3S3Client wraps aws-sdk-s3 with custom endpoint and force_path_style(true)"
    - "list_objects returns S3ListingResult with URL-decoded keys (+ to space)"
    - "download_object streams to file with progress callback"
    - "upload_object streams from file with progress callback; returns ETag"
    - "Multipart upload: 5MB part size, 4-concurrency, abort on error, progress callbacks"
    - "head_object, delete_object, delete_objects, copy_object all work against aws-sdk-s3"
    - "probe_folder_exists uses head_object on .ds3keep marker key"
    - "create_folder_marker puts empty object at marker key"
  artifacts:
    - path: "core/ds3-s3/src/client.rs"
      provides: "DS3S3Client wrapping aws_sdk_s3::Client"
      contains: "force_path_style"
    - path: "core/ds3-s3/src/multipart.rs"
      provides: "Multipart upload with concurrent parts"
      contains: "MULTIPART_PART_SIZE"
    - path: "core/ds3-s3/src/markers.rs"
      provides: ".ds3keep folder marker operations"
      contains: "ds3keep"
  key_links:
    - from: "core/ds3-s3/src/client.rs"
      to: "aws-sdk-s3"
      via: "Client::from_conf with custom endpoint"
      pattern: "endpoint_url"
    - from: "core/ds3-s3/src/multipart.rs"
      to: "core/ds3-s3/src/client.rs"
      via: "DS3S3Client methods"
      pattern: "create_multipart_upload"
---

<objective>
Implement the ds3-s3 crate: S3 client wrapping aws-sdk-s3 with all CRUD operations, multipart upload with concurrent parts and progress callbacks, and .ds3keep folder marker management.

Purpose: Implements CORE-03 (S3 CRUD), CORE-04 (multipart uploads with progress), and CORE-05 (.ds3keep markers). Uses aws-sdk-s3 per researcher discretion (development speed primary, Cubbit S3 is standard per D-10). Implements against S3 API spec directly per D-11.
Output: Complete ds3-s3 crate with unit tests for URL-decoding and marker key computation.
</objective>

<execution_context>
@.planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md
@.planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md
@.planning/phases/15-rust-core-ffi-foundation/15-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DS3S3Client, list, and CRUD operations</name>
  <files>
    core/ds3-s3/src/lib.rs,
    core/ds3-s3/src/client.rs,
    core/ds3-s3/src/list.rs,
    core/ds3-s3/src/crud.rs
  </files>
  <read_first>
    core/ds3-s3/src/lib.rs,
    core/ds3-models/src/s3.rs,
    .planning/phases/15-rust-core-ffi-foundation/15-PATTERNS.md (sections: ds3-s3/src/client.rs, ds3-s3/src/list.rs, ds3-s3/src/crud.rs),
    DS3Lib/Sources/DS3Lib/DS3S3Client.swift (lines 176-342)
  </read_first>
  <behavior>
    - Test 1: decode_s3_key replaces "+" with space and percent-decodes
    - Test 2: decode_s3_key handles keys with no encoding
    - Test 3: normalize_etag strips surrounding quotes from ETag strings
    - Test 4: normalize_etag returns None for None input
    - Test 5: DS3S3Client::new constructs with force_path_style(true) and custom endpoint_url
    - Test 6: marker_key for "folder/" returns "folder/.ds3keep"
    - Test 7: marker_key for "folder" (no trailing slash) returns "folder/.ds3keep"
    - Test 8: is_ds3keep_marker_key returns true for ".ds3keep" and "path/.ds3keep", false for "file.txt"
  </behavior>
  <action>
    In `client.rs`: pub struct DS3S3Client { client: aws_sdk_s3::Client }. Constructor `pub fn new(endpoint: &str, access_key: &str, secret_key: &str, region: Option<&str>) -> Self`. Use `aws_sdk_s3::config::Builder::new()` with `.endpoint_url(endpoint)`, `.credentials_provider(Credentials::new(access_key, secret_key, None, None, "ds3"))`, `.region(Region::new(region.unwrap_or("us-east-1")))`, `.force_path_style(true)`, `.build()`. Create client from config. Add constants module: `pub const LIST_BATCH_SIZE: i32 = 2000`, `pub const DELIMITER: &str = "/"`, `pub const MULTIPART_PART_SIZE: usize = 5 * 1024 * 1024`, `pub const MULTIPART_THRESHOLD: u64 = 5 * 1024 * 1024`, `pub const MULTIPART_CONCURRENCY: usize = 4`, `pub const TIMEOUT_SECONDS: u64 = 300`, `pub const MAX_RETRIES: u32 = 5`, `pub const MARKER_FILE_NAME: &str = ".ds3keep"`. Add helper `pub fn normalize_etag(etag: Option<&str>) -> Option<String>` that trims surrounding quotes. Add helper `pub fn decode_s3_key(key: &str) -> Result<String, DS3Error>` that replaces "+" with "%20" then percent-decodes (matching Swift `decodeS3Key`).

    In `list.rs`: impl DS3S3Client methods. `pub async fn list_objects(&self, bucket: &str, prefix: Option<&str>, delimiter: Option<&str>, max_keys: Option<i32>, continuation_token: Option<&str>) -> Result<S3ListingResult, DS3Error>`. Call `self.client.list_objects_v2()` with params. Map `contents` to `Vec<S3ObjectSummary>`, decoding keys with `decode_s3_key`. Extract `common_prefixes`, `next_continuation_token`, `is_truncated`. `pub async fn list_buckets(&self) -> Result<Vec<(String, Option<String>)>, DS3Error>`. Call `self.client.list_buckets()`, map to name + creation_date pairs.

    In `crud.rs`: impl DS3S3Client methods. `pub async fn head_object(&self, bucket: &str, key: &str) -> Result<S3ObjectMetadata, DS3Error>`. `pub async fn delete_object(&self, bucket: &str, key: &str) -> Result<(), DS3Error>`. `pub async fn delete_objects(&self, bucket: &str, keys: &[String]) -> Result<usize, DS3Error>` using batch Delete. `pub async fn copy_object(&self, bucket: &str, source_key: &str, dest_key: &str, metadata: Option<&HashMap<String, String>>) -> Result<(), DS3Error>`. Copy source format: `"{bucket}/{source_key}"`. `pub fn is_not_found_error(err: &DS3Error) -> bool` checks for S3 NoSuchKey/404.

    In `lib.rs`: pub mod client, list, crud, transfer, multipart, markers. Re-export DS3S3Client and constants.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive/core && cargo test -p ds3-s3 --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `decode_s3_key("a+b%20c")` returns `"a b c"`
    - `normalize_etag(Some("\"abc\""))` returns `Some("abc")`
    - DS3S3Client constructor includes `force_path_style(true)` and `endpoint_url`
    - `is_ds3keep_marker_key(".ds3keep")` returns true
    - `cargo test -p ds3-s3 --lib` passes
  </acceptance_criteria>
  <done>DS3S3Client wraps aws-sdk-s3 with custom endpoint. List, head, delete, copy operations implemented. S3 key decoding and ETag normalization tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transfers (upload, download) and multipart upload with progress</name>
  <files>
    core/ds3-s3/src/transfer.rs,
    core/ds3-s3/src/multipart.rs,
    core/ds3-s3/src/markers.rs
  </files>
  <read_first>
    core/ds3-s3/src/client.rs,
    .planning/phases/15-rust-core-ffi-foundation/15-PATTERNS.md (sections: ds3-s3/src/transfer.rs, ds3-s3/src/multipart.rs, ds3-s3/src/markers.rs),
    DS3Lib/Sources/DS3Lib/DS3S3Client+Transfers.swift
  </read_first>
  <behavior>
    - Test 1: compute_parts for 12MB file returns 3 parts (5MB, 5MB, 2MB) with correct offsets
    - Test 2: compute_parts for 4MB file returns 1 part (entire file)
    - Test 3: compute_parts for exactly 5MB returns 1 part
    - Test 4: marker_key("folder/") returns "folder/.ds3keep"
    - Test 5: marker_key("") returns ".ds3keep"
  </behavior>
  <action>
    In `transfer.rs`: pub struct S3DownloadResult { pub etag: Option<String>, pub content_type: Option<String>, pub last_modified: Option<String>, pub content_length: i64 }. `pub async fn download_object(&self, bucket: &str, key: &str, file_path: &std::path::Path, on_progress: Option<&dyn Fn(i64, i64)>) -> Result<S3DownloadResult, DS3Error>`. Get object via `self.client.get_object()`. Stream body to file using `tokio::io::copy` or chunk-by-chunk with progress reporting. If on_progress provided, report bytes written after each chunk. `pub async fn upload_object(&self, bucket: &str, key: &str, file_path: &std::path::Path, on_progress: Option<&dyn Fn(i64, i64)>) -> Result<Option<String>, DS3Error>`. If file size > MULTIPART_THRESHOLD, delegate to multipart. Otherwise, single PutObject via `ByteStream::from_path`. Return ETag (normalized).

    In `multipart.rs`: struct PartDescriptor { part_number: i32, offset: u64, length: usize }. `fn compute_parts(total_size: u64, part_size: usize) -> Vec<PartDescriptor>` -- divides file into parts of MULTIPART_PART_SIZE. `pub async fn upload_multipart(&self, bucket: &str, key: &str, file_path: &std::path::Path, total_size: u64, on_progress: Option<&dyn Fn(i64, i64) + Send + Sync>) -> Result<String, DS3Error>`. Steps: (1) `create_multipart_upload` -> upload_id, (2) upload parts with concurrency limit MULTIPART_CONCURRENCY using `tokio::task::JoinSet` or `futures::stream::FuturesUnordered`. Each part: open file, seek to offset, read `length` bytes, call `upload_part`. Collect CompletedPartResult (part_number + etag). (3) `complete_multipart_upload` with sorted parts. (4) Return ETag. On any error: call `abort_multipart_upload` before propagating. Progress: track total bytes uploaded across all parts, invoke callback after each part completes. `pub async fn abort_multipart_upload(&self, bucket: &str, key: &str, upload_id: &str) -> Result<(), DS3Error>`.

    In `markers.rs`: `pub fn marker_key(folder_key: &str) -> String`. If folder_key ends with "/", append MARKER_FILE_NAME. If empty, return MARKER_FILE_NAME. Otherwise append "/" + MARKER_FILE_NAME. `pub fn is_ds3keep_marker_key(key: &str) -> bool`. `pub async fn probe_folder_exists(&self, bucket: &str, folder_key: &str) -> Result<bool, DS3Error>`. head_object on marker_key; return true if exists, false if NotFound, error otherwise. `pub async fn create_folder_marker(&self, bucket: &str, folder_key: &str) -> Result<(), DS3Error>`. put_object with empty body at marker_key. `pub async fn copy_folder_marker(&self, bucket: &str, source_folder: &str, dest_folder: &str) -> Result<(), DS3Error>`. copy_object on marker keys; fallback to create_folder_marker if NotFound.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive/core && cargo test -p ds3-s3 --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `compute_parts(12 * 1024 * 1024, 5 * 1024 * 1024)` returns 3 parts with correct offsets and lengths
    - `compute_parts(4 * 1024 * 1024, 5 * 1024 * 1024)` returns 1 part
    - `marker_key("folder/")` returns `"folder/.ds3keep"`
    - `upload_multipart` calls `abort_multipart_upload` in the error path (verified by code inspection)
    - MULTIPART_CONCURRENCY is 4
    - MULTIPART_PART_SIZE is 5 * 1024 * 1024
    - `cargo test -p ds3-s3 --lib` passes
  </acceptance_criteria>
  <done>Upload/download with progress callbacks, multipart upload with 5MB parts and 4-concurrency, .ds3keep marker operations all implemented. Part computation and marker key tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust -> Cubbit S3 | S3 requests carrying access_key/secret_key |
| File system | Reading/writing local files during transfer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | S3 credentials in logs | mitigate | DS3S3Client::new uses `#[instrument(skip(access_key, secret_key))]`; aws-sdk-s3 handles credential signing internally without logging |
| T-15-10 | Tampering | HTTPS enforcement for S3 | mitigate | aws-sdk-s3 uses HTTPS by default for all requests. Custom endpoint URL must start with `https://`. |
| T-15-11 | Denial of Service | multipart upload leak | mitigate | abort_multipart_upload called in error path; leaked uploads eventually expire per S3 lifecycle rules |
| T-15-12 | Tampering | ETag validation | mitigate | complete_multipart_upload returns final ETag; normalize_etag strips quotes for consistent comparison |
| T-15-SC | Tampering | aws-sdk-s3 install | mitigate | [ASSUMED] per audit -- official AWS SDK. Human-verify checkpoint at integration test phase (15-07). |
</threat_model>

<verification>
- `cargo test -p ds3-s3 --lib` passes
- `cargo clippy -p ds3-s3 -- -D warnings` passes
- Constants match Swift: MULTIPART_PART_SIZE = 5MB, MULTIPART_CONCURRENCY = 4, MARKER_FILE_NAME = ".ds3keep"
</verification>

<success_criteria>
- DS3S3Client wraps aws-sdk-s3 with custom Cubbit endpoint
- All S3 CRUD operations (list, head, upload, download, delete, copy)
- Multipart upload with 5MB parts, 4-concurrency, abort-on-error, progress callbacks
- .ds3keep marker operations (probe, create, copy)
- Unit tests for part computation, key decoding, marker keys
</success_criteria>

<output>
Create `.planning/phases/15-rust-core-ffi-foundation/15-04-SUMMARY.md` when done
</output>
