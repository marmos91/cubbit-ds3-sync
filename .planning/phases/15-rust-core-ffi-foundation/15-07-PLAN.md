---
phase: 15-rust-core-ffi-foundation
plan: 07
type: execute
wave: 4
depends_on:
  - 15-01
  - 15-06
files_modified:
  - core/tests/swift_harness/Package.swift
  - core/tests/swift_harness/Sources/main.swift
  - core/tests/csharp_harness/DS3CoreTest.csproj
  - core/tests/csharp_harness/Program.cs
  - .github/workflows/build.yml
autonomous: false
requirements:
  - CORE-09
  - CORE-10

user_setup:
  - service: cubbit-s3
    why: "Integration tests authenticate against real Cubbit IAM and perform S3 operations"
    env_vars:
      - name: DS3_TEST_EMAIL
        source: "Cubbit account email (existing test account)"
      - name: DS3_TEST_PASSWORD
        source: "Cubbit account password (existing test account)"
      - name: DS3_TEST_BUCKET
        source: "Dedicated test bucket name on Cubbit S3"
    dashboard_config: []

must_haves:
  truths:
    - "Swift test harness calls authenticate() via UniFFI, receives valid JWT, then calls list_objects/upload_object/download_object against real Cubbit S3"
    - "C# test harness calls ds3_authenticate() + ds3_list_objects() via P/Invoke against same endpoint"
    - "Multipart upload >5MB completes with correct ETag and progress callbacks fire"
    - "Panic in any Rust function returns error code (not crash) -- verified by deliberately triggering edge cases"
    - "Integration tests gated behind 'integration' feature flag"
    - "CI pipeline runs Rust unit tests on ubuntu-latest and C# integration on windows-latest"
  artifacts:
    - path: "core/tests/swift_harness/Package.swift"
      provides: "Swift package consuming DS3CoreFFI XCFramework"
      contains: "DS3CoreFFI"
    - path: "core/tests/swift_harness/Sources/main.swift"
      provides: "Swift integration test exercising auth + S3"
      contains: "authenticate"
    - path: "core/tests/csharp_harness/Program.cs"
      provides: "C# console app testing P/Invoke bindings"
      contains: "ds3_authenticate"
    - path: ".github/workflows/build.yml"
      provides: "Full CI pipeline with Rust + Apple + C# jobs"
      contains: "rust-test-integration"
  key_links:
    - from: "core/tests/swift_harness/Sources/main.swift"
      to: "core/out/DS3CoreFFI.xcframework"
      via: "XCFramework link"
      pattern: "DS3CoreFFI"
    - from: "core/tests/csharp_harness/Program.cs"
      to: "core/ds3-ffi/out/NativeMethods.g.cs"
      via: "P/Invoke DllImport"
      pattern: "NativeMethods"
---

<objective>
Create integration test harnesses for both Swift (UniFFI) and C# (csbindgen) paths, verify they work against real Cubbit S3, add comprehensive panic safety tests, and finalize the CI pipeline with all Rust + integration jobs.

Purpose: Implements CORE-09 (integration tests against real Cubbit S3) and completes CORE-10 (FFI patterns verified end-to-end). The Swift harness proves the XCFramework works. The C# harness proves the csbindgen bindings work. Panic tests prove FFI safety.
Output: Passing integration tests on both platforms, complete CI pipeline.
</objective>

<execution_context>
@.planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md
@.planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-rust-core-ffi-foundation/15-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Rust integration tests and panic safety tests</name>
  <files>
    core/ds3-auth/tests/integration.rs,
    core/ds3-s3/tests/integration.rs,
    core/ds3-ffi/tests/panic_tests.rs
  </files>
  <read_first>
    core/ds3-auth/src/session.rs,
    core/ds3-s3/src/client.rs,
    core/ds3-ffi/src/c_exports.rs,
    core/ds3-ffi/src/panic_guard.rs,
    .planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
    Gate all integration tests behind `#[cfg(feature = "integration")]`. Add `[features] integration = []` to ds3-auth and ds3-s3 Cargo.toml files.

    In `ds3-auth/tests/integration.rs`: Read credentials from env vars DS3_TEST_EMAIL, DS3_TEST_PASSWORD. Test `authenticate`: call `DS3Session::authenticate()` with real credentials, assert account.tenant_id is non-empty, assert session token is non-empty. Test `refresh`: after authenticate, call `refresh_if_needed()`, assert no error. Test `forge_iam_token`: forge token for account's IAM user, assert returned Token has non-empty token field and exp > 0. Test `get_projects`: call via ds3-http, assert returns non-empty Vec. Skip all tests with descriptive message if env vars missing.

    In `ds3-s3/tests/integration.rs`: Read credentials + bucket from env vars. Authenticate first, then create DS3S3Client from account's endpoint_gateway. Tests: `list_objects` on test bucket (assert response), `upload_object` with small temp file (assert ETag returned), `head_object` on uploaded key (assert metadata), `download_object` to temp file (assert file contents match), `delete_object` (assert no error), `multipart_upload` with 6MB temp file (assert ETag, assert progress callback was called at least once), `marker_operations` -- create marker, probe exists, delete. Clean up all test objects in a fixture teardown.

    In `ds3-ffi/tests/panic_tests.rs`: Test `ds3_authenticate` with null email pointer returns error code (not crash). Test `ds3_session_destroy` with null pointer is no-op. Test `ds3_list_objects` with invalid (null) handle returns error code. Test `ds3_free_string` with null pointer is no-op. These tests verify CORE-10 panic safety without needing network -- they test the FFI boundary guard behavior.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive/core && cargo test -p ds3-ffi --test panic_tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ds3-ffi --test panic_tests` passes (no network needed)
    - Null handle to any `ds3_*` function returns error code -1 or -2, not a crash
    - `ds3_session_destroy(null)` does not crash
    - Integration test files exist with `#[cfg(feature = "integration")]` gate
    - `cargo test --workspace --lib` still passes (integration tests not triggered without feature)
  </acceptance_criteria>
  <done>Panic safety tests prove no Rust function crashes the caller. Integration test files exist gated behind feature flag. FFI boundary safety verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create Swift and C# test harnesses</name>
  <files>
    core/tests/swift_harness/Package.swift,
    core/tests/swift_harness/Sources/main.swift,
    core/tests/csharp_harness/DS3CoreTest.csproj,
    core/tests/csharp_harness/Program.cs
  </files>
  <read_first>
    core/ds3-ffi/src/uniffi_exports.rs,
    core/ds3-ffi/src/c_exports.rs,
    core/ds3-ffi/uniffi.toml,
    core/scripts/build-xcframework.sh,
    .planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md (Code Examples section: XCFramework Build Script)
  </read_first>
  <action>
    Swift test harness (`core/tests/swift_harness/`):
    Create `Package.swift` as a Swift executable package. Add the generated `DS3CoreFFI.swift` file and link against the XCFramework. The package should reference the XCFramework via a local path (`.binaryTarget(name: "DS3CoreFFI", path: "../../out/DS3CoreFFI.xcframework")`). In `main.swift`: read DS3_TEST_EMAIL, DS3_TEST_PASSWORD, DS3_TEST_BUCKET from environment. Call `DS3SessionHandle.authenticate(email:password:tenantId:coordinatorUrl:)`. Assert session is valid. Call `listObjects(bucket:prefix:delimiter:maxKeys:continuationToken:)`. Call `uploadObject(bucket:key:filePath:progress:)` with a small temp file. Call `downloadObject(bucket:key:filePath:progress:)` to a temp file. Verify download contents match. Call `deleteObject(bucket:key:)`. Print "All Swift integration tests passed" on success, exit 1 on any failure.

    C# test harness (`core/tests/csharp_harness/`):
    Create `DS3CoreTest.csproj` targeting `net8.0` (or latest LTS). Copy the generated `NativeMethods.g.cs` into the project. In `Program.cs`: P/Invoke `ds3_authenticate` with credentials from env vars. Check return code == 0. P/Invoke `ds3_list_objects` with the returned handle. Check return code == 0 and parse the returned JSON. P/Invoke `ds3_session_destroy` to clean up. Print "All C# integration tests passed" on success, exit 1 on failure. Per D-08: this harness runs on Windows CI only (not locally on macOS).
  </action>
  <verify>
    <automated>test -f /Users/marmos91/Projects/cubbit-ds3-drive/core/tests/swift_harness/Package.swift && test -f /Users/marmos91/Projects/cubbit-ds3-drive/core/tests/csharp_harness/Program.cs</automated>
  </verify>
  <acceptance_criteria>
    - `core/tests/swift_harness/Package.swift` exists and references DS3CoreFFI.xcframework
    - `core/tests/swift_harness/Sources/main.swift` calls authenticate, listObjects, uploadObject, downloadObject, deleteObject
    - `core/tests/csharp_harness/DS3CoreTest.csproj` targets net8.0
    - `core/tests/csharp_harness/Program.cs` P/Invokes ds3_authenticate, ds3_list_objects, ds3_session_destroy
    - Both harnesses read credentials from environment variables
    - Both harnesses print pass/fail message and return appropriate exit code
  </acceptance_criteria>
  <done>Swift and C# integration test harnesses created, ready for execution against real Cubbit S3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Rust core crates implemented (ds3-models, ds3-http, ds3-auth, ds3-s3, ds3-sync, ds3-ffi). XCFramework build script created. Swift and C# test harnesses ready.

    This checkpoint verifies two things:
    1. The XCFramework builds successfully on the developer's machine
    2. Integration tests pass against real Cubbit S3 with actual credentials
  </what-built>
  <how-to-verify>
    Step 1 - Build XCFramework:
    ```bash
    cd core && chmod +x scripts/build-xcframework.sh && ./scripts/build-xcframework.sh
    ```
    Expected: Script completes without errors. `core/out/DS3CoreFFI.xcframework` directory exists with macOS, iOS, and simulator slices.

    Step 2 - Run Rust integration tests:
    ```bash
    export DS3_TEST_EMAIL="your-test-email"
    export DS3_TEST_PASSWORD="your-test-password"
    export DS3_TEST_BUCKET="your-test-bucket"
    cd core && cargo test --workspace --features integration
    ```
    Expected: All tests pass. Auth flow works, S3 operations succeed, multipart upload completes.

    Step 3 - Run Swift integration test:
    ```bash
    cd core/tests/swift_harness && swift run
    ```
    Expected: "All Swift integration tests passed"

    Step 4 - Verify panic safety:
    ```bash
    cd core && cargo test -p ds3-ffi --test panic_tests
    ```
    Expected: All panic tests pass (null pointers return error codes, no crashes).

    Note: C# harness (Step 5) runs in CI only (Windows runner per D-08). Skip locally.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures for debugging.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test credentials | Real Cubbit credentials used in integration tests |
| CI secrets | GitHub Actions secrets exposed to test jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Information Disclosure | Test credentials in logs | mitigate | Integration tests never print credentials. Env vars used, not hardcoded. CI secrets protected by fork check (`github.event.pull_request.head.repo.full_name == github.repository`). |
| T-15-20 | Tampering | Test bucket pollution | mitigate | All test objects created with unique prefix (`ds3-test-{uuid}/`). Teardown deletes all test objects. |
| T-15-21 | Denial of Service | FFI null pointer | mitigate | Panic tests verify null handle, null string pointer, and null output pointer all return error codes without crashing. |
| T-15-SC | Tampering | All crate installs | mitigate | This is the final plan of Phase 15. All packages are [ASSUMED] per RESEARCH.md Package Legitimacy Audit. Human-verify checkpoint in this plan gates the first real credential use through the Rust core. Developer confirms packages behave correctly. |
</threat_model>

<verification>
- `cargo test --workspace --lib` passes (all unit tests)
- `cargo test -p ds3-ffi --test panic_tests` passes (panic safety)
- `cargo test --workspace --features integration` passes with credentials (auth + S3)
- XCFramework builds successfully
- Swift harness runs successfully
</verification>

<success_criteria>
- Rust integration tests authenticate and perform S3 CRUD against real Cubbit
- Swift harness proves UniFFI XCFramework works end-to-end
- C# harness proves csbindgen P/Invoke works end-to-end (CI-only verification)
- Multipart upload >5MB completes with progress callbacks
- Panic safety verified: null pointers return error codes
- CI pipeline complete with all Rust, Apple, and C# jobs
</success_criteria>

<output>
Create `.planning/phases/15-rust-core-ffi-foundation/15-07-SUMMARY.md` when done
</output>
