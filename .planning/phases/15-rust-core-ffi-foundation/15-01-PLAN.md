---
phase: 15-rust-core-ffi-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apple/DS3Drive.xcodeproj/project.pbxproj
  - apple/DS3Drive/
  - apple/DS3DriveApp/
  - apple/DS3DriveProvider/
  - apple/DS3DriveProviderTests/
  - apple/DS3DriveShareExtension/
  - apple/DS3DriveTests/
  - apple/DS3DriveAppTests/
  - apple/DS3Lib/
  - apple/DS3Thumbnails/
  - apple/Assets/
  - apple/.swiftlint.yml
  - apple/.swiftformat
  - apple/ExportOptions-appstore.plist
  - apple/ExportOptions-appstore-ios.plist
  - apple/.env.example
  - windows/.gitkeep
  - .gitignore
  - CLAUDE.md
  - .github/workflows/build.yml
  - .github/workflows/release-homebrew.yml
  - .github/workflows/release-testflight.yml
  - .github/workflows/release-testflight-ios.yml
autonomous: false
requirements:
  - CORE-01

must_haves:
  truths:
    - "D-01: All existing Apple code lives under apple/ subdirectory"
    - "D-02: Xcode project opens and builds from apple/ directory"
    - "windows/ directory exists as empty scaffold"
    - "D-03: .planning/ stays at repo root"
    - "D-06: Root-level files (CLAUDE.md, .github/, .gitignore, LICENSE) stay at repo root"
    - "D-07: CI workflows reference apple/ paths and still build green"
    - "D-04: Git LFS assets moved with apple/"
    - "D-05: All open branches/PRs merged or closed before restructure commit"
  artifacts:
    - path: "apple/DS3Drive.xcodeproj/project.pbxproj"
      provides: "Xcode project in new location"
    - path: "apple/DS3Lib/Package.swift"
      provides: "DS3Lib SPM package in new location"
    - path: "windows/.gitkeep"
      provides: "Windows directory scaffold"
    - path: ".github/workflows/build.yml"
      provides: "Updated CI with apple/ paths"
  key_links:
    - from: ".github/workflows/build.yml"
      to: "apple/DS3Drive.xcodeproj"
      via: "xcodebuild -project paths"
      pattern: "apple/DS3Drive\\.xcodeproj"
---

<objective>
Restructure the repository into a mono-repo layout: move all existing Apple code to `apple/`, scaffold `windows/`, update CI and root-level config files with new paths. This is the prerequisite for all subsequent Rust core work.

Purpose: Per D-01, the mono-repo restructure establishes the cross-platform directory layout before any Rust code is written. All subsequent plans build in the new layout.
Output: Repository with `apple/`, `windows/` directories; CI green against apple/ paths.
</objective>

<execution_context>
@.planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md
@.planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Per D-05: "Clean slate first -- merge or close all open branches/PRs before the restructure commit." This checkpoint gates the mono-repo restructure on a clean branch/PR state.
  </what-built>
  <how-to-verify>
    Step 1 - Check open PRs:
    ```bash
    gh pr list --state open
    ```
    Expected: No open PRs, or all open PRs have been intentionally kept open and are compatible with the upcoming restructure.

    Step 2 - Check open branches with unmerged work:
    ```bash
    git branch --no-merged main
    ```
    Expected: No branches with unmerged work that should be merged before the restructure, or all remaining branches are intentionally deferred.

    If there are open PRs or unmerged branches that need to be resolved first, list them and merge/close before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" if all branches/PRs are clean, or list what needs to be merged/closed first.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Move existing code to apple/ and scaffold windows/</name>
  <files>
    apple/ (entire directory -- all existing project files moved here),
    windows/.gitkeep,
    .gitignore,
    CLAUDE.md
  </files>
  <read_first>
    .gitignore,
    CLAUDE.md,
    .gitattributes,
    .swiftlint.yml,
    .swiftformat
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04, D-06: Execute the mono-repo restructure.

    1. Create `apple/` directory. Move into it: `DS3Drive/`, `DS3DriveApp/`, `DS3DriveProvider/`, `DS3DriveProviderTests/`, `DS3DriveShareExtension/`, `DS3DriveTests/`, `DS3DriveAppTests/`, `DS3Lib/`, `DS3Thumbnails/`, `DS3Drive.xcodeproj/`, `Assets/`, `.swiftlint.yml`, `.swiftformat`, `ExportOptions-appstore.plist`, `ExportOptions-appstore-ios.plist`, `.env.example`, `CODE_OF_CONDUCT.md`. Use `git mv` for each to preserve history.

    2. Create `windows/.gitkeep` (empty scaffold).

    3. Update `.gitignore`: add Rust-specific entries (`/core/target/`, `*.dylib`, `*.a` build artifacts under `core/out/`) and ensure `apple/` subpath entries still work (e.g., `apple/DS3Lib/.build/`). Keep existing Swift/Xcode ignores but prefix with `apple/` where path-specific.

    4. Update `CLAUDE.md`: all references to `DS3Drive.xcodeproj`, `DS3Lib/`, `DS3DriveProvider/`, etc. must be prefixed with `apple/`. Add a note that the project is now a mono-repo with `apple/`, `core/`, `windows/` directories.

    5. Per D-03: `.planning/` stays at repo root. Per D-06: `CLAUDE.md`, `.github/`, `.gitignore`, `LICENSE`, `.gitattributes` stay at repo root.

    6. Per D-04: Git LFS assets (`Assets/`) move into `apple/`. Update `.gitattributes` if it contains path-specific LFS tracking patterns to reflect `apple/Assets/` paths.

    7. `docs/` directory stays at repo root (cross-platform documentation).
  </action>
  <verify>
    <automated>test -d apple/DS3Drive.xcodeproj && test -d apple/DS3Lib && test -d apple/DS3DriveProvider && test -f windows/.gitkeep && test -d .planning && test -f CLAUDE.md && test -d .github && ! test -d DS3Drive.xcodeproj</automated>
  </verify>
  <done>All existing Apple code lives under apple/, windows/ scaffolded, root files updated with new paths, git history preserved via git mv.</done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflows for apple/ paths and add Rust CI jobs</name>
  <files>
    .github/workflows/build.yml,
    .github/workflows/release-homebrew.yml,
    .github/workflows/release-testflight.yml,
    .github/workflows/release-testflight-ios.yml
  </files>
  <read_first>
    .github/workflows/build.yml,
    .github/workflows/release-homebrew.yml,
    .github/workflows/release-testflight.yml,
    .github/workflows/release-testflight-ios.yml
  </read_first>
  <action>
    Per D-07: CI must build both Rust core and Apple from Phase 15 onward.

    In `build.yml`:

    1. Update all `xcodebuild` commands to reference `apple/DS3Drive.xcodeproj` (both `-project` arg and the resolve step).
    2. Update `swift test --package-path` to `apple/DS3Lib`.
    3. Update SPM cache paths from `DS3Lib/.build` to `apple/DS3Lib/.build` and from `DS3Drive.xcodeproj` to `apple/DS3Drive.xcodeproj`.
    4. Update SwiftLint config path from `.swiftlint.yml` to `apple/.swiftlint.yml`. Add `--path apple/` to constrain lint scope.
    5. Add a new `rust-check` job (runs on `ubuntu-latest`): install Rust stable via `dtolnay/rust-toolchain@stable`, cache `core/target/` via `actions/cache`, run `cd core && cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace --lib`. This job runs in parallel with the existing lint job (no dependency).
    6. Hash key for SPM caches must reflect the new `apple/` prefixed paths.

    In release workflows (`release-homebrew.yml`, `release-testflight.yml`, `release-testflight-ios.yml`):
    Update all references from `DS3Drive.xcodeproj` to `apple/DS3Drive.xcodeproj`, `DS3Lib/` to `apple/DS3Lib/`, and similar path changes.
  </action>
  <verify>
    <automated>grep -c "apple/DS3Drive.xcodeproj" .github/workflows/build.yml && grep -c "cargo" .github/workflows/build.yml && grep -c "apple/DS3Lib" .github/workflows/build.yml</automated>
  </verify>
  <done>All CI workflows reference apple/ paths, rust-check job added to build.yml, no broken path references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets | GitHub Actions secrets (DS3_TEST_EMAIL, etc.) exposed only to same-repo events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | git mv restructure | accept | Restructure is a directory move only; no code changes. Verified by CI build. |
| T-15-SC | Tampering | N/A (no package installs) | accept | No new dependencies added in this plan. |
</threat_model>

<verification>
- `git status` shows all files as renamed (not deleted + added)
- No bare `DS3Drive.xcodeproj` reference in any workflow file: `grep -r "DS3Drive.xcodeproj" .github/workflows/ | grep -v "apple/"`
- Existing Apple CI job structure is preserved (lint, build, test-unit, test-integration, build-ios)
</verification>

<success_criteria>
- Repo has apple/, windows/ directories at root alongside .planning/, .github/, CLAUDE.md
- All Apple code under apple/ with git history preserved
- CI workflows reference apple/ paths exclusively
- New rust-check job present in build.yml (will pass once Plan 02 creates the workspace)
</success_criteria>

<output>
Create `.planning/phases/15-rust-core-ffi-foundation/15-01-SUMMARY.md` when done
</output>
