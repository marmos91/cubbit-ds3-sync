---
phase: 15-rust-core-ffi-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/Cargo.toml
  - core/ds3-models/Cargo.toml
  - core/ds3-models/src/lib.rs
  - core/ds3-models/src/account.rs
  - core/ds3-models/src/auth.rs
  - core/ds3-models/src/drive.rs
  - core/ds3-models/src/project.rs
  - core/ds3-models/src/api_key.rs
  - core/ds3-models/src/s3.rs
  - core/ds3-models/src/sync.rs
  - core/ds3-models/src/error.rs
  - core/ds3-http/Cargo.toml
  - core/ds3-http/src/lib.rs
  - core/ds3-s3/Cargo.toml
  - core/ds3-s3/src/lib.rs
  - core/ds3-auth/Cargo.toml
  - core/ds3-auth/src/lib.rs
  - core/ds3-sync/Cargo.toml
  - core/ds3-sync/src/lib.rs
  - core/ds3-ffi/Cargo.toml
  - core/ds3-ffi/src/lib.rs
autonomous: true
requirements:
  - CORE-01

must_haves:
  truths:
    - "Cargo workspace with 6 crates compiles successfully"
    - "All 6 crates are listed in workspace members"
    - "ds3-models contains all domain types matching Swift model schemas"
    - "DS3Error enum covers auth, S3, HTTP, IO, and FFI error domains"
    - "Workspace dependency deduplication via [workspace.dependencies]"
  artifacts:
    - path: "core/Cargo.toml"
      provides: "Workspace manifest with 6 crate members"
      contains: "members"
    - path: "core/ds3-models/src/lib.rs"
      provides: "Re-exports for all model modules"
      exports: ["account", "auth", "drive", "project", "api_key", "s3", "sync", "error"]
    - path: "core/ds3-models/src/error.rs"
      provides: "Unified DS3Error enum with numeric codes"
      contains: "DS3Error"
  key_links:
    - from: "core/ds3-http/Cargo.toml"
      to: "core/ds3-models"
      via: "workspace dependency"
      pattern: "ds3-models"
    - from: "core/ds3-auth/Cargo.toml"
      to: "core/ds3-models"
      via: "workspace dependency"
      pattern: "ds3-models"
    - from: "core/ds3-s3/Cargo.toml"
      to: "core/ds3-models"
      via: "workspace dependency"
      pattern: "ds3-models"
---

<objective>
Create the Cargo workspace with all 6 crates and fully implement the ds3-models crate with all domain types ported from Swift. Create stub lib.rs files for the other 5 crates so the workspace compiles.

Purpose: Establishes the Rust foundation. The ds3-models crate defines the shared types that every other crate depends on. All types must match the existing Swift JSON schemas exactly so Phase 16's data migration is transparent.
Output: Compilable Cargo workspace, complete ds3-models crate with serde-compatible types.
</objective>

<execution_context>
@.planning/phases/15-rust-core-ffi-foundation/15-CONTEXT.md
@.planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md
@.planning/phases/15-rust-core-ffi-foundation/15-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo workspace and all 6 crate skeletons</name>
  <files>
    core/Cargo.toml,
    core/ds3-models/Cargo.toml,
    core/ds3-models/src/lib.rs,
    core/ds3-http/Cargo.toml,
    core/ds3-http/src/lib.rs,
    core/ds3-auth/Cargo.toml,
    core/ds3-auth/src/lib.rs,
    core/ds3-s3/Cargo.toml,
    core/ds3-s3/src/lib.rs,
    core/ds3-sync/Cargo.toml,
    core/ds3-sync/src/lib.rs,
    core/ds3-ffi/Cargo.toml,
    core/ds3-ffi/src/lib.rs
  </files>
  <read_first>
    .planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md (Installation section with workspace.dependencies),
    .planning/phases/15-rust-core-ffi-foundation/15-RESEARCH.md (Crate Dependency Graph section)
  </read_first>
  <action>
    Create the workspace root `core/Cargo.toml` with `resolver = "2"`, members list of all 6 crates, and `[workspace.dependencies]` section containing all shared dependencies at pinned versions per RESEARCH.md: tokio 1.52 (features: full), serde 1 (features: derive), serde_json 1, reqwest 0.13 (features: cookies, json), thiserror 2, tracing 0.1, uuid 1 (features: v4, serde), chrono 0.4 (features: serde), base64 0.22, sha2 0.11, ed25519-dalek 2.1, jsonwebtoken 10.

    For ds3-models Cargo.toml: depend on serde, serde_json, uuid, chrono, thiserror from workspace. Set `[lib] name = "ds3_models"`.

    For ds3-http Cargo.toml: depend on ds3-models (path), reqwest, serde, serde_json, thiserror, tracing, jsonwebtoken from workspace. Set `[lib] name = "ds3_http"`.

    For ds3-auth Cargo.toml: depend on ds3-models (path), ds3-http (path), ed25519-dalek, sha2, base64, serde, serde_json, thiserror, tracing, reqwest from workspace. Set `[lib] name = "ds3_auth"`.

    For ds3-s3 Cargo.toml: add direct dependencies `aws-sdk-s3 = "1.133"` and `aws-config = "1"` (not in workspace deps since only ds3-s3 uses them), plus ds3-models (path), tokio, serde, thiserror, tracing from workspace. Set `[lib] name = "ds3_s3"`.

    For ds3-sync Cargo.toml: depend on ds3-models (path), chrono, uuid, thiserror from workspace. Set `[lib] name = "ds3_sync"`.

    For ds3-ffi Cargo.toml: depend on ds3-models (path), ds3-auth (path), ds3-http (path), ds3-s3 (path), ds3-sync (path), tokio from workspace. Add `uniffi = "0.31"` as direct dependency. Set `[lib] name = "ds3_ffi"` with `crate-type = ["staticlib", "cdylib"]`. Add `uniffi` to `[build-dependencies]`.

    Each non-models crate gets a stub `src/lib.rs` with a doc comment describing its purpose.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive/core && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `core/Cargo.toml` contains `members = ["ds3-models", "ds3-http", "ds3-auth", "ds3-s3", "ds3-sync", "ds3-ffi"]`
    - `core/Cargo.toml` contains `[workspace.dependencies]` section with all shared deps
    - Each crate has `Cargo.toml` with correct `[lib] name` using underscores
    - `cargo check --workspace` from `core/` succeeds with zero errors
    - ds3-ffi has `crate-type = ["staticlib", "cdylib"]`
  </acceptance_criteria>
  <done>Cargo workspace with 6 crates compiles. All workspace dependencies declared. Crate dependency graph matches RESEARCH.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ds3-models crate with all domain types</name>
  <files>
    core/ds3-models/src/lib.rs,
    core/ds3-models/src/account.rs,
    core/ds3-models/src/auth.rs,
    core/ds3-models/src/drive.rs,
    core/ds3-models/src/project.rs,
    core/ds3-models/src/api_key.rs,
    core/ds3-models/src/s3.rs,
    core/ds3-models/src/sync.rs,
    core/ds3-models/src/error.rs
  </files>
  <read_first>
    .planning/phases/15-rust-core-ffi-foundation/15-PATTERNS.md (all model pattern sections),
    DS3Lib/Sources/DS3Lib/Models/Account.swift,
    DS3Lib/Sources/DS3Lib/Models/AccountSession.swift,
    DS3Lib/Sources/DS3Lib/Models/Challenge.swift,
    DS3Lib/Sources/DS3Lib/Models/Token.swift,
    DS3Lib/Sources/DS3Lib/Models/DS3Drive.swift,
    DS3Lib/Sources/DS3Lib/Models/SyncAnchor.swift,
    DS3Lib/Sources/DS3Lib/Models/Bucket.swift,
    DS3Lib/Sources/DS3Lib/Models/Project.swift,
    DS3Lib/Sources/DS3Lib/Models/IAMUser.swift,
    DS3Lib/Sources/DS3Lib/Models/DS3APIKey.swift,
    DS3Lib/Sources/DS3Lib/Models/ConflictInfo.swift
  </read_first>
  <behavior>
    - Test 1: Account deserializes from JSON with snake_case keys (first_name, last_name, tenant_id, endpoint_gateway, etc.) and special renames (internal, banned)
    - Test 2: Challenge deserializes from JSON with challenge and salt fields
    - Test 3: Token deserializes from JSON with token, exp, exp_date fields
    - Test 4: AccountSession round-trips with nested Token and refreshToken field
    - Test 5: Project deserializes with project_-prefixed JSON keys (project_id, project_name, etc.)
    - Test 6: IAMUser deserializes with user_id, user_name, is_root keys
    - Test 7: DS3ApiKey deserializes with api_key, secret_key, created_at keys; secret_key is Option
    - Test 8: DS3Drive round-trips with UUID id, nested SyncAnchor
    - Test 9: S3ListingResult, S3ObjectSummary, S3ObjectMetadata constructable with correct field types
    - Test 10: TreeEntry, DiffAction, DiffResult have correct field types for sync diff
    - Test 11: DS3Error enum has variants for all error domains with numeric code() method
  </behavior>
  <action>
    Port all Swift model types to Rust using the exact JSON field mappings from 15-PATTERNS.md.

    For `account.rs`: struct Account with fields id (String), first_name, last_name, is_internal (serde rename "internal"), is_banned (rename "banned"), created_at, deleted_at (Option), banned_at (Option), max_allowed_projects (i32), emails (Vec of AccountEmail), is_two_factor_enabled (rename "two_factor_enabled"), tenant_id, endpoint_gateway, auth_provider. Struct AccountEmail with id (String), email (String), verified (bool).

    For `auth.rs`: struct Challenge { challenge: String, salt: String }. Struct Token { token: String, exp: i64, exp_date: String } (store exp_date as String; rename "exp_date"). Struct AccountSession { token: Token, refresh_token: String } (rename refresh_token to "refreshToken" for JSON compat with existing Swift serialization).

    For `drive.rs`: struct DS3Drive { id: uuid::Uuid, sync_anchor: SyncAnchor, name: String }. Struct SyncAnchor { project: Project, iam_user: IAMUser, bucket: Bucket, prefix: Option<String> } (rename iam_user to "IAMUser" in serde). Struct Bucket { name: String }.

    For `project.rs`: struct Project with per-field serde renames: id -> "project_id", name -> "project_name", description -> "project_description", email -> "project_email", created_at -> "project_created_at", banned_at (Option) -> "project_banned_at", image_url (Option) -> "project_image_url", tenant_id -> "project_tenant_id", root_account_email (Option) -> "root_account_email", users (Option<Vec<IAMUser>>). Struct IAMUser { id: String (rename "user_id"), username: String (rename "user_name"), is_root: bool (rename "is_root") }.

    For `api_key.rs`: struct DS3ApiKey { name: String, api_key: String (rename "api_key"), secret_key: Option<String> (rename "secret_key"), created_at: String (rename "created_at") }.

    For `s3.rs`: S3ListingResult { objects: Vec<S3ObjectSummary>, common_prefixes: Vec<String>, next_continuation_token: Option<String>, is_truncated: bool }. S3ObjectSummary { key: String, etag: Option<String>, last_modified: Option<String>, size: i64 }. S3ObjectMetadata { etag: Option<String>, content_type: Option<String>, last_modified: Option<String>, version_id: Option<String>, content_length: i64, metadata: Option<HashMap<String, String>> }. TransferProgress { bytes_transferred: i64, total_bytes: Option<i64> }. MultipartUploadContext { bucket: String, key: String, upload_id: String, total_size: i64 }. CompletedPartResult { part_number: i32, etag: String }.

    For `sync.rs`: DiffResult { new_or_modified: HashSet<String>, deleted: HashSet<String> } with is_empty() method. ConflictInfo mirroring the Swift model.

    For `error.rs`: enum DS3Error with thiserror derives. Variants: InvalidUrl { url: String }, ServerError { status: u16, body: String }, JsonError { message: String }, Encoding, LoggedOut, TokenExpired, Missing2FA, CookieError, MissingUploadId, EmptyFileData, MissingETag, ParseError, UnableToOpenFile, IoError(String), HttpError(String), S3Error(String), AuthError(String). Add `pub fn code(&self) -> i32` returning unique numeric codes for FFI mapping. Implement From<reqwest::Error>, From<std::io::Error>, From<serde_json::Error>.

    All structs derive `Clone, Debug, Serialize, Deserialize`. Add `PartialEq` where useful for testing.

    In `lib.rs`: declare all modules as `pub mod` and re-export key types at crate root.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/cubbit-ds3-drive/core && cargo test -p ds3-models 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ds3-models` passes with all serde round-trip tests green
    - Account struct deserializes JSON with `"internal": true` mapping to `is_internal`
    - Project struct deserializes JSON with `"project_id"` prefix pattern
    - DS3Error has a `code()` method returning distinct i32 for each variant
    - All model structs derive Serialize + Deserialize
    - `cargo check --workspace` still succeeds (no downstream breakage)
  </acceptance_criteria>
  <done>ds3-models crate contains all domain types matching Swift model JSON schemas. Serde round-trip tests prove JSON compatibility. DS3Error covers all error domains with numeric codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON deserialization | External API responses deserialized into typed structs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information Disclosure | DS3Error display | mitigate | DS3Error Display impl must not leak sensitive data (passwords, tokens). Use generic messages for auth errors. |
| T-15-03 | Tampering | serde deserialization | mitigate | All deserialization via typed structs (no raw JSON manipulation). serde rejects unknown shapes. |
| T-15-SC | Tampering | cargo dependency installs | mitigate | All packages are [ASSUMED] per RESEARCH.md audit. Plan 15-02 is first cargo build -- human verify checkpoint in Plan 15-03 before first real credential use. |
</threat_model>

<verification>
- `cargo check --workspace` succeeds
- `cargo test -p ds3-models` passes all serde round-trip tests
- `cargo clippy --workspace -- -D warnings` passes
</verification>

<success_criteria>
- 6 crates in workspace, all compile
- ds3-models has 9 source files covering all domain types
- JSON compatibility with Swift models verified by serde tests
- DS3Error enum with numeric codes for FFI
</success_criteria>

<output>
Create `.planning/phases/15-rust-core-ffi-foundation/15-02-SUMMARY.md` when done
</output>
