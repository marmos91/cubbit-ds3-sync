---
phase: 17.1
slug: windows-s3-client-ffi-wiring-sync-enablement
status: secured
threats_open: 0
threats_total: 13
threats_closed: 10
threats_accepted: 3
asvs_level: default
register_authored_at_plan_time: true
created: 2026-06-02
---

# SECURITY.md — Phase 17.1 (Windows S3-Client FFI Wiring + Sync Enablement)

**Audited:** 2026-06-02
**Phase:** 17.1-windows-s3-client-ffi-wiring-sync-enablement
**ASVS Level:** default
**Threat register:** authored at plan time (register_authored_at_plan_time: true) across plans 17.1-01/02/03
**Result:** SECURED — 10/10 mitigate threats CLOSED; 3/3 accept threats documented
**threats_open:** 0

---

## Threat Verification (mitigate disposition)

| Threat ID | Category | Evidence (file:line) | Status |
|-----------|----------|----------------------|--------|
| T-17.1-01 | Tampering/DoS — garbage handle mint | `core/ds3-ffi/src/c_exports.rs:435-437` — `DS3S3Client::new(...)` then `Box::into_raw(Box::new(client))` into `*out_handle`; existing S3 exports take `*const DS3S3Client` (e.g. `ds3_list_buckets` c_exports.rs:499) | CLOSED |
| T-17.1-02 | DoS — panic unwind across FFI | `core/ds3-ffi/src/c_exports.rs:429` body wrapped in `ffi_guard!(out_error, { … })`; macro returns `-2` and sets `*out_error = -2` on panic, no unwind (`panic_guard.rs:33-38`) | CLOSED |
| T-17.1-03 | Info Disclosure — secret in logs | `core/ds3-s3/src/client.rs:54` — `#[tracing::instrument(skip(access_key, secret_key))]` on `DS3S3Client::new`; the export logs no string args | CLOSED |
| T-17.1-05 | Tampering/DoS — wrong-type handle deref (the AVE) | `DS3DriveS3Client.cs:42-49` owns a real `DS3S3Client` handle; `DS3Session.cs` contains zero `ds3_list_buckets/list_objects/head/download/upload/delete/copy` calls (grep: no matches) — session handle can no longer reach an S3 export | CLOSED |
| T-17.1-06 | Tampering/DoS — double-free | `DS3DriveS3Client.cs:235-242` Dispose uses `Interlocked.Exchange(ref _handle, IntPtr.Zero)`; destroy only fires when `prev != IntPtr.Zero` (single-shot) | CLOSED |
| T-17.1-08 | Info Disclosure — secret via exception message | `DS3ExceptionFactory.cs:58` logs `code={Code}` only (numeric), never the raw message; `DS3DriveS3Client.Create` (DS3DriveS3Client.cs:61-77) never logs the secret arg | CLOSED |
| T-17.1-09 | Secret Storage / Info Disclosure — secret derivation/retention | Secret resolved via `DriveS3CredentialProvider.cs:54` (`LoadOrCreateApiKeyAsync` → CredentialStore), never SQLite. CR-01 fix: `SyncHostedService.cs:43-56` stores a SHA-256 `CredsFingerprint`, NOT the plaintext; `creds` is consumed only by `DS3DriveS3Client.Create` (line 181) and never retained (lines 183-186) | CLOSED |
| T-17.1-10 | Access Control — bucket-access-denied surfacing | Bad/denied key surfaces as `DS3TransportException`/`DS3S3Exception` (per A1: code 3003 → transport); `DriveSetupViewModel.cs:250-260` `MapErrorCopy` returns fixed copy strings only (no `ex.Message` interpolation, exhaustive default) — no server text leak, no AVE; browse routes through `DS3DriveS3Client` not the session handle (`DS3SdkService.cs:85,89,125`) | CLOSED |
| T-17.1-11 | Tampering/DoS — use-after-free on teardown | `SyncHostedService.cs:226-251` `StopActiveAsync` disposes `active.S3` LAST, in a `finally` (WR-01/WR-02 fix), after `Engine.StopAsync` + `Provider.DisconnectAsync`; facade `Interlocked.Exchange` single-shot. In-flight wizard-browse drain added in `DS3SdkService` (`_drained`, Enter/ExitBrowseOperation per REVIEW-FIX WR-01) | CLOSED |
| T-17.1-12 | Tampering — stale/revoked creds (rebuild missed) | `SyncHostedService.cs:164-174` rebuild-on-change: compares `existing.CredsFingerprint == CredsFingerprintOf(creds)`; on mismatch removes + `StopActiveAsync` (Dispose) + rebuilds via `Create` (line 181) | CLOSED |
| T-17.1-13 | Input Validation — empty endpoint | `DS3AccountInfo.cs:27` surfaces `EndpointGateway` from `endpoint_gateway`; `DriveS3CredentialProvider.cs:73-80` throws on empty endpoint before minting (plus account-scope guard lines 63-71, WR-05) | CLOSED |

## Accepted Risks Log (accept disposition)

| Threat ID | Category | Rationale | Verified |
|-----------|----------|-----------|----------|
| T-17.1-04 | Tampering — region marshalling mismatch on ARM64 | `nuint`/`usize` is 64-bit and `Cdecl` on both win-x64 and win-arm64; the new export is byte-identical in shape to the existing S3 exports (no `int`-width pointer/length params — Pitfall 6). Parity gate is the ARM64 manual smoke `windows/manual-smoke-D-07.md` (CI/HUMAN-UAT-deferred per the MSVC link blocker, STATE 17-02). No code mitigation required by disposition. | Documented; smoke checklist present per plan |
| T-17.1-07 | Tampering — concurrent corruption of shared handle (~40 threads) | `aws_sdk_s3::Client` is `Clone+Send+Sync` (`core/ds3-s3/src/client.rs:40-43` `#[derive(Clone)]` over `aws_sdk_s3::Client`); every C-ABI S3 method takes `*const DS3S3Client` (shared `&self`); deliberately NO C#-side mutex (Pitfall 3) to preserve throughput. Rationale documented in the facade doc-comment `DS3DriveS3Client.cs:31-40`. | Documented in code + here |
| T-17.1-SC | Tampering — npm/pip/cargo installs | No new packages introduced this phase (`tech-stack.added: []` in 17.1-03-SUMMARY; RESEARCH §Package Legitimacy Audit: N/A); deps vetted in Phase 15/17. | Documented |

## Unregistered Flags

None. SUMMARY (`17.1-03-SUMMARY.md` `## Threat Flags`) reports: "None — no new security surface beyond the plan's `<threat_model>`." No new attack surface appeared during implementation without a threat mapping.

## Related Review Fixes Confirmed (security-relevant)

- **CR-01** (plaintext secret retained for drive lifetime) — fixed: SHA-256 fingerprint replaces retained `DriveS3Credentials` (`SyncHostedService.cs:43-56,181-186`). Supports T-17.1-09.
- **WR-01** (use-after-free window in wizard-browse dispose) — drain gate added in `DS3SdkService` (REVIEW-FIX). Supports T-17.1-11.
- **WR-02** (native handle leak on teardown throw) — `finally`-block dispose in `StopActiveAsync` (`SyncHostedService.cs:240-251`). Supports T-17.1-11.
- **WR-05** (stale `CurrentAccount` / empty endpoint) — account-scope assertion (`DriveS3CredentialProvider.cs:63-71`). Supports T-17.1-13.
- **WR-04** (skipped): `CancellationHandle` accepted then dropped on download/upload (`DS3DriveS3Client.cs:125,144`). Not a phase-17.1 threat-register item; functional cancellation gap (silent no-op), not a confidentiality/integrity mitigation. Left for human design decision (FFI cancellation vs. signature narrowing). Noted here for traceability — does NOT block this audit.

---

*Auditor: gsd-security-auditor — verifies declared mitigations exist in code; does not scan for new threats.*
