---
phase: 17.1-windows-s3-client-ffi-wiring-sync-enablement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/ds3-ffi/src/c_exports.rs
  - windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs
  - windows/DS3Drive.Tests/DriveSetupViewModelTests.cs
  - windows/manual-smoke-D-07.md
  - .github/workflows/windows-build.yml
autonomous: true
requirements: [WIN-03, WIN-04, WIN-05, WIN-06, WIN-07, WIN-08]
nyquist_compliant: true

must_haves:
  truths:
    - "An FFI export mints a DS3S3Client from S3 credentials and a matching export frees it"
    - "The DLL exposes exactly two new exports (ds3_s3_client_new, ds3_s3_client_destroy)"
    - "The integration test, faked-seam test extension, manual smoke checklist, and CI workflow_dispatch all exist as Wave 0 scaffolds before the consumers are wired"
  artifacts:
    - path: "core/ds3-ffi/src/c_exports.rs"
      provides: "ds3_s3_client_new + ds3_s3_client_destroy C-ABI exports"
      contains: "pub unsafe extern \"C\" fn ds3_s3_client_new"
    - path: "windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs"
      provides: "Integration test scaffold for criteria #1/#2/#4 round-trip through the new handle"
      contains: "Category"
    - path: "windows/manual-smoke-D-07.md"
      provides: "ARM64 first-run manual checklist (criterion #5)"
    - path: ".github/workflows/windows-build.yml"
      provides: "workflow_dispatch entry that runs the integration suite with CUBBIT_TEST_* secrets"
      contains: "workflow_dispatch"
  key_links:
    - from: "core/ds3-ffi/src/c_exports.rs ds3_s3_client_new"
      to: "ds3_s3::DS3S3Client::new"
      via: "direct constructor call (no runtime().block_on)"
      pattern: "DS3S3Client::new"
    - from: "windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs"
      to: "CubbitCredentials.FromEnvironment"
      via: "CUBBIT_TEST_* env gating"
      pattern: "CubbitCredentials"
---

<objective>
Add the two C-ABI exports that mint and free a `DS3S3Client` handle (the missing piece that causes the `AccessViolationException`), rebuild the native DLL, and lay down all Wave 0 test/verification scaffolds so the downstream C# consumer plans have a verification harness ready.

Purpose: Criterion #1 (FFI mints/frees an S3 client handle) is the root fix that unblocks every WIN-03..WIN-08 sync op. The Wave 0 scaffolds (integration test, faked-seam test extension, manual smoke doc, CI dispatch) satisfy the Nyquist contract — no consumer task ships without an automated loop already present.

Output: Two new Rust exports + a rebuilt `ds3_ffi.dll` per RID + four Wave 0 scaffold files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-CONTEXT.md
@.planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-RESEARCH.md
@.planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-PATTERNS.md
@.planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-VALIDATION.md

<interfaces>
<!-- The constructor being exposed (core/ds3-s3/src/client.rs:54). new() returns Self (NOT Result), does NO network I/O — so the export has NO runtime().block_on and the only fallible step is UTF-8 decode. -->
DS3S3Client::new(endpoint: &str, access_key: &str, secret_key: &str, region: Option<&str>) -> Self
  // region.unwrap_or("us-east-1"); force_path_style(true); #[tracing::instrument(skip(access_key, secret_key))]
  // already imported at c_exports.rs:24 via `use ds3_s3::DS3S3Client;`

<!-- Helpers to reuse from core/ds3-ffi/src/c_exports.rs (do NOT rebuild): -->
ffi_str(ptr, len) -> Result<&str>          // c_exports.rs:35 — required strings
ffi_opt_str(ptr, len) -> Result<Option<&str>>  // c_exports.rs:48 — null ptr OR len==0 => None
ffi_guard!(out_error, { ... })             // panic_guard.rs:23 — returns -2 on panic, sets *out_error

<!-- Test fixtures already present (reuse): -->
CubbitCredentials.FromEnvironment()        // Fixtures/CubbitCredentials.cs — CUBBIT_TEST_EMAIL/_PASSWORD/_TENANT/_COORDINATOR_URL
[CollectionDefinition("Integration")]      // Fixtures/CubbitCredentials.cs — serializes credential-gated tests
RequiresCredentialsAttribute               // Fixtures/CubbitCredentials.cs — a FactAttribute that sets Skip when creds absent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ds3_s3_client_new + ds3_s3_client_destroy C-ABI exports and rebuild the DLL</name>
  <files>core/ds3-ffi/src/c_exports.rs</files>
  <read_first>
    - core/ds3-ffi/src/c_exports.rs:127-155 (ds3_authenticate — the mint pattern: ffi_guard! + ffi_str + Box::into_raw into *out_handle. REMOVE the runtime().block_on line for S3 — new() is synchronous.)
    - core/ds3-ffi/src/c_exports.rs:201-205 (ds3_session_destroy — the destroy pattern: null-check + Box::from_raw + drop. Copy byte-for-byte, swap DS3Session → DS3S3Client.)
    - core/ds3-ffi/src/c_exports.rs:35,48 (ffi_str, ffi_opt_str — reuse for the 3 required strings + region)
    - core/ds3-ffi/src/c_exports.rs:24 (the existing `use ds3_s3::DS3S3Client;` import — already present, no new import)
    - core/ds3-ffi/src/c_exports.rs:440 (ds3_list_buckets — confirms the existing S3 exports already take *const DS3S3Client first arg)
    - core/ds3-s3/src/client.rs:54-79 (DS3S3Client::new — the constructor; confirm it returns Self not Result and takes region: Option<&str>)
    - core/ds3-ffi/src/panic_guard.rs:23 (ffi_guard! macro contract)
    - .planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-PATTERNS.md (§"ds3_s3_client_new" and §"ds3_s3_client_destroy" — the proven shapes)
    - core/scripts/build-dll-windows.ps1:51-99 (per-RID DLL build/stage; the rebuild step)
  </read_first>
  <action>
    Add `ds3_s3_client_new(endpoint: *const u8, endpoint_len: usize, access_key: *const u8, access_key_len: usize, secret_key: *const u8, secret_key_len: usize, region: *const u8, region_len: usize, out_handle: *mut *mut DS3S3Client, out_error: *mut i32) -> i32` to `c_exports.rs`, modeled verbatim on `ds3_authenticate` (c_exports.rs:127). Wrap the body in `ffi_guard!(out_error, { … })`. Decode the three required strings with `ffi_str` and `region` with `ffi_opt_str` (null/0 ⇒ None ⇒ Rust defaults to "us-east-1" — this is the D-discretion region value, matching macOS which passes no region). Call `DS3S3Client::new(endpoint, access_key, secret_key, region)` directly — NO `runtime().block_on` (the constructor is synchronous and does no network I/O). `Box::into_raw(Box::new(client))` into `*out_handle`; return `Ok::<i32, DS3Error>(0)`.

    Add `ds3_s3_client_destroy(handle: *mut DS3S3Client)` modeled byte-for-byte on `ds3_session_destroy` (c_exports.rs:201): null-check, `drop(Box::from_raw(handle))`. No `ffi_guard!` needed (no panic surface).

    Rebuild the native DLL per RID via `pwsh -NoProfile -File core/scripts/build-dll-windows.ps1 -BuildProfile release`. Per STATE 17-02, the local dev box lacks the MSVC C++ linker, so the actual link is CI/VM-deferred; if `cargo build -p ds3-ffi` cannot link locally, confirm the Rust source compiles via `cargo check -p ds3-ffi` and document that the staged DLL rebuild runs in `windows-build.yml`. Do NOT add `int`-width pointer/length params (ARM64 parity — Pitfall 6).
  </action>
  <verify>
    <automated>cd core; cargo check -p ds3-ffi (exits 0); then grep -c 'pub unsafe extern "C" fn ds3_s3_client_' core/ds3-ffi/src/c_exports.rs returns 2</automated>
  </verify>
  <acceptance_criteria>
    - core/ds3-ffi/src/c_exports.rs contains `pub unsafe extern "C" fn ds3_s3_client_new`
    - core/ds3-ffi/src/c_exports.rs contains `pub unsafe extern "C" fn ds3_s3_client_destroy`
    - ds3_s3_client_new wraps its body in `ffi_guard!(out_error, { … })` and contains NO `runtime().block_on` (S3 ctor is synchronous)
    - ds3_s3_client_new uses `ffi_opt_str` for the region param (not a custom null sentinel)
    - ds3_s3_client_destroy contains `Box::from_raw` and a null-check, with no ffi_guard!
    - `cargo check -p ds3-ffi` exits 0
  </acceptance_criteria>
  <done>Both exports compile; `cargo check -p ds3-ffi` passes; the export count grep returns 2; DLL rebuild path documented/run.</done>
</task>

<task type="auto">
  <name>Task 2: Lay down Wave 0 verification scaffolds (integration test, faked-seam test extension, manual smoke doc, CI dispatch)</name>
  <files>windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs, windows/DS3Drive.Tests/DriveSetupViewModelTests.cs, windows/manual-smoke-D-07.md, .github/workflows/windows-build.yml</files>
  <read_first>
    - windows/DS3Drive.Tests/DS3SessionTests.cs:26,35-68,90-97 (the whole-file analog for the integration test: [Collection("Integration")], [RequiresCredentials, Trait("Category","Integration")], the zero-handle/double-dispose managed-only lifecycle tests)
    - windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs:47,74,89 (FromEnvironment, [CollectionDefinition("Integration")], RequiresCredentialsAttribute)
    - windows/DS3Drive.Tests/DriveSetupViewModelTests.cs (existing — extend it; find the existing bucket-load test to model the D-06 faked-seam error path on)
    - windows/manual-smoke-D-33.md (Phase 17 checklist — the style/structure to mirror for D-07)
    - .github/workflows/windows-build.yml:11-15,137-146 (the anticipated workflow_dispatch path + the existing Category!=Integration test step to clone with the filter dropped)
    - .planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-VALIDATION.md (§"Per-Task Verification Map" + §"Wave 0 Requirements" — the exact test names + criteria mapped)
    - .planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-PATTERNS.md (§"DS3DriveS3ClientIntegrationTests.cs" and §"manual-smoke-D-07.md")
  </read_first>
  <action>
    Create `windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs` as a `[Collection("Integration")]` sealed class. Add: (a) managed-only lifecycle tests mirroring `DS3SessionTests.NewZeroHandleSession`/`Dispose_TwoTimes_DoesNotThrow`/`EnsureHandle_AfterDispose_ThrowsLoggedOut` — reflect the private `DS3DriveS3Client(IntPtr)` ctor with `IntPtr.Zero` to assert double-`Dispose` is a no-op and the handle guard throws after dispose (these run without the DLL, Category!=Integration); (b) `[RequiresCredentials, Trait("Category","Integration")]` placeholder methods named `Create_FromReconciledApiKey_Lists` and `RoundTrip_ListHeadUploadDownloadDeleteCopy` that use `CubbitCredentials.FromEnvironment()` — these reference `DS3DriveS3Client` (which does not exist until Plan 02). Keep the live-call bodies as `Assert.Skip`/`throw new NotImplementedException` stubs or `#if`-guarded so the file compiles against the Plan-02 facade and is filled in once the facade lands. Mark the Plan-02 dependency in a comment.

    Extend `windows/DS3Drive.Tests/DriveSetupViewModelTests.cs` with a faked-seam test: inject a `DS3SdkService`/`IDS3SessionGateway` substitute (NSubstitute) whose `GetBucketsAsync` throws `DS3S3Exception`, drive the bucket-load command, and assert `CreationError` is set (inline error, no crash, stays on step) and `Buckets` is empty. Add a second faked-seam test where the substitute throws `DS3TransportException` and assert the transport copy is shown. NOTE: per A1 verification against core/ds3-models/src/error.rs, a bad access key surfaces as code 3003 → `DS3TransportException` ("check your connection"), NOT `DS3S3Exception` — so test BOTH branches; do not assume InvalidAccessKeyId lands in the 2001-2099 range.

    Create `windows/manual-smoke-D-07.md` mirroring `manual-smoke-D-33.md` structure: a numbered ARM64 first-run checklist covering login → 2FA → wizard (project → buckets list without AVE → prefix → confirm) → create drive → drive appears in Explorer sidebar → cloud-only file hydrates on open → local edit uploads → remote change appears. State it runs on the Windows 11 ARM64 VM (the cfapi runtime + ARM64 marshalling parity gate).

    Add a `workflow_dispatch:` trigger to `.github/workflows/windows-build.yml` (the header already anticipates it at line 14) and an integration test step (gated `if: env.CUBBIT_TEST_* != ''` and only on `github.event_name == 'workflow_dispatch'`) that runs `dotnet test DS3Drive.Tests --filter "Category=Integration"` with `CUBBIT_TEST_EMAIL/_PASSWORD/_TENANT/_COORDINATOR_URL` env from secrets. Per A4 verification: this colocates the integration test with the facade using the `CUBBIT_TEST_*` secret set (the `DS3_TEST_*` set lives in build.yml for the csharp_harness — do not reuse it here).
  </action>
  <verify>
    <automated>cd windows; dotnet test DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64 (the new faked-seam DriveSetupViewModel tests pass; the integration-trait tests are excluded)</automated>
  </verify>
  <acceptance_criteria>
    - windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs exists and contains `[Trait("Category", "Integration")]` and `[Collection("Integration")]` and references `CubbitCredentials.FromEnvironment`
    - windows/DS3Drive.Tests/DriveSetupViewModelTests.cs contains a test asserting `CreationError` is set when the seam throws `DS3S3Exception` AND a test for the `DS3TransportException` branch
    - windows/manual-smoke-D-07.md exists and lists "buckets list without AVE", hydrate, upload, and remote-change steps on ARM64
    - .github/workflows/windows-build.yml contains `workflow_dispatch` and a step running `--filter "Category=Integration"` gated on `CUBBIT_TEST_*`
    - `dotnet test DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` exits 0
  </acceptance_criteria>
  <done>All four scaffold files exist; the faked-seam DriveSetupViewModel tests pass under the quick (Category!=Integration) loop; the integration test file compiles against the Plan-02 facade contract; CI dispatch + integration step present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C# (managed) → Rust C-ABI | S3 credentials (access key, secret key, endpoint) cross as UTF-8 buffers into `ds3_s3_client_new` |
| Rust → process | The minted `*mut DS3S3Client` raw pointer is owned by C# and must be freed exactly once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-01 | Tampering / DoS | ds3_s3_client_new minting the wrong/garbage handle | mitigate | Mint a real DS3S3Client via Box::into_raw (D-02); the existing S3 exports already take *const DS3S3Client — supplying the correct handle is the structural fix for the AVE |
| T-17.1-02 | DoS | Panic unwinding across the FFI boundary in ds3_s3_client_new | mitigate | Wrap the body in `ffi_guard!` (panic_guard.rs:23) → returns -2, sets *out_error, no unwind |
| T-17.1-03 | Information Disclosure | Secret key leaking into tracing/logs | mitigate | `DS3S3Client::new` is `#[tracing::instrument(skip(access_key, secret_key))]` (client.rs:54) — secrets already excluded; the export adds no logging of the string args |
| T-17.1-04 | Tampering | Use-after-free via region marshalling mismatch on ARM64 | accept | nuint/usize is 64-bit and Cdecl on both win-x64 and win-arm64; keep the export byte-identical in shape to existing S3 exports (Pitfall 6); manual ARM64 smoke (D-07) is the parity gate |
| T-17.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A); all deps vetted in Phase 15/17 |
</threat_model>

<verification>
- `cargo check -p ds3-ffi` exits 0 with both new exports present (export-count grep returns 2).
- `dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` exits 0 (faked-seam DriveSetupViewModel tests pass; integration-trait tests excluded).
- All four Wave 0 scaffold files exist on disk.
- DLL rebuild runs in CI (`windows-build.yml`) — local link is MSVC-blocked per STATE 17-02 (accepted Phase-17 workflow).
</verification>

<success_criteria>
- Criterion #1 (FFI mints + frees a DS3S3Client handle) is structurally satisfied by the two new exports.
- The Wave 0 verification harness (integration test, faked-seam tests, manual smoke, CI dispatch) exists before any consumer is wired, satisfying the Nyquist contract.
- No new external dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/17.1-windows-s3-client-ffi-wiring-sync-enablement/17.1-01-SUMMARY.md` when done
</output>
