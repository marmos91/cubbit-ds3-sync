---
phase: 17.1-windows-s3-client-ffi-wiring-sync-enablement
verified: 2026-06-01T21:30:00Z
status: human_needed
score: 4/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Full first-run smoke: login -> 2FA -> wizard -> create drive -> bidirectional sync round-trip on native ARM64"
    expected: "Login succeeds with 2FA, wizard lists real buckets without AccessViolationException, drive appears in Explorer sidebar, cloud-only file hydrates on open, local edit uploads, remote change appears as placeholder — all on the Windows 11 ARM64 VM with the linked native ds3_ffi.dll"
    why_human: "Requires live hardware (ARM64 Windows 11 VM), a 2FA-enabled Cubbit test account, and the MSVC/clang-built native DLL from CI. The DLL link is blocked on the dev box (STATE 17-02 accepted blocker). The full end-to-end native P/Invoke path, ARM64 calling-convention parity, and cfapi VFS behavior cannot be verified statically."
---

# Phase 17.1: Windows S3-Client FFI Wiring + Sync Enablement — Verification Report

**Phase Goal:** Wire the S3 client across the FFI boundary so the Windows app can list buckets, browse prefixes, and run the cfapi sync engine end-to-end — completing the sync promise of Phase 17.
**Verified:** 2026-06-01T21:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | An FFI export mints a `DS3S3Client` from S3 credentials (`ds3_s3_client_new`) and a matching `ds3_s3_client_destroy` frees it; the C# facade owns and disposes the handle | VERIFIED | `c_exports.rs:417-454`: `pub unsafe extern "C" fn ds3_s3_client_new` wraps body in `ffi_guard!`, decodes 3 required strings via `ffi_str` + region via `ffi_opt_str`, calls `DS3S3Client::new` (no `block_on`), `Box::into_raw` into `*out_handle`. `ds3_s3_client_destroy` does null-check + `Box::from_raw` + drop, no ffi_guard. `DS3DriveS3Client.cs:61-77`: `Create()` calls `DS3Native.ds3_s3_client_new`; `Dispose():225-232` uses `Interlocked.Exchange` + `DS3Native.ds3_s3_client_destroy`. P/Invoke bindings at `DS3Native.cs:111-115`. |
| 2 | S3 credentials are derived from the API-key flow per project/drive (AccessKey/SecretKey + account.endpoint_gateway), not the raw IAM session token | VERIFIED | `DS3AccountInfo.cs:27`: `[property: JsonPropertyName("endpoint_gateway")] string EndpointGateway = ""` now deserialized. `DriveS3CredentialProvider.cs:54,57`: reconciles API key via `LoadOrCreateApiKeyAsync` and reads `_session.EndpointGateway`; guards against empty endpoint. `DS3SdkService.cs:130,143`: wizard browse calls `_session.EndpointGateway` + `DS3DriveS3Client.Create(endpoint, key.AccessKey, key.SecretKey)`. No session token crosses the S3 boundary. |
| 3 | The drive-setup wizard lists real buckets for the selected project and browses prefixes without crashing; failure stays on the Bucket step with inline error and Retry | VERIFIED | `DS3SdkService.cs:66-75`: `GetBucketsAsync` routes through `GetBrowseClientAsync` → `DS3DriveS3Client.ListBuckets()` — no `_session.ListBuckets()` call remains. `DS3SdkService.cs:78-98`: `ListChildPrefixesAsync` routes through `_currentBrowseClient.ListObjects`. `DriveSetupViewModel.cs:97-122`: `LoadBucketsAsync` has try/catch setting `CreationError = MapErrorCopy(ex)` and leaving `CurrentStep` unchanged. `BucketSelectionPage.xaml:41-43`: Retry `ActionButton` bound to `LoadBucketsCommand`. `DriveSetupViewModelTests.cs:221-261`: two faked-seam tests cover DS3S3Exception and DS3TransportException branches. |
| 4 | The cfapi sync engine's list/head/download/upload/delete/copy operations route through the S3-client handle (not the session handle); a created drive hydrates cloud-only files and uploads local changes | VERIFIED | `DriveS3SessionAccess.cs:31-64`: `sealed class DriveS3SessionAccess : IDS3SessionAccess` routes all 6 ops through `_s3` (injected `DS3DriveS3Client`). `SyncHostedService.cs:169-183`: `StartDriveAsync` builds `DS3DriveS3Client.Create(creds.Endpoint, creds.AccessKey, creds.SecretKey)`, wraps it in `DriveS3SessionAccess`, and passes `access` into `UploadQueue`/`CfApiProvider`/`SyncEngine`. `StopActiveAsync:209-230`: disposes `active.S3` LAST after engine + provider stop. `DS3Session.cs` grep: zero matches for `ds3_list_buckets`, `ds3_list_objects`, `ds3_head_object`, `ds3_download_object`, `ds3_upload_object`, `ds3_delete_object`, `ds3_copy_object` — session is auth/session-only. |
| 5 | Full first-run smoke (login -> 2FA -> wizard -> create drive -> bidirectional sync round-trip) completes end-to-end on native ARM64 | HUMAN NEEDED | Cannot verify statically. Requires native DLL (MSVC/clang-gated on this dev box, STATE 17-02), live ARM64 VM, and a 2FA-enabled Cubbit test account. Routed to human verification per the accepted phase gate. |

**Score:** 4/5 truths verified (criterion 5 is human-gated by design)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/ds3-ffi/src/c_exports.rs` | `ds3_s3_client_new` + `ds3_s3_client_destroy` exports | VERIFIED | Both exports present at lines 417 and 450; `ffi_guard!` on new, no `block_on`, `ffi_opt_str` for region |
| `windows/DS3Drive.Core/DS3DriveS3Client.cs` | Per-drive handle-owning S3 facade | VERIFIED | `public sealed class DS3DriveS3Client : IDisposable`; `Create()` calls `ds3_s3_client_new`; `Dispose()` uses Interlocked + `ds3_s3_client_destroy`; all 7 S3 ops + folder-marker pair present |
| `windows/DS3Drive.Core/Generated/DS3Native.cs` | P/Invoke bindings for `ds3_s3_client_new` + `ds3_s3_client_destroy` | VERIFIED | Lines 111-115: both `[DllImport]` entries with `EntryPoint`, `Cdecl`, `ExactSpelling = true`. Export-count guard note bumped to 39. |
| `windows/DS3Drive.Core/Records/DS3AccountInfo.cs` | `EndpointGateway` deserialized from `endpoint_gateway` | VERIFIED | Line 27: `[property: JsonPropertyName("endpoint_gateway")] string EndpointGateway = ""` |
| `windows/DS3Drive.Sync/DriveS3SessionAccess.cs` | Per-drive `IDS3SessionAccess` adapter routing 6 ops through `DS3DriveS3Client` | VERIFIED | Routes `AccountId`, `ListObjects`, `DownloadObject`, `UploadObject`, `DeleteObject`, `CopyObject` through `_s3`; does not own the handle |
| `windows/DS3Drive.Sync/Hosting/SyncHostedService.cs` | Per-drive S3 client build/rebuild/dispose-last lifecycle | VERIFIED | `ActiveDrive` record holds `DS3DriveS3Client S3` + `DriveS3Credentials Creds`; rebuild-on-change via record equality; `StopActiveAsync` disposes `active.S3` last |
| `windows/DS3Drive.App/Services/DriveS3CredentialProvider.cs` | App-layer `IDriveS3CredentialProvider` resolving creds from API-key flow | VERIFIED | Reconciles via `LoadOrCreateApiKeyAsync`; reads `_session.EndpointGateway`; guards against empty endpoint |
| `windows/DS3Drive.ViewModels/Services/DS3SdkService.cs` | Wizard browse routed through per-project `DS3DriveS3Client` | VERIFIED | `GetBucketsAsync` and `ListChildPrefixesAsync` route through `DS3DriveS3Client`; no `_session.ListBuckets()` or `_session.ListObjects()` calls remain |
| `windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs` | Integration test scaffold + live round-trip bodies | VERIFIED | Wave-0 managed-only lifecycle tests + filled-in `Create_FromReconciledApiKey_Lists` and `RoundTrip_ListHeadUploadDownloadDeleteCopy` using `CubbitCredentials.FromEnvironment`; `[RequiresCredentials, Trait("Category", "Integration")]` gated |
| `windows/manual-smoke-D-07.md` | ARM64 first-run manual smoke checklist | VERIFIED | File exists; covers login → 2FA → wizard (buckets without AVE) → prefix → confirm → drive in Explorer → hydrate → upload → remote change |
| `.github/workflows/windows-build.yml` | `workflow_dispatch` trigger + gated integration test step | VERIFIED | Line 38: `workflow_dispatch:`; lines 152-171: gated `if: github.event_name == 'workflow_dispatch'` step running `--filter "Category=Integration"` with `CUBBIT_TEST_*` secrets |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `c_exports.rs ds3_s3_client_new` | `DS3S3Client::new` | direct constructor call (no `block_on`) | WIRED | Line 435: `let client = DS3S3Client::new(endpoint, access_key, secret_key, region);` |
| `DS3DriveS3Client.Create` | `DS3Native.ds3_s3_client_new` | P/Invoke mint into `IntPtr handle` | WIRED | Line 68: `rc = DS3Native.ds3_s3_client_new(...)` |
| `DS3DriveS3Client.Dispose` | `DS3Native.ds3_s3_client_destroy` | `Interlocked.Exchange` single-shot free | WIRED | Lines 227-231 |
| `SyncHostedService.StartDriveAsync` | `DS3DriveS3Client.Create` | build from reconciled creds at host start | WIRED | Line 169: `var s3 = DS3DriveS3Client.Create(creds.Endpoint, creds.AccessKey, creds.SecretKey);` |
| `DriveS3SessionAccess` | `DS3DriveS3Client` | 6-op routing through injected handle | WIRED | All 6 ops dispatch to `_s3.{Op}` |
| `DS3SdkService.GetBucketsAsync` | `DS3DriveS3Client.ListBuckets` | per-project cached client | WIRED | `GetBrowseClientAsync` → `DS3DriveS3Client.Create` → `client.ListBuckets()` |
| `DriveSetupViewModel.LoadBucketsAsync` (Retry) | `LoadBucketsCommand` | `ActionButton` in `BucketSelectionPage.xaml` | WIRED | `BucketSelectionPage.xaml:42`: `Command="{x:Bind ViewModel.LoadBucketsCommand, Mode=OneWay}"` |
| `App.xaml.cs DI` | `IDriveS3CredentialProvider` registered | `AddSingleton<IDriveS3CredentialProvider, DriveS3CredentialProvider>()` | WIRED | Line 145 in App.xaml.cs; shared `AuthenticationService` no longer registered as `IDS3SessionAccess` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `DS3DriveS3Client.ListBuckets` | `DS3Bucket[]` from Rust FFI | `DS3Native.ds3_list_buckets` → native `ds3_ffi.dll` | Yes (in CI — DLL gated locally) | WIRED (native call present; live data requires linked DLL) |
| `DS3SdkService.GetBucketsAsync` | `IReadOnlyList<DS3Bucket>` | `DS3DriveS3Client.ListBuckets()` via `GetBrowseClientAsync` | Yes — no hardcoded empty return | FLOWING |
| `DriveS3CredentialProvider.GetCredentialsAsync` | `DriveS3Credentials` | `LoadOrCreateApiKeyAsync` + `_session.EndpointGateway` | Yes — API-key reconcile + gateway from deserialized `DS3AccountInfo` | FLOWING |
| `SyncHostedService.StartDriveAsync` | `DS3DriveS3Client` | `IDriveS3CredentialProvider.GetCredentialsAsync` → `DS3DriveS3Client.Create` | Yes — credentials from OS-sealed Credential Store + endpoint from account info | FLOWING |

### Behavioral Spot-Checks

Skipped. The entry point requires the native `ds3_ffi.dll` (MSVC/clang-gated on this dev box, STATE 17-02). The .NET managed solution builds and the non-integration test suite passes 145/145 (verified by the orchestrator). Live P/Invoke-dependent behavior runs in `windows-build.yml` CI on `windows-latest`.

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` probes declared or present for this phase. The phase uses `dotnet test --filter "Category!=Integration"` (run by orchestrator, 145/145 pass) and `workflow_dispatch` CI for the credential-gated integration suite.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WIN-03 | 17.1-01, 17.1-02, 17.1-03 | Drive appears in Explorer sidebar via cfapi sync root registration | SATISFIED | Cfapi sync ops now route through real `DS3DriveS3Client` handle, unblocking the sync engine that registers the sync root. Structural fix for the AVE is in place. |
| WIN-04 | 17.1-01, 17.1-02, 17.1-03 | User can open files that hydrate on-demand (FETCH_DATA via Rust download) | SATISFIED | `DriveS3SessionAccess.DownloadObject` routes through `_s3.DownloadObject`; integration test exercises download round-trip. |
| WIN-05 | 17.1-01, 17.1-02, 17.1-03 | User can save/create files that upload to S3 | SATISFIED | `DriveS3SessionAccess.UploadObject` routes through `_s3.UploadObject`; integration test exercises upload. |
| WIN-06 | 17.1-01, 17.1-02, 17.1-03 | Remote changes sync to local placeholders via periodic polling and diff | SATISFIED | `ListObjects` and `DeleteObject`/`CopyObject` in `DriveS3SessionAccess` route through the real S3 handle; the sync engine's diff/poll path now has a functional S3 client. |

Note: REQUIREMENTS.md traceability table shows WIN-03..WIN-08 as "Phase 17 / Complete". This is not a code gap — Phase 17 shipped the shell structure and Phase 17.1 provides the missing S3-client FFI wiring that makes those features functional at runtime. The traceability table was last updated at Phase 17 creation and predates the 17.1 sub-phase; it reflects the original allocation, not the actual wiring work done here.

WIN-07 (tray status) and WIN-08 (hydration progress) appear in Plan 01/03 frontmatter requirements lists but are not primary success criteria for this phase. They are noted as "becoming reachable downstream of the now-functional sync ops" (17.1-03 SUMMARY). No new code for WIN-07/WIN-08 was added in 17.1; they remain functional as wired in Phase 17, now unblocked by the S3 handle fix.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `DS3DriveS3ClientIntegrationTests.cs` | 79-127 | Reflection-probed managed-only lifecycle tests: `NewZeroHandleClient()` resolves `DS3DriveS3Client` by `Assembly.GetType` and `no-op`s if absent | Info | Intentional Wave-0 scaffold pattern (plan 01 decision). The facade is now present (plan 02), so both tests exercise the real type and no longer no-op. Not a stub. |
| `DS3DriveS3Client.cs` | 125 | `_ = cancel;` (CancellationHandle ignored in DownloadObject/UploadObject) | Warning | Cancellation token passed to cfapi engine but not forwarded into the FFI cancellation handle. The FFI supports cancellation via `CancellationHandle` but the current wiring discards it. This is a known deferred item — the cancel param is in the interface signature for future use. Not a correctness blocker for the phase goal. |

No `TBD`, `FIXME`, or `XXX` debt markers found in phase-modified files. No unreferenced stubs that flow to user-visible output.

### Human Verification Required

#### 1. ARM64 First-Run End-to-End Smoke (criterion #5)

**Test:** Follow `windows/manual-smoke-D-07.md` on the Windows 11 ARM64 VM with the CI-built `ds3_ffi.dll` staged (run after a green `windows-build.yml` `workflow_dispatch`). Steps: launch app → log in (email + password) → enter 2FA code → observe project list loads → select a project → observe bucket list loads without `AccessViolationException` → select a bucket → select a prefix (or root) → complete wizard → verify drive appears in Explorer sidebar → open a cloud-only file and verify it hydrates (Downloads trigger) → create/edit a local file and verify it uploads → wait for remote change and verify placeholder appears.

**Expected:** All steps complete without crash. Bucket list shows real Cubbit buckets. Drive appears in Explorer sidebar. Cloud-only file hydrates on open (FETCH_DATA). Local edit triggers upload (NOTIFY_FILE_CLOSE_COMPLETION). Remote change appears as a dehydrated placeholder after the next poll interval.

**Why human:** Requires the native `ds3_ffi.dll` (MSVC/clang-linked in CI on `windows-latest`; the dev box lacks the MSVC C++ / clang toolchain, STATE 17-02 accepted blocker). Also requires a live Cubbit test account with 2FA enabled, the ARM64 VM, and real Cubbit S3 connectivity. The cfapi VFS integration (Explorer sidebar, hydration progress, upload trigger) cannot be verified statically or via managed unit tests.

### Gaps Summary

No code gaps found. All four statically-verifiable success criteria are satisfied in the codebase. Criterion 5 (end-to-end ARM64 smoke) is human-gated by design — it requires the native DLL and live hardware, as documented in the accepted Phase-17 workflow (STATE 17-02).

---

_Verified: 2026-06-01T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
