---
phase: 17.1
slug: windows-s3-client-ffi-wiring-sync-enablement
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-01
---

# Phase 17.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `17.1-RESEARCH.md` §"Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (+ NSubstitute) on .NET 8; `xunit.runner.json parallelizeAssembly=false` |
| **Config file** | `windows/DS3Drive.Tests/xunit.runner.json` |
| **Quick run command** | `dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` |
| **Full suite command** | `dotnet test windows/DS3Drive.Tests /p:Platform=x64` (drops the filter; `[RequiresCredentials]` self-skips when `CUBBIT_TEST_*` unset) |
| **Estimated runtime** | ~30s quick (faked seams); integration bounded by live Cubbit S3 round-trips |

---

## Sampling Rate

- **After every task commit:** Run `dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` (headless, faked seams)
- **After every plan wave:** Rebuild `ds3_ffi.dll` in CI + run the integration suite via `workflow_dispatch` (live `CUBBIT_TEST_*` creds)
- **Before `/gsd:verify-work`:** Integration suite green + ARM64 manual smoke signed off
- **Max feedback latency:** ~30s for the quick (faked-seam) loop

---

## Per-Task Verification Map

| Criterion / Req | Behavior | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| Criterion #1 | `ds3_s3_client_new` mints + `ds3_s3_client_destroy` frees a handle; double-dispose is a no-op | Wrong-type deref / double-free | `ffi_guard!` returns -2 (no unwind); `Interlocked.Exchange` single-shot dispose | Integration | `dotnet test --filter "FullyQualifiedName~DS3DriveS3Client"` | ❌ W0 (`DS3DriveS3ClientIntegrationTests.cs`) | ⬜ pending |
| Criterion #2 | Creds derived from API-key flow (AccessKey/SecretKey/endpoint), not session token | Secret leakage | Secret only in Credential Manager; transits FFI as UTF-8, not retained C#-side | Integration | same class, `Create_FromReconciledApiKey_Lists` | ❌ W0 | ⬜ pending |
| Criterion #3 / WIN-03 | Wizard lists real buckets + browses prefixes without crashing | — | — | Unit (faked seam) + Integration | `dotnet test --filter "FullyQualifiedName~DriveSetupViewModel"` (extend) + integration `ListBuckets`/`ListObjects` | ⚠️ Extend `DriveSetupViewModelTests.cs` | ⬜ pending |
| Criterion #4 / WIN-04..06 | list/head/download/upload/delete/copy round-trip through S3 handle | Concurrent corruption | `aws_sdk_s3::Client` is `Send+Sync`, shared `&self`, no C#-side mutex | Integration | `DS3DriveS3ClientIntegrationTests.RoundTrip_*` | ❌ W0 | ⬜ pending |
| Criterion #5 | Full first-run end-to-end on native ARM64 | ARM64 marshalling parity | cfapi runtime + ARM64 marshalling gate | Manual smoke | `windows/manual-smoke-D-07.md` checklist | ❌ W0 | ⬜ pending |
| D-06 | Bad-key bucket listing → `DS3S3Exception` → inline error + Retry, no crash | Info disclosure | `MapErrorCopy` shows fixed copy, never server text | Unit (faked seam throws `DS3S3Exception`) | extend `DriveSetupViewModelTests.cs` | ⚠️ Extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs` — covers criteria #1, #2, #4 (round-trip through the new handle); `[Trait("Category","Integration")] [RequiresCredentials] [Collection("Integration")]` (serializes like `DS3SessionTests.cs:26`)
- [ ] Extend `windows/DS3Drive.Tests/DriveSetupViewModelTests.cs` — D-06 inline-error + Retry path with a faked seam throwing `DS3S3Exception`
- [ ] `windows/manual-smoke-D-07.md` — ARM64 first-run checklist (criterion #5), in the style of Phase 17's `manual-smoke-D-33.md`
- [ ] CI: `workflow_dispatch` entry (or reuse the anticipated path at `windows-build.yml:14`) to run `windows/DS3Drive.Tests` integration with `CUBBIT_TEST_*` secrets
- [ ] No framework install needed — xUnit/NSubstitute already present.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| Full first-run round-trip on native ARM64 | #5 | Requires the cfapi runtime + Explorer shell integration + ARM64 marshalling, not reproducible in headless CI | `windows/manual-smoke-D-07.md`: login → 2FA → wizard (project → buckets list without AVE → prefix → confirm) → create drive → drive in Explorer sidebar → cloud-only file hydrates on open → local edit uploads → remote change appears. Run on Windows 11 ARM64 VM. |

---

## Validation Sign-Off

- [ ] All criteria have `<automated>` verify or Wave 0 dependencies (criterion #5 is manual-only by nature)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick loop)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
