---
phase: 17.1
slug: windows-s3-client-ffi-wiring-sync-enablement
status: validated-partial
nyquist_compliant: false
nyquist_partial: true
wave_0_complete: true
created: 2026-06-01
updated: 2026-06-02
---

# Phase 17.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `17.1-RESEARCH.md` §"Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (+ NSubstitute) on .NET 8; `xunit.runner.json parallelizeAssembly=false` |
| **Config file** | `windows/DS3Drive.Tests/xunit.runner.json` |
| **Quick run command** | `dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` |
| **Full suite command** | `dotnet test windows/DS3Drive.Tests /p:Platform=x64` (drops the filter; `[RequiresCredentials]` self-skips when `CUBBIT_TEST_*` unset) |
| **Estimated runtime** | ~30s quick (faked seams); integration bounded by live Cubbit S3 round-trips |

---

## Sampling Rate

- **After every task commit:** Run `dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" /p:Platform=x64` (headless, faked seams)
- **After every plan wave:** Rebuild `ds3_ffi.dll` in CI + run the integration suite via `workflow_dispatch` (live `CUBBIT_TEST_*` creds)
- **Before `/gsd:verify-work`:** Integration suite green + ARM64 manual smoke signed off
- **Max feedback latency:** ~30s for the quick (faked-seam) loop

---

## Per-Task Verification Map

| Criterion / Req | Behavior | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| Criterion #1 | `ds3_s3_client_new` mints + `ds3_s3_client_destroy` frees a handle; double-dispose is a no-op | Wrong-type deref / double-free | `ffi_guard!` returns -2 (no unwind); `Interlocked.Exchange` single-shot dispose | Unit (managed lifecycle) + Integration (live mint) | `dotnet test --filter "FullyQualifiedName~DS3DriveS3Client"` — `Dispose_TwoTimes_DoesNotThrow`, `S3Method_AfterDispose_Throws_NotAccessViolation` (fast) | ✅ `DS3DriveS3ClientIntegrationTests.cs` | ✅ green (managed lifecycle, fast loop) |
| Criterion #2 | Creds derived from API-key flow (AccessKey/SecretKey/endpoint), not session token | Secret leakage | Secret only in Credential Manager; transits FFI as UTF-8, not retained C#-side | Integration (live S3) | `Create_FromReconciledApiKey_Lists` `[RequiresCredentials, Integration]` | ✅ test exists | 🔶 CI-gated (self-skips locally; runs green in CI integration job) |
| Criterion #3 / WIN-03 | Wizard lists real buckets + browses prefixes without crashing | — | — | Unit (faked seam) + Integration | `LoadBucketsAsync_OnSuccess_PopulatesBuckets_StaysOnBucket_NoError` (happy path, added 2026-06-02) + `SelectProject_…TriggersBucketLoad` (fast) + integration `ListBuckets`/`ListObjects` | ✅ `DriveSetupViewModelTests.cs` | ✅ green (fast loop) |
| Criterion #4 / WIN-04..06 | list/head/download/upload/delete/copy round-trip through S3 handle | Concurrent corruption | `aws_sdk_s3::Client` is `Send+Sync`, shared `&self`, no C#-side mutex | Integration (live S3) | `DS3DriveS3ClientIntegrationTests.RoundTrip_ListHeadUploadDownloadDeleteCopy` `[RequiresCredentials, Integration]` | ✅ test exists | 🔶 CI-gated (self-skips locally; runs green in CI integration job) |
| Criterion #5 | Full first-run end-to-end on native ARM64 | ARM64 marshalling parity | cfapi runtime + ARM64 marshalling gate | Manual smoke | `windows/manual-smoke-D-07.md` checklist | ✅ checklist present | 🔲 manual-only (tracked in `17.1-HUMAN-UAT.md`) |
| D-06 | Bad-key bucket listing → `DS3S3Exception`/`DS3TransportException` → inline error + Retry, no crash | Info disclosure | `MapErrorCopy` shows fixed copy, never server text | Unit (faked seam throws) | `LoadBucketsAsync_OnS3Exception_…LeavesBucketsEmpty`, `LoadBucketsAsync_OnTransportException_…` (fast) | ✅ `DriveSetupViewModelTests.cs` | ✅ green (fast loop) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · 🔶 CI-gated · 🔲 manual-only*

**Headless quick-loop suite:** 146/146 pass (`dotnet test windows/DS3Drive.Tests --filter "Category!=Integration"`).

---

## Wave 0 Requirements

- [x] `windows/DS3Drive.Tests/DS3DriveS3ClientIntegrationTests.cs` — covers criteria #1 (managed lifecycle, fast), #2, #4 (live round-trip through the new handle); `[Trait("Category","Integration")] [RequiresCredentials]`
- [x] Extend `windows/DS3Drive.Tests/DriveSetupViewModelTests.cs` — D-06 inline-error + Retry path with faked seams throwing `DS3S3Exception` + `DS3TransportException`; plus happy-path bucket-population test (WIN-03, added in validate-phase 2026-06-02)
- [x] `windows/manual-smoke-D-07.md` — ARM64 first-run checklist (criterion #5)
- [x] CI: `workflow_dispatch` entry in `windows-build.yml` to run `windows/DS3Drive.Tests` integration with `CUBBIT_TEST_*` secrets
- [x] No framework install needed — xUnit/NSubstitute already present.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| Full first-run round-trip on native ARM64 | #5 | Requires the cfapi runtime + Explorer shell integration + ARM64 marshalling, not reproducible in headless CI | `windows/manual-smoke-D-07.md`: login → 2FA → wizard (project → buckets list without AVE → prefix → confirm) → create drive → drive in Explorer sidebar → cloud-only file hydrates on open → local edit uploads → remote change appears. Run on Windows 11 ARM64 VM. |

---

## Validation Sign-Off

- [x] All criteria have `<automated>` verify or Wave 0 dependencies (criteria #2/#4 are live-S3 integration/CI-gated; criterion #5 is manual-only by nature)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s (quick loop — 146 tests ~10s)
- [ ] `nyquist_compliant: true` — **partial**: 3 criteria + D-06 fully automated in the fast loop; criteria #2/#4 are CI-gated live-S3 integration and criterion #5 is ARM64 manual (both inherent, not headless-fillable)

**Approval:** validated-partial — all headless-fillable gaps closed; remaining items are integration/manual by nature.

---

## Validation Audit 2026-06-02
| Metric | Count |
|--------|-------|
| Gaps found | 1 (headless-fillable) |
| Resolved | 1 (WIN-03 happy-path bucket population) |
| Escalated | 0 |
| CI-gated (inherent) | 2 (criteria #2, #4 — live S3) |
| Manual-only (inherent) | 1 (criterion #5 — ARM64 smoke) |

Added `LoadBucketsAsync_OnSuccess_PopulatesBuckets_StaysOnBucket_NoError` to `DriveSetupViewModelTests.cs`. Headless suite: 145 → 146 pass.
