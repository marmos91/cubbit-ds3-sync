# Phase 17.3 · Plan 01 (Wave 0): Local build enablement + enumeration test harness

**Goal:** Unblock a local Windows build/test loop (resolving STATE.md blocker 17-02) and land the reusable large-listing test fixture that Waves 1–3 assert against. No production behavior change.

**Depends on:** nothing (first wave).
**Decisions:** Wave-0 provisioning (CONTEXT §Sign-off #3); enables D-07 verification.
**Criteria touched:** none directly — infrastructure for all three.

## Context

17.1 built Windows only on `windows-latest` CI because the dev box lacked the MSVC C++ workload and Rust (STATE.md blocker **17-02**). 17.3 changes correctness-critical sync code, so a local build+test loop is required. The toolchain is now provisioned (see Task 1). The rest of the phase needs a deterministic way to exercise >2000-key paginated listings without live S3 — a fake `IDS3SessionAccess` that returns scripted pages.

## Tasks

1. **Record + verify the local toolchain** (already installed 2026-07-07):
   - Visual Studio 2026 `Microsoft.VisualStudio.Workload.NativeDesktop` (MSVC `link.exe` at `VC\Tools\MSVC\14.51.36231\bin\Hostx64\x64`).
   - Rust `stable-x86_64-pc-windows-msvc` (rustc/cargo 1.96.1), target `x86_64-pc-windows-msvc`.
   - .NET SDK 10.0.301 + WindowsDesktop runtime 8.0.28 (builds the `net8.0-windows10.0.19041.0` TFM).
   - Verify end-to-end: `cargo build -p ds3-ffi` (links the cdylib), then stage the DLL and `dotnet build windows/DS3Drive.sln` + `dotnet test --filter Category!=Integration`. Mirror the CI staging in `.github/workflows/windows-build.yml` (build the DLL once, then `DS3SkipRustCore=true` / `DS3Native` DLL copy — decision [17-03]).
2. **Resolve STATE.md blocker 17-02** — mark it cleared with the provisioning note; update the STATE `Blockers` section.
3. **Add `FakePagedSession`** to `windows/DS3Drive.Tests` (or the existing test seam project): an `IDS3SessionAccess` test double whose `ListObjectsListing(bucket, prefix, delim, token)` returns scripted `DS3ObjectListing` pages driven by an in-memory `IReadOnlyList<DS3Object>` + `CommonPrefixes`, honoring `max_keys`/page size and round-tripping an opaque continuation token (page index). This is the shared fixture for the Wave-1 pagination test and Wave-2 idempotency tests. Reuse the existing `DS3ObjectListing` record (`DS3Drive.Core/Records`) and `IDS3SessionAccess` seam.
4. **Scaffold `manual-smoke-17.3.md`** — a large-prefix checklist (thousands of objects) in the style of `windows/manual-smoke-D-07.md`: incremental appearance, no Explorer freeze, tray progress visible, no ghosts after a remote delete. Filled in during Wave 4.

## Tests
- Toolchain smoke: `cargo build -p ds3-ffi` exit 0 + `ds3_ffi.dll` present; `dotnet test --filter Category!=Integration` green locally.
- `FakePagedSession` self-test: scripting 2500 objects across a 2000 page size yields two pages with a valid continuation token and a correct final `IsTruncated=false`.

## Done when
- Local `dotnet test` (non-integration) runs green on this machine.
- `FakePagedSession` is committed and referenced by at least one passing test.
- STATE.md blocker 17-02 marked resolved.
