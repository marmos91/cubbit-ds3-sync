# Phase 17.3 · Plan 05 (Wave 4): Integration tests, manual smoke, verification & docs

**Goal:** Prove the phase against real Cubbit S3 at scale, complete the manual ARM64-style smoke, and close out the GSD artifacts (VALIDATION / VERIFICATION / SUMMARY, STATE, ROADMAP).

**Depends on:** Waves 1–3.
**Decisions:** D-07.
**Criteria touched:** all three (verification).

## Context

Matches the 17.1 two-layer verification (D-07 there): automated `Category=Integration` tests gated on Cubbit secrets like the existing "C# P/Invoke Tests (Windows)" / "Integration Tests (DS3)" jobs, plus a manual smoke checklist for the cfapi/Explorer behavior that is not unit-testable.

## Tasks

1. **Integration tests (`Category=Integration`, `RequiresCredentials`)** against a real Cubbit bucket seeded with a large prefix (>2000 objects):
   - Pagination: full enumeration returns every object; nothing pruned (D-01).
   - Idempotency: enumerate twice → identical placeholder set, no duplicates (D-02).
   - Ghost removal: delete a remote object → placeholder + on-disk entry gone; dirty local edit preserved (D-03).
   - Anchor: re-poll with no remote change performs no writes (D-06).
   - Keep them out of untrusted PR runs via the existing `Category!=Integration` CI filter + `github.actor != 'dependabot[bot]'` guard (added to `build.yml` this session).
2. **Execute `manual-smoke-17.3.md`** on the ARM64 dev VM (or this x64 box): register a large-prefix drive; observe incremental placeholder appearance, no Explorer freeze, tray progress readout, correct behavior after a remote delete and a remote add. Record results.
3. **CI:** ensure `windows-build.yml` still builds + runs the non-integration suite; add the new tests to the run. Confirm the phase is green on `windows-latest` as well as locally.
4. **GSD close-out docs:** `17.3-VALIDATION.md` (criteria → evidence), `17.3-VERIFICATION.md`, `17.3-0N-SUMMARY.md` per plan; update `STATE.md` (progress, 17-02 resolved, decisions log for D-01..D-06) and `ROADMAP.md` (Phase 17.3 → In Progress/Complete, plans count 5/5).

## Tests
- Full local `dotnet test` (non-integration) green; `Category=Integration` green against Cubbit with secrets.
- Manual smoke checklist signed off.

## Done when
- All three success criteria have automated + manual evidence in VALIDATION.
- STATE/ROADMAP reflect 17.3 completion; docs committed.
