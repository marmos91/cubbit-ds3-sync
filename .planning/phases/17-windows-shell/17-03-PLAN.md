---
phase: 17-windows-shell
plan: 03
type: execute
wave: 0
depends_on: [17-02]
files_modified:
  - .github/workflows/windows-build.yml
  - windows/DS3Drive.Tests/xunit.runner.json
  - windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs
  - windows/manual-smoke-D-33.md
autonomous: true
requirements: [WIN-09]
tags: [ci, test-harness, fixtures, smoke-test]
must_haves:
  truths:
    - "windows-build.yml runs on windows-latest, builds Rust ds3_ffi.dll for x64, restores + builds the .NET solution, and runs non-integration tests"
    - "xunit.runner.json disables assembly parallelization so cfapi tests don't interleave"
    - "CubbitCredentials fixture reads env vars (CUBBIT_TEST_EMAIL, CUBBIT_TEST_PASSWORD, CUBBIT_TEST_TENANT, CUBBIT_TEST_COORDINATOR_URL) and skips integration tests if any are missing"
    - "manual-smoke-D-33.md enumerates every smoke item from CONTEXT D-33 with a pass/fail checkbox per macOS APPLE-05 parity"
  artifacts:
    - path: ".github/workflows/windows-build.yml"
      provides: "CI pipeline for Windows builds"
      contains: "windows-latest, cargo build --target x86_64-pc-windows-msvc, dotnet test"
    - path: "windows/DS3Drive.Tests/xunit.runner.json"
      provides: "xUnit test runner configuration"
      contains: "parallelizeAssembly"
    - path: "windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs"
      provides: "Env-var-backed integration test fixture"
      exports: ["CubbitCredentials"]
    - path: "windows/manual-smoke-D-33.md"
      provides: "Manual smoke checklist matching CONTEXT D-33"
  key_links:
    - from: ".github/workflows/windows-build.yml"
      to: "core/scripts/build-dll-windows.ps1"
      via: "shell: pwsh"
      pattern: "build-dll-windows"
    - from: ".github/workflows/windows-build.yml"
      to: "windows/DS3Drive.sln"
      via: "dotnet build"
      pattern: "dotnet (build|test)"
---

<objective>
Stand up CI for the Windows shell, the xUnit test harness configuration, the integration-test credential fixture, and the manual smoke checklist that gates phase sign-off. These are the testing scaffolds that every subsequent plan's `<verify>` blocks reference.

Purpose: Per VALIDATION.md Wave 0 Requirements, no automated tests can run until the test project has a runner config, fixtures, and a CI pipeline. The smoke checklist (D-33) mirrors macOS APPLE-05 and is the last gate before `/gsd:verify-work`.

Output: A `windows-build.yml` workflow that builds the Rust DLL + .NET solution + runs non-integration tests on every push touching `windows/**` or `core/**`; xUnit runner config that disables assembly parallelization (cfapi tests can't interleave per VALIDATION); env-var-backed credential fixture; manual smoke checklist as a Markdown table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-windows-shell/17-CONTEXT.md
@.planning/phases/17-windows-shell/17-RESEARCH.md
@.planning/phases/17-windows-shell/17-VALIDATION.md
@.planning/phases/17-windows-shell/17-02-PLAN.md
@.planning/phases/17-windows-shell/17-01-PLAN.md
@.github/workflows/

<interfaces>
<!-- Test fixture contract (PATTERNS §1.4) -->
public sealed class CubbitCredentials
{
    public string Email { get; }
    public string Password { get; }
    public string? Tenant { get; }
    public string CoordinatorUrl { get; }
    public bool IsAvailable { get; }   // true iff all required env vars set
    public static CubbitCredentials FromEnvironment();
}

<!-- xunit.runner.json schema (subset) -->
{
  "$schema": "https://xunit.net/schema/v3/xunit.runner.schema.json",
  "parallelizeAssembly": false,
  "parallelizeTestCollections": false,
  "longRunningTestSeconds": 60
}

<!-- CI workflow contract (RESEARCH §Validation Architecture, VALIDATION §Sampling Rate) -->
on: push to gsd/phase-17-windows-shell, main; pull_request to main
runs-on: windows-latest
steps:
  1. checkout (with LFS for assets)
  2. setup-dotnet @ 8.0.x
  3. setup-rust (install x86_64-pc-windows-msvc target)
  4. cargo build -p ds3-ffi --target x86_64-pc-windows-msvc
  5. copy DLL into windows/DS3Drive.Core/runtimes/win-x64/native/
  6. dotnet restore windows/DS3Drive.sln
  7. dotnet build windows/DS3Drive.sln --configuration Release --no-restore
  8. dotnet test windows/DS3Drive.Tests --filter "Category!=Integration" --no-build
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author windows-build.yml CI pipeline</name>
  <files>.github/workflows/windows-build.yml</files>
  <read_first>
    - .github/workflows/ (existing workflows — match naming, secret usage, concurrency settings)
    - .planning/phases/17-windows-shell/17-RESEARCH.md §"Validation Architecture" + §"Environment Availability"
    - .planning/phases/17-windows-shell/17-VALIDATION.md §"Sampling Rate"
    - .planning/phases/17-windows-shell/17-01-PLAN.md (build-dll-windows.ps1 path)
  </read_first>
  <action>
    Create `.github/workflows/windows-build.yml`. Trigger: push to `gsd/phase-17-windows-shell` and `main`, plus pull_request to `main`, plus paths filter `["windows/**", "core/**", ".github/workflows/windows-build.yml"]`.

    Single job `build`, `runs-on: windows-latest`, steps:
    1. `actions/checkout@v4` with `lfs: true`.
    2. `actions/setup-dotnet@v4` with `dotnet-version: 8.0.x`.
    3. `dtolnay/rust-toolchain@stable` with `targets: x86_64-pc-windows-msvc`.
    4. Cache cargo registry + target dir (path: `core/target`, key on `core/Cargo.lock` + `runner.os`).
    5. `pwsh -File core/scripts/build-dll-windows.ps1 -profile release` (script from Plan 01).
    6. `dotnet restore windows/DS3Drive.sln`.
    7. `dotnet build windows/DS3Drive.sln --configuration Release --no-restore /p:RuntimeIdentifier=win-x64`.
    8. `dotnet test windows/DS3Drive.Tests --no-build --configuration Release --filter "Category!=Integration" --logger "trx" --results-directory TestResults`.
    9. `actions/upload-artifact@v4` for `TestResults` on failure.

    `concurrency: group: windows-build-${{ github.ref }}, cancel-in-progress: true`.

    `permissions: contents: read`.

    Comment block at top citing CONTEXT D-32 (Windows 11 ARM64 dev VM is for manual smoke; CI runs x64 only).
  </action>
  <verify>
    <automated>test -f .github/workflows/windows-build.yml &amp;&amp; grep -c "windows-latest" .github/workflows/windows-build.yml &amp;&amp; grep -c "x86_64-pc-windows-msvc" .github/workflows/windows-build.yml</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -c "windows-latest" .github/workflows/windows-build.yml` ≥ 1.
    - `grep -c "x86_64-pc-windows-msvc" .github/workflows/windows-build.yml` ≥ 1.
    - `grep -c "build-dll-windows.ps1" .github/workflows/windows-build.yml` ≥ 1.
    - `grep -c "dotnet test" .github/workflows/windows-build.yml` ≥ 1.
    - `grep -c "Category!=Integration" .github/workflows/windows-build.yml` ≥ 1 (non-integration filter per VALIDATION sampling rate).
    - `grep -c "lfs: true" .github/workflows/windows-build.yml` ≥ 1.
    - `grep -c "concurrency" .github/workflows/windows-build.yml` ≥ 1.
    - YAML lints clean: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/windows-build.yml'))"` exits 0.
  </acceptance_criteria>
  <done>CI pipeline declared; on the next push CI will pick it up; non-integration tests gate every PR touching windows/ or core/.</done>
</task>

<task type="auto">
  <name>Task 2: Create xUnit runner config + CubbitCredentials fixture</name>
  <files>windows/DS3Drive.Tests/xunit.runner.json, windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs</files>
  <read_first>
    - .planning/phases/17-windows-shell/17-VALIDATION.md §"Wave 0 Requirements" (parallelizeAssembly=false)
    - .planning/phases/17-windows-shell/17-PATTERNS.md §1.4 + §2.18 (test patterns)
    - apple/DS3DriveProviderTests/TestFixtures.swift (env-var-backed credential pattern)
  </read_first>
  <action>
    Create `windows/DS3Drive.Tests/xunit.runner.json` with `parallelizeAssembly: false`, `parallelizeTestCollections: false`, `longRunningTestSeconds: 60`, `maxParallelThreads: 1`. Add `<None Update="xunit.runner.json"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>` to the Tests csproj (or instruct in a code comment that 17-02 already covered it — verify with grep).

    Create `windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs` exposing `public sealed class CubbitCredentials` with the contract from `<interfaces>`. `FromEnvironment()` reads `CUBBIT_TEST_EMAIL`, `CUBBIT_TEST_PASSWORD`, `CUBBIT_TEST_TENANT` (optional, may be null), `CUBBIT_TEST_COORDINATOR_URL` (defaults to `https://api.eu00wi.cubbit.services` if unset). `IsAvailable` returns true iff Email and Password are non-empty.

    Add `[CollectionDefinition("Integration")] public class IntegrationCollection { }` and a `public class RequiresCredentialsAttribute : FactAttribute` whose ctor checks `CubbitCredentials.FromEnvironment().IsAvailable` and sets `Skip = "Cubbit credentials not set"` if false. Integration tests use `[RequiresCredentials, Trait("Category", "Integration")]` so the CI `Category!=Integration` filter skips them on PR builds but full-suite (with secrets) runs them.

    Reference `apple/DS3DriveProviderTests/TestFixtures.swift` pattern — same env-var names mapped from Apple convention.
  </action>
  <verify>
    <automated>test -f windows/DS3Drive.Tests/xunit.runner.json &amp;&amp; test -f windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs &amp;&amp; grep -c "parallelizeAssembly" windows/DS3Drive.Tests/xunit.runner.json &amp;&amp; grep -c "CUBBIT_TEST_EMAIL" windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist.
    - `grep -c "\"parallelizeAssembly\": false" windows/DS3Drive.Tests/xunit.runner.json` ≥ 1.
    - `grep -c "\"parallelizeTestCollections\": false" windows/DS3Drive.Tests/xunit.runner.json` ≥ 1.
    - `grep -c "CUBBIT_TEST_EMAIL\|CUBBIT_TEST_PASSWORD\|CUBBIT_TEST_COORDINATOR_URL" windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs` ≥ 3.
    - `grep -c "RequiresCredentialsAttribute\|public sealed class CubbitCredentials" windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs` ≥ 2.
    - `grep -c "api.eu00wi.cubbit.services" windows/DS3Drive.Tests/Fixtures/CubbitCredentials.cs` ≥ 1 (default coordinator URL).
  </acceptance_criteria>
  <done>Test harness ready for tests to be written in subsequent plans; CI's `Category!=Integration` filter correctly excludes credential-gated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Author manual-smoke-D-33.md checklist</name>
  <files>windows/manual-smoke-D-33.md</files>
  <read_first>
    - .planning/phases/17-windows-shell/17-CONTEXT.md D-33 (smoke checklist scope), D-32 (Windows 11 ARM64 VM target environment)
    - .planning/phases/17-windows-shell/17-VALIDATION.md §"Manual-Only Verifications" (canonical smoke items)
    - apple/manual-smoke-checklist.md or .planning/phases/16-apple-incremental-swap/16-07-PLAN.md (Apple APPLE-05 smoke parity reference, if exists)
  </read_first>
  <action>
    Create `windows/manual-smoke-D-33.md` documenting every smoke item enumerated in VALIDATION §"Manual-Only Verifications" plus CONTEXT D-33 (wizard step-by-step, hydrate-on-double-click, save→upload no spurious re-upload after hydration, rename, move, delete, conflict copy, pause/resume, multi-drive setup at 2 and 3 drives, Explorer state-icon transitions across synced/syncing/error/cloud-only).

    Structure:
    1. **Environment** — Windows 11 ARM64 VM (Parallels/UTM on Apple Silicon, per D-32); NTFS sync root volume; Cubbit DS3 test account credentials.
    2. **Pre-flight** — Uninstall any prior version; verify no leftover cfapi sync root via `Get-StorageProviderSyncRoots` (PowerShell); reset Credential Manager target `Cubbit DS3 Drive — *`.
    3. **Smoke matrix** — Numbered checklist items 1-15 covering every requirement WIN-01 through WIN-09 and every parity scenario, each row with: `[ ]` checkbox, item title, requirement IDs covered, expected behavior, observation column.
       Each item references the corresponding RESEARCH.md pitfall (e.g. item "Open-then-close hydrated file → zero PUTs" → RESEARCH Pitfall 3).
    4. **Sign-off** — Two signature lines (engineer, second reviewer); date field; pass/fail summary.

    Mirror the macOS APPLE-05 D-24 checklist structure verbatim where applicable so future maintainers can diff Apple ↔ Windows smoke results side-by-side. Document MUST include the requirement-ID column so /gsd:verify-work can correlate checklist items to WIN-XX.
  </action>
  <verify>
    <automated>test -f windows/manual-smoke-D-33.md &amp;&amp; grep -c "WIN-0" windows/manual-smoke-D-33.md &amp;&amp; grep -c "\[ \]" windows/manual-smoke-D-33.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -oE "WIN-0[1-9]" windows/manual-smoke-D-33.md | sort -u | wc -l` ≥ 9 (every WIN-XX appears at least once).
    - `grep -c "\[ \]" windows/manual-smoke-D-33.md` ≥ 15 (smoke matrix items).
    - `grep -c "NTFS" windows/manual-smoke-D-33.md` ≥ 1 (pre-flight reference per Pitfall 8).
    - `grep -c "Pitfall" windows/manual-smoke-D-33.md` ≥ 3 (cross-references RESEARCH pitfalls).
    - `grep -c "ARM64" windows/manual-smoke-D-33.md` ≥ 1 (D-32 environment).
    - `grep -c "no spurious\|zero PUT\|no PUT\|open-then-close" windows/manual-smoke-D-33.md` ≥ 1 (Pitfall 3 hydration loop check).
    - `grep -c "Sign-off\|Signature" windows/manual-smoke-D-33.md` ≥ 1.
  </acceptance_criteria>
  <done>Smoke checklist exists; gates phase sign-off per VALIDATION §Sampling Rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets → workflow | If integration tests run in CI later, CUBBIT_TEST_* secrets must be repo secrets (not env files) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | InfoDisclosure | CUBBIT_TEST_* env vars in CI logs | mitigate | CI filter excludes Category=Integration by default; fixture never logs values; when secrets enabled in workflow_dispatch run, GitHub's secret masking applies |
| T-17-03-02 | Tampering | windows-build.yml workflow | mitigate | `permissions: contents: read`; no write tokens; paths filter so unrelated changes don't trigger |
| T-17-03-03 | DoS | CI cargo cache miss | accept | First build of each cache key may take 15+ min; cache hits restore in seconds; acceptable trade-off |
</threat_model>

<verification>
- CI workflow valid YAML, runs on windows-latest, builds Rust + .NET, runs non-integration tests.
- xunit.runner.json disables parallelization.
- CubbitCredentials fixture reads env vars and skips integration tests if unset.
- Smoke checklist enumerates all WIN-XX requirements and every D-33 scenario.
</verification>

<success_criteria>
- First push triggers CI green (or red with actionable error if Rust toolchain install fails).
- Tests can be written in subsequent plans and discovered by the runner.
- Phase sign-off (Plan 12) walks the smoke checklist with passes recorded.
</success_criteria>

<output>
Create `.planning/phases/17-windows-shell/17-03-SUMMARY.md` when done.
</output>
